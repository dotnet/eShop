// aspire.ts - Capability-based Aspire SDK
// This SDK uses the ATS (Aspire Type System) capability API.
// Capabilities are endpoints like 'Aspire.Hosting/createBuilder'.
//
// GENERATED CODE - DO NOT EDIT

import {
    AspireClient as AspireClientRpc,
    Handle,
    MarshalledHandle,
    AppHostUsageError,
    CapabilityError,
    registerCallback,
    wrapIfHandle,
    registerHandleWrapper
} from './transport.js';

import {
    ResourceBuilderBase,
    ReferenceExpression,
    refExpr,
    AspireDict,
    AspireList
} from './base.js';

// ============================================================================
// Handle Type Aliases (Internal - not exported to users)
// ============================================================================

/** Handle to AzureContainerAppEnvironmentResource */
type AzureContainerAppEnvironmentResourceHandle = Handle<'Aspire.Hosting.Azure.AppContainers/Aspire.Hosting.Azure.AppContainers.AzureContainerAppEnvironmentResource'>;

/** Handle to AzureOpenAIDeploymentResource */
type AzureOpenAIDeploymentResourceHandle = Handle<'Aspire.Hosting.Azure.CognitiveServices/Aspire.Hosting.ApplicationModel.AzureOpenAIDeploymentResource'>;

/** Handle to AzureOpenAIResource */
type AzureOpenAIResourceHandle = Handle<'Aspire.Hosting.Azure.CognitiveServices/Aspire.Hosting.ApplicationModel.AzureOpenAIResource'>;

/** Handle to AzureContainerRegistryResource */
type AzureContainerRegistryResourceHandle = Handle<'Aspire.Hosting.Azure.ContainerRegistry/Aspire.Hosting.Azure.AzureContainerRegistryResource'>;

/** Handle to AzureLogAnalyticsWorkspaceResource */
type AzureLogAnalyticsWorkspaceResourceHandle = Handle<'Aspire.Hosting.Azure.OperationalInsights/Aspire.Hosting.Azure.AzureLogAnalyticsWorkspaceResource'>;

/** Handle to IAzureResource */
type IAzureResourceHandle = Handle<'Aspire.Hosting.Azure/Aspire.Hosting.ApplicationModel.IAzureResource'>;

/** Handle to AzureBicepResource */
type AzureBicepResourceHandle = Handle<'Aspire.Hosting.Azure/Aspire.Hosting.Azure.AzureBicepResource'>;

/** Handle to AzureEnvironmentResource */
type AzureEnvironmentResourceHandle = Handle<'Aspire.Hosting.Azure/Aspire.Hosting.Azure.AzureEnvironmentResource'>;

/** Handle to AzureProvisioningResource */
type AzureProvisioningResourceHandle = Handle<'Aspire.Hosting.Azure/Aspire.Hosting.Azure.AzureProvisioningResource'>;

/** Handle to AzureResourceInfrastructure */
type AzureResourceInfrastructureHandle = Handle<'Aspire.Hosting.Azure/Aspire.Hosting.Azure.AzureResourceInfrastructure'>;

/** Handle to AzureUserAssignedIdentityResource */
type AzureUserAssignedIdentityResourceHandle = Handle<'Aspire.Hosting.Azure/Aspire.Hosting.Azure.AzureUserAssignedIdentityResource'>;

/** Handle to BicepOutputReference */
type BicepOutputReferenceHandle = Handle<'Aspire.Hosting.Azure/Aspire.Hosting.Azure.BicepOutputReference'>;

/** Handle to IAzureKeyVaultSecretReference */
type IAzureKeyVaultSecretReferenceHandle = Handle<'Aspire.Hosting.Azure/Aspire.Hosting.Azure.IAzureKeyVaultSecretReference'>;

/** Handle to PostgresDatabaseResource */
type PostgresDatabaseResourceHandle = Handle<'Aspire.Hosting.PostgreSQL/Aspire.Hosting.ApplicationModel.PostgresDatabaseResource'>;

/** Handle to PostgresServerResource */
type PostgresServerResourceHandle = Handle<'Aspire.Hosting.PostgreSQL/Aspire.Hosting.ApplicationModel.PostgresServerResource'>;

/** Handle to PgAdminContainerResource */
type PgAdminContainerResourceHandle = Handle<'Aspire.Hosting.PostgreSQL/Aspire.Hosting.Postgres.PgAdminContainerResource'>;

/** Handle to PgWebContainerResource */
type PgWebContainerResourceHandle = Handle<'Aspire.Hosting.PostgreSQL/Aspire.Hosting.Postgres.PgWebContainerResource'>;

/** Handle to PostgresMcpContainerResource */
type PostgresMcpContainerResourceHandle = Handle<'Aspire.Hosting.PostgreSQL/Aspire.Hosting.Postgres.PostgresMcpContainerResource'>;

/** Handle to RabbitMQServerResource */
type RabbitMQServerResourceHandle = Handle<'Aspire.Hosting.RabbitMQ/Aspire.Hosting.ApplicationModel.RabbitMQServerResource'>;

/** Handle to RedisResource */
type RedisResourceHandle = Handle<'Aspire.Hosting.Redis/Aspire.Hosting.ApplicationModel.RedisResource'>;

/** Handle to RedisCommanderResource */
type RedisCommanderResourceHandle = Handle<'Aspire.Hosting.Redis/Aspire.Hosting.Redis.RedisCommanderResource'>;

/** Handle to RedisInsightResource */
type RedisInsightResourceHandle = Handle<'Aspire.Hosting.Redis/Aspire.Hosting.Redis.RedisInsightResource'>;

/** Handle to IYarpConfigurationBuilder */
type IYarpConfigurationBuilderHandle = Handle<'Aspire.Hosting.Yarp/Aspire.Hosting.IYarpConfigurationBuilder'>;

/** Handle to YarpCluster */
type YarpClusterHandle = Handle<'Aspire.Hosting.Yarp/Aspire.Hosting.Yarp.YarpCluster'>;

/** Handle to YarpResource */
type YarpResourceHandle = Handle<'Aspire.Hosting.Yarp/Aspire.Hosting.Yarp.YarpResource'>;

/** Handle to YarpRoute */
type YarpRouteHandle = Handle<'Aspire.Hosting.Yarp/Aspire.Hosting.Yarp.YarpRoute'>;

/** Handle to AfterResourcesCreatedEvent */
type AfterResourcesCreatedEventHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.AfterResourcesCreatedEvent'>;

/** Handle to BeforeResourceStartedEvent */
type BeforeResourceStartedEventHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.BeforeResourceStartedEvent'>;

/** Handle to BeforeStartEvent */
type BeforeStartEventHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.BeforeStartEvent'>;

/** Handle to CommandLineArgsCallbackContext */
type CommandLineArgsCallbackContextHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.CommandLineArgsCallbackContext'>;

/** Handle to ConnectionStringAvailableEvent */
type ConnectionStringAvailableEventHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.ConnectionStringAvailableEvent'>;

/** Handle to ContainerRegistryResource */
type ContainerRegistryResourceHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.ContainerRegistryResource'>;

/** Handle to ContainerResource */
type ContainerResourceHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.ContainerResource'>;

/** Handle to CSharpAppResource */
type CSharpAppResourceHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.CSharpAppResource'>;

/** Handle to DistributedApplicationModel */
type DistributedApplicationModelHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.DistributedApplicationModel'>;

/** Handle to DotnetToolResource */
type DotnetToolResourceHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.DotnetToolResource'>;

/** Handle to EndpointReference */
type EndpointReferenceHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.EndpointReference'>;

/** Handle to EndpointReferenceExpression */
type EndpointReferenceExpressionHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.EndpointReferenceExpression'>;

/** Handle to EnvironmentCallbackContext */
type EnvironmentCallbackContextHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.EnvironmentCallbackContext'>;

/** Handle to ExecutableResource */
type ExecutableResourceHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.ExecutableResource'>;

/** Handle to ExecuteCommandContext */
type ExecuteCommandContextHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.ExecuteCommandContext'>;

/** Handle to IComputeResource */
type IComputeResourceHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.IComputeResource'>;

/** Handle to IContainerFilesDestinationResource */
type IContainerFilesDestinationResourceHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.IContainerFilesDestinationResource'>;

/** Handle to InitializeResourceEvent */
type InitializeResourceEventHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.InitializeResourceEvent'>;

/** Handle to IResource */
type IResourceHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResource'>;

/** Handle to IResourceWithArgs */
type IResourceWithArgsHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResourceWithArgs'>;

/** Handle to IResourceWithConnectionString */
type IResourceWithConnectionStringHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResourceWithConnectionString'>;

/** Handle to IResourceWithEndpoints */
type IResourceWithEndpointsHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResourceWithEndpoints'>;

/** Handle to IResourceWithEnvironment */
type IResourceWithEnvironmentHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResourceWithEnvironment'>;

/** Handle to IResourceWithWaitSupport */
type IResourceWithWaitSupportHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResourceWithWaitSupport'>;

/** Handle to ParameterResource */
type ParameterResourceHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.ParameterResource'>;

/** Handle to ProjectResource */
type ProjectResourceHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.ProjectResource'>;

/** Handle to ReferenceExpression */
type ReferenceExpressionHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.ReferenceExpression'>;

/** Handle to ReferenceExpressionBuilder */
type ReferenceExpressionBuilderHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.ReferenceExpressionBuilder'>;

/** Handle to ResourceEndpointsAllocatedEvent */
type ResourceEndpointsAllocatedEventHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceEndpointsAllocatedEvent'>;

/** Handle to ResourceLoggerService */
type ResourceLoggerServiceHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceLoggerService'>;

/** Handle to ResourceNotificationService */
type ResourceNotificationServiceHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceNotificationService'>;

/** Handle to ResourceReadyEvent */
type ResourceReadyEventHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceReadyEvent'>;

/** Handle to ResourceStoppedEvent */
type ResourceStoppedEventHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceStoppedEvent'>;

/** Handle to ResourceUrlsCallbackContext */
type ResourceUrlsCallbackContextHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceUrlsCallbackContext'>;

/** Handle to UpdateCommandStateContext */
type UpdateCommandStateContextHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ApplicationModel.UpdateCommandStateContext'>;

/** Handle to ConnectionStringResource */
type ConnectionStringResourceHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ConnectionStringResource'>;

/** Handle to DistributedApplication */
type DistributedApplicationHandle = Handle<'Aspire.Hosting/Aspire.Hosting.DistributedApplication'>;

/** Handle to DistributedApplicationExecutionContext */
type DistributedApplicationExecutionContextHandle = Handle<'Aspire.Hosting/Aspire.Hosting.DistributedApplicationExecutionContext'>;

/** Handle to DistributedApplicationEventSubscription */
type DistributedApplicationEventSubscriptionHandle = Handle<'Aspire.Hosting/Aspire.Hosting.Eventing.DistributedApplicationEventSubscription'>;

/** Handle to IDistributedApplicationEventing */
type IDistributedApplicationEventingHandle = Handle<'Aspire.Hosting/Aspire.Hosting.Eventing.IDistributedApplicationEventing'>;

/** Handle to ExternalServiceResource */
type ExternalServiceResourceHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ExternalServiceResource'>;

/** Handle to IDistributedApplicationBuilder */
type IDistributedApplicationBuilderHandle = Handle<'Aspire.Hosting/Aspire.Hosting.IDistributedApplicationBuilder'>;

/** Handle to IResourceWithContainerFiles */
type IResourceWithContainerFilesHandle = Handle<'Aspire.Hosting/Aspire.Hosting.IResourceWithContainerFiles'>;

/** Handle to IResourceWithServiceDiscovery */
type IResourceWithServiceDiscoveryHandle = Handle<'Aspire.Hosting/Aspire.Hosting.IResourceWithServiceDiscovery'>;

/** Handle to IUserSecretsManager */
type IUserSecretsManagerHandle = Handle<'Aspire.Hosting/Aspire.Hosting.IUserSecretsManager'>;

/** Handle to IReportingStep */
type IReportingStepHandle = Handle<'Aspire.Hosting/Aspire.Hosting.Pipelines.IReportingStep'>;

/** Handle to IReportingTask */
type IReportingTaskHandle = Handle<'Aspire.Hosting/Aspire.Hosting.Pipelines.IReportingTask'>;

/** Handle to PipelineConfigurationContext */
type PipelineConfigurationContextHandle = Handle<'Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineConfigurationContext'>;

/** Handle to PipelineContext */
type PipelineContextHandle = Handle<'Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineContext'>;

/** Handle to PipelineStep */
type PipelineStepHandle = Handle<'Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineStep'>;

/** Handle to PipelineStepContext */
type PipelineStepContextHandle = Handle<'Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineStepContext'>;

/** Handle to PipelineStepFactoryContext */
type PipelineStepFactoryContextHandle = Handle<'Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineStepFactoryContext'>;

/** Handle to PipelineSummary */
type PipelineSummaryHandle = Handle<'Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineSummary'>;

/** Handle to ProjectResourceOptions */
type ProjectResourceOptionsHandle = Handle<'Aspire.Hosting/Aspire.Hosting.ProjectResourceOptions'>;

/** Handle to Dict<string,any> */
type DictstringanyHandle = Handle<'Aspire.Hosting/Dict<string,any>'>;

/** Handle to List<any> */
type ListanyHandle = Handle<'Aspire.Hosting/List<any>'>;

/** Handle to ContainerApp */
type ContainerAppHandle = Handle<'Azure.Provisioning.AppContainers/Azure.Provisioning.AppContainers.ContainerApp'>;

/** Handle to ContainerAppJob */
type ContainerAppJobHandle = Handle<'Azure.Provisioning.AppContainers/Azure.Provisioning.AppContainers.ContainerAppJob'>;

/** Handle to IConfiguration */
type IConfigurationHandle = Handle<'Microsoft.Extensions.Configuration.Abstractions/Microsoft.Extensions.Configuration.IConfiguration'>;

/** Handle to IConfigurationSection */
type IConfigurationSectionHandle = Handle<'Microsoft.Extensions.Configuration.Abstractions/Microsoft.Extensions.Configuration.IConfigurationSection'>;

/** Handle to IHostEnvironment */
type IHostEnvironmentHandle = Handle<'Microsoft.Extensions.Hosting.Abstractions/Microsoft.Extensions.Hosting.IHostEnvironment'>;

/** Handle to ILogger */
type ILoggerHandle = Handle<'Microsoft.Extensions.Logging.Abstractions/Microsoft.Extensions.Logging.ILogger'>;

/** Handle to ILoggerFactory */
type ILoggerFactoryHandle = Handle<'Microsoft.Extensions.Logging.Abstractions/Microsoft.Extensions.Logging.ILoggerFactory'>;

/** Handle to string[] */
type stringArrayHandle = Handle<'string[]'>;

/** Handle to IServiceProvider */
type IServiceProviderHandle = Handle<'System.ComponentModel/System.IServiceProvider'>;

// ============================================================================
// Enum Types
// ============================================================================

/** Enum type for AzureContainerRegistryRole */
export enum AzureContainerRegistryRole {
    AcrDelete = "AcrDelete",
    AcrImageSigner = "AcrImageSigner",
    AcrPull = "AcrPull",
    AcrPush = "AcrPush",
    AcrQuarantineReader = "AcrQuarantineReader",
    AcrQuarantineWriter = "AcrQuarantineWriter",
}

/** Enum type for AzureOpenAIRole */
export enum AzureOpenAIRole {
    CognitiveServicesOpenAIContributor = "CognitiveServicesOpenAIContributor",
    CognitiveServicesOpenAIUser = "CognitiveServicesOpenAIUser",
    CognitiveServicesUser = "CognitiveServicesUser",
}

/** Enum type for CertificateTrustScope */
export enum CertificateTrustScope {
    None = "None",
    Append = "Append",
    Override = "Override",
    System = "System",
}

/** Enum type for ContainerLifetime */
export enum ContainerLifetime {
    Session = "Session",
    Persistent = "Persistent",
}

/** Enum type for CookieSecurePolicy */
export enum CookieSecurePolicy {
    SameAsRequest = "SameAsRequest",
    Always = "Always",
    None = "None",
}

/** Enum type for DeploymentScope */
export enum DeploymentScope {
    ResourceGroup = "ResourceGroup",
    Subscription = "Subscription",
    ManagementGroup = "ManagementGroup",
    Tenant = "Tenant",
}

/** Enum type for DistributedApplicationOperation */
export enum DistributedApplicationOperation {
    Run = "Run",
    Publish = "Publish",
}

/** Enum type for EndpointProperty */
export enum EndpointProperty {
    Url = "Url",
    Host = "Host",
    IPV4Host = "IPV4Host",
    Port = "Port",
    Scheme = "Scheme",
    TargetPort = "TargetPort",
    HostAndPort = "HostAndPort",
    TlsEnabled = "TlsEnabled",
}

/** Enum type for ForwardedTransformActions */
export enum ForwardedTransformActions {
    Off = "Off",
    Set = "Set",
    Append = "Append",
    Remove = "Remove",
}

/** Enum type for HeaderMatchMode */
export enum HeaderMatchMode {
    ExactHeader = "ExactHeader",
    HeaderPrefix = "HeaderPrefix",
    Contains = "Contains",
    NotContains = "NotContains",
    Exists = "Exists",
    NotExists = "NotExists",
}

/** Enum type for HttpVersionPolicy */
export enum HttpVersionPolicy {
    RequestVersionOrLower = "RequestVersionOrLower",
    RequestVersionOrHigher = "RequestVersionOrHigher",
    RequestVersionExact = "RequestVersionExact",
}

/** Enum type for IconVariant */
export enum IconVariant {
    Regular = "Regular",
    Filled = "Filled",
}

/** Enum type for ImagePullPolicy */
export enum ImagePullPolicy {
    Default = "Default",
    Always = "Always",
    Missing = "Missing",
    Never = "Never",
}

/** Enum type for NodeFormat */
export enum NodeFormat {
    None = "None",
    Random = "Random",
    RandomAndPort = "RandomAndPort",
    RandomAndRandomPort = "RandomAndRandomPort",
    Unknown = "Unknown",
    UnknownAndPort = "UnknownAndPort",
    UnknownAndRandomPort = "UnknownAndRandomPort",
    Ip = "Ip",
    IpAndPort = "IpAndPort",
    IpAndRandomPort = "IpAndRandomPort",
}

/** Enum type for OtlpProtocol */
export enum OtlpProtocol {
    Grpc = "Grpc",
    HttpProtobuf = "HttpProtobuf",
    HttpJson = "HttpJson",
}

/** Enum type for ProbeType */
export enum ProbeType {
    Startup = "Startup",
    Readiness = "Readiness",
    Liveness = "Liveness",
}

/** Enum type for ProtocolType */
export enum ProtocolType {
    IP = "IP",
    IPv6HopByHopOptions = "IPv6HopByHopOptions",
    Unspecified = "Unspecified",
    Icmp = "Icmp",
    Igmp = "Igmp",
    Ggp = "Ggp",
    IPv4 = "IPv4",
    Tcp = "Tcp",
    Pup = "Pup",
    Udp = "Udp",
    Idp = "Idp",
    IPv6 = "IPv6",
    IPv6RoutingHeader = "IPv6RoutingHeader",
    IPv6FragmentHeader = "IPv6FragmentHeader",
    IPSecEncapsulatingSecurityPayload = "IPSecEncapsulatingSecurityPayload",
    IPSecAuthenticationHeader = "IPSecAuthenticationHeader",
    IcmpV6 = "IcmpV6",
    IPv6NoNextHeader = "IPv6NoNextHeader",
    IPv6DestinationOptions = "IPv6DestinationOptions",
    ND = "ND",
    Raw = "Raw",
    Ipx = "Ipx",
    Spx = "Spx",
    SpxII = "SpxII",
    Unknown = "Unknown",
}

/** Enum type for QueryParameterMatchMode */
export enum QueryParameterMatchMode {
    Exact = "Exact",
    Contains = "Contains",
    NotContains = "NotContains",
    Prefix = "Prefix",
    Exists = "Exists",
}

/** Enum type for ResponseCondition */
export enum ResponseCondition {
    Always = "Always",
    Success = "Success",
    Failure = "Failure",
}

/** Enum type for SameSiteMode */
export enum SameSiteMode {
    None = "None",
    Lax = "Lax",
    Strict = "Strict",
    Unspecified = "Unspecified",
}

/** Enum type for UrlDisplayLocation */
export enum UrlDisplayLocation {
    SummaryAndDetails = "SummaryAndDetails",
    DetailsOnly = "DetailsOnly",
}

/** Enum type for WaitBehavior */
export enum WaitBehavior {
    WaitOnResourceUnavailable = "WaitOnResourceUnavailable",
    StopOnResourceUnavailable = "StopOnResourceUnavailable",
}

/** Enum type for YarpSslProtocol */
export enum YarpSslProtocol {
    None = "None",
    Tls12 = "Tls12",
    Tls13 = "Tls13",
}

// ============================================================================
// DTO Interfaces
// ============================================================================

/** DTO interface for CommandOptions */
export interface CommandOptions {
    description?: string;
    parameter?: any;
    confirmationMessage?: string;
    iconName?: string;
    iconVariant?: IconVariant;
    isHighlighted?: boolean;
    updateState?: any;
}

/** DTO interface for CreateBuilderOptions */
export interface CreateBuilderOptions {
    args?: string[];
    projectDirectory?: string;
    appHostFilePath?: string;
    containerRegistryOverride?: string;
    disableDashboard?: boolean;
    dashboardApplicationName?: string;
    allowUnsecuredTransport?: boolean;
    enableResourceLogging?: boolean;
}

/** DTO interface for ExecuteCommandResult */
export interface ExecuteCommandResult {
    success?: boolean;
    canceled?: boolean;
    errorMessage?: string;
}

/** DTO interface for ResourceEventDto */
export interface ResourceEventDto {
    resourceName?: string;
    resourceId?: string;
    state?: string;
    stateStyle?: string;
    healthStatus?: string;
    exitCode?: number;
}

/** DTO interface for ResourceUrlAnnotation */
export interface ResourceUrlAnnotation {
    url?: string;
    displayText?: string;
    endpoint?: EndpointReferenceHandle;
    displayLocation?: UrlDisplayLocation;
}

/** DTO interface for YarpActiveHealthCheckConfig */
export interface YarpActiveHealthCheckConfig {
    enabled?: boolean;
    interval?: number;
    path?: string;
    policy?: string;
    query?: string;
    timeout?: number;
}

/** DTO interface for YarpForwarderRequestConfig */
export interface YarpForwarderRequestConfig {
    activityTimeout?: number;
    allowResponseBuffering?: boolean;
    version?: string;
    versionPolicy?: HttpVersionPolicy;
}

/** DTO interface for YarpHealthCheckConfig */
export interface YarpHealthCheckConfig {
    active?: YarpActiveHealthCheckConfig;
    availableDestinationsPolicy?: string;
    passive?: YarpPassiveHealthCheckConfig;
}

/** DTO interface for YarpHttpClientConfig */
export interface YarpHttpClientConfig {
    dangerousAcceptAnyServerCertificate?: boolean;
    enableMultipleHttp2Connections?: boolean;
    maxConnectionsPerServer?: number;
    requestHeaderEncoding?: string;
    responseHeaderEncoding?: string;
    sslProtocols?: YarpSslProtocol[];
    webProxy?: YarpWebProxyConfig;
}

/** DTO interface for YarpPassiveHealthCheckConfig */
export interface YarpPassiveHealthCheckConfig {
    enabled?: boolean;
    policy?: string;
    reactivationPeriod?: number;
}

/** DTO interface for YarpRouteHeaderMatch */
export interface YarpRouteHeaderMatch {
    name?: string;
    values?: string[];
    isCaseSensitive?: boolean;
    mode?: HeaderMatchMode;
}

/** DTO interface for YarpRouteMatch */
export interface YarpRouteMatch {
    path?: string;
    methods?: string[];
    hosts?: string[];
    headers?: YarpRouteHeaderMatch[];
    queryParameters?: YarpRouteQueryParameterMatch[];
}

/** DTO interface for YarpRouteQueryParameterMatch */
export interface YarpRouteQueryParameterMatch {
    name?: string;
    values?: string[];
    isCaseSensitive?: boolean;
    mode?: QueryParameterMatchMode;
}

/** DTO interface for YarpSessionAffinityConfig */
export interface YarpSessionAffinityConfig {
    affinityKeyName?: string;
    cookie?: YarpSessionAffinityCookieConfig;
    enabled?: boolean;
    failurePolicy?: string;
    policy?: string;
}

/** DTO interface for YarpSessionAffinityCookieConfig */
export interface YarpSessionAffinityCookieConfig {
    domain?: string;
    expiration?: number;
    httpOnly?: boolean;
    isEssential?: boolean;
    maxAge?: number;
    path?: string;
    sameSite?: SameSiteMode;
    securePolicy?: CookieSecurePolicy;
}

/** DTO interface for YarpWebProxyConfig */
export interface YarpWebProxyConfig {
    address?: string;
    bypassOnLocal?: boolean;
    useDefaultCredentials?: boolean;
}

// ============================================================================
// Options Interfaces
// ============================================================================

export interface AddConnectionStringOptions {
    environmentVariableName?: string;
}

export interface AddContainerRegistryOptions {
    repository?: ParameterResource;
}

export interface AddDatabaseOptions {
    databaseName?: string;
}

export interface AddDockerfileOptions {
    dockerfilePath?: string;
    stage?: string;
}

export interface AddParameterFromConfigurationOptions {
    secret?: boolean;
}

export interface AddParameterOptions {
    secret?: boolean;
}

export interface AddPostgresOptions {
    userName?: ParameterResource;
    password?: ParameterResource;
    port?: number;
}

export interface AddRabbitMQOptions {
    userName?: ParameterResource;
    password?: ParameterResource;
    port?: number;
}

export interface AddRedisOptions {
    port?: number;
    password?: ParameterResource;
}

export interface AddRedisWithPortOptions {
    port?: number;
}

export interface AppendFormattedOptions {
    format?: string;
}

export interface AppendValueProviderOptions {
    format?: string;
}

export interface CompleteStepMarkdownOptions {
    completionState?: string;
    cancellationToken?: AbortSignal;
}

export interface CompleteStepOptions {
    completionState?: string;
    cancellationToken?: AbortSignal;
}

export interface CompleteTaskMarkdownOptions {
    completionState?: string;
    cancellationToken?: AbortSignal;
}

export interface CompleteTaskOptions {
    completionMessage?: string;
    completionState?: string;
    cancellationToken?: AbortSignal;
}

export interface CreateMarkdownTaskOptions {
    cancellationToken?: AbortSignal;
}

export interface CreateTaskOptions {
    cancellationToken?: AbortSignal;
}

export interface GetValueAsyncOptions {
    cancellationToken?: AbortSignal;
}

export interface PublishAsConfiguredScheduledAzureContainerAppJobOptions {
    configure?: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>;
}

export interface PublishAsDockerFileOptions {
    configure?: (obj: ContainerResource) => Promise<void>;
}

export interface PublishResourceUpdateOptions {
    state?: string;
    stateStyle?: string;
}

export interface RunOptions {
    cancellationToken?: AbortSignal;
}

export interface SaveStateJsonOptions {
    cancellationToken?: AbortSignal;
}

export interface UpdateTaskMarkdownOptions {
    cancellationToken?: AbortSignal;
}

export interface UpdateTaskOptions {
    cancellationToken?: AbortSignal;
}

export interface WaitForCompletionOptions {
    exitCode?: number;
}

export interface WaitForResourceStateOptions {
    targetState?: string;
}

export interface WithBindMountOptions {
    isReadOnly?: boolean;
}

export interface WithCommandOptions {
    commandOptions?: CommandOptions;
}

export interface WithDashboardOptions {
    enable?: boolean;
}

export interface WithDataBindMountOptions {
    isReadOnly?: boolean;
}

export interface WithDataVolumeOptions {
    name?: string;
    isReadOnly?: boolean;
}

export interface WithDescriptionOptions {
    enableMarkdown?: boolean;
}

export interface WithDockerfileBaseImageOptions {
    buildImage?: string;
    runtimeImage?: string;
}

export interface WithDockerfileOptions {
    dockerfilePath?: string;
    stage?: string;
}

export interface WithEndpointOptions {
    port?: number;
    targetPort?: number;
    scheme?: string;
    name?: string;
    env?: string;
    isProxied?: boolean;
    isExternal?: boolean;
    protocol?: ProtocolType;
}

export interface WithExternalServiceHttpHealthCheckOptions {
    path?: string;
    statusCode?: number;
}

export interface WithHostHttpsPortOptions {
    port?: number;
}

export interface WithHostPortOptions {
    port?: number;
}

export interface WithHttpEndpointOptions {
    port?: number;
    targetPort?: number;
    name?: string;
    env?: string;
    isProxied?: boolean;
}

export interface WithHttpHealthCheckOptions {
    path?: string;
    statusCode?: number;
    endpointName?: string;
}

export interface WithHttpProbeOptions {
    path?: string;
    initialDelaySeconds?: number;
    periodSeconds?: number;
    timeoutSeconds?: number;
    failureThreshold?: number;
    successThreshold?: number;
    endpointName?: string;
}

export interface WithHttpsDeveloperCertificateOptions {
    password?: ParameterResource;
}

export interface WithHttpsEndpointOptions {
    port?: number;
    targetPort?: number;
    name?: string;
    env?: string;
    isProxied?: boolean;
}

export interface WithHttpsUpgradeOptions {
    upgrade?: boolean;
}

export interface WithIconNameOptions {
    iconVariant?: IconVariant;
}

export interface WithImageOptions {
    tag?: string;
}

export interface WithManagementPluginWithPortOptions {
    port?: number;
}

export interface WithMcpServerOptions {
    path?: string;
    endpointName?: string;
}

export interface WithOrderOptions {
    order?: number;
}

export interface WithPersistenceOptions {
    interval?: number;
    keysChangedThreshold?: number;
}

export interface WithPgAdminOptions {
    configureContainer?: (obj: PgAdminContainerResource) => Promise<void>;
    containerName?: string;
}

export interface WithPgWebOptions {
    configureContainer?: (obj: PgWebContainerResource) => Promise<void>;
    containerName?: string;
}

export interface WithPipelineStepFactoryOptions {
    dependsOn?: string[];
    requiredBy?: string[];
    tags?: string[];
    description?: string;
}

export interface WithPostgresMcpOptions {
    configureContainer?: (obj: PostgresMcpContainerResource) => Promise<void>;
    containerName?: string;
}

export interface WithPurgeTaskOptions {
    filter?: string;
    ago?: number;
    keep?: number;
    taskName?: string;
}

export interface WithRedisCommanderOptions {
    configureContainer?: (obj: RedisCommanderResource) => Promise<void>;
    containerName?: string;
}

export interface WithRedisInsightOptions {
    configureContainer?: (obj: RedisInsightResource) => Promise<void>;
    containerName?: string;
}

export interface WithReferenceOptions {
    connectionName?: string;
    optional?: boolean;
}

export interface WithRequiredCommandOptions {
    helpLink?: string;
}

export interface WithTransformCopyRequestHeadersOptions {
    copy?: boolean;
}

export interface WithTransformCopyResponseHeadersOptions {
    copy?: boolean;
}

export interface WithTransformCopyResponseTrailersOptions {
    copy?: boolean;
}

export interface WithTransformForwardedOptions {
    useHost?: boolean;
    useProto?: boolean;
    forFormat?: NodeFormat;
    byFormat?: NodeFormat;
    action?: ForwardedTransformActions;
}

export interface WithTransformQueryRouteValueOptions {
    append?: boolean;
}

export interface WithTransformQueryValueOptions {
    append?: boolean;
}

export interface WithTransformRequestHeaderOptions {
    append?: boolean;
}

export interface WithTransformRequestHeaderRouteValueOptions {
    append?: boolean;
}

export interface WithTransformResponseHeaderOptions {
    append?: boolean;
    condition?: ResponseCondition;
}

export interface WithTransformResponseHeaderRemoveOptions {
    condition?: ResponseCondition;
}

export interface WithTransformResponseTrailerOptions {
    append?: boolean;
    condition?: ResponseCondition;
}

export interface WithTransformResponseTrailerRemoveOptions {
    condition?: ResponseCondition;
}

export interface WithTransformUseOriginalHostHeaderOptions {
    useOriginal?: boolean;
}

export interface WithTransformXForwardedOptions {
    headerPrefix?: string;
    xDefault?: ForwardedTransformActions;
    xFor?: ForwardedTransformActions;
    xHost?: ForwardedTransformActions;
    xProto?: ForwardedTransformActions;
    xPrefix?: ForwardedTransformActions;
}

export interface WithUrlExpressionOptions {
    displayText?: string;
}

export interface WithUrlOptions {
    displayText?: string;
}

export interface WithVolumeOptions {
    name?: string;
    isReadOnly?: boolean;
}

// ============================================================================
// AfterResourcesCreatedEvent
// ============================================================================

/**
 * Type class for AfterResourcesCreatedEvent.
 */
export class AfterResourcesCreatedEvent {
    constructor(private _handle: AfterResourcesCreatedEventHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the Services property */
    services = {
        get: async (): Promise<ServiceProvider> => {
            const handle = await this._client.invokeCapability<IServiceProviderHandle>(
                'Aspire.Hosting.ApplicationModel/AfterResourcesCreatedEvent.services',
                { context: this._handle }
            );
            return new ServiceProvider(handle, this._client);
        },
    };

    /** Gets the Model property */
    model = {
        get: async (): Promise<DistributedApplicationModel> => {
            const handle = await this._client.invokeCapability<DistributedApplicationModelHandle>(
                'Aspire.Hosting.ApplicationModel/AfterResourcesCreatedEvent.model',
                { context: this._handle }
            );
            return new DistributedApplicationModel(handle, this._client);
        },
    };

}

// ============================================================================
// AzureResourceInfrastructure
// ============================================================================

/**
 * Type class for AzureResourceInfrastructure.
 */
export class AzureResourceInfrastructure {
    constructor(private _handle: AzureResourceInfrastructureHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the BicepName property */
    bicepName = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.Azure/AzureResourceInfrastructure.bicepName',
                { context: this._handle }
            );
        },
    };

    /** Gets the TargetScope property */
    targetScope = {
        get: async (): Promise<DeploymentScope> => {
            return await this._client.invokeCapability<DeploymentScope>(
                'Aspire.Hosting.Azure/AzureResourceInfrastructure.targetScope',
                { context: this._handle }
            );
        },
        set: async (value: DeploymentScope): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.Azure/AzureResourceInfrastructure.setTargetScope',
                { context: this._handle, value }
            );
        }
    };

}

// ============================================================================
// BeforeResourceStartedEvent
// ============================================================================

/**
 * Type class for BeforeResourceStartedEvent.
 */
export class BeforeResourceStartedEvent {
    constructor(private _handle: BeforeResourceStartedEventHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the Resource property */
    resource = {
        get: async (): Promise<Resource> => {
            const handle = await this._client.invokeCapability<IResourceHandle>(
                'Aspire.Hosting.ApplicationModel/BeforeResourceStartedEvent.resource',
                { context: this._handle }
            );
            return new Resource(handle, this._client);
        },
    };

    /** Gets the Services property */
    services = {
        get: async (): Promise<ServiceProvider> => {
            const handle = await this._client.invokeCapability<IServiceProviderHandle>(
                'Aspire.Hosting.ApplicationModel/BeforeResourceStartedEvent.services',
                { context: this._handle }
            );
            return new ServiceProvider(handle, this._client);
        },
    };

}

// ============================================================================
// BeforeStartEvent
// ============================================================================

/**
 * Type class for BeforeStartEvent.
 */
export class BeforeStartEvent {
    constructor(private _handle: BeforeStartEventHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the Services property */
    services = {
        get: async (): Promise<ServiceProvider> => {
            const handle = await this._client.invokeCapability<IServiceProviderHandle>(
                'Aspire.Hosting.ApplicationModel/BeforeStartEvent.services',
                { context: this._handle }
            );
            return new ServiceProvider(handle, this._client);
        },
    };

    /** Gets the Model property */
    model = {
        get: async (): Promise<DistributedApplicationModel> => {
            const handle = await this._client.invokeCapability<DistributedApplicationModelHandle>(
                'Aspire.Hosting.ApplicationModel/BeforeStartEvent.model',
                { context: this._handle }
            );
            return new DistributedApplicationModel(handle, this._client);
        },
    };

}

// ============================================================================
// BicepOutputReference
// ============================================================================

/**
 * Type class for BicepOutputReference.
 */
export class BicepOutputReference {
    constructor(private _handle: BicepOutputReferenceHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the Name property */
    name = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.Azure/BicepOutputReference.name',
                { context: this._handle }
            );
        },
    };

    /** Gets the Value property */
    value = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.Azure/BicepOutputReference.value',
                { context: this._handle }
            );
        },
    };

    /** Gets the ValueExpression property */
    valueExpression = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.Azure/BicepOutputReference.valueExpression',
                { context: this._handle }
            );
        },
    };

}

// ============================================================================
// CommandLineArgsCallbackContext
// ============================================================================

/**
 * Type class for CommandLineArgsCallbackContext.
 */
export class CommandLineArgsCallbackContext {
    constructor(private _handle: CommandLineArgsCallbackContextHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the Args property */
    private _args?: AspireList<any>;
    get args(): AspireList<any> {
        if (!this._args) {
            this._args = new AspireList<any>(
                this._handle,
                this._client,
                'Aspire.Hosting.ApplicationModel/CommandLineArgsCallbackContext.args',
                'Aspire.Hosting.ApplicationModel/CommandLineArgsCallbackContext.args'
            );
        }
        return this._args;
    }

    /** Gets the CancellationToken property */
    cancellationToken = {
        get: async (): Promise<AbortSignal> => {
            return await this._client.invokeCapability<AbortSignal>(
                'Aspire.Hosting.ApplicationModel/CommandLineArgsCallbackContext.cancellationToken',
                { context: this._handle }
            );
        },
    };

    /** Gets the ExecutionContext property */
    executionContext = {
        get: async (): Promise<DistributedApplicationExecutionContext> => {
            const handle = await this._client.invokeCapability<DistributedApplicationExecutionContextHandle>(
                'Aspire.Hosting.ApplicationModel/CommandLineArgsCallbackContext.executionContext',
                { context: this._handle }
            );
            return new DistributedApplicationExecutionContext(handle, this._client);
        },
    };

    /** Gets the Logger property */
    logger = {
        get: async (): Promise<Logger> => {
            const handle = await this._client.invokeCapability<ILoggerHandle>(
                'Aspire.Hosting.ApplicationModel/CommandLineArgsCallbackContext.logger',
                { context: this._handle }
            );
            return new Logger(handle, this._client);
        },
    };

    /** Gets the Resource property */
    resource = {
        get: async (): Promise<Resource> => {
            const handle = await this._client.invokeCapability<IResourceHandle>(
                'Aspire.Hosting.ApplicationModel/CommandLineArgsCallbackContext.resource',
                { context: this._handle }
            );
            return new Resource(handle, this._client);
        },
    };

}

// ============================================================================
// ConnectionStringAvailableEvent
// ============================================================================

/**
 * Type class for ConnectionStringAvailableEvent.
 */
export class ConnectionStringAvailableEvent {
    constructor(private _handle: ConnectionStringAvailableEventHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the Resource property */
    resource = {
        get: async (): Promise<Resource> => {
            const handle = await this._client.invokeCapability<IResourceHandle>(
                'Aspire.Hosting.ApplicationModel/ConnectionStringAvailableEvent.resource',
                { context: this._handle }
            );
            return new Resource(handle, this._client);
        },
    };

    /** Gets the Services property */
    services = {
        get: async (): Promise<ServiceProvider> => {
            const handle = await this._client.invokeCapability<IServiceProviderHandle>(
                'Aspire.Hosting.ApplicationModel/ConnectionStringAvailableEvent.services',
                { context: this._handle }
            );
            return new ServiceProvider(handle, this._client);
        },
    };

}

// ============================================================================
// DistributedApplication
// ============================================================================

/**
 * Type class for DistributedApplication.
 */
export class DistributedApplication {
    constructor(private _handle: DistributedApplicationHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Runs the distributed application */
    /** @internal */
    async _runInternal(cancellationToken?: AbortSignal): Promise<DistributedApplication> {
        const rpcArgs: Record<string, unknown> = { context: this._handle };
        if (cancellationToken !== undefined) rpcArgs.cancellationToken = cancellationToken;
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/run',
            rpcArgs
        );
        return this;
    }

    run(options?: RunOptions): DistributedApplicationPromise {
        const cancellationToken = options?.cancellationToken;
        return new DistributedApplicationPromise(this._runInternal(cancellationToken));
    }

}

/**
 * Thenable wrapper for DistributedApplication that enables fluent chaining.
 */
export class DistributedApplicationPromise implements PromiseLike<DistributedApplication> {
    constructor(private _promise: Promise<DistributedApplication>) {}

    then<TResult1 = DistributedApplication, TResult2 = never>(
        onfulfilled?: ((value: DistributedApplication) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Runs the distributed application */
    run(options?: RunOptions): DistributedApplicationPromise {
        return new DistributedApplicationPromise(this._promise.then(obj => obj.run(options)));
    }

}

// ============================================================================
// DistributedApplicationExecutionContext
// ============================================================================

/**
 * Type class for DistributedApplicationExecutionContext.
 */
export class DistributedApplicationExecutionContext {
    constructor(private _handle: DistributedApplicationExecutionContextHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the PublisherName property */
    publisherName = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting/DistributedApplicationExecutionContext.publisherName',
                { context: this._handle }
            );
        },
        set: async (value: string): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting/DistributedApplicationExecutionContext.setPublisherName',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the Operation property */
    operation = {
        get: async (): Promise<DistributedApplicationOperation> => {
            return await this._client.invokeCapability<DistributedApplicationOperation>(
                'Aspire.Hosting/DistributedApplicationExecutionContext.operation',
                { context: this._handle }
            );
        },
    };

    /** Gets the ServiceProvider property */
    serviceProvider = {
        get: async (): Promise<ServiceProvider> => {
            const handle = await this._client.invokeCapability<IServiceProviderHandle>(
                'Aspire.Hosting/DistributedApplicationExecutionContext.serviceProvider',
                { context: this._handle }
            );
            return new ServiceProvider(handle, this._client);
        },
    };

    /** Gets the IsPublishMode property */
    isPublishMode = {
        get: async (): Promise<boolean> => {
            return await this._client.invokeCapability<boolean>(
                'Aspire.Hosting/DistributedApplicationExecutionContext.isPublishMode',
                { context: this._handle }
            );
        },
    };

    /** Gets the IsRunMode property */
    isRunMode = {
        get: async (): Promise<boolean> => {
            return await this._client.invokeCapability<boolean>(
                'Aspire.Hosting/DistributedApplicationExecutionContext.isRunMode',
                { context: this._handle }
            );
        },
    };

}

// ============================================================================
// DistributedApplicationModel
// ============================================================================

/**
 * Type class for DistributedApplicationModel.
 */
export class DistributedApplicationModel {
    constructor(private _handle: DistributedApplicationModelHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets resources from the distributed application model */
    async getResources(): Promise<Resource[]> {
        const rpcArgs: Record<string, unknown> = { model: this._handle };
        return await this._client.invokeCapability<Resource[]>(
            'Aspire.Hosting/getResources',
            rpcArgs
        );
    }

    /** Finds a resource by name */
    /** @internal */
    async _findResourceByNameInternal(name: string): Promise<Resource> {
        const rpcArgs: Record<string, unknown> = { model: this._handle, name };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/findResourceByName',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    findResourceByName(name: string): ResourcePromise {
        return new ResourcePromise(this._findResourceByNameInternal(name));
    }

}

/**
 * Thenable wrapper for DistributedApplicationModel that enables fluent chaining.
 */
export class DistributedApplicationModelPromise implements PromiseLike<DistributedApplicationModel> {
    constructor(private _promise: Promise<DistributedApplicationModel>) {}

    then<TResult1 = DistributedApplicationModel, TResult2 = never>(
        onfulfilled?: ((value: DistributedApplicationModel) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Gets resources from the distributed application model */
    getResources(): Promise<Resource[]> {
        return this._promise.then(obj => obj.getResources());
    }

    /** Finds a resource by name */
    findResourceByName(name: string): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.findResourceByName(name)));
    }

}

// ============================================================================
// EndpointReference
// ============================================================================

/**
 * Type class for EndpointReference.
 */
export class EndpointReference {
    constructor(private _handle: EndpointReferenceHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the Resource property */
    resource = {
        get: async (): Promise<ResourceWithEndpoints> => {
            const handle = await this._client.invokeCapability<IResourceWithEndpointsHandle>(
                'Aspire.Hosting.ApplicationModel/EndpointReference.resource',
                { context: this._handle }
            );
            return new ResourceWithEndpoints(handle, this._client);
        },
    };

    /** Gets the EndpointName property */
    endpointName = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/EndpointReference.endpointName',
                { context: this._handle }
            );
        },
    };

    /** Gets the ErrorMessage property */
    errorMessage = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/EndpointReference.errorMessage',
                { context: this._handle }
            );
        },
        set: async (value: string): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.ApplicationModel/EndpointReference.setErrorMessage',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the IsAllocated property */
    isAllocated = {
        get: async (): Promise<boolean> => {
            return await this._client.invokeCapability<boolean>(
                'Aspire.Hosting.ApplicationModel/EndpointReference.isAllocated',
                { context: this._handle }
            );
        },
    };

    /** Gets the Exists property */
    exists = {
        get: async (): Promise<boolean> => {
            return await this._client.invokeCapability<boolean>(
                'Aspire.Hosting.ApplicationModel/EndpointReference.exists',
                { context: this._handle }
            );
        },
    };

    /** Gets the IsHttp property */
    isHttp = {
        get: async (): Promise<boolean> => {
            return await this._client.invokeCapability<boolean>(
                'Aspire.Hosting.ApplicationModel/EndpointReference.isHttp',
                { context: this._handle }
            );
        },
    };

    /** Gets the IsHttps property */
    isHttps = {
        get: async (): Promise<boolean> => {
            return await this._client.invokeCapability<boolean>(
                'Aspire.Hosting.ApplicationModel/EndpointReference.isHttps',
                { context: this._handle }
            );
        },
    };

    /** Gets the TlsEnabled property */
    tlsEnabled = {
        get: async (): Promise<boolean> => {
            return await this._client.invokeCapability<boolean>(
                'Aspire.Hosting.ApplicationModel/EndpointReference.tlsEnabled',
                { context: this._handle }
            );
        },
    };

    /** Gets the Port property */
    port = {
        get: async (): Promise<number> => {
            return await this._client.invokeCapability<number>(
                'Aspire.Hosting.ApplicationModel/EndpointReference.port',
                { context: this._handle }
            );
        },
    };

    /** Gets the TargetPort property */
    targetPort = {
        get: async (): Promise<number> => {
            return await this._client.invokeCapability<number>(
                'Aspire.Hosting.ApplicationModel/EndpointReference.targetPort',
                { context: this._handle }
            );
        },
    };

    /** Gets the Host property */
    host = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/EndpointReference.host',
                { context: this._handle }
            );
        },
    };

    /** Gets the Scheme property */
    scheme = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/EndpointReference.scheme',
                { context: this._handle }
            );
        },
    };

    /** Gets the Url property */
    url = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/EndpointReference.url',
                { context: this._handle }
            );
        },
    };

    /** Gets the URL of the endpoint asynchronously */
    async getValueAsync(options?: GetValueAsyncOptions): Promise<string> {
        const cancellationToken = options?.cancellationToken;
        const rpcArgs: Record<string, unknown> = { context: this._handle };
        if (cancellationToken !== undefined) rpcArgs.cancellationToken = cancellationToken;
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting.ApplicationModel/getValueAsync',
            rpcArgs
        );
    }

    /** Gets a conditional expression that resolves to the enabledValue when TLS is enabled on the endpoint, or to the disabledValue otherwise. */
    async getTlsValue(enabledValue: ReferenceExpression, disabledValue: ReferenceExpression): Promise<ReferenceExpression> {
        const rpcArgs: Record<string, unknown> = { context: this._handle, enabledValue, disabledValue };
        return await this._client.invokeCapability<ReferenceExpression>(
            'Aspire.Hosting.ApplicationModel/EndpointReference.getTlsValue',
            rpcArgs
        );
    }

}

/**
 * Thenable wrapper for EndpointReference that enables fluent chaining.
 */
export class EndpointReferencePromise implements PromiseLike<EndpointReference> {
    constructor(private _promise: Promise<EndpointReference>) {}

    then<TResult1 = EndpointReference, TResult2 = never>(
        onfulfilled?: ((value: EndpointReference) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Gets the URL of the endpoint asynchronously */
    getValueAsync(options?: GetValueAsyncOptions): Promise<string> {
        return this._promise.then(obj => obj.getValueAsync(options));
    }

    /** Gets a conditional expression that resolves to the enabledValue when TLS is enabled on the endpoint, or to the disabledValue otherwise. */
    getTlsValue(enabledValue: ReferenceExpression, disabledValue: ReferenceExpression): Promise<ReferenceExpression> {
        return this._promise.then(obj => obj.getTlsValue(enabledValue, disabledValue));
    }

}

// ============================================================================
// EndpointReferenceExpression
// ============================================================================

/**
 * Type class for EndpointReferenceExpression.
 */
export class EndpointReferenceExpression {
    constructor(private _handle: EndpointReferenceExpressionHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the Endpoint property */
    endpoint = {
        get: async (): Promise<EndpointReference> => {
            const handle = await this._client.invokeCapability<EndpointReferenceHandle>(
                'Aspire.Hosting.ApplicationModel/EndpointReferenceExpression.endpoint',
                { context: this._handle }
            );
            return new EndpointReference(handle, this._client);
        },
    };

    /** Gets the Property property */
    property = {
        get: async (): Promise<EndpointProperty> => {
            return await this._client.invokeCapability<EndpointProperty>(
                'Aspire.Hosting.ApplicationModel/EndpointReferenceExpression.property',
                { context: this._handle }
            );
        },
    };

    /** Gets the ValueExpression property */
    valueExpression = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/EndpointReferenceExpression.valueExpression',
                { context: this._handle }
            );
        },
    };

}

// ============================================================================
// EnvironmentCallbackContext
// ============================================================================

/**
 * Type class for EnvironmentCallbackContext.
 */
export class EnvironmentCallbackContext {
    constructor(private _handle: EnvironmentCallbackContextHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the EnvironmentVariables property */
    private _environmentVariables?: AspireDict<string, string | ReferenceExpression>;
    get environmentVariables(): AspireDict<string, string | ReferenceExpression> {
        if (!this._environmentVariables) {
            this._environmentVariables = new AspireDict<string, string | ReferenceExpression>(
                this._handle,
                this._client,
                'Aspire.Hosting.ApplicationModel/EnvironmentCallbackContext.environmentVariables',
                'Aspire.Hosting.ApplicationModel/EnvironmentCallbackContext.environmentVariables'
            );
        }
        return this._environmentVariables;
    }

    /** Gets the CancellationToken property */
    cancellationToken = {
        get: async (): Promise<AbortSignal> => {
            return await this._client.invokeCapability<AbortSignal>(
                'Aspire.Hosting.ApplicationModel/EnvironmentCallbackContext.cancellationToken',
                { context: this._handle }
            );
        },
    };

    /** Gets the Logger property */
    logger = {
        get: async (): Promise<Logger> => {
            const handle = await this._client.invokeCapability<ILoggerHandle>(
                'Aspire.Hosting.ApplicationModel/EnvironmentCallbackContext.logger',
                { context: this._handle }
            );
            return new Logger(handle, this._client);
        },
    };

    /** Gets the Resource property */
    resource = {
        get: async (): Promise<Resource> => {
            const handle = await this._client.invokeCapability<IResourceHandle>(
                'Aspire.Hosting.ApplicationModel/EnvironmentCallbackContext.resource',
                { context: this._handle }
            );
            return new Resource(handle, this._client);
        },
    };

    /** Gets the ExecutionContext property */
    executionContext = {
        get: async (): Promise<DistributedApplicationExecutionContext> => {
            const handle = await this._client.invokeCapability<DistributedApplicationExecutionContextHandle>(
                'Aspire.Hosting.ApplicationModel/EnvironmentCallbackContext.executionContext',
                { context: this._handle }
            );
            return new DistributedApplicationExecutionContext(handle, this._client);
        },
    };

}

// ============================================================================
// ExecuteCommandContext
// ============================================================================

/**
 * Type class for ExecuteCommandContext.
 */
export class ExecuteCommandContext {
    constructor(private _handle: ExecuteCommandContextHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the ServiceProvider property */
    serviceProvider = {
        get: async (): Promise<ServiceProvider> => {
            const handle = await this._client.invokeCapability<IServiceProviderHandle>(
                'Aspire.Hosting.ApplicationModel/ExecuteCommandContext.serviceProvider',
                { context: this._handle }
            );
            return new ServiceProvider(handle, this._client);
        },
    };

    /** Gets the ResourceName property */
    resourceName = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/ExecuteCommandContext.resourceName',
                { context: this._handle }
            );
        },
        set: async (value: string): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.ApplicationModel/ExecuteCommandContext.setResourceName',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the CancellationToken property */
    cancellationToken = {
        get: async (): Promise<AbortSignal> => {
            return await this._client.invokeCapability<AbortSignal>(
                'Aspire.Hosting.ApplicationModel/ExecuteCommandContext.cancellationToken',
                { context: this._handle }
            );
        },
        set: async (value: AbortSignal): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.ApplicationModel/ExecuteCommandContext.setCancellationToken',
                { context: this._handle, value }
            );
        }
    };

}

// ============================================================================
// InitializeResourceEvent
// ============================================================================

/**
 * Type class for InitializeResourceEvent.
 */
export class InitializeResourceEvent {
    constructor(private _handle: InitializeResourceEventHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the Resource property */
    resource = {
        get: async (): Promise<Resource> => {
            const handle = await this._client.invokeCapability<IResourceHandle>(
                'Aspire.Hosting.ApplicationModel/InitializeResourceEvent.resource',
                { context: this._handle }
            );
            return new Resource(handle, this._client);
        },
    };

    /** Gets the Eventing property */
    eventing = {
        get: async (): Promise<DistributedApplicationEventing> => {
            const handle = await this._client.invokeCapability<IDistributedApplicationEventingHandle>(
                'Aspire.Hosting.ApplicationModel/InitializeResourceEvent.eventing',
                { context: this._handle }
            );
            return new DistributedApplicationEventing(handle, this._client);
        },
    };

    /** Gets the Logger property */
    logger = {
        get: async (): Promise<Logger> => {
            const handle = await this._client.invokeCapability<ILoggerHandle>(
                'Aspire.Hosting.ApplicationModel/InitializeResourceEvent.logger',
                { context: this._handle }
            );
            return new Logger(handle, this._client);
        },
    };

    /** Gets the Notifications property */
    notifications = {
        get: async (): Promise<ResourceNotificationService> => {
            const handle = await this._client.invokeCapability<ResourceNotificationServiceHandle>(
                'Aspire.Hosting.ApplicationModel/InitializeResourceEvent.notifications',
                { context: this._handle }
            );
            return new ResourceNotificationService(handle, this._client);
        },
    };

    /** Gets the Services property */
    services = {
        get: async (): Promise<ServiceProvider> => {
            const handle = await this._client.invokeCapability<IServiceProviderHandle>(
                'Aspire.Hosting.ApplicationModel/InitializeResourceEvent.services',
                { context: this._handle }
            );
            return new ServiceProvider(handle, this._client);
        },
    };

}

// ============================================================================
// PipelineConfigurationContext
// ============================================================================

/**
 * Type class for PipelineConfigurationContext.
 */
export class PipelineConfigurationContext {
    constructor(private _handle: PipelineConfigurationContextHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the Services property */
    services = {
        get: async (): Promise<ServiceProvider> => {
            const handle = await this._client.invokeCapability<IServiceProviderHandle>(
                'Aspire.Hosting.Pipelines/PipelineConfigurationContext.services',
                { context: this._handle }
            );
            return new ServiceProvider(handle, this._client);
        },
    };

    /** Gets the Steps property */
    steps = {
        get: async (): Promise<PipelineStep[]> => {
            return await this._client.invokeCapability<PipelineStep[]>(
                'Aspire.Hosting.Pipelines/PipelineConfigurationContext.steps',
                { context: this._handle }
            );
        },
        set: async (value: PipelineStep[]): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.Pipelines/PipelineConfigurationContext.setSteps',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the Model property */
    model = {
        get: async (): Promise<DistributedApplicationModel> => {
            const handle = await this._client.invokeCapability<DistributedApplicationModelHandle>(
                'Aspire.Hosting.Pipelines/PipelineConfigurationContext.model',
                { context: this._handle }
            );
            return new DistributedApplicationModel(handle, this._client);
        },
    };

    /** Gets pipeline steps with the specified tag */
    async getStepsByTag(tag: string): Promise<PipelineStep[]> {
        const rpcArgs: Record<string, unknown> = { context: this._handle, tag };
        return await this._client.invokeCapability<PipelineStep[]>(
            'Aspire.Hosting.Pipelines/getStepsByTag',
            rpcArgs
        );
    }

}

/**
 * Thenable wrapper for PipelineConfigurationContext that enables fluent chaining.
 */
export class PipelineConfigurationContextPromise implements PromiseLike<PipelineConfigurationContext> {
    constructor(private _promise: Promise<PipelineConfigurationContext>) {}

    then<TResult1 = PipelineConfigurationContext, TResult2 = never>(
        onfulfilled?: ((value: PipelineConfigurationContext) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Gets pipeline steps with the specified tag */
    getStepsByTag(tag: string): Promise<PipelineStep[]> {
        return this._promise.then(obj => obj.getStepsByTag(tag));
    }

}

// ============================================================================
// PipelineContext
// ============================================================================

/**
 * Type class for PipelineContext.
 */
export class PipelineContext {
    constructor(private _handle: PipelineContextHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the Model property */
    model = {
        get: async (): Promise<DistributedApplicationModel> => {
            const handle = await this._client.invokeCapability<DistributedApplicationModelHandle>(
                'Aspire.Hosting.Pipelines/PipelineContext.model',
                { context: this._handle }
            );
            return new DistributedApplicationModel(handle, this._client);
        },
    };

    /** Gets the ExecutionContext property */
    executionContext = {
        get: async (): Promise<DistributedApplicationExecutionContext> => {
            const handle = await this._client.invokeCapability<DistributedApplicationExecutionContextHandle>(
                'Aspire.Hosting.Pipelines/PipelineContext.executionContext',
                { context: this._handle }
            );
            return new DistributedApplicationExecutionContext(handle, this._client);
        },
    };

    /** Gets the Services property */
    services = {
        get: async (): Promise<ServiceProvider> => {
            const handle = await this._client.invokeCapability<IServiceProviderHandle>(
                'Aspire.Hosting.Pipelines/PipelineContext.services',
                { context: this._handle }
            );
            return new ServiceProvider(handle, this._client);
        },
    };

    /** Gets the Logger property */
    logger = {
        get: async (): Promise<Logger> => {
            const handle = await this._client.invokeCapability<ILoggerHandle>(
                'Aspire.Hosting.Pipelines/PipelineContext.logger',
                { context: this._handle }
            );
            return new Logger(handle, this._client);
        },
    };

    /** Gets the CancellationToken property */
    cancellationToken = {
        get: async (): Promise<AbortSignal> => {
            return await this._client.invokeCapability<AbortSignal>(
                'Aspire.Hosting.Pipelines/PipelineContext.cancellationToken',
                { context: this._handle }
            );
        },
        set: async (value: AbortSignal): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.Pipelines/PipelineContext.setCancellationToken',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the Summary property */
    summary = {
        get: async (): Promise<PipelineSummary> => {
            const handle = await this._client.invokeCapability<PipelineSummaryHandle>(
                'Aspire.Hosting.Pipelines/PipelineContext.summary',
                { context: this._handle }
            );
            return new PipelineSummary(handle, this._client);
        },
    };

}

// ============================================================================
// PipelineStep
// ============================================================================

/**
 * Type class for PipelineStep.
 */
export class PipelineStep {
    constructor(private _handle: PipelineStepHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the Name property */
    name = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.Pipelines/PipelineStep.name',
                { context: this._handle }
            );
        },
        set: async (value: string): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.Pipelines/PipelineStep.setName',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the Description property */
    description = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.Pipelines/PipelineStep.description',
                { context: this._handle }
            );
        },
        set: async (value: string): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.Pipelines/PipelineStep.setDescription',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the DependsOnSteps property */
    private _dependsOnSteps?: AspireList<string>;
    get dependsOnSteps(): AspireList<string> {
        if (!this._dependsOnSteps) {
            this._dependsOnSteps = new AspireList<string>(
                this._handle,
                this._client,
                'Aspire.Hosting.Pipelines/PipelineStep.dependsOnSteps',
                'Aspire.Hosting.Pipelines/PipelineStep.dependsOnSteps'
            );
        }
        return this._dependsOnSteps;
    }

    /** Gets the RequiredBySteps property */
    private _requiredBySteps?: AspireList<string>;
    get requiredBySteps(): AspireList<string> {
        if (!this._requiredBySteps) {
            this._requiredBySteps = new AspireList<string>(
                this._handle,
                this._client,
                'Aspire.Hosting.Pipelines/PipelineStep.requiredBySteps',
                'Aspire.Hosting.Pipelines/PipelineStep.requiredBySteps'
            );
        }
        return this._requiredBySteps;
    }

    /** Gets the Tags property */
    private _tags?: AspireList<string>;
    get tags(): AspireList<string> {
        if (!this._tags) {
            this._tags = new AspireList<string>(
                this._handle,
                this._client,
                'Aspire.Hosting.Pipelines/PipelineStep.tags',
                'Aspire.Hosting.Pipelines/PipelineStep.tags'
            );
        }
        return this._tags;
    }

    /** Gets the Resource property */
    resource = {
        get: async (): Promise<Resource> => {
            const handle = await this._client.invokeCapability<IResourceHandle>(
                'Aspire.Hosting.Pipelines/PipelineStep.resource',
                { context: this._handle }
            );
            return new Resource(handle, this._client);
        },
    };

    /** Adds a dependency on another step by name */
    /** @internal */
    async _dependsOnInternal(stepName: string): Promise<PipelineStep> {
        const rpcArgs: Record<string, unknown> = { context: this._handle, stepName };
        await this._client.invokeCapability<void>(
            'Aspire.Hosting.Pipelines/dependsOn',
            rpcArgs
        );
        return this;
    }

    dependsOn(stepName: string): PipelineStepPromise {
        return new PipelineStepPromise(this._dependsOnInternal(stepName));
    }

    /** Specifies that another step requires this step by name */
    /** @internal */
    async _requiredByInternal(stepName: string): Promise<PipelineStep> {
        const rpcArgs: Record<string, unknown> = { context: this._handle, stepName };
        await this._client.invokeCapability<void>(
            'Aspire.Hosting.Pipelines/requiredBy',
            rpcArgs
        );
        return this;
    }

    requiredBy(stepName: string): PipelineStepPromise {
        return new PipelineStepPromise(this._requiredByInternal(stepName));
    }

}

/**
 * Thenable wrapper for PipelineStep that enables fluent chaining.
 */
export class PipelineStepPromise implements PromiseLike<PipelineStep> {
    constructor(private _promise: Promise<PipelineStep>) {}

    then<TResult1 = PipelineStep, TResult2 = never>(
        onfulfilled?: ((value: PipelineStep) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Adds a dependency on another step by name */
    dependsOn(stepName: string): PipelineStepPromise {
        return new PipelineStepPromise(this._promise.then(obj => obj.dependsOn(stepName)));
    }

    /** Specifies that another step requires this step by name */
    requiredBy(stepName: string): PipelineStepPromise {
        return new PipelineStepPromise(this._promise.then(obj => obj.requiredBy(stepName)));
    }

}

// ============================================================================
// PipelineStepContext
// ============================================================================

/**
 * Type class for PipelineStepContext.
 */
export class PipelineStepContext {
    constructor(private _handle: PipelineStepContextHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the PipelineContext property */
    pipelineContext = {
        get: async (): Promise<PipelineContext> => {
            const handle = await this._client.invokeCapability<PipelineContextHandle>(
                'Aspire.Hosting.Pipelines/PipelineStepContext.pipelineContext',
                { context: this._handle }
            );
            return new PipelineContext(handle, this._client);
        },
    };

    /** Gets the ReportingStep property */
    reportingStep = {
        get: async (): Promise<ReportingStep> => {
            const handle = await this._client.invokeCapability<IReportingStepHandle>(
                'Aspire.Hosting.Pipelines/PipelineStepContext.reportingStep',
                { context: this._handle }
            );
            return new ReportingStep(handle, this._client);
        },
    };

    /** Gets the Model property */
    model = {
        get: async (): Promise<DistributedApplicationModel> => {
            const handle = await this._client.invokeCapability<DistributedApplicationModelHandle>(
                'Aspire.Hosting.Pipelines/PipelineStepContext.model',
                { context: this._handle }
            );
            return new DistributedApplicationModel(handle, this._client);
        },
    };

    /** Gets the ExecutionContext property */
    executionContext = {
        get: async (): Promise<DistributedApplicationExecutionContext> => {
            const handle = await this._client.invokeCapability<DistributedApplicationExecutionContextHandle>(
                'Aspire.Hosting.Pipelines/PipelineStepContext.executionContext',
                { context: this._handle }
            );
            return new DistributedApplicationExecutionContext(handle, this._client);
        },
    };

    /** Gets the Services property */
    services = {
        get: async (): Promise<ServiceProvider> => {
            const handle = await this._client.invokeCapability<IServiceProviderHandle>(
                'Aspire.Hosting.Pipelines/PipelineStepContext.services',
                { context: this._handle }
            );
            return new ServiceProvider(handle, this._client);
        },
    };

    /** Gets the Logger property */
    logger = {
        get: async (): Promise<Logger> => {
            const handle = await this._client.invokeCapability<ILoggerHandle>(
                'Aspire.Hosting.Pipelines/PipelineStepContext.logger',
                { context: this._handle }
            );
            return new Logger(handle, this._client);
        },
    };

    /** Gets the CancellationToken property */
    cancellationToken = {
        get: async (): Promise<AbortSignal> => {
            return await this._client.invokeCapability<AbortSignal>(
                'Aspire.Hosting.Pipelines/PipelineStepContext.cancellationToken',
                { context: this._handle }
            );
        },
    };

    /** Gets the Summary property */
    summary = {
        get: async (): Promise<PipelineSummary> => {
            const handle = await this._client.invokeCapability<PipelineSummaryHandle>(
                'Aspire.Hosting.Pipelines/PipelineStepContext.summary',
                { context: this._handle }
            );
            return new PipelineSummary(handle, this._client);
        },
    };

}

// ============================================================================
// PipelineStepFactoryContext
// ============================================================================

/**
 * Type class for PipelineStepFactoryContext.
 */
export class PipelineStepFactoryContext {
    constructor(private _handle: PipelineStepFactoryContextHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the PipelineContext property */
    pipelineContext = {
        get: async (): Promise<PipelineContext> => {
            const handle = await this._client.invokeCapability<PipelineContextHandle>(
                'Aspire.Hosting.Pipelines/PipelineStepFactoryContext.pipelineContext',
                { context: this._handle }
            );
            return new PipelineContext(handle, this._client);
        },
    };

    /** Gets the Resource property */
    resource = {
        get: async (): Promise<Resource> => {
            const handle = await this._client.invokeCapability<IResourceHandle>(
                'Aspire.Hosting.Pipelines/PipelineStepFactoryContext.resource',
                { context: this._handle }
            );
            return new Resource(handle, this._client);
        },
    };

}

// ============================================================================
// PipelineSummary
// ============================================================================

/**
 * Type class for PipelineSummary.
 */
export class PipelineSummary {
    constructor(private _handle: PipelineSummaryHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Invokes the Add method */
    /** @internal */
    async _addInternal(key: string, value: string): Promise<PipelineSummary> {
        const rpcArgs: Record<string, unknown> = { context: this._handle, key, value };
        await this._client.invokeCapability<void>(
            'Aspire.Hosting.Pipelines/PipelineSummary.add',
            rpcArgs
        );
        return this;
    }

    add(key: string, value: string): PipelineSummaryPromise {
        return new PipelineSummaryPromise(this._addInternal(key, value));
    }

    /** Adds a Markdown-formatted value to the pipeline summary */
    /** @internal */
    async _addMarkdownInternal(key: string, markdownString: string): Promise<PipelineSummary> {
        const rpcArgs: Record<string, unknown> = { summary: this._handle, key, markdownString };
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/addMarkdown',
            rpcArgs
        );
        return this;
    }

    addMarkdown(key: string, markdownString: string): PipelineSummaryPromise {
        return new PipelineSummaryPromise(this._addMarkdownInternal(key, markdownString));
    }

}

/**
 * Thenable wrapper for PipelineSummary that enables fluent chaining.
 */
export class PipelineSummaryPromise implements PromiseLike<PipelineSummary> {
    constructor(private _promise: Promise<PipelineSummary>) {}

    then<TResult1 = PipelineSummary, TResult2 = never>(
        onfulfilled?: ((value: PipelineSummary) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Invokes the Add method */
    add(key: string, value: string): PipelineSummaryPromise {
        return new PipelineSummaryPromise(this._promise.then(obj => obj.add(key, value)));
    }

    /** Adds a Markdown-formatted value to the pipeline summary */
    addMarkdown(key: string, markdownString: string): PipelineSummaryPromise {
        return new PipelineSummaryPromise(this._promise.then(obj => obj.addMarkdown(key, markdownString)));
    }

}

// ============================================================================
// ProjectResourceOptions
// ============================================================================

/**
 * Type class for ProjectResourceOptions.
 */
export class ProjectResourceOptions {
    constructor(private _handle: ProjectResourceOptionsHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the LaunchProfileName property */
    launchProfileName = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting/ProjectResourceOptions.launchProfileName',
                { context: this._handle }
            );
        },
        set: async (value: string): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting/ProjectResourceOptions.setLaunchProfileName',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the ExcludeLaunchProfile property */
    excludeLaunchProfile = {
        get: async (): Promise<boolean> => {
            return await this._client.invokeCapability<boolean>(
                'Aspire.Hosting/ProjectResourceOptions.excludeLaunchProfile',
                { context: this._handle }
            );
        },
        set: async (value: boolean): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting/ProjectResourceOptions.setExcludeLaunchProfile',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the ExcludeKestrelEndpoints property */
    excludeKestrelEndpoints = {
        get: async (): Promise<boolean> => {
            return await this._client.invokeCapability<boolean>(
                'Aspire.Hosting/ProjectResourceOptions.excludeKestrelEndpoints',
                { context: this._handle }
            );
        },
        set: async (value: boolean): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting/ProjectResourceOptions.setExcludeKestrelEndpoints',
                { context: this._handle, value }
            );
        }
    };

}

// ============================================================================
// ReferenceExpressionBuilder
// ============================================================================

/**
 * Type class for ReferenceExpressionBuilder.
 */
export class ReferenceExpressionBuilder {
    constructor(private _handle: ReferenceExpressionBuilderHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the IsEmpty property */
    isEmpty = {
        get: async (): Promise<boolean> => {
            return await this._client.invokeCapability<boolean>(
                'Aspire.Hosting.ApplicationModel/ReferenceExpressionBuilder.isEmpty',
                { context: this._handle }
            );
        },
    };

    /** Appends a literal string to the reference expression */
    /** @internal */
    async _appendLiteralInternal(value: string): Promise<ReferenceExpressionBuilder> {
        const rpcArgs: Record<string, unknown> = { context: this._handle, value };
        await this._client.invokeCapability<void>(
            'Aspire.Hosting.ApplicationModel/appendLiteral',
            rpcArgs
        );
        return this;
    }

    appendLiteral(value: string): ReferenceExpressionBuilderPromise {
        return new ReferenceExpressionBuilderPromise(this._appendLiteralInternal(value));
    }

    /** Appends a formatted string value to the reference expression */
    /** @internal */
    async _appendFormattedInternal(value: string, format?: string): Promise<ReferenceExpressionBuilder> {
        const rpcArgs: Record<string, unknown> = { context: this._handle, value };
        if (format !== undefined) rpcArgs.format = format;
        await this._client.invokeCapability<void>(
            'Aspire.Hosting.ApplicationModel/appendFormatted',
            rpcArgs
        );
        return this;
    }

    appendFormatted(value: string, options?: AppendFormattedOptions): ReferenceExpressionBuilderPromise {
        const format = options?.format;
        return new ReferenceExpressionBuilderPromise(this._appendFormattedInternal(value, format));
    }

    /** Appends a value provider to the reference expression */
    /** @internal */
    async _appendValueProviderInternal(valueProvider: any, format?: string): Promise<ReferenceExpressionBuilder> {
        const rpcArgs: Record<string, unknown> = { context: this._handle, valueProvider };
        if (format !== undefined) rpcArgs.format = format;
        await this._client.invokeCapability<void>(
            'Aspire.Hosting.ApplicationModel/appendValueProvider',
            rpcArgs
        );
        return this;
    }

    appendValueProvider(valueProvider: any, options?: AppendValueProviderOptions): ReferenceExpressionBuilderPromise {
        const format = options?.format;
        return new ReferenceExpressionBuilderPromise(this._appendValueProviderInternal(valueProvider, format));
    }

    /** Builds the reference expression */
    async build(): Promise<ReferenceExpression> {
        const rpcArgs: Record<string, unknown> = { context: this._handle };
        return await this._client.invokeCapability<ReferenceExpression>(
            'Aspire.Hosting.ApplicationModel/build',
            rpcArgs
        );
    }

}

/**
 * Thenable wrapper for ReferenceExpressionBuilder that enables fluent chaining.
 */
export class ReferenceExpressionBuilderPromise implements PromiseLike<ReferenceExpressionBuilder> {
    constructor(private _promise: Promise<ReferenceExpressionBuilder>) {}

    then<TResult1 = ReferenceExpressionBuilder, TResult2 = never>(
        onfulfilled?: ((value: ReferenceExpressionBuilder) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Appends a literal string to the reference expression */
    appendLiteral(value: string): ReferenceExpressionBuilderPromise {
        return new ReferenceExpressionBuilderPromise(this._promise.then(obj => obj.appendLiteral(value)));
    }

    /** Appends a formatted string value to the reference expression */
    appendFormatted(value: string, options?: AppendFormattedOptions): ReferenceExpressionBuilderPromise {
        return new ReferenceExpressionBuilderPromise(this._promise.then(obj => obj.appendFormatted(value, options)));
    }

    /** Appends a value provider to the reference expression */
    appendValueProvider(valueProvider: any, options?: AppendValueProviderOptions): ReferenceExpressionBuilderPromise {
        return new ReferenceExpressionBuilderPromise(this._promise.then(obj => obj.appendValueProvider(valueProvider, options)));
    }

    /** Builds the reference expression */
    build(): Promise<ReferenceExpression> {
        return this._promise.then(obj => obj.build());
    }

}

// ============================================================================
// ResourceEndpointsAllocatedEvent
// ============================================================================

/**
 * Type class for ResourceEndpointsAllocatedEvent.
 */
export class ResourceEndpointsAllocatedEvent {
    constructor(private _handle: ResourceEndpointsAllocatedEventHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the Resource property */
    resource = {
        get: async (): Promise<Resource> => {
            const handle = await this._client.invokeCapability<IResourceHandle>(
                'Aspire.Hosting.ApplicationModel/ResourceEndpointsAllocatedEvent.resource',
                { context: this._handle }
            );
            return new Resource(handle, this._client);
        },
    };

    /** Gets the Services property */
    services = {
        get: async (): Promise<ServiceProvider> => {
            const handle = await this._client.invokeCapability<IServiceProviderHandle>(
                'Aspire.Hosting.ApplicationModel/ResourceEndpointsAllocatedEvent.services',
                { context: this._handle }
            );
            return new ServiceProvider(handle, this._client);
        },
    };

}

// ============================================================================
// ResourceLoggerService
// ============================================================================

/**
 * Type class for ResourceLoggerService.
 */
export class ResourceLoggerService {
    constructor(private _handle: ResourceLoggerServiceHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Completes the log stream for a resource */
    /** @internal */
    async _completeLogInternal(resource: ResourceBuilderBase): Promise<ResourceLoggerService> {
        const rpcArgs: Record<string, unknown> = { loggerService: this._handle, resource };
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/completeLog',
            rpcArgs
        );
        return this;
    }

    completeLog(resource: ResourceBuilderBase): ResourceLoggerServicePromise {
        return new ResourceLoggerServicePromise(this._completeLogInternal(resource));
    }

    /** Completes the log stream by resource name */
    /** @internal */
    async _completeLogByNameInternal(resourceName: string): Promise<ResourceLoggerService> {
        const rpcArgs: Record<string, unknown> = { loggerService: this._handle, resourceName };
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/completeLogByName',
            rpcArgs
        );
        return this;
    }

    completeLogByName(resourceName: string): ResourceLoggerServicePromise {
        return new ResourceLoggerServicePromise(this._completeLogByNameInternal(resourceName));
    }

}

/**
 * Thenable wrapper for ResourceLoggerService that enables fluent chaining.
 */
export class ResourceLoggerServicePromise implements PromiseLike<ResourceLoggerService> {
    constructor(private _promise: Promise<ResourceLoggerService>) {}

    then<TResult1 = ResourceLoggerService, TResult2 = never>(
        onfulfilled?: ((value: ResourceLoggerService) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Completes the log stream for a resource */
    completeLog(resource: ResourceBuilderBase): ResourceLoggerServicePromise {
        return new ResourceLoggerServicePromise(this._promise.then(obj => obj.completeLog(resource)));
    }

    /** Completes the log stream by resource name */
    completeLogByName(resourceName: string): ResourceLoggerServicePromise {
        return new ResourceLoggerServicePromise(this._promise.then(obj => obj.completeLogByName(resourceName)));
    }

}

// ============================================================================
// ResourceNotificationService
// ============================================================================

/**
 * Type class for ResourceNotificationService.
 */
export class ResourceNotificationService {
    constructor(private _handle: ResourceNotificationServiceHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Waits for a resource to reach a specified state */
    /** @internal */
    async _waitForResourceStateInternal(resourceName: string, targetState?: string): Promise<ResourceNotificationService> {
        const rpcArgs: Record<string, unknown> = { notificationService: this._handle, resourceName };
        if (targetState !== undefined) rpcArgs.targetState = targetState;
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/waitForResourceState',
            rpcArgs
        );
        return this;
    }

    waitForResourceState(resourceName: string, options?: WaitForResourceStateOptions): ResourceNotificationServicePromise {
        const targetState = options?.targetState;
        return new ResourceNotificationServicePromise(this._waitForResourceStateInternal(resourceName, targetState));
    }

    /** Waits for a resource to reach one of the specified states */
    async waitForResourceStates(resourceName: string, targetStates: string[]): Promise<string> {
        const rpcArgs: Record<string, unknown> = { notificationService: this._handle, resourceName, targetStates };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/waitForResourceStates',
            rpcArgs
        );
    }

    /** Waits for a resource to become healthy */
    async waitForResourceHealthy(resourceName: string): Promise<ResourceEventDto> {
        const rpcArgs: Record<string, unknown> = { notificationService: this._handle, resourceName };
        return await this._client.invokeCapability<ResourceEventDto>(
            'Aspire.Hosting/waitForResourceHealthy',
            rpcArgs
        );
    }

    /** Waits for all dependencies of a resource to be ready */
    /** @internal */
    async _waitForDependenciesInternal(resource: ResourceBuilderBase): Promise<ResourceNotificationService> {
        const rpcArgs: Record<string, unknown> = { notificationService: this._handle, resource };
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/waitForDependencies',
            rpcArgs
        );
        return this;
    }

    waitForDependencies(resource: ResourceBuilderBase): ResourceNotificationServicePromise {
        return new ResourceNotificationServicePromise(this._waitForDependenciesInternal(resource));
    }

    /** Tries to get the current state of a resource */
    async tryGetResourceState(resourceName: string): Promise<ResourceEventDto> {
        const rpcArgs: Record<string, unknown> = { notificationService: this._handle, resourceName };
        return await this._client.invokeCapability<ResourceEventDto>(
            'Aspire.Hosting/tryGetResourceState',
            rpcArgs
        );
    }

    /** Publishes an update for a resource's state */
    /** @internal */
    async _publishResourceUpdateInternal(resource: ResourceBuilderBase, state?: string, stateStyle?: string): Promise<ResourceNotificationService> {
        const rpcArgs: Record<string, unknown> = { notificationService: this._handle, resource };
        if (state !== undefined) rpcArgs.state = state;
        if (stateStyle !== undefined) rpcArgs.stateStyle = stateStyle;
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/publishResourceUpdate',
            rpcArgs
        );
        return this;
    }

    publishResourceUpdate(resource: ResourceBuilderBase, options?: PublishResourceUpdateOptions): ResourceNotificationServicePromise {
        const state = options?.state;
        const stateStyle = options?.stateStyle;
        return new ResourceNotificationServicePromise(this._publishResourceUpdateInternal(resource, state, stateStyle));
    }

}

/**
 * Thenable wrapper for ResourceNotificationService that enables fluent chaining.
 */
export class ResourceNotificationServicePromise implements PromiseLike<ResourceNotificationService> {
    constructor(private _promise: Promise<ResourceNotificationService>) {}

    then<TResult1 = ResourceNotificationService, TResult2 = never>(
        onfulfilled?: ((value: ResourceNotificationService) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Waits for a resource to reach a specified state */
    waitForResourceState(resourceName: string, options?: WaitForResourceStateOptions): ResourceNotificationServicePromise {
        return new ResourceNotificationServicePromise(this._promise.then(obj => obj.waitForResourceState(resourceName, options)));
    }

    /** Waits for a resource to reach one of the specified states */
    waitForResourceStates(resourceName: string, targetStates: string[]): Promise<string> {
        return this._promise.then(obj => obj.waitForResourceStates(resourceName, targetStates));
    }

    /** Waits for a resource to become healthy */
    waitForResourceHealthy(resourceName: string): Promise<ResourceEventDto> {
        return this._promise.then(obj => obj.waitForResourceHealthy(resourceName));
    }

    /** Waits for all dependencies of a resource to be ready */
    waitForDependencies(resource: ResourceBuilderBase): ResourceNotificationServicePromise {
        return new ResourceNotificationServicePromise(this._promise.then(obj => obj.waitForDependencies(resource)));
    }

    /** Tries to get the current state of a resource */
    tryGetResourceState(resourceName: string): Promise<ResourceEventDto> {
        return this._promise.then(obj => obj.tryGetResourceState(resourceName));
    }

    /** Publishes an update for a resource's state */
    publishResourceUpdate(resource: ResourceBuilderBase, options?: PublishResourceUpdateOptions): ResourceNotificationServicePromise {
        return new ResourceNotificationServicePromise(this._promise.then(obj => obj.publishResourceUpdate(resource, options)));
    }

}

// ============================================================================
// ResourceReadyEvent
// ============================================================================

/**
 * Type class for ResourceReadyEvent.
 */
export class ResourceReadyEvent {
    constructor(private _handle: ResourceReadyEventHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the Resource property */
    resource = {
        get: async (): Promise<Resource> => {
            const handle = await this._client.invokeCapability<IResourceHandle>(
                'Aspire.Hosting.ApplicationModel/ResourceReadyEvent.resource',
                { context: this._handle }
            );
            return new Resource(handle, this._client);
        },
    };

    /** Gets the Services property */
    services = {
        get: async (): Promise<ServiceProvider> => {
            const handle = await this._client.invokeCapability<IServiceProviderHandle>(
                'Aspire.Hosting.ApplicationModel/ResourceReadyEvent.services',
                { context: this._handle }
            );
            return new ServiceProvider(handle, this._client);
        },
    };

}

// ============================================================================
// ResourceStoppedEvent
// ============================================================================

/**
 * Type class for ResourceStoppedEvent.
 */
export class ResourceStoppedEvent {
    constructor(private _handle: ResourceStoppedEventHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the Resource property */
    resource = {
        get: async (): Promise<Resource> => {
            const handle = await this._client.invokeCapability<IResourceHandle>(
                'Aspire.Hosting.ApplicationModel/ResourceStoppedEvent.resource',
                { context: this._handle }
            );
            return new Resource(handle, this._client);
        },
    };

    /** Gets the Services property */
    services = {
        get: async (): Promise<ServiceProvider> => {
            const handle = await this._client.invokeCapability<IServiceProviderHandle>(
                'Aspire.Hosting.ApplicationModel/ResourceStoppedEvent.services',
                { context: this._handle }
            );
            return new ServiceProvider(handle, this._client);
        },
    };

}

// ============================================================================
// ResourceUrlsCallbackContext
// ============================================================================

/**
 * Type class for ResourceUrlsCallbackContext.
 */
export class ResourceUrlsCallbackContext {
    constructor(private _handle: ResourceUrlsCallbackContextHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the Resource property */
    resource = {
        get: async (): Promise<Resource> => {
            const handle = await this._client.invokeCapability<IResourceHandle>(
                'Aspire.Hosting.ApplicationModel/ResourceUrlsCallbackContext.resource',
                { context: this._handle }
            );
            return new Resource(handle, this._client);
        },
    };

    /** Gets the Urls property */
    private _urls?: AspireList<ResourceUrlAnnotation>;
    get urls(): AspireList<ResourceUrlAnnotation> {
        if (!this._urls) {
            this._urls = new AspireList<ResourceUrlAnnotation>(
                this._handle,
                this._client,
                'Aspire.Hosting.ApplicationModel/ResourceUrlsCallbackContext.urls',
                'Aspire.Hosting.ApplicationModel/ResourceUrlsCallbackContext.urls'
            );
        }
        return this._urls;
    }

    /** Gets the CancellationToken property */
    cancellationToken = {
        get: async (): Promise<AbortSignal> => {
            return await this._client.invokeCapability<AbortSignal>(
                'Aspire.Hosting.ApplicationModel/ResourceUrlsCallbackContext.cancellationToken',
                { context: this._handle }
            );
        },
    };

    /** Gets the Logger property */
    logger = {
        get: async (): Promise<Logger> => {
            const handle = await this._client.invokeCapability<ILoggerHandle>(
                'Aspire.Hosting.ApplicationModel/ResourceUrlsCallbackContext.logger',
                { context: this._handle }
            );
            return new Logger(handle, this._client);
        },
    };

    /** Gets the ExecutionContext property */
    executionContext = {
        get: async (): Promise<DistributedApplicationExecutionContext> => {
            const handle = await this._client.invokeCapability<DistributedApplicationExecutionContextHandle>(
                'Aspire.Hosting.ApplicationModel/ResourceUrlsCallbackContext.executionContext',
                { context: this._handle }
            );
            return new DistributedApplicationExecutionContext(handle, this._client);
        },
    };

}

// ============================================================================
// UpdateCommandStateContext
// ============================================================================

/**
 * Type class for UpdateCommandStateContext.
 */
export class UpdateCommandStateContext {
    constructor(private _handle: UpdateCommandStateContextHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the ServiceProvider property */
    serviceProvider = {
        get: async (): Promise<ServiceProvider> => {
            const handle = await this._client.invokeCapability<IServiceProviderHandle>(
                'Aspire.Hosting.ApplicationModel/UpdateCommandStateContext.serviceProvider',
                { context: this._handle }
            );
            return new ServiceProvider(handle, this._client);
        },
    };

}

// ============================================================================
// YarpCluster
// ============================================================================

/**
 * Type class for YarpCluster.
 */
export class YarpCluster {
    constructor(private _handle: YarpClusterHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Sets the forwarder request configuration for the cluster. */
    /** @internal */
    async _withForwarderRequestConfigInternal(config: YarpForwarderRequestConfig): Promise<YarpCluster> {
        const rpcArgs: Record<string, unknown> = { cluster: this._handle, config };
        const result = await this._client.invokeCapability<YarpClusterHandle>(
            'Aspire.Hosting.Yarp/withForwarderRequestConfig',
            rpcArgs
        );
        return new YarpCluster(result, this._client);
    }

    withForwarderRequestConfig(config: YarpForwarderRequestConfig): YarpClusterPromise {
        return new YarpClusterPromise(this._withForwarderRequestConfigInternal(config));
    }

    /** Sets the HTTP client configuration for the cluster. */
    /** @internal */
    async _withHttpClientConfigInternal(config: YarpHttpClientConfig): Promise<YarpCluster> {
        const rpcArgs: Record<string, unknown> = { cluster: this._handle, config };
        const result = await this._client.invokeCapability<YarpClusterHandle>(
            'Aspire.Hosting.Yarp/withHttpClientConfig',
            rpcArgs
        );
        return new YarpCluster(result, this._client);
    }

    withHttpClientConfig(config: YarpHttpClientConfig): YarpClusterPromise {
        return new YarpClusterPromise(this._withHttpClientConfigInternal(config));
    }

    /** Sets the session affinity configuration for the cluster. */
    /** @internal */
    async _withSessionAffinityConfigInternal(config: YarpSessionAffinityConfig): Promise<YarpCluster> {
        const rpcArgs: Record<string, unknown> = { cluster: this._handle, config };
        const result = await this._client.invokeCapability<YarpClusterHandle>(
            'Aspire.Hosting.Yarp/withSessionAffinityConfig',
            rpcArgs
        );
        return new YarpCluster(result, this._client);
    }

    withSessionAffinityConfig(config: YarpSessionAffinityConfig): YarpClusterPromise {
        return new YarpClusterPromise(this._withSessionAffinityConfigInternal(config));
    }

    /** Sets the health check configuration for the cluster. */
    /** @internal */
    async _withHealthCheckConfigInternal(config: YarpHealthCheckConfig): Promise<YarpCluster> {
        const rpcArgs: Record<string, unknown> = { cluster: this._handle, config };
        const result = await this._client.invokeCapability<YarpClusterHandle>(
            'Aspire.Hosting.Yarp/withHealthCheckConfig',
            rpcArgs
        );
        return new YarpCluster(result, this._client);
    }

    withHealthCheckConfig(config: YarpHealthCheckConfig): YarpClusterPromise {
        return new YarpClusterPromise(this._withHealthCheckConfigInternal(config));
    }

    /** Sets the load balancing policy for the cluster. */
    /** @internal */
    async _withLoadBalancingPolicyInternal(policy: string): Promise<YarpCluster> {
        const rpcArgs: Record<string, unknown> = { cluster: this._handle, policy };
        const result = await this._client.invokeCapability<YarpClusterHandle>(
            'Aspire.Hosting.Yarp/withLoadBalancingPolicy',
            rpcArgs
        );
        return new YarpCluster(result, this._client);
    }

    withLoadBalancingPolicy(policy: string): YarpClusterPromise {
        return new YarpClusterPromise(this._withLoadBalancingPolicyInternal(policy));
    }

    /** Sets metadata for the cluster. */
    /** @internal */
    async _withMetadataInternal(metadata: Record<string, string>): Promise<YarpCluster> {
        const rpcArgs: Record<string, unknown> = { cluster: this._handle, metadata };
        const result = await this._client.invokeCapability<YarpClusterHandle>(
            'Aspire.Hosting.Yarp/withClusterMetadata',
            rpcArgs
        );
        return new YarpCluster(result, this._client);
    }

    withMetadata(metadata: Record<string, string>): YarpClusterPromise {
        return new YarpClusterPromise(this._withMetadataInternal(metadata));
    }

}

/**
 * Thenable wrapper for YarpCluster that enables fluent chaining.
 */
export class YarpClusterPromise implements PromiseLike<YarpCluster> {
    constructor(private _promise: Promise<YarpCluster>) {}

    then<TResult1 = YarpCluster, TResult2 = never>(
        onfulfilled?: ((value: YarpCluster) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Sets the forwarder request configuration for the cluster. */
    withForwarderRequestConfig(config: YarpForwarderRequestConfig): YarpClusterPromise {
        return new YarpClusterPromise(this._promise.then(obj => obj.withForwarderRequestConfig(config)));
    }

    /** Sets the HTTP client configuration for the cluster. */
    withHttpClientConfig(config: YarpHttpClientConfig): YarpClusterPromise {
        return new YarpClusterPromise(this._promise.then(obj => obj.withHttpClientConfig(config)));
    }

    /** Sets the session affinity configuration for the cluster. */
    withSessionAffinityConfig(config: YarpSessionAffinityConfig): YarpClusterPromise {
        return new YarpClusterPromise(this._promise.then(obj => obj.withSessionAffinityConfig(config)));
    }

    /** Sets the health check configuration for the cluster. */
    withHealthCheckConfig(config: YarpHealthCheckConfig): YarpClusterPromise {
        return new YarpClusterPromise(this._promise.then(obj => obj.withHealthCheckConfig(config)));
    }

    /** Sets the load balancing policy for the cluster. */
    withLoadBalancingPolicy(policy: string): YarpClusterPromise {
        return new YarpClusterPromise(this._promise.then(obj => obj.withLoadBalancingPolicy(policy)));
    }

    /** Sets metadata for the cluster. */
    withMetadata(metadata: Record<string, string>): YarpClusterPromise {
        return new YarpClusterPromise(this._promise.then(obj => obj.withMetadata(metadata)));
    }

}

// ============================================================================
// YarpRoute
// ============================================================================

/**
 * Type class for YarpRoute.
 */
export class YarpRoute {
    constructor(private _handle: YarpRouteHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Sets the route match criteria. */
    /** @internal */
    async _withMatchInternal(match: YarpRouteMatch): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, match };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withMatch',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withMatch(match: YarpRouteMatch): YarpRoutePromise {
        return new YarpRoutePromise(this._withMatchInternal(match));
    }

    /** Matches requests with the specified path pattern. */
    /** @internal */
    async _withMatchPathInternal(path: string): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, path };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withMatchPath',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withMatchPath(path: string): YarpRoutePromise {
        return new YarpRoutePromise(this._withMatchPathInternal(path));
    }

    /** Matches requests that use the specified HTTP methods. */
    /** @internal */
    async _withMatchMethodsInternal(methods: string[]): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, methods };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withMatchMethods',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withMatchMethods(methods: string[]): YarpRoutePromise {
        return new YarpRoutePromise(this._withMatchMethodsInternal(methods));
    }

    /** Matches requests that contain the specified headers. */
    /** @internal */
    async _withMatchHeadersInternal(headers: YarpRouteHeaderMatch[]): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, headers };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withMatchHeaders',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withMatchHeaders(headers: YarpRouteHeaderMatch[]): YarpRoutePromise {
        return new YarpRoutePromise(this._withMatchHeadersInternal(headers));
    }

    /** Matches requests that contain the specified host headers. */
    /** @internal */
    async _withMatchHostsInternal(hosts: string[]): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, hosts };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withMatchHosts',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withMatchHosts(hosts: string[]): YarpRoutePromise {
        return new YarpRoutePromise(this._withMatchHostsInternal(hosts));
    }

    /** Matches requests that contain the specified query parameters. */
    /** @internal */
    async _withMatchRouteQueryParameterInternal(queryParameters: YarpRouteQueryParameterMatch[]): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, queryParameters };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withMatchRouteQueryParameter',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withMatchRouteQueryParameter(queryParameters: YarpRouteQueryParameterMatch[]): YarpRoutePromise {
        return new YarpRoutePromise(this._withMatchRouteQueryParameterInternal(queryParameters));
    }

    /** Sets the route order. */
    /** @internal */
    async _withOrderInternal(order?: number): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle };
        if (order !== undefined) rpcArgs.order = order;
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withOrder',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withOrder(options?: WithOrderOptions): YarpRoutePromise {
        const order = options?.order;
        return new YarpRoutePromise(this._withOrderInternal(order));
    }

    /** Sets the maximum request body size for the route. */
    /** @internal */
    async _withMaxRequestBodySizeInternal(maxRequestBodySize: number): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, maxRequestBodySize };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withMaxRequestBodySize',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withMaxRequestBodySize(maxRequestBodySize: number): YarpRoutePromise {
        return new YarpRoutePromise(this._withMaxRequestBodySizeInternal(maxRequestBodySize));
    }

    /** Sets metadata for the route. */
    /** @internal */
    async _withMetadataInternal(metadata: Record<string, string>): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, metadata };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withRouteMetadata',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withMetadata(metadata: Record<string, string>): YarpRoutePromise {
        return new YarpRoutePromise(this._withMetadataInternal(metadata));
    }

    /** Sets the transforms for the route. */
    /** @internal */
    async _withTransformsInternal(transforms: Record<string, string>[]): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, transforms };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransforms',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransforms(transforms: Record<string, string>[]): YarpRoutePromise {
        return new YarpRoutePromise(this._withTransformsInternal(transforms));
    }

    /** Adds a transform to the route. */
    /** @internal */
    async _withTransformInternal(transform: Record<string, string>): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, transform };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransform',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransform(transform: Record<string, string>): YarpRoutePromise {
        return new YarpRoutePromise(this._withTransformInternal(transform));
    }

    /** Adds the transform which will add X-Forwarded-* headers. */
    /** @internal */
    async _withTransformXForwardedInternal(headerPrefix?: string, xDefault?: ForwardedTransformActions, xFor?: ForwardedTransformActions, xHost?: ForwardedTransformActions, xProto?: ForwardedTransformActions, xPrefix?: ForwardedTransformActions): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle };
        if (headerPrefix !== undefined) rpcArgs.headerPrefix = headerPrefix;
        if (xDefault !== undefined) rpcArgs.xDefault = xDefault;
        if (xFor !== undefined) rpcArgs.xFor = xFor;
        if (xHost !== undefined) rpcArgs.xHost = xHost;
        if (xProto !== undefined) rpcArgs.xProto = xProto;
        if (xPrefix !== undefined) rpcArgs.xPrefix = xPrefix;
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformXForwarded',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformXForwarded(options?: WithTransformXForwardedOptions): YarpRoutePromise {
        const headerPrefix = options?.headerPrefix;
        const xDefault = options?.xDefault;
        const xFor = options?.xFor;
        const xHost = options?.xHost;
        const xProto = options?.xProto;
        const xPrefix = options?.xPrefix;
        return new YarpRoutePromise(this._withTransformXForwardedInternal(headerPrefix, xDefault, xFor, xHost, xProto, xPrefix));
    }

    /** Adds the transform which will add the Forwarded header as defined by [RFC 7239](https://tools.ietf.org/html/rfc7239). */
    /** @internal */
    async _withTransformForwardedInternal(useHost?: boolean, useProto?: boolean, forFormat?: NodeFormat, byFormat?: NodeFormat, action?: ForwardedTransformActions): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle };
        if (useHost !== undefined) rpcArgs.useHost = useHost;
        if (useProto !== undefined) rpcArgs.useProto = useProto;
        if (forFormat !== undefined) rpcArgs.forFormat = forFormat;
        if (byFormat !== undefined) rpcArgs.byFormat = byFormat;
        if (action !== undefined) rpcArgs.action = action;
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformForwarded',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformForwarded(options?: WithTransformForwardedOptions): YarpRoutePromise {
        const useHost = options?.useHost;
        const useProto = options?.useProto;
        const forFormat = options?.forFormat;
        const byFormat = options?.byFormat;
        const action = options?.action;
        return new YarpRoutePromise(this._withTransformForwardedInternal(useHost, useProto, forFormat, byFormat, action));
    }

    /** Adds the transform which will set the given header with the Base64 encoded client certificate. */
    /** @internal */
    async _withTransformClientCertHeaderInternal(headerName: string): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, headerName };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformClientCertHeader',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformClientCertHeader(headerName: string): YarpRoutePromise {
        return new YarpRoutePromise(this._withTransformClientCertHeaderInternal(headerName));
    }

    /** Adds the transform that will replace the HTTP method if it matches. */
    /** @internal */
    async _withTransformHttpMethodChangeInternal(fromHttpMethod: string, toHttpMethod: string): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, fromHttpMethod, toHttpMethod };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformHttpMethodChange',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformHttpMethodChange(fromHttpMethod: string, toHttpMethod: string): YarpRoutePromise {
        return new YarpRoutePromise(this._withTransformHttpMethodChangeInternal(fromHttpMethod, toHttpMethod));
    }

    /** Adds the transform which sets the request path with the given value. */
    /** @internal */
    async _withTransformPathSetInternal(path: string): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, path };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformPathSet',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformPathSet(path: string): YarpRoutePromise {
        return new YarpRoutePromise(this._withTransformPathSetInternal(path));
    }

    /** Adds the transform which will prefix the request path with the given value. */
    /** @internal */
    async _withTransformPathPrefixInternal(prefix: string): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, prefix };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformPathPrefix',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformPathPrefix(prefix: string): YarpRoutePromise {
        return new YarpRoutePromise(this._withTransformPathPrefixInternal(prefix));
    }

    /** Adds the transform which will remove the matching prefix from the request path. */
    /** @internal */
    async _withTransformPathRemovePrefixInternal(prefix: string): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, prefix };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformPathRemovePrefix',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformPathRemovePrefix(prefix: string): YarpRoutePromise {
        return new YarpRoutePromise(this._withTransformPathRemovePrefixInternal(prefix));
    }

    /** Adds the transform which will set the request path with route values. */
    /** @internal */
    async _withTransformPathRouteValuesInternal(pattern: string): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, pattern };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformPathRouteValues',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformPathRouteValues(pattern: string): YarpRoutePromise {
        return new YarpRoutePromise(this._withTransformPathRouteValuesInternal(pattern));
    }

    /** Adds the transform that will append or set the query parameter from the given value. */
    /** @internal */
    async _withTransformQueryValueInternal(queryKey: string, value: string, append?: boolean): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, queryKey, value };
        if (append !== undefined) rpcArgs.append = append;
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformQueryValue',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformQueryValue(queryKey: string, value: string, options?: WithTransformQueryValueOptions): YarpRoutePromise {
        const append = options?.append;
        return new YarpRoutePromise(this._withTransformQueryValueInternal(queryKey, value, append));
    }

    /** Adds the transform that will append or set the query parameter from a route value. */
    /** @internal */
    async _withTransformQueryRouteValueInternal(queryKey: string, routeValueKey: string, append?: boolean): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, queryKey, routeValueKey };
        if (append !== undefined) rpcArgs.append = append;
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformQueryRouteValue',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformQueryRouteValue(queryKey: string, routeValueKey: string, options?: WithTransformQueryRouteValueOptions): YarpRoutePromise {
        const append = options?.append;
        return new YarpRoutePromise(this._withTransformQueryRouteValueInternal(queryKey, routeValueKey, append));
    }

    /** Adds the transform that will remove the given query key. */
    /** @internal */
    async _withTransformQueryRemoveKeyInternal(queryKey: string): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, queryKey };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformQueryRemoveKey',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformQueryRemoveKey(queryKey: string): YarpRoutePromise {
        return new YarpRoutePromise(this._withTransformQueryRemoveKeyInternal(queryKey));
    }

    /** Adds the transform which will enable or suppress copying request headers to the proxy request. */
    /** @internal */
    async _withTransformCopyRequestHeadersInternal(copy?: boolean): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle };
        if (copy !== undefined) rpcArgs.copy = copy;
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformCopyRequestHeaders',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformCopyRequestHeaders(options?: WithTransformCopyRequestHeadersOptions): YarpRoutePromise {
        const copy = options?.copy;
        return new YarpRoutePromise(this._withTransformCopyRequestHeadersInternal(copy));
    }

    /** Adds the transform which will copy the incoming request Host header to the proxy request. */
    /** @internal */
    async _withTransformUseOriginalHostHeaderInternal(useOriginal?: boolean): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle };
        if (useOriginal !== undefined) rpcArgs.useOriginal = useOriginal;
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformUseOriginalHostHeader',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformUseOriginalHostHeader(options?: WithTransformUseOriginalHostHeaderOptions): YarpRoutePromise {
        const useOriginal = options?.useOriginal;
        return new YarpRoutePromise(this._withTransformUseOriginalHostHeaderInternal(useOriginal));
    }

    /** Adds the transform which will append or set the request header. */
    /** @internal */
    async _withTransformRequestHeaderInternal(headerName: string, value: string, append?: boolean): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, headerName, value };
        if (append !== undefined) rpcArgs.append = append;
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformRequestHeader',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformRequestHeader(headerName: string, value: string, options?: WithTransformRequestHeaderOptions): YarpRoutePromise {
        const append = options?.append;
        return new YarpRoutePromise(this._withTransformRequestHeaderInternal(headerName, value, append));
    }

    /** Adds the transform which will append or set the request header from a route value. */
    /** @internal */
    async _withTransformRequestHeaderRouteValueInternal(headerName: string, routeValueKey: string, append?: boolean): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, headerName, routeValueKey };
        if (append !== undefined) rpcArgs.append = append;
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformRequestHeaderRouteValue',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformRequestHeaderRouteValue(headerName: string, routeValueKey: string, options?: WithTransformRequestHeaderRouteValueOptions): YarpRoutePromise {
        const append = options?.append;
        return new YarpRoutePromise(this._withTransformRequestHeaderRouteValueInternal(headerName, routeValueKey, append));
    }

    /** Adds the transform which will remove the request header. */
    /** @internal */
    async _withTransformRequestHeaderRemoveInternal(headerName: string): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, headerName };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformRequestHeaderRemove',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformRequestHeaderRemove(headerName: string): YarpRoutePromise {
        return new YarpRoutePromise(this._withTransformRequestHeaderRemoveInternal(headerName));
    }

    /** Adds the transform which will only copy the allowed request headers. Other transforms */
    /** @internal */
    async _withTransformRequestHeadersAllowedInternal(allowedHeaders: string[]): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, allowedHeaders };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformRequestHeadersAllowed',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformRequestHeadersAllowed(allowedHeaders: string[]): YarpRoutePromise {
        return new YarpRoutePromise(this._withTransformRequestHeadersAllowedInternal(allowedHeaders));
    }

    /** Adds the transform which will enable or suppress copying response headers to the client response. */
    /** @internal */
    async _withTransformCopyResponseHeadersInternal(copy?: boolean): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle };
        if (copy !== undefined) rpcArgs.copy = copy;
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformCopyResponseHeaders',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformCopyResponseHeaders(options?: WithTransformCopyResponseHeadersOptions): YarpRoutePromise {
        const copy = options?.copy;
        return new YarpRoutePromise(this._withTransformCopyResponseHeadersInternal(copy));
    }

    /** Adds the transform which will enable or suppress copying response trailers to the client response. */
    /** @internal */
    async _withTransformCopyResponseTrailersInternal(copy?: boolean): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle };
        if (copy !== undefined) rpcArgs.copy = copy;
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformCopyResponseTrailers',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformCopyResponseTrailers(options?: WithTransformCopyResponseTrailersOptions): YarpRoutePromise {
        const copy = options?.copy;
        return new YarpRoutePromise(this._withTransformCopyResponseTrailersInternal(copy));
    }

    /** Adds the transform which will append or set the response header. */
    /** @internal */
    async _withTransformResponseHeaderInternal(headerName: string, value: string, append?: boolean, condition?: ResponseCondition): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, headerName, value };
        if (append !== undefined) rpcArgs.append = append;
        if (condition !== undefined) rpcArgs.condition = condition;
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformResponseHeader',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformResponseHeader(headerName: string, value: string, options?: WithTransformResponseHeaderOptions): YarpRoutePromise {
        const append = options?.append;
        const condition = options?.condition;
        return new YarpRoutePromise(this._withTransformResponseHeaderInternal(headerName, value, append, condition));
    }

    /** Adds the transform which will remove the response header. */
    /** @internal */
    async _withTransformResponseHeaderRemoveInternal(headerName: string, condition?: ResponseCondition): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, headerName };
        if (condition !== undefined) rpcArgs.condition = condition;
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformResponseHeaderRemove',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformResponseHeaderRemove(headerName: string, options?: WithTransformResponseHeaderRemoveOptions): YarpRoutePromise {
        const condition = options?.condition;
        return new YarpRoutePromise(this._withTransformResponseHeaderRemoveInternal(headerName, condition));
    }

    /** Adds the transform which will only copy the allowed response headers. Other transforms */
    /** @internal */
    async _withTransformResponseHeadersAllowedInternal(allowedHeaders: string[]): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, allowedHeaders };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformResponseHeadersAllowed',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformResponseHeadersAllowed(allowedHeaders: string[]): YarpRoutePromise {
        return new YarpRoutePromise(this._withTransformResponseHeadersAllowedInternal(allowedHeaders));
    }

    /** Adds the transform which will append or set the response trailer. */
    /** @internal */
    async _withTransformResponseTrailerInternal(headerName: string, value: string, append?: boolean, condition?: ResponseCondition): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, headerName, value };
        if (append !== undefined) rpcArgs.append = append;
        if (condition !== undefined) rpcArgs.condition = condition;
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformResponseTrailer',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformResponseTrailer(headerName: string, value: string, options?: WithTransformResponseTrailerOptions): YarpRoutePromise {
        const append = options?.append;
        const condition = options?.condition;
        return new YarpRoutePromise(this._withTransformResponseTrailerInternal(headerName, value, append, condition));
    }

    /** Adds the transform which will remove the response trailer. */
    /** @internal */
    async _withTransformResponseTrailerRemoveInternal(headerName: string, condition?: ResponseCondition): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, headerName };
        if (condition !== undefined) rpcArgs.condition = condition;
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformResponseTrailerRemove',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformResponseTrailerRemove(headerName: string, options?: WithTransformResponseTrailerRemoveOptions): YarpRoutePromise {
        const condition = options?.condition;
        return new YarpRoutePromise(this._withTransformResponseTrailerRemoveInternal(headerName, condition));
    }

    /** Adds the transform which will only copy the allowed response trailers. Other transforms */
    /** @internal */
    async _withTransformResponseTrailersAllowedInternal(allowedHeaders: string[]): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { route: this._handle, allowedHeaders };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/withTransformResponseTrailersAllowed',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    withTransformResponseTrailersAllowed(allowedHeaders: string[]): YarpRoutePromise {
        return new YarpRoutePromise(this._withTransformResponseTrailersAllowedInternal(allowedHeaders));
    }

}

/**
 * Thenable wrapper for YarpRoute that enables fluent chaining.
 */
export class YarpRoutePromise implements PromiseLike<YarpRoute> {
    constructor(private _promise: Promise<YarpRoute>) {}

    then<TResult1 = YarpRoute, TResult2 = never>(
        onfulfilled?: ((value: YarpRoute) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Sets the route match criteria. */
    withMatch(match: YarpRouteMatch): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withMatch(match)));
    }

    /** Matches requests with the specified path pattern. */
    withMatchPath(path: string): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withMatchPath(path)));
    }

    /** Matches requests that use the specified HTTP methods. */
    withMatchMethods(methods: string[]): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withMatchMethods(methods)));
    }

    /** Matches requests that contain the specified headers. */
    withMatchHeaders(headers: YarpRouteHeaderMatch[]): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withMatchHeaders(headers)));
    }

    /** Matches requests that contain the specified host headers. */
    withMatchHosts(hosts: string[]): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withMatchHosts(hosts)));
    }

    /** Matches requests that contain the specified query parameters. */
    withMatchRouteQueryParameter(queryParameters: YarpRouteQueryParameterMatch[]): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withMatchRouteQueryParameter(queryParameters)));
    }

    /** Sets the route order. */
    withOrder(options?: WithOrderOptions): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withOrder(options)));
    }

    /** Sets the maximum request body size for the route. */
    withMaxRequestBodySize(maxRequestBodySize: number): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withMaxRequestBodySize(maxRequestBodySize)));
    }

    /** Sets metadata for the route. */
    withMetadata(metadata: Record<string, string>): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withMetadata(metadata)));
    }

    /** Sets the transforms for the route. */
    withTransforms(transforms: Record<string, string>[]): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransforms(transforms)));
    }

    /** Adds a transform to the route. */
    withTransform(transform: Record<string, string>): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransform(transform)));
    }

    /** Adds the transform which will add X-Forwarded-* headers. */
    withTransformXForwarded(options?: WithTransformXForwardedOptions): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformXForwarded(options)));
    }

    /** Adds the transform which will add the Forwarded header as defined by [RFC 7239](https://tools.ietf.org/html/rfc7239). */
    withTransformForwarded(options?: WithTransformForwardedOptions): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformForwarded(options)));
    }

    /** Adds the transform which will set the given header with the Base64 encoded client certificate. */
    withTransformClientCertHeader(headerName: string): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformClientCertHeader(headerName)));
    }

    /** Adds the transform that will replace the HTTP method if it matches. */
    withTransformHttpMethodChange(fromHttpMethod: string, toHttpMethod: string): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformHttpMethodChange(fromHttpMethod, toHttpMethod)));
    }

    /** Adds the transform which sets the request path with the given value. */
    withTransformPathSet(path: string): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformPathSet(path)));
    }

    /** Adds the transform which will prefix the request path with the given value. */
    withTransformPathPrefix(prefix: string): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformPathPrefix(prefix)));
    }

    /** Adds the transform which will remove the matching prefix from the request path. */
    withTransformPathRemovePrefix(prefix: string): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformPathRemovePrefix(prefix)));
    }

    /** Adds the transform which will set the request path with route values. */
    withTransformPathRouteValues(pattern: string): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformPathRouteValues(pattern)));
    }

    /** Adds the transform that will append or set the query parameter from the given value. */
    withTransformQueryValue(queryKey: string, value: string, options?: WithTransformQueryValueOptions): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformQueryValue(queryKey, value, options)));
    }

    /** Adds the transform that will append or set the query parameter from a route value. */
    withTransformQueryRouteValue(queryKey: string, routeValueKey: string, options?: WithTransformQueryRouteValueOptions): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformQueryRouteValue(queryKey, routeValueKey, options)));
    }

    /** Adds the transform that will remove the given query key. */
    withTransformQueryRemoveKey(queryKey: string): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformQueryRemoveKey(queryKey)));
    }

    /** Adds the transform which will enable or suppress copying request headers to the proxy request. */
    withTransformCopyRequestHeaders(options?: WithTransformCopyRequestHeadersOptions): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformCopyRequestHeaders(options)));
    }

    /** Adds the transform which will copy the incoming request Host header to the proxy request. */
    withTransformUseOriginalHostHeader(options?: WithTransformUseOriginalHostHeaderOptions): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformUseOriginalHostHeader(options)));
    }

    /** Adds the transform which will append or set the request header. */
    withTransformRequestHeader(headerName: string, value: string, options?: WithTransformRequestHeaderOptions): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformRequestHeader(headerName, value, options)));
    }

    /** Adds the transform which will append or set the request header from a route value. */
    withTransformRequestHeaderRouteValue(headerName: string, routeValueKey: string, options?: WithTransformRequestHeaderRouteValueOptions): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformRequestHeaderRouteValue(headerName, routeValueKey, options)));
    }

    /** Adds the transform which will remove the request header. */
    withTransformRequestHeaderRemove(headerName: string): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformRequestHeaderRemove(headerName)));
    }

    /** Adds the transform which will only copy the allowed request headers. Other transforms */
    withTransformRequestHeadersAllowed(allowedHeaders: string[]): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformRequestHeadersAllowed(allowedHeaders)));
    }

    /** Adds the transform which will enable or suppress copying response headers to the client response. */
    withTransformCopyResponseHeaders(options?: WithTransformCopyResponseHeadersOptions): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformCopyResponseHeaders(options)));
    }

    /** Adds the transform which will enable or suppress copying response trailers to the client response. */
    withTransformCopyResponseTrailers(options?: WithTransformCopyResponseTrailersOptions): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformCopyResponseTrailers(options)));
    }

    /** Adds the transform which will append or set the response header. */
    withTransformResponseHeader(headerName: string, value: string, options?: WithTransformResponseHeaderOptions): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformResponseHeader(headerName, value, options)));
    }

    /** Adds the transform which will remove the response header. */
    withTransformResponseHeaderRemove(headerName: string, options?: WithTransformResponseHeaderRemoveOptions): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformResponseHeaderRemove(headerName, options)));
    }

    /** Adds the transform which will only copy the allowed response headers. Other transforms */
    withTransformResponseHeadersAllowed(allowedHeaders: string[]): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformResponseHeadersAllowed(allowedHeaders)));
    }

    /** Adds the transform which will append or set the response trailer. */
    withTransformResponseTrailer(headerName: string, value: string, options?: WithTransformResponseTrailerOptions): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformResponseTrailer(headerName, value, options)));
    }

    /** Adds the transform which will remove the response trailer. */
    withTransformResponseTrailerRemove(headerName: string, options?: WithTransformResponseTrailerRemoveOptions): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformResponseTrailerRemove(headerName, options)));
    }

    /** Adds the transform which will only copy the allowed response trailers. Other transforms */
    withTransformResponseTrailersAllowed(allowedHeaders: string[]): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.withTransformResponseTrailersAllowed(allowedHeaders)));
    }

}

// ============================================================================
// Configuration
// ============================================================================

/**
 * Type class for Configuration.
 */
export class Configuration {
    constructor(private _handle: IConfigurationHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets a configuration value by key */
    async getConfigValue(key: string): Promise<string> {
        const rpcArgs: Record<string, unknown> = { configuration: this._handle, key };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getConfigValue',
            rpcArgs
        );
    }

    /** Gets a connection string by name */
    async getConnectionString(name: string): Promise<string> {
        const rpcArgs: Record<string, unknown> = { configuration: this._handle, name };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getConnectionString',
            rpcArgs
        );
    }

    /** Gets a configuration section by key */
    async getSection(key: string): Promise<IConfigurationSectionHandle> {
        const rpcArgs: Record<string, unknown> = { configuration: this._handle, key };
        return await this._client.invokeCapability<IConfigurationSectionHandle>(
            'Aspire.Hosting/getSection',
            rpcArgs
        );
    }

    /** Gets child configuration sections */
    async getChildren(): Promise<IConfigurationSectionHandle[]> {
        const rpcArgs: Record<string, unknown> = { configuration: this._handle };
        return await this._client.invokeCapability<IConfigurationSectionHandle[]>(
            'Aspire.Hosting/getChildren',
            rpcArgs
        );
    }

    /** Checks whether a configuration section exists */
    async exists(key: string): Promise<boolean> {
        const rpcArgs: Record<string, unknown> = { configuration: this._handle, key };
        return await this._client.invokeCapability<boolean>(
            'Aspire.Hosting/exists',
            rpcArgs
        );
    }

}

/**
 * Thenable wrapper for Configuration that enables fluent chaining.
 */
export class ConfigurationPromise implements PromiseLike<Configuration> {
    constructor(private _promise: Promise<Configuration>) {}

    then<TResult1 = Configuration, TResult2 = never>(
        onfulfilled?: ((value: Configuration) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Gets a configuration value by key */
    getConfigValue(key: string): Promise<string> {
        return this._promise.then(obj => obj.getConfigValue(key));
    }

    /** Gets a connection string by name */
    getConnectionString(name: string): Promise<string> {
        return this._promise.then(obj => obj.getConnectionString(name));
    }

    /** Gets a configuration section by key */
    getSection(key: string): Promise<IConfigurationSectionHandle> {
        return this._promise.then(obj => obj.getSection(key));
    }

    /** Gets child configuration sections */
    getChildren(): Promise<IConfigurationSectionHandle[]> {
        return this._promise.then(obj => obj.getChildren());
    }

    /** Checks whether a configuration section exists */
    exists(key: string): Promise<boolean> {
        return this._promise.then(obj => obj.exists(key));
    }

}

// ============================================================================
// DistributedApplicationBuilder
// ============================================================================

/**
 * Type class for DistributedApplicationBuilder.
 */
export class DistributedApplicationBuilder {
    constructor(private _handle: IDistributedApplicationBuilderHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the AppHostDirectory property */
    appHostDirectory = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting/IDistributedApplicationBuilder.appHostDirectory',
                { context: this._handle }
            );
        },
    };

    /** Gets the Environment property */
    environment = {
        get: async (): Promise<HostEnvironment> => {
            const handle = await this._client.invokeCapability<IHostEnvironmentHandle>(
                'Aspire.Hosting/IDistributedApplicationBuilder.environment',
                { context: this._handle }
            );
            return new HostEnvironment(handle, this._client);
        },
    };

    /** Gets the Eventing property */
    eventing = {
        get: async (): Promise<DistributedApplicationEventing> => {
            const handle = await this._client.invokeCapability<IDistributedApplicationEventingHandle>(
                'Aspire.Hosting/IDistributedApplicationBuilder.eventing',
                { context: this._handle }
            );
            return new DistributedApplicationEventing(handle, this._client);
        },
    };

    /** Gets the ExecutionContext property */
    executionContext = {
        get: async (): Promise<DistributedApplicationExecutionContext> => {
            const handle = await this._client.invokeCapability<DistributedApplicationExecutionContextHandle>(
                'Aspire.Hosting/IDistributedApplicationBuilder.executionContext',
                { context: this._handle }
            );
            return new DistributedApplicationExecutionContext(handle, this._client);
        },
    };

    /** Gets the UserSecretsManager property */
    userSecretsManager = {
        get: async (): Promise<UserSecretsManager> => {
            const handle = await this._client.invokeCapability<IUserSecretsManagerHandle>(
                'Aspire.Hosting/IDistributedApplicationBuilder.userSecretsManager',
                { context: this._handle }
            );
            return new UserSecretsManager(handle, this._client);
        },
    };

    /** Builds the distributed application */
    /** @internal */
    async _buildInternal(): Promise<DistributedApplication> {
        const rpcArgs: Record<string, unknown> = { context: this._handle };
        const result = await this._client.invokeCapability<DistributedApplicationHandle>(
            'Aspire.Hosting/build',
            rpcArgs
        );
        return new DistributedApplication(result, this._client);
    }

    build(): DistributedApplicationPromise {
        return new DistributedApplicationPromise(this._buildInternal());
    }

    /** Adds a connection string with a builder callback */
    /** @internal */
    async _addConnectionStringBuilderInternal(name: string, connectionStringBuilder: (obj: ReferenceExpressionBuilder) => Promise<void>): Promise<ConnectionStringResource> {
        const connectionStringBuilderId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ReferenceExpressionBuilderHandle;
            const obj = new ReferenceExpressionBuilder(objHandle, this._client);
            await connectionStringBuilder(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, connectionStringBuilder: connectionStringBuilderId };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/addConnectionStringBuilder',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    addConnectionStringBuilder(name: string, connectionStringBuilder: (obj: ReferenceExpressionBuilder) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._addConnectionStringBuilderInternal(name, connectionStringBuilder));
    }

    /** Adds a container registry resource */
    /** @internal */
    async _addContainerRegistryInternal(name: string, endpoint: ParameterResource, repository?: ParameterResource): Promise<ContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, endpoint };
        if (repository !== undefined) rpcArgs.repository = repository;
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/addContainerRegistry',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    addContainerRegistry(name: string, endpoint: ParameterResource, options?: AddContainerRegistryOptions): ContainerRegistryResourcePromise {
        const repository = options?.repository;
        return new ContainerRegistryResourcePromise(this._addContainerRegistryInternal(name, endpoint, repository));
    }

    /** Adds a container resource */
    /** @internal */
    async _addContainerInternal(name: string, image: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, image };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/addContainer',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    addContainer(name: string, image: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._addContainerInternal(name, image));
    }

    /** Adds a container resource built from a Dockerfile */
    /** @internal */
    async _addDockerfileInternal(name: string, contextPath: string, dockerfilePath?: string, stage?: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, contextPath };
        if (dockerfilePath !== undefined) rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined) rpcArgs.stage = stage;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/addDockerfile',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    addDockerfile(name: string, contextPath: string, options?: AddDockerfileOptions): ContainerResourcePromise {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new ContainerResourcePromise(this._addDockerfileInternal(name, contextPath, dockerfilePath, stage));
    }

    /** Adds a .NET tool resource */
    /** @internal */
    async _addDotnetToolInternal(name: string, packageId: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, packageId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/addDotnetTool',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    addDotnetTool(name: string, packageId: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._addDotnetToolInternal(name, packageId));
    }

    /** Adds an executable resource */
    /** @internal */
    async _addExecutableInternal(name: string, command: string, workingDirectory: string, args: string[]): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, command, workingDirectory, args };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/addExecutable',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    addExecutable(name: string, command: string, workingDirectory: string, args: string[]): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._addExecutableInternal(name, command, workingDirectory, args));
    }

    /** Adds an external service resource */
    /** @internal */
    async _addExternalServiceInternal(name: string, url: string): Promise<ExternalServiceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, url };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/addExternalService',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    addExternalService(name: string, url: string): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._addExternalServiceInternal(name, url));
    }

    /** Adds a parameter resource */
    /** @internal */
    async _addParameterInternal(name: string, secret?: boolean): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        if (secret !== undefined) rpcArgs.secret = secret;
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/addParameter',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    addParameter(name: string, options?: AddParameterOptions): ParameterResourcePromise {
        const secret = options?.secret;
        return new ParameterResourcePromise(this._addParameterInternal(name, secret));
    }

    /** Adds a parameter sourced from configuration */
    /** @internal */
    async _addParameterFromConfigurationInternal(name: string, configurationKey: string, secret?: boolean): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, configurationKey };
        if (secret !== undefined) rpcArgs.secret = secret;
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/addParameterFromConfiguration',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    addParameterFromConfiguration(name: string, configurationKey: string, options?: AddParameterFromConfigurationOptions): ParameterResourcePromise {
        const secret = options?.secret;
        return new ParameterResourcePromise(this._addParameterFromConfigurationInternal(name, configurationKey, secret));
    }

    /** Adds a connection string resource */
    /** @internal */
    async _addConnectionStringInternal(name: string, environmentVariableName?: string): Promise<ResourceWithConnectionString> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        if (environmentVariableName !== undefined) rpcArgs.environmentVariableName = environmentVariableName;
        const result = await this._client.invokeCapability<IResourceWithConnectionStringHandle>(
            'Aspire.Hosting/addConnectionString',
            rpcArgs
        );
        return new ResourceWithConnectionString(result, this._client);
    }

    addConnectionString(name: string, options?: AddConnectionStringOptions): ResourceWithConnectionStringPromise {
        const environmentVariableName = options?.environmentVariableName;
        return new ResourceWithConnectionStringPromise(this._addConnectionStringInternal(name, environmentVariableName));
    }

    /** Adds a .NET project resource */
    /** @internal */
    async _addProjectInternal(name: string, projectPath: string, launchProfileName: string): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, projectPath, launchProfileName };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/addProject',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    addProject(name: string, projectPath: string, launchProfileName: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._addProjectInternal(name, projectPath, launchProfileName));
    }

    /** Adds a project resource with configuration options */
    /** @internal */
    async _addProjectWithOptionsInternal(name: string, projectPath: string, configure: (obj: ProjectResourceOptions) => Promise<void>): Promise<ProjectResource> {
        const configureId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ProjectResourceOptionsHandle;
            const obj = new ProjectResourceOptions(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, projectPath, configure: configureId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/addProjectWithOptions',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    addProjectWithOptions(name: string, projectPath: string, configure: (obj: ProjectResourceOptions) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._addProjectWithOptionsInternal(name, projectPath, configure));
    }

    /** Adds a C# application resource */
    /** @internal */
    async _addCSharpAppInternal(name: string, path: string): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, path };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/addCSharpApp',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    addCSharpApp(name: string, path: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._addCSharpAppInternal(name, path));
    }

    /** Adds a C# application resource with configuration options */
    /** @internal */
    async _addCSharpAppWithOptionsInternal(name: string, path: string, configure: (obj: ProjectResourceOptions) => Promise<void>): Promise<CSharpAppResource> {
        const configureId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ProjectResourceOptionsHandle;
            const obj = new ProjectResourceOptions(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, path, configure: configureId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/addCSharpAppWithOptions',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    addCSharpAppWithOptions(name: string, path: string, configure: (obj: ProjectResourceOptions) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._addCSharpAppWithOptionsInternal(name, path, configure));
    }

    /** Gets the application configuration */
    /** @internal */
    async _getConfigurationInternal(): Promise<Configuration> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<IConfigurationHandle>(
            'Aspire.Hosting/getConfiguration',
            rpcArgs
        );
        return new Configuration(result, this._client);
    }

    getConfiguration(): ConfigurationPromise {
        return new ConfigurationPromise(this._getConfigurationInternal());
    }

    /** Subscribes to the BeforeStart event */
    async subscribeBeforeStart(callback: (arg: BeforeStartEvent) => Promise<void>): Promise<DistributedApplicationEventSubscriptionHandle> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeStartEventHandle;
            const arg = new BeforeStartEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        return await this._client.invokeCapability<DistributedApplicationEventSubscriptionHandle>(
            'Aspire.Hosting/subscribeBeforeStart',
            rpcArgs
        );
    }

    /** Subscribes to the AfterResourcesCreated event */
    async subscribeAfterResourcesCreated(callback: (arg: AfterResourcesCreatedEvent) => Promise<void>): Promise<DistributedApplicationEventSubscriptionHandle> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as AfterResourcesCreatedEventHandle;
            const arg = new AfterResourcesCreatedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        return await this._client.invokeCapability<DistributedApplicationEventSubscriptionHandle>(
            'Aspire.Hosting/subscribeAfterResourcesCreated',
            rpcArgs
        );
    }

    /** Adds a Redis container resource with specific port */
    /** @internal */
    async _addRedisWithPortInternal(name: string, port?: number): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        if (port !== undefined) rpcArgs.port = port;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Redis/addRedisWithPort',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    addRedisWithPort(name: string, options?: AddRedisWithPortOptions): RedisResourcePromise {
        const port = options?.port;
        return new RedisResourcePromise(this._addRedisWithPortInternal(name, port));
    }

    /** Adds a Redis container resource */
    /** @internal */
    async _addRedisInternal(name: string, port?: number, password?: ParameterResource): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        if (port !== undefined) rpcArgs.port = port;
        if (password !== undefined) rpcArgs.password = password;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Redis/addRedis',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    addRedis(name: string, options?: AddRedisOptions): RedisResourcePromise {
        const port = options?.port;
        const password = options?.password;
        return new RedisResourcePromise(this._addRedisInternal(name, port, password));
    }

    /** Adds a RabbitMQ container resource */
    /** @internal */
    async _addRabbitMQInternal(name: string, userName?: ParameterResource, password?: ParameterResource, port?: number): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        if (userName !== undefined) rpcArgs.userName = userName;
        if (password !== undefined) rpcArgs.password = password;
        if (port !== undefined) rpcArgs.port = port;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting.RabbitMQ/addRabbitMQ',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    addRabbitMQ(name: string, options?: AddRabbitMQOptions): RabbitMQServerResourcePromise {
        const userName = options?.userName;
        const password = options?.password;
        const port = options?.port;
        return new RabbitMQServerResourcePromise(this._addRabbitMQInternal(name, userName, password, port));
    }

    /** Adds a PostgreSQL server resource */
    /** @internal */
    async _addPostgresInternal(name: string, userName?: ParameterResource, password?: ParameterResource, port?: number): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        if (userName !== undefined) rpcArgs.userName = userName;
        if (password !== undefined) rpcArgs.password = password;
        if (port !== undefined) rpcArgs.port = port;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.PostgreSQL/addPostgres',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    addPostgres(name: string, options?: AddPostgresOptions): PostgresServerResourcePromise {
        const userName = options?.userName;
        const password = options?.password;
        const port = options?.port;
        return new PostgresServerResourcePromise(this._addPostgresInternal(name, userName, password, port));
    }

    /** Adds an Azure OpenAI resource */
    /** @internal */
    async _addAzureOpenAIInternal(name: string): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/addAzureOpenAI',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    addAzureOpenAI(name: string): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._addAzureOpenAIInternal(name));
    }

    /** Adds a YARP container to the application model. */
    /** @internal */
    async _addYarpInternal(name: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting.Yarp/addYarp',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    addYarp(name: string): YarpResourcePromise {
        return new YarpResourcePromise(this._addYarpInternal(name));
    }

    /** Adds an Azure Container App Environment resource */
    /** @internal */
    async _addAzureContainerAppEnvironmentInternal(name: string): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/addAzureContainerAppEnvironment',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    addAzureContainerAppEnvironment(name: string): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._addAzureContainerAppEnvironmentInternal(name));
    }

    /** Adds an Azure Bicep template resource from a file */
    /** @internal */
    async _addBicepTemplateInternal(name: string, bicepFile: string): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, bicepFile };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure/addBicepTemplate',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    addBicepTemplate(name: string, bicepFile: string): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._addBicepTemplateInternal(name, bicepFile));
    }

    /** Adds an Azure Bicep template resource from inline Bicep content */
    /** @internal */
    async _addBicepTemplateStringInternal(name: string, bicepContent: string): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, bicepContent };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure/addBicepTemplateString',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    addBicepTemplateString(name: string, bicepContent: string): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._addBicepTemplateStringInternal(name, bicepContent));
    }

    /** Adds an Azure provisioning resource to the application model */
    /** @internal */
    async _addAzureInfrastructureInternal(name: string, configureInfrastructure: (obj: AzureResourceInfrastructure) => Promise<void>): Promise<AzureProvisioningResource> {
        const configureInfrastructureId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as AzureResourceInfrastructureHandle;
            const obj = new AzureResourceInfrastructure(objHandle, this._client);
            await configureInfrastructure(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, configureInfrastructure: configureInfrastructureId };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure/addAzureInfrastructure',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    addAzureInfrastructure(name: string, configureInfrastructure: (obj: AzureResourceInfrastructure) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._addAzureInfrastructureInternal(name, configureInfrastructure));
    }

    /** Adds Azure provisioning services to the distributed application builder */
    /** @internal */
    async _addAzureProvisioningInternal(): Promise<DistributedApplicationBuilder> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<IDistributedApplicationBuilderHandle>(
            'Aspire.Hosting.Azure/addAzureProvisioning',
            rpcArgs
        );
        return new DistributedApplicationBuilder(result, this._client);
    }

    addAzureProvisioning(): DistributedApplicationBuilderPromise {
        return new DistributedApplicationBuilderPromise(this._addAzureProvisioningInternal());
    }

    /** Adds the shared Azure environment resource to the application model */
    /** @internal */
    async _addAzureEnvironmentInternal(): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/addAzureEnvironment',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    addAzureEnvironment(): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._addAzureEnvironmentInternal());
    }

    /** Adds an Azure user-assigned identity resource */
    /** @internal */
    async _addAzureUserAssignedIdentityInternal(name: string): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure/addAzureUserAssignedIdentity',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    addAzureUserAssignedIdentity(name: string): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._addAzureUserAssignedIdentityInternal(name));
    }

    /** Adds an Azure Container Registry resource to the distributed application model. */
    /** @internal */
    async _addAzureContainerRegistryInternal(name: string): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/addAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    addAzureContainerRegistry(name: string): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._addAzureContainerRegistryInternal(name));
    }

    /** Adds an Azure Log Analytics Workspace resource */
    /** @internal */
    async _addAzureLogAnalyticsWorkspaceInternal(name: string): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure.OperationalInsights/addAzureLogAnalyticsWorkspace',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    addAzureLogAnalyticsWorkspace(name: string): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._addAzureLogAnalyticsWorkspaceInternal(name));
    }

}

/**
 * Thenable wrapper for DistributedApplicationBuilder that enables fluent chaining.
 */
export class DistributedApplicationBuilderPromise implements PromiseLike<DistributedApplicationBuilder> {
    constructor(private _promise: Promise<DistributedApplicationBuilder>) {}

    then<TResult1 = DistributedApplicationBuilder, TResult2 = never>(
        onfulfilled?: ((value: DistributedApplicationBuilder) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Builds the distributed application */
    build(): DistributedApplicationPromise {
        return new DistributedApplicationPromise(this._promise.then(obj => obj.build()));
    }

    /** Adds a connection string with a builder callback */
    addConnectionStringBuilder(name: string, connectionStringBuilder: (obj: ReferenceExpressionBuilder) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.addConnectionStringBuilder(name, connectionStringBuilder)));
    }

    /** Adds a container registry resource */
    addContainerRegistry(name: string, endpoint: ParameterResource, options?: AddContainerRegistryOptions): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.addContainerRegistry(name, endpoint, options)));
    }

    /** Adds a container resource */
    addContainer(name: string, image: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.addContainer(name, image)));
    }

    /** Adds a container resource built from a Dockerfile */
    addDockerfile(name: string, contextPath: string, options?: AddDockerfileOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.addDockerfile(name, contextPath, options)));
    }

    /** Adds a .NET tool resource */
    addDotnetTool(name: string, packageId: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.addDotnetTool(name, packageId)));
    }

    /** Adds an executable resource */
    addExecutable(name: string, command: string, workingDirectory: string, args: string[]): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.addExecutable(name, command, workingDirectory, args)));
    }

    /** Adds an external service resource */
    addExternalService(name: string, url: string): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.addExternalService(name, url)));
    }

    /** Adds a parameter resource */
    addParameter(name: string, options?: AddParameterOptions): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.addParameter(name, options)));
    }

    /** Adds a parameter sourced from configuration */
    addParameterFromConfiguration(name: string, configurationKey: string, options?: AddParameterFromConfigurationOptions): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.addParameterFromConfiguration(name, configurationKey, options)));
    }

    /** Adds a connection string resource */
    addConnectionString(name: string, options?: AddConnectionStringOptions): ResourceWithConnectionStringPromise {
        return new ResourceWithConnectionStringPromise(this._promise.then(obj => obj.addConnectionString(name, options)));
    }

    /** Adds a .NET project resource */
    addProject(name: string, projectPath: string, launchProfileName: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.addProject(name, projectPath, launchProfileName)));
    }

    /** Adds a project resource with configuration options */
    addProjectWithOptions(name: string, projectPath: string, configure: (obj: ProjectResourceOptions) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.addProjectWithOptions(name, projectPath, configure)));
    }

    /** Adds a C# application resource */
    addCSharpApp(name: string, path: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.addCSharpApp(name, path)));
    }

    /** Adds a C# application resource with configuration options */
    addCSharpAppWithOptions(name: string, path: string, configure: (obj: ProjectResourceOptions) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.addCSharpAppWithOptions(name, path, configure)));
    }

    /** Gets the application configuration */
    getConfiguration(): ConfigurationPromise {
        return new ConfigurationPromise(this._promise.then(obj => obj.getConfiguration()));
    }

    /** Subscribes to the BeforeStart event */
    subscribeBeforeStart(callback: (arg: BeforeStartEvent) => Promise<void>): Promise<DistributedApplicationEventSubscriptionHandle> {
        return this._promise.then(obj => obj.subscribeBeforeStart(callback));
    }

    /** Subscribes to the AfterResourcesCreated event */
    subscribeAfterResourcesCreated(callback: (arg: AfterResourcesCreatedEvent) => Promise<void>): Promise<DistributedApplicationEventSubscriptionHandle> {
        return this._promise.then(obj => obj.subscribeAfterResourcesCreated(callback));
    }

    /** Adds a Redis container resource with specific port */
    addRedisWithPort(name: string, options?: AddRedisWithPortOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.addRedisWithPort(name, options)));
    }

    /** Adds a Redis container resource */
    addRedis(name: string, options?: AddRedisOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.addRedis(name, options)));
    }

    /** Adds a RabbitMQ container resource */
    addRabbitMQ(name: string, options?: AddRabbitMQOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.addRabbitMQ(name, options)));
    }

    /** Adds a PostgreSQL server resource */
    addPostgres(name: string, options?: AddPostgresOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.addPostgres(name, options)));
    }

    /** Adds an Azure OpenAI resource */
    addAzureOpenAI(name: string): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.addAzureOpenAI(name)));
    }

    /** Adds a YARP container to the application model. */
    addYarp(name: string): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.addYarp(name)));
    }

    /** Adds an Azure Container App Environment resource */
    addAzureContainerAppEnvironment(name: string): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.addAzureContainerAppEnvironment(name)));
    }

    /** Adds an Azure Bicep template resource from a file */
    addBicepTemplate(name: string, bicepFile: string): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.addBicepTemplate(name, bicepFile)));
    }

    /** Adds an Azure Bicep template resource from inline Bicep content */
    addBicepTemplateString(name: string, bicepContent: string): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.addBicepTemplateString(name, bicepContent)));
    }

    /** Adds an Azure provisioning resource to the application model */
    addAzureInfrastructure(name: string, configureInfrastructure: (obj: AzureResourceInfrastructure) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.addAzureInfrastructure(name, configureInfrastructure)));
    }

    /** Adds Azure provisioning services to the distributed application builder */
    addAzureProvisioning(): DistributedApplicationBuilderPromise {
        return new DistributedApplicationBuilderPromise(this._promise.then(obj => obj.addAzureProvisioning()));
    }

    /** Adds the shared Azure environment resource to the application model */
    addAzureEnvironment(): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.addAzureEnvironment()));
    }

    /** Adds an Azure user-assigned identity resource */
    addAzureUserAssignedIdentity(name: string): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.addAzureUserAssignedIdentity(name)));
    }

    /** Adds an Azure Container Registry resource to the distributed application model. */
    addAzureContainerRegistry(name: string): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.addAzureContainerRegistry(name)));
    }

    /** Adds an Azure Log Analytics Workspace resource */
    addAzureLogAnalyticsWorkspace(name: string): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.addAzureLogAnalyticsWorkspace(name)));
    }

}

// ============================================================================
// DistributedApplicationEventing
// ============================================================================

/**
 * Type class for DistributedApplicationEventing.
 */
export class DistributedApplicationEventing {
    constructor(private _handle: IDistributedApplicationEventingHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Invokes the Unsubscribe method */
    /** @internal */
    async _unsubscribeInternal(subscription: DistributedApplicationEventSubscriptionHandle): Promise<DistributedApplicationEventing> {
        const rpcArgs: Record<string, unknown> = { context: this._handle, subscription };
        await this._client.invokeCapability<void>(
            'Aspire.Hosting.Eventing/IDistributedApplicationEventing.unsubscribe',
            rpcArgs
        );
        return this;
    }

    unsubscribe(subscription: DistributedApplicationEventSubscriptionHandle): DistributedApplicationEventingPromise {
        return new DistributedApplicationEventingPromise(this._unsubscribeInternal(subscription));
    }

}

/**
 * Thenable wrapper for DistributedApplicationEventing that enables fluent chaining.
 */
export class DistributedApplicationEventingPromise implements PromiseLike<DistributedApplicationEventing> {
    constructor(private _promise: Promise<DistributedApplicationEventing>) {}

    then<TResult1 = DistributedApplicationEventing, TResult2 = never>(
        onfulfilled?: ((value: DistributedApplicationEventing) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Invokes the Unsubscribe method */
    unsubscribe(subscription: DistributedApplicationEventSubscriptionHandle): DistributedApplicationEventingPromise {
        return new DistributedApplicationEventingPromise(this._promise.then(obj => obj.unsubscribe(subscription)));
    }

}

// ============================================================================
// HostEnvironment
// ============================================================================

/**
 * Type class for HostEnvironment.
 */
export class HostEnvironment {
    constructor(private _handle: IHostEnvironmentHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Checks if running in Development environment */
    async isDevelopment(): Promise<boolean> {
        const rpcArgs: Record<string, unknown> = { environment: this._handle };
        return await this._client.invokeCapability<boolean>(
            'Aspire.Hosting/isDevelopment',
            rpcArgs
        );
    }

    /** Checks if running in Production environment */
    async isProduction(): Promise<boolean> {
        const rpcArgs: Record<string, unknown> = { environment: this._handle };
        return await this._client.invokeCapability<boolean>(
            'Aspire.Hosting/isProduction',
            rpcArgs
        );
    }

    /** Checks if running in Staging environment */
    async isStaging(): Promise<boolean> {
        const rpcArgs: Record<string, unknown> = { environment: this._handle };
        return await this._client.invokeCapability<boolean>(
            'Aspire.Hosting/isStaging',
            rpcArgs
        );
    }

    /** Checks if the environment matches the specified name */
    async isEnvironment(environmentName: string): Promise<boolean> {
        const rpcArgs: Record<string, unknown> = { environment: this._handle, environmentName };
        return await this._client.invokeCapability<boolean>(
            'Aspire.Hosting/isEnvironment',
            rpcArgs
        );
    }

}

/**
 * Thenable wrapper for HostEnvironment that enables fluent chaining.
 */
export class HostEnvironmentPromise implements PromiseLike<HostEnvironment> {
    constructor(private _promise: Promise<HostEnvironment>) {}

    then<TResult1 = HostEnvironment, TResult2 = never>(
        onfulfilled?: ((value: HostEnvironment) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Checks if running in Development environment */
    isDevelopment(): Promise<boolean> {
        return this._promise.then(obj => obj.isDevelopment());
    }

    /** Checks if running in Production environment */
    isProduction(): Promise<boolean> {
        return this._promise.then(obj => obj.isProduction());
    }

    /** Checks if running in Staging environment */
    isStaging(): Promise<boolean> {
        return this._promise.then(obj => obj.isStaging());
    }

    /** Checks if the environment matches the specified name */
    isEnvironment(environmentName: string): Promise<boolean> {
        return this._promise.then(obj => obj.isEnvironment(environmentName));
    }

}

// ============================================================================
// Logger
// ============================================================================

/**
 * Type class for Logger.
 */
export class Logger {
    constructor(private _handle: ILoggerHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Logs an information message */
    /** @internal */
    async _logInformationInternal(message: string): Promise<Logger> {
        const rpcArgs: Record<string, unknown> = { logger: this._handle, message };
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/logInformation',
            rpcArgs
        );
        return this;
    }

    logInformation(message: string): LoggerPromise {
        return new LoggerPromise(this._logInformationInternal(message));
    }

    /** Logs a warning message */
    /** @internal */
    async _logWarningInternal(message: string): Promise<Logger> {
        const rpcArgs: Record<string, unknown> = { logger: this._handle, message };
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/logWarning',
            rpcArgs
        );
        return this;
    }

    logWarning(message: string): LoggerPromise {
        return new LoggerPromise(this._logWarningInternal(message));
    }

    /** Logs an error message */
    /** @internal */
    async _logErrorInternal(message: string): Promise<Logger> {
        const rpcArgs: Record<string, unknown> = { logger: this._handle, message };
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/logError',
            rpcArgs
        );
        return this;
    }

    logError(message: string): LoggerPromise {
        return new LoggerPromise(this._logErrorInternal(message));
    }

    /** Logs a debug message */
    /** @internal */
    async _logDebugInternal(message: string): Promise<Logger> {
        const rpcArgs: Record<string, unknown> = { logger: this._handle, message };
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/logDebug',
            rpcArgs
        );
        return this;
    }

    logDebug(message: string): LoggerPromise {
        return new LoggerPromise(this._logDebugInternal(message));
    }

    /** Logs a message with specified level */
    /** @internal */
    async _logInternal(level: string, message: string): Promise<Logger> {
        const rpcArgs: Record<string, unknown> = { logger: this._handle, level, message };
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/log',
            rpcArgs
        );
        return this;
    }

    log(level: string, message: string): LoggerPromise {
        return new LoggerPromise(this._logInternal(level, message));
    }

}

/**
 * Thenable wrapper for Logger that enables fluent chaining.
 */
export class LoggerPromise implements PromiseLike<Logger> {
    constructor(private _promise: Promise<Logger>) {}

    then<TResult1 = Logger, TResult2 = never>(
        onfulfilled?: ((value: Logger) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Logs an information message */
    logInformation(message: string): LoggerPromise {
        return new LoggerPromise(this._promise.then(obj => obj.logInformation(message)));
    }

    /** Logs a warning message */
    logWarning(message: string): LoggerPromise {
        return new LoggerPromise(this._promise.then(obj => obj.logWarning(message)));
    }

    /** Logs an error message */
    logError(message: string): LoggerPromise {
        return new LoggerPromise(this._promise.then(obj => obj.logError(message)));
    }

    /** Logs a debug message */
    logDebug(message: string): LoggerPromise {
        return new LoggerPromise(this._promise.then(obj => obj.logDebug(message)));
    }

    /** Logs a message with specified level */
    log(level: string, message: string): LoggerPromise {
        return new LoggerPromise(this._promise.then(obj => obj.log(level, message)));
    }

}

// ============================================================================
// LoggerFactory
// ============================================================================

/**
 * Type class for LoggerFactory.
 */
export class LoggerFactory {
    constructor(private _handle: ILoggerFactoryHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Creates a logger for a category */
    /** @internal */
    async _createLoggerInternal(categoryName: string): Promise<Logger> {
        const rpcArgs: Record<string, unknown> = { loggerFactory: this._handle, categoryName };
        const result = await this._client.invokeCapability<ILoggerHandle>(
            'Aspire.Hosting/createLogger',
            rpcArgs
        );
        return new Logger(result, this._client);
    }

    createLogger(categoryName: string): LoggerPromise {
        return new LoggerPromise(this._createLoggerInternal(categoryName));
    }

}

/**
 * Thenable wrapper for LoggerFactory that enables fluent chaining.
 */
export class LoggerFactoryPromise implements PromiseLike<LoggerFactory> {
    constructor(private _promise: Promise<LoggerFactory>) {}

    then<TResult1 = LoggerFactory, TResult2 = never>(
        onfulfilled?: ((value: LoggerFactory) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Creates a logger for a category */
    createLogger(categoryName: string): LoggerPromise {
        return new LoggerPromise(this._promise.then(obj => obj.createLogger(categoryName)));
    }

}

// ============================================================================
// ReportingStep
// ============================================================================

/**
 * Type class for ReportingStep.
 */
export class ReportingStep {
    constructor(private _handle: IReportingStepHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Creates a reporting task with plain-text status text */
    /** @internal */
    async _createTaskInternal(statusText: string, cancellationToken?: AbortSignal): Promise<ReportingTask> {
        const rpcArgs: Record<string, unknown> = { reportingStep: this._handle, statusText };
        if (cancellationToken !== undefined) rpcArgs.cancellationToken = cancellationToken;
        const result = await this._client.invokeCapability<IReportingTaskHandle>(
            'Aspire.Hosting/createTask',
            rpcArgs
        );
        return new ReportingTask(result, this._client);
    }

    createTask(statusText: string, options?: CreateTaskOptions): ReportingTaskPromise {
        const cancellationToken = options?.cancellationToken;
        return new ReportingTaskPromise(this._createTaskInternal(statusText, cancellationToken));
    }

    /** Creates a reporting task with Markdown-formatted status text */
    /** @internal */
    async _createMarkdownTaskInternal(markdownString: string, cancellationToken?: AbortSignal): Promise<ReportingTask> {
        const rpcArgs: Record<string, unknown> = { reportingStep: this._handle, markdownString };
        if (cancellationToken !== undefined) rpcArgs.cancellationToken = cancellationToken;
        const result = await this._client.invokeCapability<IReportingTaskHandle>(
            'Aspire.Hosting/createMarkdownTask',
            rpcArgs
        );
        return new ReportingTask(result, this._client);
    }

    createMarkdownTask(markdownString: string, options?: CreateMarkdownTaskOptions): ReportingTaskPromise {
        const cancellationToken = options?.cancellationToken;
        return new ReportingTaskPromise(this._createMarkdownTaskInternal(markdownString, cancellationToken));
    }

    /** Logs a plain-text message for the reporting step */
    /** @internal */
    async _logStepInternal(level: string, message: string): Promise<ReportingStep> {
        const rpcArgs: Record<string, unknown> = { reportingStep: this._handle, level, message };
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/logStep',
            rpcArgs
        );
        return this;
    }

    logStep(level: string, message: string): ReportingStepPromise {
        return new ReportingStepPromise(this._logStepInternal(level, message));
    }

    /** Logs a Markdown-formatted message for the reporting step */
    /** @internal */
    async _logStepMarkdownInternal(level: string, markdownString: string): Promise<ReportingStep> {
        const rpcArgs: Record<string, unknown> = { reportingStep: this._handle, level, markdownString };
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/logStepMarkdown',
            rpcArgs
        );
        return this;
    }

    logStepMarkdown(level: string, markdownString: string): ReportingStepPromise {
        return new ReportingStepPromise(this._logStepMarkdownInternal(level, markdownString));
    }

    /** Completes the reporting step with plain-text completion text */
    /** @internal */
    async _completeStepInternal(completionText: string, completionState?: string, cancellationToken?: AbortSignal): Promise<ReportingStep> {
        const rpcArgs: Record<string, unknown> = { reportingStep: this._handle, completionText };
        if (completionState !== undefined) rpcArgs.completionState = completionState;
        if (cancellationToken !== undefined) rpcArgs.cancellationToken = cancellationToken;
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/completeStep',
            rpcArgs
        );
        return this;
    }

    completeStep(completionText: string, options?: CompleteStepOptions): ReportingStepPromise {
        const completionState = options?.completionState;
        const cancellationToken = options?.cancellationToken;
        return new ReportingStepPromise(this._completeStepInternal(completionText, completionState, cancellationToken));
    }

    /** Completes the reporting step with Markdown-formatted completion text */
    /** @internal */
    async _completeStepMarkdownInternal(markdownString: string, completionState?: string, cancellationToken?: AbortSignal): Promise<ReportingStep> {
        const rpcArgs: Record<string, unknown> = { reportingStep: this._handle, markdownString };
        if (completionState !== undefined) rpcArgs.completionState = completionState;
        if (cancellationToken !== undefined) rpcArgs.cancellationToken = cancellationToken;
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/completeStepMarkdown',
            rpcArgs
        );
        return this;
    }

    completeStepMarkdown(markdownString: string, options?: CompleteStepMarkdownOptions): ReportingStepPromise {
        const completionState = options?.completionState;
        const cancellationToken = options?.cancellationToken;
        return new ReportingStepPromise(this._completeStepMarkdownInternal(markdownString, completionState, cancellationToken));
    }

}

/**
 * Thenable wrapper for ReportingStep that enables fluent chaining.
 */
export class ReportingStepPromise implements PromiseLike<ReportingStep> {
    constructor(private _promise: Promise<ReportingStep>) {}

    then<TResult1 = ReportingStep, TResult2 = never>(
        onfulfilled?: ((value: ReportingStep) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Creates a reporting task with plain-text status text */
    createTask(statusText: string, options?: CreateTaskOptions): ReportingTaskPromise {
        return new ReportingTaskPromise(this._promise.then(obj => obj.createTask(statusText, options)));
    }

    /** Creates a reporting task with Markdown-formatted status text */
    createMarkdownTask(markdownString: string, options?: CreateMarkdownTaskOptions): ReportingTaskPromise {
        return new ReportingTaskPromise(this._promise.then(obj => obj.createMarkdownTask(markdownString, options)));
    }

    /** Logs a plain-text message for the reporting step */
    logStep(level: string, message: string): ReportingStepPromise {
        return new ReportingStepPromise(this._promise.then(obj => obj.logStep(level, message)));
    }

    /** Logs a Markdown-formatted message for the reporting step */
    logStepMarkdown(level: string, markdownString: string): ReportingStepPromise {
        return new ReportingStepPromise(this._promise.then(obj => obj.logStepMarkdown(level, markdownString)));
    }

    /** Completes the reporting step with plain-text completion text */
    completeStep(completionText: string, options?: CompleteStepOptions): ReportingStepPromise {
        return new ReportingStepPromise(this._promise.then(obj => obj.completeStep(completionText, options)));
    }

    /** Completes the reporting step with Markdown-formatted completion text */
    completeStepMarkdown(markdownString: string, options?: CompleteStepMarkdownOptions): ReportingStepPromise {
        return new ReportingStepPromise(this._promise.then(obj => obj.completeStepMarkdown(markdownString, options)));
    }

}

// ============================================================================
// ReportingTask
// ============================================================================

/**
 * Type class for ReportingTask.
 */
export class ReportingTask {
    constructor(private _handle: IReportingTaskHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Updates the reporting task with plain-text status text */
    /** @internal */
    async _updateTaskInternal(statusText: string, cancellationToken?: AbortSignal): Promise<ReportingTask> {
        const rpcArgs: Record<string, unknown> = { reportingTask: this._handle, statusText };
        if (cancellationToken !== undefined) rpcArgs.cancellationToken = cancellationToken;
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/updateTask',
            rpcArgs
        );
        return this;
    }

    updateTask(statusText: string, options?: UpdateTaskOptions): ReportingTaskPromise {
        const cancellationToken = options?.cancellationToken;
        return new ReportingTaskPromise(this._updateTaskInternal(statusText, cancellationToken));
    }

    /** Updates the reporting task with Markdown-formatted status text */
    /** @internal */
    async _updateTaskMarkdownInternal(markdownString: string, cancellationToken?: AbortSignal): Promise<ReportingTask> {
        const rpcArgs: Record<string, unknown> = { reportingTask: this._handle, markdownString };
        if (cancellationToken !== undefined) rpcArgs.cancellationToken = cancellationToken;
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/updateTaskMarkdown',
            rpcArgs
        );
        return this;
    }

    updateTaskMarkdown(markdownString: string, options?: UpdateTaskMarkdownOptions): ReportingTaskPromise {
        const cancellationToken = options?.cancellationToken;
        return new ReportingTaskPromise(this._updateTaskMarkdownInternal(markdownString, cancellationToken));
    }

    /** Completes the reporting task with plain-text completion text */
    /** @internal */
    async _completeTaskInternal(completionMessage?: string, completionState?: string, cancellationToken?: AbortSignal): Promise<ReportingTask> {
        const rpcArgs: Record<string, unknown> = { reportingTask: this._handle };
        if (completionMessage !== undefined) rpcArgs.completionMessage = completionMessage;
        if (completionState !== undefined) rpcArgs.completionState = completionState;
        if (cancellationToken !== undefined) rpcArgs.cancellationToken = cancellationToken;
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/completeTask',
            rpcArgs
        );
        return this;
    }

    completeTask(options?: CompleteTaskOptions): ReportingTaskPromise {
        const completionMessage = options?.completionMessage;
        const completionState = options?.completionState;
        const cancellationToken = options?.cancellationToken;
        return new ReportingTaskPromise(this._completeTaskInternal(completionMessage, completionState, cancellationToken));
    }

    /** Completes the reporting task with Markdown-formatted completion text */
    /** @internal */
    async _completeTaskMarkdownInternal(markdownString: string, completionState?: string, cancellationToken?: AbortSignal): Promise<ReportingTask> {
        const rpcArgs: Record<string, unknown> = { reportingTask: this._handle, markdownString };
        if (completionState !== undefined) rpcArgs.completionState = completionState;
        if (cancellationToken !== undefined) rpcArgs.cancellationToken = cancellationToken;
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/completeTaskMarkdown',
            rpcArgs
        );
        return this;
    }

    completeTaskMarkdown(markdownString: string, options?: CompleteTaskMarkdownOptions): ReportingTaskPromise {
        const completionState = options?.completionState;
        const cancellationToken = options?.cancellationToken;
        return new ReportingTaskPromise(this._completeTaskMarkdownInternal(markdownString, completionState, cancellationToken));
    }

}

/**
 * Thenable wrapper for ReportingTask that enables fluent chaining.
 */
export class ReportingTaskPromise implements PromiseLike<ReportingTask> {
    constructor(private _promise: Promise<ReportingTask>) {}

    then<TResult1 = ReportingTask, TResult2 = never>(
        onfulfilled?: ((value: ReportingTask) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Updates the reporting task with plain-text status text */
    updateTask(statusText: string, options?: UpdateTaskOptions): ReportingTaskPromise {
        return new ReportingTaskPromise(this._promise.then(obj => obj.updateTask(statusText, options)));
    }

    /** Updates the reporting task with Markdown-formatted status text */
    updateTaskMarkdown(markdownString: string, options?: UpdateTaskMarkdownOptions): ReportingTaskPromise {
        return new ReportingTaskPromise(this._promise.then(obj => obj.updateTaskMarkdown(markdownString, options)));
    }

    /** Completes the reporting task with plain-text completion text */
    completeTask(options?: CompleteTaskOptions): ReportingTaskPromise {
        return new ReportingTaskPromise(this._promise.then(obj => obj.completeTask(options)));
    }

    /** Completes the reporting task with Markdown-formatted completion text */
    completeTaskMarkdown(markdownString: string, options?: CompleteTaskMarkdownOptions): ReportingTaskPromise {
        return new ReportingTaskPromise(this._promise.then(obj => obj.completeTaskMarkdown(markdownString, options)));
    }

}

// ============================================================================
// ServiceProvider
// ============================================================================

/**
 * Type class for ServiceProvider.
 */
export class ServiceProvider {
    constructor(private _handle: IServiceProviderHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the distributed application eventing service from the service provider */
    /** @internal */
    async _getEventingInternal(): Promise<DistributedApplicationEventing> {
        const rpcArgs: Record<string, unknown> = { serviceProvider: this._handle };
        const result = await this._client.invokeCapability<IDistributedApplicationEventingHandle>(
            'Aspire.Hosting/getEventing',
            rpcArgs
        );
        return new DistributedApplicationEventing(result, this._client);
    }

    getEventing(): DistributedApplicationEventingPromise {
        return new DistributedApplicationEventingPromise(this._getEventingInternal());
    }

    /** Gets the logger factory from the service provider */
    /** @internal */
    async _getLoggerFactoryInternal(): Promise<LoggerFactory> {
        const rpcArgs: Record<string, unknown> = { serviceProvider: this._handle };
        const result = await this._client.invokeCapability<ILoggerFactoryHandle>(
            'Aspire.Hosting/getLoggerFactory',
            rpcArgs
        );
        return new LoggerFactory(result, this._client);
    }

    getLoggerFactory(): LoggerFactoryPromise {
        return new LoggerFactoryPromise(this._getLoggerFactoryInternal());
    }

    /** Gets the resource logger service from the service provider */
    /** @internal */
    async _getResourceLoggerServiceInternal(): Promise<ResourceLoggerService> {
        const rpcArgs: Record<string, unknown> = { serviceProvider: this._handle };
        const result = await this._client.invokeCapability<ResourceLoggerServiceHandle>(
            'Aspire.Hosting/getResourceLoggerService',
            rpcArgs
        );
        return new ResourceLoggerService(result, this._client);
    }

    getResourceLoggerService(): ResourceLoggerServicePromise {
        return new ResourceLoggerServicePromise(this._getResourceLoggerServiceInternal());
    }

    /** Gets the distributed application model from the service provider */
    /** @internal */
    async _getDistributedApplicationModelInternal(): Promise<DistributedApplicationModel> {
        const rpcArgs: Record<string, unknown> = { serviceProvider: this._handle };
        const result = await this._client.invokeCapability<DistributedApplicationModelHandle>(
            'Aspire.Hosting/getDistributedApplicationModel',
            rpcArgs
        );
        return new DistributedApplicationModel(result, this._client);
    }

    getDistributedApplicationModel(): DistributedApplicationModelPromise {
        return new DistributedApplicationModelPromise(this._getDistributedApplicationModelInternal());
    }

    /** Gets the resource notification service from the service provider */
    /** @internal */
    async _getResourceNotificationServiceInternal(): Promise<ResourceNotificationService> {
        const rpcArgs: Record<string, unknown> = { serviceProvider: this._handle };
        const result = await this._client.invokeCapability<ResourceNotificationServiceHandle>(
            'Aspire.Hosting/getResourceNotificationService',
            rpcArgs
        );
        return new ResourceNotificationService(result, this._client);
    }

    getResourceNotificationService(): ResourceNotificationServicePromise {
        return new ResourceNotificationServicePromise(this._getResourceNotificationServiceInternal());
    }

    /** Gets the user secrets manager from the service provider */
    /** @internal */
    async _getUserSecretsManagerInternal(): Promise<UserSecretsManager> {
        const rpcArgs: Record<string, unknown> = { serviceProvider: this._handle };
        const result = await this._client.invokeCapability<IUserSecretsManagerHandle>(
            'Aspire.Hosting/getUserSecretsManager',
            rpcArgs
        );
        return new UserSecretsManager(result, this._client);
    }

    getUserSecretsManager(): UserSecretsManagerPromise {
        return new UserSecretsManagerPromise(this._getUserSecretsManagerInternal());
    }

}

/**
 * Thenable wrapper for ServiceProvider that enables fluent chaining.
 */
export class ServiceProviderPromise implements PromiseLike<ServiceProvider> {
    constructor(private _promise: Promise<ServiceProvider>) {}

    then<TResult1 = ServiceProvider, TResult2 = never>(
        onfulfilled?: ((value: ServiceProvider) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Gets the distributed application eventing service from the service provider */
    getEventing(): DistributedApplicationEventingPromise {
        return new DistributedApplicationEventingPromise(this._promise.then(obj => obj.getEventing()));
    }

    /** Gets the logger factory from the service provider */
    getLoggerFactory(): LoggerFactoryPromise {
        return new LoggerFactoryPromise(this._promise.then(obj => obj.getLoggerFactory()));
    }

    /** Gets the resource logger service from the service provider */
    getResourceLoggerService(): ResourceLoggerServicePromise {
        return new ResourceLoggerServicePromise(this._promise.then(obj => obj.getResourceLoggerService()));
    }

    /** Gets the distributed application model from the service provider */
    getDistributedApplicationModel(): DistributedApplicationModelPromise {
        return new DistributedApplicationModelPromise(this._promise.then(obj => obj.getDistributedApplicationModel()));
    }

    /** Gets the resource notification service from the service provider */
    getResourceNotificationService(): ResourceNotificationServicePromise {
        return new ResourceNotificationServicePromise(this._promise.then(obj => obj.getResourceNotificationService()));
    }

    /** Gets the user secrets manager from the service provider */
    getUserSecretsManager(): UserSecretsManagerPromise {
        return new UserSecretsManagerPromise(this._promise.then(obj => obj.getUserSecretsManager()));
    }

}

// ============================================================================
// UserSecretsManager
// ============================================================================

/**
 * Type class for UserSecretsManager.
 */
export class UserSecretsManager {
    constructor(private _handle: IUserSecretsManagerHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Gets the IsAvailable property */
    isAvailable = {
        get: async (): Promise<boolean> => {
            return await this._client.invokeCapability<boolean>(
                'Aspire.Hosting/IUserSecretsManager.isAvailable',
                { context: this._handle }
            );
        },
    };

    /** Gets the FilePath property */
    filePath = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting/IUserSecretsManager.filePath',
                { context: this._handle }
            );
        },
    };

    /** Attempts to set a user secret value */
    async trySetSecret(name: string, value: string): Promise<boolean> {
        const rpcArgs: Record<string, unknown> = { context: this._handle, name, value };
        return await this._client.invokeCapability<boolean>(
            'Aspire.Hosting/IUserSecretsManager.trySetSecret',
            rpcArgs
        );
    }

    /** Saves state to user secrets from a JSON string */
    /** @internal */
    async _saveStateJsonInternal(json: string, cancellationToken?: AbortSignal): Promise<UserSecretsManager> {
        const rpcArgs: Record<string, unknown> = { userSecretsManager: this._handle, json };
        if (cancellationToken !== undefined) rpcArgs.cancellationToken = cancellationToken;
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/saveStateJson',
            rpcArgs
        );
        return this;
    }

    saveStateJson(json: string, options?: SaveStateJsonOptions): UserSecretsManagerPromise {
        const cancellationToken = options?.cancellationToken;
        return new UserSecretsManagerPromise(this._saveStateJsonInternal(json, cancellationToken));
    }

    /** Gets a secret value if it exists, or sets it to the provided value if it does not */
    /** @internal */
    async _getOrSetSecretInternal(resourceBuilder: ResourceBuilderBase, name: string, value: string): Promise<UserSecretsManager> {
        const rpcArgs: Record<string, unknown> = { userSecretsManager: this._handle, resourceBuilder, name, value };
        await this._client.invokeCapability<void>(
            'Aspire.Hosting/getOrSetSecret',
            rpcArgs
        );
        return this;
    }

    getOrSetSecret(resourceBuilder: ResourceBuilderBase, name: string, value: string): UserSecretsManagerPromise {
        return new UserSecretsManagerPromise(this._getOrSetSecretInternal(resourceBuilder, name, value));
    }

}

/**
 * Thenable wrapper for UserSecretsManager that enables fluent chaining.
 */
export class UserSecretsManagerPromise implements PromiseLike<UserSecretsManager> {
    constructor(private _promise: Promise<UserSecretsManager>) {}

    then<TResult1 = UserSecretsManager, TResult2 = never>(
        onfulfilled?: ((value: UserSecretsManager) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Attempts to set a user secret value */
    trySetSecret(name: string, value: string): Promise<boolean> {
        return this._promise.then(obj => obj.trySetSecret(name, value));
    }

    /** Saves state to user secrets from a JSON string */
    saveStateJson(json: string, options?: SaveStateJsonOptions): UserSecretsManagerPromise {
        return new UserSecretsManagerPromise(this._promise.then(obj => obj.saveStateJson(json, options)));
    }

    /** Gets a secret value if it exists, or sets it to the provided value if it does not */
    getOrSetSecret(resourceBuilder: ResourceBuilderBase, name: string, value: string): UserSecretsManagerPromise {
        return new UserSecretsManagerPromise(this._promise.then(obj => obj.getOrSetSecret(resourceBuilder, name, value)));
    }

}

// ============================================================================
// YarpConfigurationBuilder
// ============================================================================

/**
 * Type class for YarpConfigurationBuilder.
 */
export class YarpConfigurationBuilder {
    constructor(private _handle: IYarpConfigurationBuilderHandle, private _client: AspireClientRpc) {}

    /** Serialize for JSON-RPC transport */
    toJSON(): MarshalledHandle { return this._handle.toJSON(); }

    /** Invokes the AddRoute method */
    /** @internal */
    async _addRouteInternal(path: string, cluster: YarpCluster): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { context: this._handle, path, cluster };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting/IYarpConfigurationBuilder.addRoute',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    addRoute(path: string, cluster: YarpCluster): YarpRoutePromise {
        return new YarpRoutePromise(this._addRouteInternal(path, cluster));
    }

    /** Adds a YARP cluster for an endpoint reference. */
    /** @internal */
    async _addClusterFromEndpointInternal(endpoint: EndpointReference): Promise<YarpCluster> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpoint };
        const result = await this._client.invokeCapability<YarpClusterHandle>(
            'Aspire.Hosting.Yarp/addClusterFromEndpoint',
            rpcArgs
        );
        return new YarpCluster(result, this._client);
    }

    addClusterFromEndpoint(endpoint: EndpointReference): YarpClusterPromise {
        return new YarpClusterPromise(this._addClusterFromEndpointInternal(endpoint));
    }

    /** Adds a YARP cluster for a resource that supports service discovery. */
    /** @internal */
    async _addClusterFromResourceInternal(resource: ResourceBuilderBase): Promise<YarpCluster> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, resource };
        const result = await this._client.invokeCapability<YarpClusterHandle>(
            'Aspire.Hosting.Yarp/addClusterFromResource',
            rpcArgs
        );
        return new YarpCluster(result, this._client);
    }

    addClusterFromResource(resource: ResourceBuilderBase): YarpClusterPromise {
        return new YarpClusterPromise(this._addClusterFromResourceInternal(resource));
    }

    /** Adds a YARP cluster for an external service resource. */
    /** @internal */
    async _addClusterFromExternalServiceInternal(externalService: ExternalServiceResource): Promise<YarpCluster> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, externalService };
        const result = await this._client.invokeCapability<YarpClusterHandle>(
            'Aspire.Hosting.Yarp/addClusterFromExternalService',
            rpcArgs
        );
        return new YarpCluster(result, this._client);
    }

    addClusterFromExternalService(externalService: ExternalServiceResource): YarpClusterPromise {
        return new YarpClusterPromise(this._addClusterFromExternalServiceInternal(externalService));
    }

    /** Adds a YARP cluster with multiple destinations. */
    /** @internal */
    async _addClusterWithDestinationsInternal(clusterName: string, destinations: any[]): Promise<YarpCluster> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, clusterName, destinations };
        const result = await this._client.invokeCapability<YarpClusterHandle>(
            'Aspire.Hosting.Yarp/addClusterWithDestinations',
            rpcArgs
        );
        return new YarpCluster(result, this._client);
    }

    addClusterWithDestinations(clusterName: string, destinations: any[]): YarpClusterPromise {
        return new YarpClusterPromise(this._addClusterWithDestinationsInternal(clusterName, destinations));
    }

    /** Adds a YARP cluster with a single destination. */
    /** @internal */
    async _addClusterWithDestinationInternal(clusterName: string, destination: any): Promise<YarpCluster> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, clusterName, destination };
        const result = await this._client.invokeCapability<YarpClusterHandle>(
            'Aspire.Hosting.Yarp/addClusterWithDestination',
            rpcArgs
        );
        return new YarpCluster(result, this._client);
    }

    addClusterWithDestination(clusterName: string, destination: any): YarpClusterPromise {
        return new YarpClusterPromise(this._addClusterWithDestinationInternal(clusterName, destination));
    }

    /** Adds a YARP catch-all route for an existing cluster. */
    /** @internal */
    async _addCatchAllRouteInternal(cluster: YarpCluster): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, cluster };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/addCatchAllRoute',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    addCatchAllRoute(cluster: YarpCluster): YarpRoutePromise {
        return new YarpRoutePromise(this._addCatchAllRouteInternal(cluster));
    }

    /** Adds a YARP catch-all route for an endpoint reference. */
    /** @internal */
    async _addCatchAllRouteFromEndpointInternal(endpoint: EndpointReference): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpoint };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/addCatchAllRouteFromEndpoint',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    addCatchAllRouteFromEndpoint(endpoint: EndpointReference): YarpRoutePromise {
        return new YarpRoutePromise(this._addCatchAllRouteFromEndpointInternal(endpoint));
    }

    /** Adds a YARP catch-all route for a resource that supports service discovery. */
    /** @internal */
    async _addCatchAllRouteFromResourceInternal(resource: ResourceBuilderBase): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, resource };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/addCatchAllRouteFromResource',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    addCatchAllRouteFromResource(resource: ResourceBuilderBase): YarpRoutePromise {
        return new YarpRoutePromise(this._addCatchAllRouteFromResourceInternal(resource));
    }

    /** Adds a YARP route for an endpoint reference. */
    /** @internal */
    async _addRouteFromEndpointInternal(path: string, endpoint: EndpointReference): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, path, endpoint };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/addRouteFromEndpoint',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    addRouteFromEndpoint(path: string, endpoint: EndpointReference): YarpRoutePromise {
        return new YarpRoutePromise(this._addRouteFromEndpointInternal(path, endpoint));
    }

    /** Adds a YARP route for a resource that supports service discovery. */
    /** @internal */
    async _addRouteFromResourceInternal(path: string, resource: ResourceBuilderBase): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, path, resource };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/addRouteFromResource',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    addRouteFromResource(path: string, resource: ResourceBuilderBase): YarpRoutePromise {
        return new YarpRoutePromise(this._addRouteFromResourceInternal(path, resource));
    }

    /** Adds a YARP route for an external service resource. */
    /** @internal */
    async _addRouteFromExternalServiceInternal(path: string, externalService: ExternalServiceResource): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, path, externalService };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/addRouteFromExternalService',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    addRouteFromExternalService(path: string, externalService: ExternalServiceResource): YarpRoutePromise {
        return new YarpRoutePromise(this._addRouteFromExternalServiceInternal(path, externalService));
    }

    /** Adds a YARP catch-all route for an external service resource. */
    /** @internal */
    async _addCatchAllRouteFromExternalServiceInternal(externalService: ExternalServiceResource): Promise<YarpRoute> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, externalService };
        const result = await this._client.invokeCapability<YarpRouteHandle>(
            'Aspire.Hosting.Yarp/addCatchAllRouteFromExternalService',
            rpcArgs
        );
        return new YarpRoute(result, this._client);
    }

    addCatchAllRouteFromExternalService(externalService: ExternalServiceResource): YarpRoutePromise {
        return new YarpRoutePromise(this._addCatchAllRouteFromExternalServiceInternal(externalService));
    }

}

/**
 * Thenable wrapper for YarpConfigurationBuilder that enables fluent chaining.
 */
export class YarpConfigurationBuilderPromise implements PromiseLike<YarpConfigurationBuilder> {
    constructor(private _promise: Promise<YarpConfigurationBuilder>) {}

    then<TResult1 = YarpConfigurationBuilder, TResult2 = never>(
        onfulfilled?: ((value: YarpConfigurationBuilder) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Invokes the AddRoute method */
    addRoute(path: string, cluster: YarpCluster): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.addRoute(path, cluster)));
    }

    /** Adds a YARP cluster for an endpoint reference. */
    addClusterFromEndpoint(endpoint: EndpointReference): YarpClusterPromise {
        return new YarpClusterPromise(this._promise.then(obj => obj.addClusterFromEndpoint(endpoint)));
    }

    /** Adds a YARP cluster for a resource that supports service discovery. */
    addClusterFromResource(resource: ResourceBuilderBase): YarpClusterPromise {
        return new YarpClusterPromise(this._promise.then(obj => obj.addClusterFromResource(resource)));
    }

    /** Adds a YARP cluster for an external service resource. */
    addClusterFromExternalService(externalService: ExternalServiceResource): YarpClusterPromise {
        return new YarpClusterPromise(this._promise.then(obj => obj.addClusterFromExternalService(externalService)));
    }

    /** Adds a YARP cluster with multiple destinations. */
    addClusterWithDestinations(clusterName: string, destinations: any[]): YarpClusterPromise {
        return new YarpClusterPromise(this._promise.then(obj => obj.addClusterWithDestinations(clusterName, destinations)));
    }

    /** Adds a YARP cluster with a single destination. */
    addClusterWithDestination(clusterName: string, destination: any): YarpClusterPromise {
        return new YarpClusterPromise(this._promise.then(obj => obj.addClusterWithDestination(clusterName, destination)));
    }

    /** Adds a YARP catch-all route for an existing cluster. */
    addCatchAllRoute(cluster: YarpCluster): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.addCatchAllRoute(cluster)));
    }

    /** Adds a YARP catch-all route for an endpoint reference. */
    addCatchAllRouteFromEndpoint(endpoint: EndpointReference): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.addCatchAllRouteFromEndpoint(endpoint)));
    }

    /** Adds a YARP catch-all route for a resource that supports service discovery. */
    addCatchAllRouteFromResource(resource: ResourceBuilderBase): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.addCatchAllRouteFromResource(resource)));
    }

    /** Adds a YARP route for an endpoint reference. */
    addRouteFromEndpoint(path: string, endpoint: EndpointReference): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.addRouteFromEndpoint(path, endpoint)));
    }

    /** Adds a YARP route for a resource that supports service discovery. */
    addRouteFromResource(path: string, resource: ResourceBuilderBase): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.addRouteFromResource(path, resource)));
    }

    /** Adds a YARP route for an external service resource. */
    addRouteFromExternalService(path: string, externalService: ExternalServiceResource): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.addRouteFromExternalService(path, externalService)));
    }

    /** Adds a YARP catch-all route for an external service resource. */
    addCatchAllRouteFromExternalService(externalService: ExternalServiceResource): YarpRoutePromise {
        return new YarpRoutePromise(this._promise.then(obj => obj.addCatchAllRouteFromExternalService(externalService)));
    }

}

// ============================================================================
// AzureBicepResource
// ============================================================================

export class AzureBicepResource extends ResourceBuilderBase<AzureBicepResourceHandle> {
    constructor(handle: AzureBicepResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureBicepResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new AzureBicepResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureBicepResourcePromise {
        const helpLink = options?.helpLink;
        return new AzureBicepResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureBicepResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureBicepResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureBicepResourcePromise {
        const displayText = options?.displayText;
        return new AzureBicepResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureBicepResourcePromise {
        const displayText = options?.displayText;
        return new AzureBicepResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<AzureBicepResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<AzureBicepResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureBicepResourcePromise {
        const commandOptions = options?.commandOptions;
        return new AzureBicepResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureBicepResourcePromise {
        const iconVariant = options?.iconVariant;
        return new AzureBicepResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<AzureBicepResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureBicepResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new AzureBicepResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<AzureBicepResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<AzureBicepResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<AzureBicepResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<AzureBicepResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<AzureBicepResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<AzureBicepResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** Gets an output reference from an Azure Bicep template resource */
    async getOutput(name: string): Promise<BicepOutputReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<BicepOutputReference>(
            'Aspire.Hosting.Azure/getOutput',
            rpcArgs
        );
    }

    /** @internal */
    private async _withParameterInternal(name: string): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure/withParameter',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Adds a Bicep parameter without a value */
    withParameter(name: string): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withParameterInternal(name));
    }

    /** @internal */
    private async _withParameterStringValueInternal(name: string, value: string): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure/withParameterStringValue',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Adds a Bicep parameter with a string value */
    withParameterStringValue(name: string, value: string): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withParameterStringValueInternal(name, value));
    }

    /** @internal */
    private async _withParameterStringValuesInternal(name: string, value: string[]): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure/withParameterStringValues',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Adds a Bicep parameter with a string list value */
    withParameterStringValues(name: string, value: string[]): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withParameterStringValuesInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromParameterInternal(name: string, value: ParameterResource): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromParameter',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Adds a Bicep parameter from a parameter resource builder */
    withParameterFromParameter(name: string, value: ParameterResource): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withParameterFromParameterInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromConnectionStringInternal(name: string, value: ResourceBuilderBase): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromConnectionString',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Adds a Bicep parameter from a connection string resource builder */
    withParameterFromConnectionString(name: string, value: ResourceBuilderBase): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withParameterFromConnectionStringInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromOutputInternal(name: string, value: BicepOutputReference): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromOutput',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Adds a Bicep parameter from another Bicep output reference */
    withParameterFromOutput(name: string, value: BicepOutputReference): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withParameterFromOutputInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromReferenceExpressionInternal(name: string, value: ReferenceExpression): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromReferenceExpression',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Adds a Bicep parameter from a reference expression */
    withParameterFromReferenceExpression(name: string, value: ReferenceExpression): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withParameterFromReferenceExpressionInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromEndpointInternal(name: string, value: EndpointReference): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromEndpoint',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Adds a Bicep parameter from an endpoint reference */
    withParameterFromEndpoint(name: string, value: EndpointReference): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withParameterFromEndpointInternal(name, value));
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure/publishAsConnectionString',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Publishes an Azure resource to the manifest as a connection string */
    publishAsConnectionString(): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** Gets the normalized Bicep identifier for an Azure resource */
    async getBicepIdentifier(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting.Azure/getBicepIdentifier',
            rpcArgs
        );
    }

    /** @internal */
    private async _clearDefaultRoleAssignmentsInternal(): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure/clearDefaultRoleAssignments',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Clears the default Azure role assignments from a resource */
    clearDefaultRoleAssignments(): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._clearDefaultRoleAssignmentsInternal());
    }

    /** Determines whether a resource is marked as existing */
    async isExisting(): Promise<boolean> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<boolean>(
            'Aspire.Hosting.Azure/isExisting',
            rpcArgs
        );
    }

    /** @internal */
    private async _runAsExistingFromParametersInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure/runAsExistingFromParameters',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Marks an Azure resource as existing in run mode by using parameter resources */
    runAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._runAsExistingFromParametersInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _runAsExistingInternal(name: string, resourceGroup: string): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, resourceGroup };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure/runAsExisting',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Marks an Azure resource as existing in run mode */
    runAsExisting(name: string, resourceGroup: string): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._runAsExistingInternal(name, resourceGroup));
    }

    /** @internal */
    private async _publishAsExistingFromParametersInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure/publishAsExistingFromParameters',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Marks an Azure resource as existing in publish mode by using parameter resources */
    publishAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._publishAsExistingFromParametersInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _publishAsExistingInternal(name: string, resourceGroup: string): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, resourceGroup };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure/publishAsExisting',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Marks an Azure resource as existing in publish mode */
    publishAsExisting(name: string, resourceGroup: string): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._publishAsExistingInternal(name, resourceGroup));
    }

    /** @internal */
    private async _asExistingInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure/asExistingFromParameters',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Marks an Azure resource as existing in both run and publish modes by using parameter resources */
    asExisting(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._asExistingInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<AzureBicepResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureBicepResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new AzureBicepResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for AzureBicepResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class AzureBicepResourcePromise implements PromiseLike<AzureBicepResource> {
    constructor(private _promise: Promise<AzureBicepResource>) {}

    then<TResult1 = AzureBicepResource, TResult2 = never>(
        onfulfilled?: ((value: AzureBicepResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Gets an output reference from an Azure Bicep template resource */
    getOutput(name: string): Promise<BicepOutputReference> {
        return this._promise.then(obj => obj.getOutput(name));
    }

    /** Adds a Bicep parameter without a value */
    withParameter(name: string): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withParameter(name)));
    }

    /** Adds a Bicep parameter with a string value */
    withParameterStringValue(name: string, value: string): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withParameterStringValue(name, value)));
    }

    /** Adds a Bicep parameter with a string list value */
    withParameterStringValues(name: string, value: string[]): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withParameterStringValues(name, value)));
    }

    /** Adds a Bicep parameter from a parameter resource builder */
    withParameterFromParameter(name: string, value: ParameterResource): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withParameterFromParameter(name, value)));
    }

    /** Adds a Bicep parameter from a connection string resource builder */
    withParameterFromConnectionString(name: string, value: ResourceBuilderBase): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withParameterFromConnectionString(name, value)));
    }

    /** Adds a Bicep parameter from another Bicep output reference */
    withParameterFromOutput(name: string, value: BicepOutputReference): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withParameterFromOutput(name, value)));
    }

    /** Adds a Bicep parameter from a reference expression */
    withParameterFromReferenceExpression(name: string, value: ReferenceExpression): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withParameterFromReferenceExpression(name, value)));
    }

    /** Adds a Bicep parameter from an endpoint reference */
    withParameterFromEndpoint(name: string, value: EndpointReference): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withParameterFromEndpoint(name, value)));
    }

    /** Publishes an Azure resource to the manifest as a connection string */
    publishAsConnectionString(): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Gets the normalized Bicep identifier for an Azure resource */
    getBicepIdentifier(): Promise<string> {
        return this._promise.then(obj => obj.getBicepIdentifier());
    }

    /** Clears the default Azure role assignments from a resource */
    clearDefaultRoleAssignments(): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.clearDefaultRoleAssignments()));
    }

    /** Determines whether a resource is marked as existing */
    isExisting(): Promise<boolean> {
        return this._promise.then(obj => obj.isExisting());
    }

    /** Marks an Azure resource as existing in run mode by using parameter resources */
    runAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.runAsExistingFromParameters(nameParameter, resourceGroupParameter)));
    }

    /** Marks an Azure resource as existing in run mode */
    runAsExisting(name: string, resourceGroup: string): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.runAsExisting(name, resourceGroup)));
    }

    /** Marks an Azure resource as existing in publish mode by using parameter resources */
    publishAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.publishAsExistingFromParameters(nameParameter, resourceGroupParameter)));
    }

    /** Marks an Azure resource as existing in publish mode */
    publishAsExisting(name: string, resourceGroup: string): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.publishAsExisting(name, resourceGroup)));
    }

    /** Marks an Azure resource as existing in both run and publish modes by using parameter resources */
    asExisting(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.asExisting(nameParameter, resourceGroupParameter)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureBicepResourcePromise {
        return new AzureBicepResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// AzureContainerAppEnvironmentResource
// ============================================================================

export class AzureContainerAppEnvironmentResource extends ResourceBuilderBase<AzureContainerAppEnvironmentResourceHandle> {
    constructor(handle: AzureContainerAppEnvironmentResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureContainerAppEnvironmentResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new AzureContainerAppEnvironmentResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureContainerAppEnvironmentResourcePromise {
        const helpLink = options?.helpLink;
        return new AzureContainerAppEnvironmentResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureContainerAppEnvironmentResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureContainerAppEnvironmentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureContainerAppEnvironmentResourcePromise {
        const displayText = options?.displayText;
        return new AzureContainerAppEnvironmentResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureContainerAppEnvironmentResourcePromise {
        const displayText = options?.displayText;
        return new AzureContainerAppEnvironmentResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<AzureContainerAppEnvironmentResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<AzureContainerAppEnvironmentResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureContainerAppEnvironmentResourcePromise {
        const commandOptions = options?.commandOptions;
        return new AzureContainerAppEnvironmentResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureContainerAppEnvironmentResourcePromise {
        const iconVariant = options?.iconVariant;
        return new AzureContainerAppEnvironmentResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<AzureContainerAppEnvironmentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureContainerAppEnvironmentResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new AzureContainerAppEnvironmentResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<AzureContainerAppEnvironmentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<AzureContainerAppEnvironmentResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<AzureContainerAppEnvironmentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<AzureContainerAppEnvironmentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<AzureContainerAppEnvironmentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<AzureContainerAppEnvironmentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _withAzdResourceNamingInternal(): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/withAzdResourceNaming',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Configures resources to use azd naming conventions */
    withAzdResourceNaming(): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withAzdResourceNamingInternal());
    }

    /** @internal */
    private async _withCompactResourceNamingInternal(): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/withCompactResourceNaming',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Configures resources to use compact naming for length-constrained Azure resources */
    withCompactResourceNaming(): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withCompactResourceNamingInternal());
    }

    /** @internal */
    private async _withDashboardInternal(enable?: boolean): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (enable !== undefined) rpcArgs.enable = enable;
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/withDashboard',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Configures whether the Aspire dashboard is included in the container app environment */
    withDashboard(options?: WithDashboardOptions): AzureContainerAppEnvironmentResourcePromise {
        const enable = options?.enable;
        return new AzureContainerAppEnvironmentResourcePromise(this._withDashboardInternal(enable));
    }

    /** @internal */
    private async _withHttpsUpgradeInternal(upgrade?: boolean): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (upgrade !== undefined) rpcArgs.upgrade = upgrade;
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/withHttpsUpgrade',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Configures whether HTTP endpoints are upgraded to HTTPS */
    withHttpsUpgrade(options?: WithHttpsUpgradeOptions): AzureContainerAppEnvironmentResourcePromise {
        const upgrade = options?.upgrade;
        return new AzureContainerAppEnvironmentResourcePromise(this._withHttpsUpgradeInternal(upgrade));
    }

    /** @internal */
    private async _withAzureLogAnalyticsWorkspaceInternal(workspaceBuilder: AzureLogAnalyticsWorkspaceResource): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, workspaceBuilder };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/withAzureLogAnalyticsWorkspace',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Configures the container app environment to use a specific Log Analytics Workspace */
    withAzureLogAnalyticsWorkspace(workspaceBuilder: AzureLogAnalyticsWorkspaceResource): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withAzureLogAnalyticsWorkspaceInternal(workspaceBuilder));
    }

    /** Gets an output reference from an Azure Bicep template resource */
    async getOutput(name: string): Promise<BicepOutputReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<BicepOutputReference>(
            'Aspire.Hosting.Azure/getOutput',
            rpcArgs
        );
    }

    /** @internal */
    private async _withParameterInternal(name: string): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/withParameter',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Adds a Bicep parameter without a value */
    withParameter(name: string): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withParameterInternal(name));
    }

    /** @internal */
    private async _withParameterStringValueInternal(name: string, value: string): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/withParameterStringValue',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Adds a Bicep parameter with a string value */
    withParameterStringValue(name: string, value: string): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withParameterStringValueInternal(name, value));
    }

    /** @internal */
    private async _withParameterStringValuesInternal(name: string, value: string[]): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/withParameterStringValues',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Adds a Bicep parameter with a string list value */
    withParameterStringValues(name: string, value: string[]): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withParameterStringValuesInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromParameterInternal(name: string, value: ParameterResource): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromParameter',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Adds a Bicep parameter from a parameter resource builder */
    withParameterFromParameter(name: string, value: ParameterResource): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withParameterFromParameterInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromConnectionStringInternal(name: string, value: ResourceBuilderBase): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromConnectionString',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Adds a Bicep parameter from a connection string resource builder */
    withParameterFromConnectionString(name: string, value: ResourceBuilderBase): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withParameterFromConnectionStringInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromOutputInternal(name: string, value: BicepOutputReference): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromOutput',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Adds a Bicep parameter from another Bicep output reference */
    withParameterFromOutput(name: string, value: BicepOutputReference): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withParameterFromOutputInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromReferenceExpressionInternal(name: string, value: ReferenceExpression): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromReferenceExpression',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Adds a Bicep parameter from a reference expression */
    withParameterFromReferenceExpression(name: string, value: ReferenceExpression): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withParameterFromReferenceExpressionInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromEndpointInternal(name: string, value: EndpointReference): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromEndpoint',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Adds a Bicep parameter from an endpoint reference */
    withParameterFromEndpoint(name: string, value: EndpointReference): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withParameterFromEndpointInternal(name, value));
    }

    /** @internal */
    private async _configureInfrastructureInternal(configure: (obj: AzureResourceInfrastructure) => Promise<void>): Promise<AzureContainerAppEnvironmentResource> {
        const configureId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as AzureResourceInfrastructureHandle;
            const obj = new AzureResourceInfrastructure(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/configureInfrastructure',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Configures the Azure provisioning infrastructure callback */
    configureInfrastructure(configure: (obj: AzureResourceInfrastructure) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._configureInfrastructureInternal(configure));
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/publishAsConnectionString',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Publishes an Azure resource to the manifest as a connection string */
    publishAsConnectionString(): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** Gets the normalized Bicep identifier for an Azure resource */
    async getBicepIdentifier(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting.Azure/getBicepIdentifier',
            rpcArgs
        );
    }

    /** @internal */
    private async _clearDefaultRoleAssignmentsInternal(): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/clearDefaultRoleAssignments',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Clears the default Azure role assignments from a resource */
    clearDefaultRoleAssignments(): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._clearDefaultRoleAssignmentsInternal());
    }

    /** Determines whether a resource is marked as existing */
    async isExisting(): Promise<boolean> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<boolean>(
            'Aspire.Hosting.Azure/isExisting',
            rpcArgs
        );
    }

    /** @internal */
    private async _runAsExistingFromParametersInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/runAsExistingFromParameters',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Marks an Azure resource as existing in run mode by using parameter resources */
    runAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._runAsExistingFromParametersInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _runAsExistingInternal(name: string, resourceGroup: string): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, resourceGroup };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/runAsExisting',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Marks an Azure resource as existing in run mode */
    runAsExisting(name: string, resourceGroup: string): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._runAsExistingInternal(name, resourceGroup));
    }

    /** @internal */
    private async _publishAsExistingFromParametersInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/publishAsExistingFromParameters',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Marks an Azure resource as existing in publish mode by using parameter resources */
    publishAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._publishAsExistingFromParametersInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _publishAsExistingInternal(name: string, resourceGroup: string): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, resourceGroup };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/publishAsExisting',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Marks an Azure resource as existing in publish mode */
    publishAsExisting(name: string, resourceGroup: string): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._publishAsExistingInternal(name, resourceGroup));
    }

    /** @internal */
    private async _asExistingInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/asExistingFromParameters',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Marks an Azure resource as existing in both run and publish modes by using parameter resources */
    asExisting(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._asExistingInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<AzureContainerAppEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureContainerAppEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new AzureContainerAppEnvironmentResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for AzureContainerAppEnvironmentResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class AzureContainerAppEnvironmentResourcePromise implements PromiseLike<AzureContainerAppEnvironmentResource> {
    constructor(private _promise: Promise<AzureContainerAppEnvironmentResource>) {}

    then<TResult1 = AzureContainerAppEnvironmentResource, TResult2 = never>(
        onfulfilled?: ((value: AzureContainerAppEnvironmentResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures resources to use azd naming conventions */
    withAzdResourceNaming(): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withAzdResourceNaming()));
    }

    /** Configures resources to use compact naming for length-constrained Azure resources */
    withCompactResourceNaming(): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withCompactResourceNaming()));
    }

    /** Configures whether the Aspire dashboard is included in the container app environment */
    withDashboard(options?: WithDashboardOptions): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withDashboard(options)));
    }

    /** Configures whether HTTP endpoints are upgraded to HTTPS */
    withHttpsUpgrade(options?: WithHttpsUpgradeOptions): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withHttpsUpgrade(options)));
    }

    /** Configures the container app environment to use a specific Log Analytics Workspace */
    withAzureLogAnalyticsWorkspace(workspaceBuilder: AzureLogAnalyticsWorkspaceResource): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withAzureLogAnalyticsWorkspace(workspaceBuilder)));
    }

    /** Gets an output reference from an Azure Bicep template resource */
    getOutput(name: string): Promise<BicepOutputReference> {
        return this._promise.then(obj => obj.getOutput(name));
    }

    /** Adds a Bicep parameter without a value */
    withParameter(name: string): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withParameter(name)));
    }

    /** Adds a Bicep parameter with a string value */
    withParameterStringValue(name: string, value: string): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withParameterStringValue(name, value)));
    }

    /** Adds a Bicep parameter with a string list value */
    withParameterStringValues(name: string, value: string[]): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withParameterStringValues(name, value)));
    }

    /** Adds a Bicep parameter from a parameter resource builder */
    withParameterFromParameter(name: string, value: ParameterResource): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withParameterFromParameter(name, value)));
    }

    /** Adds a Bicep parameter from a connection string resource builder */
    withParameterFromConnectionString(name: string, value: ResourceBuilderBase): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withParameterFromConnectionString(name, value)));
    }

    /** Adds a Bicep parameter from another Bicep output reference */
    withParameterFromOutput(name: string, value: BicepOutputReference): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withParameterFromOutput(name, value)));
    }

    /** Adds a Bicep parameter from a reference expression */
    withParameterFromReferenceExpression(name: string, value: ReferenceExpression): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withParameterFromReferenceExpression(name, value)));
    }

    /** Adds a Bicep parameter from an endpoint reference */
    withParameterFromEndpoint(name: string, value: EndpointReference): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withParameterFromEndpoint(name, value)));
    }

    /** Configures the Azure provisioning infrastructure callback */
    configureInfrastructure(configure: (obj: AzureResourceInfrastructure) => Promise<void>): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.configureInfrastructure(configure)));
    }

    /** Publishes an Azure resource to the manifest as a connection string */
    publishAsConnectionString(): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Gets the normalized Bicep identifier for an Azure resource */
    getBicepIdentifier(): Promise<string> {
        return this._promise.then(obj => obj.getBicepIdentifier());
    }

    /** Clears the default Azure role assignments from a resource */
    clearDefaultRoleAssignments(): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.clearDefaultRoleAssignments()));
    }

    /** Determines whether a resource is marked as existing */
    isExisting(): Promise<boolean> {
        return this._promise.then(obj => obj.isExisting());
    }

    /** Marks an Azure resource as existing in run mode by using parameter resources */
    runAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.runAsExistingFromParameters(nameParameter, resourceGroupParameter)));
    }

    /** Marks an Azure resource as existing in run mode */
    runAsExisting(name: string, resourceGroup: string): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.runAsExisting(name, resourceGroup)));
    }

    /** Marks an Azure resource as existing in publish mode by using parameter resources */
    publishAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.publishAsExistingFromParameters(nameParameter, resourceGroupParameter)));
    }

    /** Marks an Azure resource as existing in publish mode */
    publishAsExisting(name: string, resourceGroup: string): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.publishAsExisting(name, resourceGroup)));
    }

    /** Marks an Azure resource as existing in both run and publish modes by using parameter resources */
    asExisting(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.asExisting(nameParameter, resourceGroupParameter)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureContainerAppEnvironmentResourcePromise {
        return new AzureContainerAppEnvironmentResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// AzureContainerRegistryResource
// ============================================================================

export class AzureContainerRegistryResource extends ResourceBuilderBase<AzureContainerRegistryResourceHandle> {
    constructor(handle: AzureContainerRegistryResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureContainerRegistryResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new AzureContainerRegistryResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureContainerRegistryResourcePromise {
        const helpLink = options?.helpLink;
        return new AzureContainerRegistryResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureContainerRegistryResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureContainerRegistryResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureContainerRegistryResourcePromise {
        const displayText = options?.displayText;
        return new AzureContainerRegistryResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureContainerRegistryResourcePromise {
        const displayText = options?.displayText;
        return new AzureContainerRegistryResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<AzureContainerRegistryResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<AzureContainerRegistryResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureContainerRegistryResourcePromise {
        const commandOptions = options?.commandOptions;
        return new AzureContainerRegistryResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureContainerRegistryResourcePromise {
        const iconVariant = options?.iconVariant;
        return new AzureContainerRegistryResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<AzureContainerRegistryResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureContainerRegistryResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new AzureContainerRegistryResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<AzureContainerRegistryResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<AzureContainerRegistryResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<AzureContainerRegistryResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<AzureContainerRegistryResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<AzureContainerRegistryResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<AzureContainerRegistryResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** Gets an output reference from an Azure Bicep template resource */
    async getOutput(name: string): Promise<BicepOutputReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<BicepOutputReference>(
            'Aspire.Hosting.Azure/getOutput',
            rpcArgs
        );
    }

    /** @internal */
    private async _withParameterInternal(name: string): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure/withParameter',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Adds a Bicep parameter without a value */
    withParameter(name: string): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withParameterInternal(name));
    }

    /** @internal */
    private async _withParameterStringValueInternal(name: string, value: string): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure/withParameterStringValue',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Adds a Bicep parameter with a string value */
    withParameterStringValue(name: string, value: string): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withParameterStringValueInternal(name, value));
    }

    /** @internal */
    private async _withParameterStringValuesInternal(name: string, value: string[]): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure/withParameterStringValues',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Adds a Bicep parameter with a string list value */
    withParameterStringValues(name: string, value: string[]): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withParameterStringValuesInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromParameterInternal(name: string, value: ParameterResource): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromParameter',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Adds a Bicep parameter from a parameter resource builder */
    withParameterFromParameter(name: string, value: ParameterResource): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withParameterFromParameterInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromConnectionStringInternal(name: string, value: ResourceBuilderBase): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromConnectionString',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Adds a Bicep parameter from a connection string resource builder */
    withParameterFromConnectionString(name: string, value: ResourceBuilderBase): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withParameterFromConnectionStringInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromOutputInternal(name: string, value: BicepOutputReference): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromOutput',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Adds a Bicep parameter from another Bicep output reference */
    withParameterFromOutput(name: string, value: BicepOutputReference): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withParameterFromOutputInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromReferenceExpressionInternal(name: string, value: ReferenceExpression): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromReferenceExpression',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Adds a Bicep parameter from a reference expression */
    withParameterFromReferenceExpression(name: string, value: ReferenceExpression): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withParameterFromReferenceExpressionInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromEndpointInternal(name: string, value: EndpointReference): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromEndpoint',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Adds a Bicep parameter from an endpoint reference */
    withParameterFromEndpoint(name: string, value: EndpointReference): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withParameterFromEndpointInternal(name, value));
    }

    /** @internal */
    private async _configureInfrastructureInternal(configure: (obj: AzureResourceInfrastructure) => Promise<void>): Promise<AzureContainerRegistryResource> {
        const configureId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as AzureResourceInfrastructureHandle;
            const obj = new AzureResourceInfrastructure(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure/configureInfrastructure',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Configures the Azure provisioning infrastructure callback */
    configureInfrastructure(configure: (obj: AzureResourceInfrastructure) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._configureInfrastructureInternal(configure));
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure/publishAsConnectionString',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Publishes an Azure resource to the manifest as a connection string */
    publishAsConnectionString(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** Gets the normalized Bicep identifier for an Azure resource */
    async getBicepIdentifier(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting.Azure/getBicepIdentifier',
            rpcArgs
        );
    }

    /** @internal */
    private async _clearDefaultRoleAssignmentsInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure/clearDefaultRoleAssignments',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Clears the default Azure role assignments from a resource */
    clearDefaultRoleAssignments(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._clearDefaultRoleAssignmentsInternal());
    }

    /** Determines whether a resource is marked as existing */
    async isExisting(): Promise<boolean> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<boolean>(
            'Aspire.Hosting.Azure/isExisting',
            rpcArgs
        );
    }

    /** @internal */
    private async _runAsExistingFromParametersInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure/runAsExistingFromParameters',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Marks an Azure resource as existing in run mode by using parameter resources */
    runAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._runAsExistingFromParametersInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _runAsExistingInternal(name: string, resourceGroup: string): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, resourceGroup };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure/runAsExisting',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Marks an Azure resource as existing in run mode */
    runAsExisting(name: string, resourceGroup: string): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._runAsExistingInternal(name, resourceGroup));
    }

    /** @internal */
    private async _publishAsExistingFromParametersInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure/publishAsExistingFromParameters',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Marks an Azure resource as existing in publish mode by using parameter resources */
    publishAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._publishAsExistingFromParametersInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _publishAsExistingInternal(name: string, resourceGroup: string): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, resourceGroup };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure/publishAsExisting',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Marks an Azure resource as existing in publish mode */
    publishAsExisting(name: string, resourceGroup: string): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._publishAsExistingInternal(name, resourceGroup));
    }

    /** @internal */
    private async _asExistingInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure/asExistingFromParameters',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Marks an Azure resource as existing in both run and publish modes by using parameter resources */
    asExisting(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._asExistingInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withPurgeTaskInternal(schedule: string, filter?: string, ago?: number, keep?: number, taskName?: string): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, schedule };
        if (filter !== undefined) rpcArgs.filter = filter;
        if (ago !== undefined) rpcArgs.ago = ago;
        if (keep !== undefined) rpcArgs.keep = keep;
        if (taskName !== undefined) rpcArgs.taskName = taskName;
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withPurgeTask',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Configures a purge task for the Azure Container Registry resource. */
    withPurgeTask(schedule: string, options?: WithPurgeTaskOptions): AzureContainerRegistryResourcePromise {
        const filter = options?.filter;
        const ago = options?.ago;
        const keep = options?.keep;
        const taskName = options?.taskName;
        return new AzureContainerRegistryResourcePromise(this._withPurgeTaskInternal(schedule, filter, ago, keep, taskName));
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for AzureContainerRegistryResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class AzureContainerRegistryResourcePromise implements PromiseLike<AzureContainerRegistryResource> {
    constructor(private _promise: Promise<AzureContainerRegistryResource>) {}

    then<TResult1 = AzureContainerRegistryResource, TResult2 = never>(
        onfulfilled?: ((value: AzureContainerRegistryResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Gets an output reference from an Azure Bicep template resource */
    getOutput(name: string): Promise<BicepOutputReference> {
        return this._promise.then(obj => obj.getOutput(name));
    }

    /** Adds a Bicep parameter without a value */
    withParameter(name: string): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withParameter(name)));
    }

    /** Adds a Bicep parameter with a string value */
    withParameterStringValue(name: string, value: string): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withParameterStringValue(name, value)));
    }

    /** Adds a Bicep parameter with a string list value */
    withParameterStringValues(name: string, value: string[]): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withParameterStringValues(name, value)));
    }

    /** Adds a Bicep parameter from a parameter resource builder */
    withParameterFromParameter(name: string, value: ParameterResource): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withParameterFromParameter(name, value)));
    }

    /** Adds a Bicep parameter from a connection string resource builder */
    withParameterFromConnectionString(name: string, value: ResourceBuilderBase): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withParameterFromConnectionString(name, value)));
    }

    /** Adds a Bicep parameter from another Bicep output reference */
    withParameterFromOutput(name: string, value: BicepOutputReference): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withParameterFromOutput(name, value)));
    }

    /** Adds a Bicep parameter from a reference expression */
    withParameterFromReferenceExpression(name: string, value: ReferenceExpression): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withParameterFromReferenceExpression(name, value)));
    }

    /** Adds a Bicep parameter from an endpoint reference */
    withParameterFromEndpoint(name: string, value: EndpointReference): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withParameterFromEndpoint(name, value)));
    }

    /** Configures the Azure provisioning infrastructure callback */
    configureInfrastructure(configure: (obj: AzureResourceInfrastructure) => Promise<void>): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.configureInfrastructure(configure)));
    }

    /** Publishes an Azure resource to the manifest as a connection string */
    publishAsConnectionString(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Gets the normalized Bicep identifier for an Azure resource */
    getBicepIdentifier(): Promise<string> {
        return this._promise.then(obj => obj.getBicepIdentifier());
    }

    /** Clears the default Azure role assignments from a resource */
    clearDefaultRoleAssignments(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.clearDefaultRoleAssignments()));
    }

    /** Determines whether a resource is marked as existing */
    isExisting(): Promise<boolean> {
        return this._promise.then(obj => obj.isExisting());
    }

    /** Marks an Azure resource as existing in run mode by using parameter resources */
    runAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.runAsExistingFromParameters(nameParameter, resourceGroupParameter)));
    }

    /** Marks an Azure resource as existing in run mode */
    runAsExisting(name: string, resourceGroup: string): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.runAsExisting(name, resourceGroup)));
    }

    /** Marks an Azure resource as existing in publish mode by using parameter resources */
    publishAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.publishAsExistingFromParameters(nameParameter, resourceGroupParameter)));
    }

    /** Marks an Azure resource as existing in publish mode */
    publishAsExisting(name: string, resourceGroup: string): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.publishAsExisting(name, resourceGroup)));
    }

    /** Marks an Azure resource as existing in both run and publish modes by using parameter resources */
    asExisting(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.asExisting(nameParameter, resourceGroupParameter)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Configures a purge task for the Azure Container Registry resource. */
    withPurgeTask(schedule: string, options?: WithPurgeTaskOptions): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withPurgeTask(schedule, options)));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// AzureEnvironmentResource
// ============================================================================

export class AzureEnvironmentResource extends ResourceBuilderBase<AzureEnvironmentResourceHandle> {
    constructor(handle: AzureEnvironmentResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureEnvironmentResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new AzureEnvironmentResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureEnvironmentResourcePromise {
        const helpLink = options?.helpLink;
        return new AzureEnvironmentResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureEnvironmentResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureEnvironmentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureEnvironmentResourcePromise {
        const displayText = options?.displayText;
        return new AzureEnvironmentResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureEnvironmentResourcePromise {
        const displayText = options?.displayText;
        return new AzureEnvironmentResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<AzureEnvironmentResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<AzureEnvironmentResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureEnvironmentResourcePromise {
        const commandOptions = options?.commandOptions;
        return new AzureEnvironmentResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureEnvironmentResourcePromise {
        const iconVariant = options?.iconVariant;
        return new AzureEnvironmentResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<AzureEnvironmentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureEnvironmentResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new AzureEnvironmentResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<AzureEnvironmentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<AzureEnvironmentResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<AzureEnvironmentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<AzureEnvironmentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<AzureEnvironmentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<AzureEnvironmentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _withLocationInternal(location: ParameterResource): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, location };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/withLocation',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Sets the Azure location for the shared Azure environment resource */
    withLocation(location: ParameterResource): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._withLocationInternal(location));
    }

    /** @internal */
    private async _withResourceGroupInternal(resourceGroup: ParameterResource): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, resourceGroup };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure/withResourceGroup',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Sets the Azure resource group for the shared Azure environment resource */
    withResourceGroup(resourceGroup: ParameterResource): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._withResourceGroupInternal(resourceGroup));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<AzureEnvironmentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureEnvironmentResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new AzureEnvironmentResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for AzureEnvironmentResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class AzureEnvironmentResourcePromise implements PromiseLike<AzureEnvironmentResource> {
    constructor(private _promise: Promise<AzureEnvironmentResource>) {}

    then<TResult1 = AzureEnvironmentResource, TResult2 = never>(
        onfulfilled?: ((value: AzureEnvironmentResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Sets the Azure location for the shared Azure environment resource */
    withLocation(location: ParameterResource): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withLocation(location)));
    }

    /** Sets the Azure resource group for the shared Azure environment resource */
    withResourceGroup(resourceGroup: ParameterResource): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withResourceGroup(resourceGroup)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureEnvironmentResourcePromise {
        return new AzureEnvironmentResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// AzureLogAnalyticsWorkspaceResource
// ============================================================================

export class AzureLogAnalyticsWorkspaceResource extends ResourceBuilderBase<AzureLogAnalyticsWorkspaceResourceHandle> {
    constructor(handle: AzureLogAnalyticsWorkspaceResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureLogAnalyticsWorkspaceResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureLogAnalyticsWorkspaceResourcePromise {
        const helpLink = options?.helpLink;
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureLogAnalyticsWorkspaceResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureLogAnalyticsWorkspaceResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureLogAnalyticsWorkspaceResourcePromise {
        const displayText = options?.displayText;
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureLogAnalyticsWorkspaceResourcePromise {
        const displayText = options?.displayText;
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<AzureLogAnalyticsWorkspaceResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<AzureLogAnalyticsWorkspaceResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureLogAnalyticsWorkspaceResourcePromise {
        const commandOptions = options?.commandOptions;
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureLogAnalyticsWorkspaceResourcePromise {
        const iconVariant = options?.iconVariant;
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<AzureLogAnalyticsWorkspaceResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureLogAnalyticsWorkspaceResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<AzureLogAnalyticsWorkspaceResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<AzureLogAnalyticsWorkspaceResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<AzureLogAnalyticsWorkspaceResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<AzureLogAnalyticsWorkspaceResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<AzureLogAnalyticsWorkspaceResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<AzureLogAnalyticsWorkspaceResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** Gets an output reference from an Azure Bicep template resource */
    async getOutput(name: string): Promise<BicepOutputReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<BicepOutputReference>(
            'Aspire.Hosting.Azure/getOutput',
            rpcArgs
        );
    }

    /** @internal */
    private async _withParameterInternal(name: string): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure/withParameter',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Adds a Bicep parameter without a value */
    withParameter(name: string): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withParameterInternal(name));
    }

    /** @internal */
    private async _withParameterStringValueInternal(name: string, value: string): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure/withParameterStringValue',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Adds a Bicep parameter with a string value */
    withParameterStringValue(name: string, value: string): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withParameterStringValueInternal(name, value));
    }

    /** @internal */
    private async _withParameterStringValuesInternal(name: string, value: string[]): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure/withParameterStringValues',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Adds a Bicep parameter with a string list value */
    withParameterStringValues(name: string, value: string[]): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withParameterStringValuesInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromParameterInternal(name: string, value: ParameterResource): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromParameter',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Adds a Bicep parameter from a parameter resource builder */
    withParameterFromParameter(name: string, value: ParameterResource): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withParameterFromParameterInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromConnectionStringInternal(name: string, value: ResourceBuilderBase): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromConnectionString',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Adds a Bicep parameter from a connection string resource builder */
    withParameterFromConnectionString(name: string, value: ResourceBuilderBase): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withParameterFromConnectionStringInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromOutputInternal(name: string, value: BicepOutputReference): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromOutput',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Adds a Bicep parameter from another Bicep output reference */
    withParameterFromOutput(name: string, value: BicepOutputReference): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withParameterFromOutputInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromReferenceExpressionInternal(name: string, value: ReferenceExpression): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromReferenceExpression',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Adds a Bicep parameter from a reference expression */
    withParameterFromReferenceExpression(name: string, value: ReferenceExpression): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withParameterFromReferenceExpressionInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromEndpointInternal(name: string, value: EndpointReference): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromEndpoint',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Adds a Bicep parameter from an endpoint reference */
    withParameterFromEndpoint(name: string, value: EndpointReference): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withParameterFromEndpointInternal(name, value));
    }

    /** @internal */
    private async _configureInfrastructureInternal(configure: (obj: AzureResourceInfrastructure) => Promise<void>): Promise<AzureLogAnalyticsWorkspaceResource> {
        const configureId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as AzureResourceInfrastructureHandle;
            const obj = new AzureResourceInfrastructure(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure/configureInfrastructure',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Configures the Azure provisioning infrastructure callback */
    configureInfrastructure(configure: (obj: AzureResourceInfrastructure) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._configureInfrastructureInternal(configure));
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure/publishAsConnectionString',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Publishes an Azure resource to the manifest as a connection string */
    publishAsConnectionString(): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** Gets the normalized Bicep identifier for an Azure resource */
    async getBicepIdentifier(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting.Azure/getBicepIdentifier',
            rpcArgs
        );
    }

    /** @internal */
    private async _clearDefaultRoleAssignmentsInternal(): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure/clearDefaultRoleAssignments',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Clears the default Azure role assignments from a resource */
    clearDefaultRoleAssignments(): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._clearDefaultRoleAssignmentsInternal());
    }

    /** Determines whether a resource is marked as existing */
    async isExisting(): Promise<boolean> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<boolean>(
            'Aspire.Hosting.Azure/isExisting',
            rpcArgs
        );
    }

    /** @internal */
    private async _runAsExistingFromParametersInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure/runAsExistingFromParameters',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Marks an Azure resource as existing in run mode by using parameter resources */
    runAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._runAsExistingFromParametersInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _runAsExistingInternal(name: string, resourceGroup: string): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, resourceGroup };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure/runAsExisting',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Marks an Azure resource as existing in run mode */
    runAsExisting(name: string, resourceGroup: string): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._runAsExistingInternal(name, resourceGroup));
    }

    /** @internal */
    private async _publishAsExistingFromParametersInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure/publishAsExistingFromParameters',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Marks an Azure resource as existing in publish mode by using parameter resources */
    publishAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._publishAsExistingFromParametersInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _publishAsExistingInternal(name: string, resourceGroup: string): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, resourceGroup };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure/publishAsExisting',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Marks an Azure resource as existing in publish mode */
    publishAsExisting(name: string, resourceGroup: string): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._publishAsExistingInternal(name, resourceGroup));
    }

    /** @internal */
    private async _asExistingInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure/asExistingFromParameters',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Marks an Azure resource as existing in both run and publish modes by using parameter resources */
    asExisting(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._asExistingInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<AzureLogAnalyticsWorkspaceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureLogAnalyticsWorkspaceResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new AzureLogAnalyticsWorkspaceResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for AzureLogAnalyticsWorkspaceResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class AzureLogAnalyticsWorkspaceResourcePromise implements PromiseLike<AzureLogAnalyticsWorkspaceResource> {
    constructor(private _promise: Promise<AzureLogAnalyticsWorkspaceResource>) {}

    then<TResult1 = AzureLogAnalyticsWorkspaceResource, TResult2 = never>(
        onfulfilled?: ((value: AzureLogAnalyticsWorkspaceResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Gets an output reference from an Azure Bicep template resource */
    getOutput(name: string): Promise<BicepOutputReference> {
        return this._promise.then(obj => obj.getOutput(name));
    }

    /** Adds a Bicep parameter without a value */
    withParameter(name: string): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withParameter(name)));
    }

    /** Adds a Bicep parameter with a string value */
    withParameterStringValue(name: string, value: string): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withParameterStringValue(name, value)));
    }

    /** Adds a Bicep parameter with a string list value */
    withParameterStringValues(name: string, value: string[]): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withParameterStringValues(name, value)));
    }

    /** Adds a Bicep parameter from a parameter resource builder */
    withParameterFromParameter(name: string, value: ParameterResource): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withParameterFromParameter(name, value)));
    }

    /** Adds a Bicep parameter from a connection string resource builder */
    withParameterFromConnectionString(name: string, value: ResourceBuilderBase): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withParameterFromConnectionString(name, value)));
    }

    /** Adds a Bicep parameter from another Bicep output reference */
    withParameterFromOutput(name: string, value: BicepOutputReference): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withParameterFromOutput(name, value)));
    }

    /** Adds a Bicep parameter from a reference expression */
    withParameterFromReferenceExpression(name: string, value: ReferenceExpression): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withParameterFromReferenceExpression(name, value)));
    }

    /** Adds a Bicep parameter from an endpoint reference */
    withParameterFromEndpoint(name: string, value: EndpointReference): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withParameterFromEndpoint(name, value)));
    }

    /** Configures the Azure provisioning infrastructure callback */
    configureInfrastructure(configure: (obj: AzureResourceInfrastructure) => Promise<void>): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.configureInfrastructure(configure)));
    }

    /** Publishes an Azure resource to the manifest as a connection string */
    publishAsConnectionString(): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Gets the normalized Bicep identifier for an Azure resource */
    getBicepIdentifier(): Promise<string> {
        return this._promise.then(obj => obj.getBicepIdentifier());
    }

    /** Clears the default Azure role assignments from a resource */
    clearDefaultRoleAssignments(): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.clearDefaultRoleAssignments()));
    }

    /** Determines whether a resource is marked as existing */
    isExisting(): Promise<boolean> {
        return this._promise.then(obj => obj.isExisting());
    }

    /** Marks an Azure resource as existing in run mode by using parameter resources */
    runAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.runAsExistingFromParameters(nameParameter, resourceGroupParameter)));
    }

    /** Marks an Azure resource as existing in run mode */
    runAsExisting(name: string, resourceGroup: string): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.runAsExisting(name, resourceGroup)));
    }

    /** Marks an Azure resource as existing in publish mode by using parameter resources */
    publishAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.publishAsExistingFromParameters(nameParameter, resourceGroupParameter)));
    }

    /** Marks an Azure resource as existing in publish mode */
    publishAsExisting(name: string, resourceGroup: string): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.publishAsExisting(name, resourceGroup)));
    }

    /** Marks an Azure resource as existing in both run and publish modes by using parameter resources */
    asExisting(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.asExisting(nameParameter, resourceGroupParameter)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureLogAnalyticsWorkspaceResourcePromise {
        return new AzureLogAnalyticsWorkspaceResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// AzureOpenAIDeploymentResource
// ============================================================================

export class AzureOpenAIDeploymentResource extends ResourceBuilderBase<AzureOpenAIDeploymentResourceHandle> {
    constructor(handle: AzureOpenAIDeploymentResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** Gets the DeploymentName property */
    deploymentName = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/AzureOpenAIDeploymentResource.deploymentName',
                { context: this._handle }
            );
        },
        set: async (value: string): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.ApplicationModel/AzureOpenAIDeploymentResource.setDeploymentName',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the ModelName property */
    modelName = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/AzureOpenAIDeploymentResource.modelName',
                { context: this._handle }
            );
        },
        set: async (value: string): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.ApplicationModel/AzureOpenAIDeploymentResource.setModelName',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the ModelVersion property */
    modelVersion = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/AzureOpenAIDeploymentResource.modelVersion',
                { context: this._handle }
            );
        },
        set: async (value: string): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.ApplicationModel/AzureOpenAIDeploymentResource.setModelVersion',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the SkuName property */
    skuName = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/AzureOpenAIDeploymentResource.skuName',
                { context: this._handle }
            );
        },
        set: async (value: string): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.ApplicationModel/AzureOpenAIDeploymentResource.setSkuName',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the SkuCapacity property */
    skuCapacity = {
        get: async (): Promise<number> => {
            return await this._client.invokeCapability<number>(
                'Aspire.Hosting.ApplicationModel/AzureOpenAIDeploymentResource.skuCapacity',
                { context: this._handle }
            );
        },
        set: async (value: number): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.ApplicationModel/AzureOpenAIDeploymentResource.setSkuCapacity',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the Name property */
    name = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/AzureOpenAIDeploymentResource.name',
                { context: this._handle }
            );
        },
    };

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureOpenAIDeploymentResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new AzureOpenAIDeploymentResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureOpenAIDeploymentResourcePromise {
        const helpLink = options?.helpLink;
        return new AzureOpenAIDeploymentResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withConnectionPropertyInternal(name: string, value: ReferenceExpression): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withConnectionProperty',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Adds a connection property with a reference expression */
    withConnectionProperty(name: string, value: ReferenceExpression): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._withConnectionPropertyInternal(name, value));
    }

    /** @internal */
    private async _withConnectionPropertyValueInternal(name: string, value: string): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withConnectionPropertyValue',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Adds a connection property with a string value */
    withConnectionPropertyValue(name: string, value: string): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._withConnectionPropertyValueInternal(name, value));
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureOpenAIDeploymentResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureOpenAIDeploymentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureOpenAIDeploymentResourcePromise {
        const displayText = options?.displayText;
        return new AzureOpenAIDeploymentResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureOpenAIDeploymentResourcePromise {
        const displayText = options?.displayText;
        return new AzureOpenAIDeploymentResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<AzureOpenAIDeploymentResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<AzureOpenAIDeploymentResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureOpenAIDeploymentResourcePromise {
        const commandOptions = options?.commandOptions;
        return new AzureOpenAIDeploymentResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureOpenAIDeploymentResourcePromise {
        const iconVariant = options?.iconVariant;
        return new AzureOpenAIDeploymentResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<AzureOpenAIDeploymentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureOpenAIDeploymentResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new AzureOpenAIDeploymentResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<AzureOpenAIDeploymentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<AzureOpenAIDeploymentResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<AzureOpenAIDeploymentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<AzureOpenAIDeploymentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onConnectionStringAvailableInternal(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): Promise<AzureOpenAIDeploymentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ConnectionStringAvailableEventHandle;
            const arg = new ConnectionStringAvailableEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/onConnectionStringAvailable',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Subscribes to the ConnectionStringAvailable event */
    onConnectionStringAvailable(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._onConnectionStringAvailableInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<AzureOpenAIDeploymentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<AzureOpenAIDeploymentResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withPropertiesInternal(configure: (obj: AzureOpenAIDeploymentResource) => Promise<void>): Promise<AzureOpenAIDeploymentResource> {
        const configureId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as AzureOpenAIDeploymentResourceHandle;
            const obj = new AzureOpenAIDeploymentResource(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withProperties',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Configures properties of an Azure OpenAI deployment */
    withProperties(configure: (obj: AzureOpenAIDeploymentResource) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._withPropertiesInternal(configure));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for AzureOpenAIDeploymentResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class AzureOpenAIDeploymentResourcePromise implements PromiseLike<AzureOpenAIDeploymentResource> {
    constructor(private _promise: Promise<AzureOpenAIDeploymentResource>) {}

    then<TResult1 = AzureOpenAIDeploymentResource, TResult2 = never>(
        onfulfilled?: ((value: AzureOpenAIDeploymentResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Adds a connection property with a reference expression */
    withConnectionProperty(name: string, value: ReferenceExpression): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withConnectionProperty(name, value)));
    }

    /** Adds a connection property with a string value */
    withConnectionPropertyValue(name: string, value: string): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withConnectionPropertyValue(name, value)));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the ConnectionStringAvailable event */
    onConnectionStringAvailable(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.onConnectionStringAvailable(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Configures properties of an Azure OpenAI deployment */
    withProperties(configure: (obj: AzureOpenAIDeploymentResource) => Promise<void>): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withProperties(configure)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// AzureOpenAIResource
// ============================================================================

export class AzureOpenAIResource extends ResourceBuilderBase<AzureOpenAIResourceHandle> {
    constructor(handle: AzureOpenAIResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureOpenAIResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new AzureOpenAIResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureOpenAIResourcePromise {
        const helpLink = options?.helpLink;
        return new AzureOpenAIResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withConnectionPropertyInternal(name: string, value: ReferenceExpression): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withConnectionProperty',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Adds a connection property with a reference expression */
    withConnectionProperty(name: string, value: ReferenceExpression): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withConnectionPropertyInternal(name, value));
    }

    /** @internal */
    private async _withConnectionPropertyValueInternal(name: string, value: string): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withConnectionPropertyValue',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Adds a connection property with a string value */
    withConnectionPropertyValue(name: string, value: string): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withConnectionPropertyValueInternal(name, value));
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureOpenAIResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureOpenAIResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureOpenAIResourcePromise {
        const displayText = options?.displayText;
        return new AzureOpenAIResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureOpenAIResourcePromise {
        const displayText = options?.displayText;
        return new AzureOpenAIResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<AzureOpenAIResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<AzureOpenAIResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureOpenAIResourcePromise {
        const commandOptions = options?.commandOptions;
        return new AzureOpenAIResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureOpenAIResourcePromise {
        const iconVariant = options?.iconVariant;
        return new AzureOpenAIResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<AzureOpenAIResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureOpenAIResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new AzureOpenAIResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<AzureOpenAIResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<AzureOpenAIResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<AzureOpenAIResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<AzureOpenAIResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onConnectionStringAvailableInternal(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): Promise<AzureOpenAIResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ConnectionStringAvailableEventHandle;
            const arg = new ConnectionStringAvailableEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/onConnectionStringAvailable',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Subscribes to the ConnectionStringAvailable event */
    onConnectionStringAvailable(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._onConnectionStringAvailableInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<AzureOpenAIResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<AzureOpenAIResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _addDeploymentInternal(name: string, modelName: string, modelVersion: string): Promise<AzureOpenAIDeploymentResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, modelName, modelVersion };
        const result = await this._client.invokeCapability<AzureOpenAIDeploymentResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/addDeployment',
            rpcArgs
        );
        return new AzureOpenAIDeploymentResource(result, this._client);
    }

    /** Adds an Azure OpenAI deployment resource */
    addDeployment(name: string, modelName: string, modelVersion: string): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._addDeploymentInternal(name, modelName, modelVersion));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** Gets an output reference from an Azure Bicep template resource */
    async getOutput(name: string): Promise<BicepOutputReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<BicepOutputReference>(
            'Aspire.Hosting.Azure/getOutput',
            rpcArgs
        );
    }

    /** @internal */
    private async _withParameterInternal(name: string): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure/withParameter',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Adds a Bicep parameter without a value */
    withParameter(name: string): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withParameterInternal(name));
    }

    /** @internal */
    private async _withParameterStringValueInternal(name: string, value: string): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure/withParameterStringValue',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Adds a Bicep parameter with a string value */
    withParameterStringValue(name: string, value: string): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withParameterStringValueInternal(name, value));
    }

    /** @internal */
    private async _withParameterStringValuesInternal(name: string, value: string[]): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure/withParameterStringValues',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Adds a Bicep parameter with a string list value */
    withParameterStringValues(name: string, value: string[]): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withParameterStringValuesInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromParameterInternal(name: string, value: ParameterResource): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromParameter',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Adds a Bicep parameter from a parameter resource builder */
    withParameterFromParameter(name: string, value: ParameterResource): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withParameterFromParameterInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromConnectionStringInternal(name: string, value: ResourceBuilderBase): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromConnectionString',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Adds a Bicep parameter from a connection string resource builder */
    withParameterFromConnectionString(name: string, value: ResourceBuilderBase): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withParameterFromConnectionStringInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromOutputInternal(name: string, value: BicepOutputReference): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromOutput',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Adds a Bicep parameter from another Bicep output reference */
    withParameterFromOutput(name: string, value: BicepOutputReference): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withParameterFromOutputInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromReferenceExpressionInternal(name: string, value: ReferenceExpression): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromReferenceExpression',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Adds a Bicep parameter from a reference expression */
    withParameterFromReferenceExpression(name: string, value: ReferenceExpression): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withParameterFromReferenceExpressionInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromEndpointInternal(name: string, value: EndpointReference): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromEndpoint',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Adds a Bicep parameter from an endpoint reference */
    withParameterFromEndpoint(name: string, value: EndpointReference): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withParameterFromEndpointInternal(name, value));
    }

    /** @internal */
    private async _configureInfrastructureInternal(configure: (obj: AzureResourceInfrastructure) => Promise<void>): Promise<AzureOpenAIResource> {
        const configureId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as AzureResourceInfrastructureHandle;
            const obj = new AzureResourceInfrastructure(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure/configureInfrastructure',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Configures the Azure provisioning infrastructure callback */
    configureInfrastructure(configure: (obj: AzureResourceInfrastructure) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._configureInfrastructureInternal(configure));
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure/publishAsConnectionString',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Publishes an Azure resource to the manifest as a connection string */
    publishAsConnectionString(): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** Gets the normalized Bicep identifier for an Azure resource */
    async getBicepIdentifier(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting.Azure/getBicepIdentifier',
            rpcArgs
        );
    }

    /** @internal */
    private async _clearDefaultRoleAssignmentsInternal(): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure/clearDefaultRoleAssignments',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Clears the default Azure role assignments from a resource */
    clearDefaultRoleAssignments(): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._clearDefaultRoleAssignmentsInternal());
    }

    /** Determines whether a resource is marked as existing */
    async isExisting(): Promise<boolean> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<boolean>(
            'Aspire.Hosting.Azure/isExisting',
            rpcArgs
        );
    }

    /** @internal */
    private async _runAsExistingFromParametersInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure/runAsExistingFromParameters',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Marks an Azure resource as existing in run mode by using parameter resources */
    runAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._runAsExistingFromParametersInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _runAsExistingInternal(name: string, resourceGroup: string): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, resourceGroup };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure/runAsExisting',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Marks an Azure resource as existing in run mode */
    runAsExisting(name: string, resourceGroup: string): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._runAsExistingInternal(name, resourceGroup));
    }

    /** @internal */
    private async _publishAsExistingFromParametersInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure/publishAsExistingFromParameters',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Marks an Azure resource as existing in publish mode by using parameter resources */
    publishAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._publishAsExistingFromParametersInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _publishAsExistingInternal(name: string, resourceGroup: string): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, resourceGroup };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure/publishAsExisting',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Marks an Azure resource as existing in publish mode */
    publishAsExisting(name: string, resourceGroup: string): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._publishAsExistingInternal(name, resourceGroup));
    }

    /** @internal */
    private async _asExistingInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure/asExistingFromParameters',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Marks an Azure resource as existing in both run and publish modes by using parameter resources */
    asExisting(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._asExistingInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<AzureOpenAIResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureOpenAIResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new AzureOpenAIResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for AzureOpenAIResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class AzureOpenAIResourcePromise implements PromiseLike<AzureOpenAIResource> {
    constructor(private _promise: Promise<AzureOpenAIResource>) {}

    then<TResult1 = AzureOpenAIResource, TResult2 = never>(
        onfulfilled?: ((value: AzureOpenAIResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Adds a connection property with a reference expression */
    withConnectionProperty(name: string, value: ReferenceExpression): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withConnectionProperty(name, value)));
    }

    /** Adds a connection property with a string value */
    withConnectionPropertyValue(name: string, value: string): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withConnectionPropertyValue(name, value)));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the ConnectionStringAvailable event */
    onConnectionStringAvailable(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.onConnectionStringAvailable(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Adds an Azure OpenAI deployment resource */
    addDeployment(name: string, modelName: string, modelVersion: string): AzureOpenAIDeploymentResourcePromise {
        return new AzureOpenAIDeploymentResourcePromise(this._promise.then(obj => obj.addDeployment(name, modelName, modelVersion)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Gets an output reference from an Azure Bicep template resource */
    getOutput(name: string): Promise<BicepOutputReference> {
        return this._promise.then(obj => obj.getOutput(name));
    }

    /** Adds a Bicep parameter without a value */
    withParameter(name: string): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withParameter(name)));
    }

    /** Adds a Bicep parameter with a string value */
    withParameterStringValue(name: string, value: string): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withParameterStringValue(name, value)));
    }

    /** Adds a Bicep parameter with a string list value */
    withParameterStringValues(name: string, value: string[]): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withParameterStringValues(name, value)));
    }

    /** Adds a Bicep parameter from a parameter resource builder */
    withParameterFromParameter(name: string, value: ParameterResource): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withParameterFromParameter(name, value)));
    }

    /** Adds a Bicep parameter from a connection string resource builder */
    withParameterFromConnectionString(name: string, value: ResourceBuilderBase): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withParameterFromConnectionString(name, value)));
    }

    /** Adds a Bicep parameter from another Bicep output reference */
    withParameterFromOutput(name: string, value: BicepOutputReference): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withParameterFromOutput(name, value)));
    }

    /** Adds a Bicep parameter from a reference expression */
    withParameterFromReferenceExpression(name: string, value: ReferenceExpression): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withParameterFromReferenceExpression(name, value)));
    }

    /** Adds a Bicep parameter from an endpoint reference */
    withParameterFromEndpoint(name: string, value: EndpointReference): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withParameterFromEndpoint(name, value)));
    }

    /** Configures the Azure provisioning infrastructure callback */
    configureInfrastructure(configure: (obj: AzureResourceInfrastructure) => Promise<void>): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.configureInfrastructure(configure)));
    }

    /** Publishes an Azure resource to the manifest as a connection string */
    publishAsConnectionString(): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Gets the normalized Bicep identifier for an Azure resource */
    getBicepIdentifier(): Promise<string> {
        return this._promise.then(obj => obj.getBicepIdentifier());
    }

    /** Clears the default Azure role assignments from a resource */
    clearDefaultRoleAssignments(): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.clearDefaultRoleAssignments()));
    }

    /** Determines whether a resource is marked as existing */
    isExisting(): Promise<boolean> {
        return this._promise.then(obj => obj.isExisting());
    }

    /** Marks an Azure resource as existing in run mode by using parameter resources */
    runAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.runAsExistingFromParameters(nameParameter, resourceGroupParameter)));
    }

    /** Marks an Azure resource as existing in run mode */
    runAsExisting(name: string, resourceGroup: string): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.runAsExisting(name, resourceGroup)));
    }

    /** Marks an Azure resource as existing in publish mode by using parameter resources */
    publishAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.publishAsExistingFromParameters(nameParameter, resourceGroupParameter)));
    }

    /** Marks an Azure resource as existing in publish mode */
    publishAsExisting(name: string, resourceGroup: string): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.publishAsExisting(name, resourceGroup)));
    }

    /** Marks an Azure resource as existing in both run and publish modes by using parameter resources */
    asExisting(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.asExisting(nameParameter, resourceGroupParameter)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureOpenAIResourcePromise {
        return new AzureOpenAIResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// AzureProvisioningResource
// ============================================================================

export class AzureProvisioningResource extends ResourceBuilderBase<AzureProvisioningResourceHandle> {
    constructor(handle: AzureProvisioningResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureProvisioningResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new AzureProvisioningResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureProvisioningResourcePromise {
        const helpLink = options?.helpLink;
        return new AzureProvisioningResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureProvisioningResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureProvisioningResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureProvisioningResourcePromise {
        const displayText = options?.displayText;
        return new AzureProvisioningResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureProvisioningResourcePromise {
        const displayText = options?.displayText;
        return new AzureProvisioningResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<AzureProvisioningResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<AzureProvisioningResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureProvisioningResourcePromise {
        const commandOptions = options?.commandOptions;
        return new AzureProvisioningResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureProvisioningResourcePromise {
        const iconVariant = options?.iconVariant;
        return new AzureProvisioningResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<AzureProvisioningResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureProvisioningResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new AzureProvisioningResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<AzureProvisioningResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<AzureProvisioningResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<AzureProvisioningResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<AzureProvisioningResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<AzureProvisioningResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<AzureProvisioningResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** Gets an output reference from an Azure Bicep template resource */
    async getOutput(name: string): Promise<BicepOutputReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<BicepOutputReference>(
            'Aspire.Hosting.Azure/getOutput',
            rpcArgs
        );
    }

    /** @internal */
    private async _withParameterInternal(name: string): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure/withParameter',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Adds a Bicep parameter without a value */
    withParameter(name: string): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withParameterInternal(name));
    }

    /** @internal */
    private async _withParameterStringValueInternal(name: string, value: string): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure/withParameterStringValue',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Adds a Bicep parameter with a string value */
    withParameterStringValue(name: string, value: string): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withParameterStringValueInternal(name, value));
    }

    /** @internal */
    private async _withParameterStringValuesInternal(name: string, value: string[]): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure/withParameterStringValues',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Adds a Bicep parameter with a string list value */
    withParameterStringValues(name: string, value: string[]): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withParameterStringValuesInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromParameterInternal(name: string, value: ParameterResource): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromParameter',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Adds a Bicep parameter from a parameter resource builder */
    withParameterFromParameter(name: string, value: ParameterResource): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withParameterFromParameterInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromConnectionStringInternal(name: string, value: ResourceBuilderBase): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromConnectionString',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Adds a Bicep parameter from a connection string resource builder */
    withParameterFromConnectionString(name: string, value: ResourceBuilderBase): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withParameterFromConnectionStringInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromOutputInternal(name: string, value: BicepOutputReference): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromOutput',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Adds a Bicep parameter from another Bicep output reference */
    withParameterFromOutput(name: string, value: BicepOutputReference): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withParameterFromOutputInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromReferenceExpressionInternal(name: string, value: ReferenceExpression): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromReferenceExpression',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Adds a Bicep parameter from a reference expression */
    withParameterFromReferenceExpression(name: string, value: ReferenceExpression): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withParameterFromReferenceExpressionInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromEndpointInternal(name: string, value: EndpointReference): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromEndpoint',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Adds a Bicep parameter from an endpoint reference */
    withParameterFromEndpoint(name: string, value: EndpointReference): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withParameterFromEndpointInternal(name, value));
    }

    /** @internal */
    private async _configureInfrastructureInternal(configure: (obj: AzureResourceInfrastructure) => Promise<void>): Promise<AzureProvisioningResource> {
        const configureId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as AzureResourceInfrastructureHandle;
            const obj = new AzureResourceInfrastructure(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure/configureInfrastructure',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Configures the Azure provisioning infrastructure callback */
    configureInfrastructure(configure: (obj: AzureResourceInfrastructure) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._configureInfrastructureInternal(configure));
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure/publishAsConnectionString',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Publishes an Azure resource to the manifest as a connection string */
    publishAsConnectionString(): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** Gets the normalized Bicep identifier for an Azure resource */
    async getBicepIdentifier(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting.Azure/getBicepIdentifier',
            rpcArgs
        );
    }

    /** @internal */
    private async _clearDefaultRoleAssignmentsInternal(): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure/clearDefaultRoleAssignments',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Clears the default Azure role assignments from a resource */
    clearDefaultRoleAssignments(): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._clearDefaultRoleAssignmentsInternal());
    }

    /** Determines whether a resource is marked as existing */
    async isExisting(): Promise<boolean> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<boolean>(
            'Aspire.Hosting.Azure/isExisting',
            rpcArgs
        );
    }

    /** @internal */
    private async _runAsExistingFromParametersInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure/runAsExistingFromParameters',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Marks an Azure resource as existing in run mode by using parameter resources */
    runAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._runAsExistingFromParametersInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _runAsExistingInternal(name: string, resourceGroup: string): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, resourceGroup };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure/runAsExisting',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Marks an Azure resource as existing in run mode */
    runAsExisting(name: string, resourceGroup: string): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._runAsExistingInternal(name, resourceGroup));
    }

    /** @internal */
    private async _publishAsExistingFromParametersInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure/publishAsExistingFromParameters',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Marks an Azure resource as existing in publish mode by using parameter resources */
    publishAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._publishAsExistingFromParametersInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _publishAsExistingInternal(name: string, resourceGroup: string): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, resourceGroup };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure/publishAsExisting',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Marks an Azure resource as existing in publish mode */
    publishAsExisting(name: string, resourceGroup: string): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._publishAsExistingInternal(name, resourceGroup));
    }

    /** @internal */
    private async _asExistingInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure/asExistingFromParameters',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Marks an Azure resource as existing in both run and publish modes by using parameter resources */
    asExisting(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._asExistingInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<AzureProvisioningResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureProvisioningResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new AzureProvisioningResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for AzureProvisioningResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class AzureProvisioningResourcePromise implements PromiseLike<AzureProvisioningResource> {
    constructor(private _promise: Promise<AzureProvisioningResource>) {}

    then<TResult1 = AzureProvisioningResource, TResult2 = never>(
        onfulfilled?: ((value: AzureProvisioningResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Gets an output reference from an Azure Bicep template resource */
    getOutput(name: string): Promise<BicepOutputReference> {
        return this._promise.then(obj => obj.getOutput(name));
    }

    /** Adds a Bicep parameter without a value */
    withParameter(name: string): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withParameter(name)));
    }

    /** Adds a Bicep parameter with a string value */
    withParameterStringValue(name: string, value: string): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withParameterStringValue(name, value)));
    }

    /** Adds a Bicep parameter with a string list value */
    withParameterStringValues(name: string, value: string[]): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withParameterStringValues(name, value)));
    }

    /** Adds a Bicep parameter from a parameter resource builder */
    withParameterFromParameter(name: string, value: ParameterResource): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withParameterFromParameter(name, value)));
    }

    /** Adds a Bicep parameter from a connection string resource builder */
    withParameterFromConnectionString(name: string, value: ResourceBuilderBase): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withParameterFromConnectionString(name, value)));
    }

    /** Adds a Bicep parameter from another Bicep output reference */
    withParameterFromOutput(name: string, value: BicepOutputReference): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withParameterFromOutput(name, value)));
    }

    /** Adds a Bicep parameter from a reference expression */
    withParameterFromReferenceExpression(name: string, value: ReferenceExpression): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withParameterFromReferenceExpression(name, value)));
    }

    /** Adds a Bicep parameter from an endpoint reference */
    withParameterFromEndpoint(name: string, value: EndpointReference): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withParameterFromEndpoint(name, value)));
    }

    /** Configures the Azure provisioning infrastructure callback */
    configureInfrastructure(configure: (obj: AzureResourceInfrastructure) => Promise<void>): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.configureInfrastructure(configure)));
    }

    /** Publishes an Azure resource to the manifest as a connection string */
    publishAsConnectionString(): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Gets the normalized Bicep identifier for an Azure resource */
    getBicepIdentifier(): Promise<string> {
        return this._promise.then(obj => obj.getBicepIdentifier());
    }

    /** Clears the default Azure role assignments from a resource */
    clearDefaultRoleAssignments(): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.clearDefaultRoleAssignments()));
    }

    /** Determines whether a resource is marked as existing */
    isExisting(): Promise<boolean> {
        return this._promise.then(obj => obj.isExisting());
    }

    /** Marks an Azure resource as existing in run mode by using parameter resources */
    runAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.runAsExistingFromParameters(nameParameter, resourceGroupParameter)));
    }

    /** Marks an Azure resource as existing in run mode */
    runAsExisting(name: string, resourceGroup: string): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.runAsExisting(name, resourceGroup)));
    }

    /** Marks an Azure resource as existing in publish mode by using parameter resources */
    publishAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.publishAsExistingFromParameters(nameParameter, resourceGroupParameter)));
    }

    /** Marks an Azure resource as existing in publish mode */
    publishAsExisting(name: string, resourceGroup: string): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.publishAsExisting(name, resourceGroup)));
    }

    /** Marks an Azure resource as existing in both run and publish modes by using parameter resources */
    asExisting(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.asExisting(nameParameter, resourceGroupParameter)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureProvisioningResourcePromise {
        return new AzureProvisioningResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// AzureUserAssignedIdentityResource
// ============================================================================

export class AzureUserAssignedIdentityResource extends ResourceBuilderBase<AzureUserAssignedIdentityResourceHandle> {
    constructor(handle: AzureUserAssignedIdentityResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureUserAssignedIdentityResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new AzureUserAssignedIdentityResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureUserAssignedIdentityResourcePromise {
        const helpLink = options?.helpLink;
        return new AzureUserAssignedIdentityResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureUserAssignedIdentityResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<AzureUserAssignedIdentityResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureUserAssignedIdentityResourcePromise {
        const displayText = options?.displayText;
        return new AzureUserAssignedIdentityResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureUserAssignedIdentityResourcePromise {
        const displayText = options?.displayText;
        return new AzureUserAssignedIdentityResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<AzureUserAssignedIdentityResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<AzureUserAssignedIdentityResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureUserAssignedIdentityResourcePromise {
        const commandOptions = options?.commandOptions;
        return new AzureUserAssignedIdentityResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureUserAssignedIdentityResourcePromise {
        const iconVariant = options?.iconVariant;
        return new AzureUserAssignedIdentityResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<AzureUserAssignedIdentityResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureUserAssignedIdentityResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new AzureUserAssignedIdentityResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<AzureUserAssignedIdentityResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<AzureUserAssignedIdentityResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<AzureUserAssignedIdentityResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<AzureUserAssignedIdentityResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<AzureUserAssignedIdentityResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<AzureUserAssignedIdentityResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** Gets an output reference from an Azure Bicep template resource */
    async getOutput(name: string): Promise<BicepOutputReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<BicepOutputReference>(
            'Aspire.Hosting.Azure/getOutput',
            rpcArgs
        );
    }

    /** @internal */
    private async _withParameterInternal(name: string): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure/withParameter',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Adds a Bicep parameter without a value */
    withParameter(name: string): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withParameterInternal(name));
    }

    /** @internal */
    private async _withParameterStringValueInternal(name: string, value: string): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure/withParameterStringValue',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Adds a Bicep parameter with a string value */
    withParameterStringValue(name: string, value: string): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withParameterStringValueInternal(name, value));
    }

    /** @internal */
    private async _withParameterStringValuesInternal(name: string, value: string[]): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure/withParameterStringValues',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Adds a Bicep parameter with a string list value */
    withParameterStringValues(name: string, value: string[]): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withParameterStringValuesInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromParameterInternal(name: string, value: ParameterResource): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromParameter',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Adds a Bicep parameter from a parameter resource builder */
    withParameterFromParameter(name: string, value: ParameterResource): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withParameterFromParameterInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromConnectionStringInternal(name: string, value: ResourceBuilderBase): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromConnectionString',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Adds a Bicep parameter from a connection string resource builder */
    withParameterFromConnectionString(name: string, value: ResourceBuilderBase): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withParameterFromConnectionStringInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromOutputInternal(name: string, value: BicepOutputReference): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromOutput',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Adds a Bicep parameter from another Bicep output reference */
    withParameterFromOutput(name: string, value: BicepOutputReference): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withParameterFromOutputInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromReferenceExpressionInternal(name: string, value: ReferenceExpression): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromReferenceExpression',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Adds a Bicep parameter from a reference expression */
    withParameterFromReferenceExpression(name: string, value: ReferenceExpression): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withParameterFromReferenceExpressionInternal(name, value));
    }

    /** @internal */
    private async _withParameterFromEndpointInternal(name: string, value: EndpointReference): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure/withParameterFromEndpoint',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Adds a Bicep parameter from an endpoint reference */
    withParameterFromEndpoint(name: string, value: EndpointReference): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withParameterFromEndpointInternal(name, value));
    }

    /** @internal */
    private async _configureInfrastructureInternal(configure: (obj: AzureResourceInfrastructure) => Promise<void>): Promise<AzureUserAssignedIdentityResource> {
        const configureId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as AzureResourceInfrastructureHandle;
            const obj = new AzureResourceInfrastructure(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure/configureInfrastructure',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Configures the Azure provisioning infrastructure callback */
    configureInfrastructure(configure: (obj: AzureResourceInfrastructure) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._configureInfrastructureInternal(configure));
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure/publishAsConnectionString',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Publishes an Azure resource to the manifest as a connection string */
    publishAsConnectionString(): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** Gets the normalized Bicep identifier for an Azure resource */
    async getBicepIdentifier(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting.Azure/getBicepIdentifier',
            rpcArgs
        );
    }

    /** @internal */
    private async _clearDefaultRoleAssignmentsInternal(): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure/clearDefaultRoleAssignments',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Clears the default Azure role assignments from a resource */
    clearDefaultRoleAssignments(): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._clearDefaultRoleAssignmentsInternal());
    }

    /** Determines whether a resource is marked as existing */
    async isExisting(): Promise<boolean> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<boolean>(
            'Aspire.Hosting.Azure/isExisting',
            rpcArgs
        );
    }

    /** @internal */
    private async _runAsExistingFromParametersInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure/runAsExistingFromParameters',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Marks an Azure resource as existing in run mode by using parameter resources */
    runAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._runAsExistingFromParametersInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _runAsExistingInternal(name: string, resourceGroup: string): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, resourceGroup };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure/runAsExisting',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Marks an Azure resource as existing in run mode */
    runAsExisting(name: string, resourceGroup: string): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._runAsExistingInternal(name, resourceGroup));
    }

    /** @internal */
    private async _publishAsExistingFromParametersInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure/publishAsExistingFromParameters',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Marks an Azure resource as existing in publish mode by using parameter resources */
    publishAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._publishAsExistingFromParametersInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _publishAsExistingInternal(name: string, resourceGroup: string): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, resourceGroup };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure/publishAsExisting',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Marks an Azure resource as existing in publish mode */
    publishAsExisting(name: string, resourceGroup: string): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._publishAsExistingInternal(name, resourceGroup));
    }

    /** @internal */
    private async _asExistingInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure/asExistingFromParameters',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Marks an Azure resource as existing in both run and publish modes by using parameter resources */
    asExisting(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._asExistingInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<AzureUserAssignedIdentityResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<AzureUserAssignedIdentityResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new AzureUserAssignedIdentityResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for AzureUserAssignedIdentityResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class AzureUserAssignedIdentityResourcePromise implements PromiseLike<AzureUserAssignedIdentityResource> {
    constructor(private _promise: Promise<AzureUserAssignedIdentityResource>) {}

    then<TResult1 = AzureUserAssignedIdentityResource, TResult2 = never>(
        onfulfilled?: ((value: AzureUserAssignedIdentityResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Gets an output reference from an Azure Bicep template resource */
    getOutput(name: string): Promise<BicepOutputReference> {
        return this._promise.then(obj => obj.getOutput(name));
    }

    /** Adds a Bicep parameter without a value */
    withParameter(name: string): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withParameter(name)));
    }

    /** Adds a Bicep parameter with a string value */
    withParameterStringValue(name: string, value: string): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withParameterStringValue(name, value)));
    }

    /** Adds a Bicep parameter with a string list value */
    withParameterStringValues(name: string, value: string[]): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withParameterStringValues(name, value)));
    }

    /** Adds a Bicep parameter from a parameter resource builder */
    withParameterFromParameter(name: string, value: ParameterResource): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withParameterFromParameter(name, value)));
    }

    /** Adds a Bicep parameter from a connection string resource builder */
    withParameterFromConnectionString(name: string, value: ResourceBuilderBase): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withParameterFromConnectionString(name, value)));
    }

    /** Adds a Bicep parameter from another Bicep output reference */
    withParameterFromOutput(name: string, value: BicepOutputReference): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withParameterFromOutput(name, value)));
    }

    /** Adds a Bicep parameter from a reference expression */
    withParameterFromReferenceExpression(name: string, value: ReferenceExpression): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withParameterFromReferenceExpression(name, value)));
    }

    /** Adds a Bicep parameter from an endpoint reference */
    withParameterFromEndpoint(name: string, value: EndpointReference): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withParameterFromEndpoint(name, value)));
    }

    /** Configures the Azure provisioning infrastructure callback */
    configureInfrastructure(configure: (obj: AzureResourceInfrastructure) => Promise<void>): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.configureInfrastructure(configure)));
    }

    /** Publishes an Azure resource to the manifest as a connection string */
    publishAsConnectionString(): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Gets the normalized Bicep identifier for an Azure resource */
    getBicepIdentifier(): Promise<string> {
        return this._promise.then(obj => obj.getBicepIdentifier());
    }

    /** Clears the default Azure role assignments from a resource */
    clearDefaultRoleAssignments(): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.clearDefaultRoleAssignments()));
    }

    /** Determines whether a resource is marked as existing */
    isExisting(): Promise<boolean> {
        return this._promise.then(obj => obj.isExisting());
    }

    /** Marks an Azure resource as existing in run mode by using parameter resources */
    runAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.runAsExistingFromParameters(nameParameter, resourceGroupParameter)));
    }

    /** Marks an Azure resource as existing in run mode */
    runAsExisting(name: string, resourceGroup: string): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.runAsExisting(name, resourceGroup)));
    }

    /** Marks an Azure resource as existing in publish mode by using parameter resources */
    publishAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.publishAsExistingFromParameters(nameParameter, resourceGroupParameter)));
    }

    /** Marks an Azure resource as existing in publish mode */
    publishAsExisting(name: string, resourceGroup: string): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.publishAsExisting(name, resourceGroup)));
    }

    /** Marks an Azure resource as existing in both run and publish modes by using parameter resources */
    asExisting(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.asExisting(nameParameter, resourceGroupParameter)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): AzureUserAssignedIdentityResourcePromise {
        return new AzureUserAssignedIdentityResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// ConnectionStringResource
// ============================================================================

export class ConnectionStringResource extends ResourceBuilderBase<ConnectionStringResourceHandle> {
    constructor(handle: ConnectionStringResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): ConnectionStringResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new ConnectionStringResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): ConnectionStringResourcePromise {
        const helpLink = options?.helpLink;
        return new ConnectionStringResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withConnectionPropertyInternal(name: string, value: ReferenceExpression): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withConnectionProperty',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Adds a connection property with a reference expression */
    withConnectionProperty(name: string, value: ReferenceExpression): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._withConnectionPropertyInternal(name, value));
    }

    /** @internal */
    private async _withConnectionPropertyValueInternal(name: string, value: string): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withConnectionPropertyValue',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Adds a connection property with a string value */
    withConnectionPropertyValue(name: string, value: string): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._withConnectionPropertyValueInternal(name, value));
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<ConnectionStringResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<ConnectionStringResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): ConnectionStringResourcePromise {
        const displayText = options?.displayText;
        return new ConnectionStringResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): ConnectionStringResourcePromise {
        const displayText = options?.displayText;
        return new ConnectionStringResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<ConnectionStringResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _waitForInternal(dependency: ResourceBuilderBase): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/waitFor',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._waitForInternal(dependency));
    }

    /** @internal */
    private async _waitForWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/waitForWithBehavior',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._waitForWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _waitForStartInternal(dependency: ResourceBuilderBase): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/waitForStart',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._waitForStartInternal(dependency));
    }

    /** @internal */
    private async _waitForStartWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/waitForStartWithBehavior',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._waitForStartWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _waitForCompletionInternal(dependency: ResourceBuilderBase, exitCode?: number): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        if (exitCode !== undefined) rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/waitForCompletion',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): ConnectionStringResourcePromise {
        const exitCode = options?.exitCode;
        return new ConnectionStringResourcePromise(this._waitForCompletionInternal(dependency, exitCode));
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<ConnectionStringResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): ConnectionStringResourcePromise {
        const commandOptions = options?.commandOptions;
        return new ConnectionStringResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): ConnectionStringResourcePromise {
        const iconVariant = options?.iconVariant;
        return new ConnectionStringResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<ConnectionStringResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): ConnectionStringResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new ConnectionStringResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<ConnectionStringResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<ConnectionStringResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<ConnectionStringResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<ConnectionStringResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onConnectionStringAvailableInternal(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): Promise<ConnectionStringResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ConnectionStringAvailableEventHandle;
            const arg = new ConnectionStringAvailableEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/onConnectionStringAvailable',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Subscribes to the ConnectionStringAvailable event */
    onConnectionStringAvailable(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._onConnectionStringAvailableInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<ConnectionStringResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<ConnectionStringResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<ConnectionStringResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<ConnectionStringResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new ConnectionStringResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for ConnectionStringResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class ConnectionStringResourcePromise implements PromiseLike<ConnectionStringResource> {
    constructor(private _promise: Promise<ConnectionStringResource>) {}

    then<TResult1 = ConnectionStringResource, TResult2 = never>(
        onfulfilled?: ((value: ConnectionStringResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Adds a connection property with a reference expression */
    withConnectionProperty(name: string, value: ReferenceExpression): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withConnectionProperty(name, value)));
    }

    /** Adds a connection property with a string value */
    withConnectionPropertyValue(name: string, value: string): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withConnectionPropertyValue(name, value)));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.waitFor(dependency)));
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.waitForWithBehavior(dependency, waitBehavior)));
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.waitForStart(dependency)));
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.waitForStartWithBehavior(dependency, waitBehavior)));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.waitForCompletion(dependency, options)));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the ConnectionStringAvailable event */
    onConnectionStringAvailable(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.onConnectionStringAvailable(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): ConnectionStringResourcePromise {
        return new ConnectionStringResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// ContainerRegistryResource
// ============================================================================

export class ContainerRegistryResource extends ResourceBuilderBase<ContainerRegistryResourceHandle> {
    constructor(handle: ContainerRegistryResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<ContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<ContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): ContainerRegistryResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new ContainerRegistryResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<ContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): ContainerRegistryResourcePromise {
        const helpLink = options?.helpLink;
        return new ContainerRegistryResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<ContainerRegistryResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<ContainerRegistryResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<ContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): ContainerRegistryResourcePromise {
        const displayText = options?.displayText;
        return new ContainerRegistryResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<ContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): ContainerRegistryResourcePromise {
        const displayText = options?.displayText;
        return new ContainerRegistryResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<ContainerRegistryResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<ContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<ContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<ContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<ContainerRegistryResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): ContainerRegistryResourcePromise {
        const commandOptions = options?.commandOptions;
        return new ContainerRegistryResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<ContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<ContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<ContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): ContainerRegistryResourcePromise {
        const iconVariant = options?.iconVariant;
        return new ContainerRegistryResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<ContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<ContainerRegistryResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): ContainerRegistryResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new ContainerRegistryResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<ContainerRegistryResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<ContainerRegistryResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<ContainerRegistryResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<ContainerRegistryResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<ContainerRegistryResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<ContainerRegistryResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<ContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<ContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<ContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<ContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new ContainerRegistryResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for ContainerRegistryResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class ContainerRegistryResourcePromise implements PromiseLike<ContainerRegistryResource> {
    constructor(private _promise: Promise<ContainerRegistryResource>) {}

    then<TResult1 = ContainerRegistryResource, TResult2 = never>(
        onfulfilled?: ((value: ContainerRegistryResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): ContainerRegistryResourcePromise {
        return new ContainerRegistryResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// ContainerResource
// ============================================================================

export class ContainerResource extends ResourceBuilderBase<ContainerResourceHandle> {
    constructor(handle: ContainerResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withBindMountInternal(source: string, target: string, isReadOnly?: boolean): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, target };
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withBindMount',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): ContainerResourcePromise {
        const isReadOnly = options?.isReadOnly;
        return new ContainerResourcePromise(this._withBindMountInternal(source, target, isReadOnly));
    }

    /** @internal */
    private async _withEntrypointInternal(entrypoint: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withEntrypoint',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withEntrypointInternal(entrypoint));
    }

    /** @internal */
    private async _withImageTagInternal(tag: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, tag };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withImageTag',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets the container image tag */
    withImageTag(tag: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withImageTagInternal(tag));
    }

    /** @internal */
    private async _withImageRegistryInternal(registry: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withImageRegistry',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withImageRegistryInternal(registry));
    }

    /** @internal */
    private async _withImageInternal(image: string, tag?: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, image };
        if (tag !== undefined) rpcArgs.tag = tag;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withImage',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): ContainerResourcePromise {
        const tag = options?.tag;
        return new ContainerResourcePromise(this._withImageInternal(image, tag));
    }

    /** @internal */
    private async _withImageSHA256Internal(sha256: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withImageSHA256',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withImageSHA256Internal(sha256));
    }

    /** @internal */
    private async _withContainerRuntimeArgsInternal(args: string[]): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withContainerRuntimeArgs',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withContainerRuntimeArgsInternal(args));
    }

    /** @internal */
    private async _withLifetimeInternal(lifetime: ContainerLifetime): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withLifetime',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withLifetimeInternal(lifetime));
    }

    /** @internal */
    private async _withImagePullPolicyInternal(pullPolicy: ImagePullPolicy): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withImagePullPolicy',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withImagePullPolicyInternal(pullPolicy));
    }

    /** @internal */
    private async _publishAsContainerInternal(): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/publishAsContainer',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._publishAsContainerInternal());
    }

    /** @internal */
    private async _withDockerfileInternal(contextPath: string, dockerfilePath?: string, stage?: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined) rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined) rpcArgs.stage = stage;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withDockerfile',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): ContainerResourcePromise {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new ContainerResourcePromise(this._withDockerfileInternal(contextPath, dockerfilePath, stage));
    }

    /** @internal */
    private async _withContainerNameInternal(name: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withContainerName',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets the container name */
    withContainerName(name: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withContainerNameInternal(name));
    }

    /** @internal */
    private async _withBuildArgInternal(name: string, value: ParameterResource): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withBuildArg',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withBuildArgInternal(name, value));
    }

    /** @internal */
    private async _withBuildSecretInternal(name: string, value: ParameterResource): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withBuildSecret',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withBuildSecretInternal(name, value));
    }

    /** @internal */
    private async _withEndpointProxySupportInternal(proxyEnabled: boolean): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withEndpointProxySupport',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withEndpointProxySupportInternal(proxyEnabled));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): ContainerResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new ContainerResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withContainerNetworkAliasInternal(alias: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, alias };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withContainerNetworkAlias',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withContainerNetworkAliasInternal(alias));
    }

    /** @internal */
    private async _withMcpServerInternal(path?: string, endpointName?: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withMcpServer',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): ContainerResourcePromise {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new ContainerResourcePromise(this._withMcpServerInternal(path, endpointName));
    }

    /** @internal */
    private async _withOtlpExporterInternal(): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withOtlpExporter',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withOtlpExporterInternal());
    }

    /** @internal */
    private async _withOtlpExporterProtocolInternal(protocol: OtlpProtocol): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, protocol };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withOtlpExporterProtocol',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withOtlpExporterProtocolInternal(protocol));
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/publishAsConnectionString',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): ContainerResourcePromise {
        const helpLink = options?.helpLink;
        return new ContainerResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withEnvironmentInternal(name: string, value: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withEnvironment',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withEnvironmentInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentExpressionInternal(name: string, value: ReferenceExpression): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentExpression',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withEnvironmentExpressionInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentCallbackInternal(callback: (obj: EnvironmentCallbackContext) => Promise<void>): Promise<ContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as EnvironmentCallbackContextHandle;
            const obj = new EnvironmentCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallback',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withEnvironmentCallbackInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentCallbackAsyncInternal(callback: (arg: EnvironmentCallbackContext) => Promise<void>): Promise<ContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EnvironmentCallbackContextHandle;
            const arg = new EnvironmentCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallbackAsync',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withEnvironmentCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentEndpointInternal(name: string, endpointReference: EndpointReference): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, endpointReference };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentEndpoint',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withEnvironmentEndpointInternal(name, endpointReference));
    }

    /** @internal */
    private async _withEnvironmentParameterInternal(name: string, parameter: ParameterResource): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, parameter };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentParameter',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withEnvironmentParameterInternal(name, parameter));
    }

    /** @internal */
    private async _withEnvironmentConnectionStringInternal(envVarName: string, resource: ResourceBuilderBase): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, envVarName, resource };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentConnectionString',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withEnvironmentConnectionStringInternal(envVarName, resource));
    }

    /** @internal */
    private async _withArgsInternal(args: string[]): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withArgs',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds arguments */
    withArgs(args: string[]): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withArgsInternal(args));
    }

    /** @internal */
    private async _withArgsCallbackInternal(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): Promise<ContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as CommandLineArgsCallbackContextHandle;
            const obj = new CommandLineArgsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withArgsCallback',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withArgsCallbackInternal(callback));
    }

    /** @internal */
    private async _withArgsCallbackAsyncInternal(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): Promise<ContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as CommandLineArgsCallbackContextHandle;
            const arg = new CommandLineArgsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withArgsCallbackAsync',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withArgsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withReferenceInternal(source: ResourceBuilderBase, connectionName?: string, optional?: boolean): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (connectionName !== undefined) rpcArgs.connectionName = connectionName;
        if (optional !== undefined) rpcArgs.optional = optional;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withReference',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): ContainerResourcePromise {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        return new ContainerResourcePromise(this._withReferenceInternal(source, connectionName, optional));
    }

    /** @internal */
    private async _withServiceReferenceInternal(source: ResourceBuilderBase): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withServiceReference',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withServiceReferenceInternal(source));
    }

    /** @internal */
    private async _withServiceReferenceNamedInternal(source: ResourceBuilderBase, name: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, name };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withServiceReferenceNamed',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withServiceReferenceNamedInternal(source, name));
    }

    /** @internal */
    private async _withReferenceUriInternal(name: string, uri: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, uri };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withReferenceUri',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withReferenceUriInternal(name, uri));
    }

    /** @internal */
    private async _withReferenceExternalServiceInternal(externalService: ExternalServiceResource): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, externalService };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withReferenceExternalService',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withReferenceExternalServiceInternal(externalService));
    }

    /** @internal */
    private async _withReferenceEndpointInternal(endpointReference: EndpointReference): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointReference };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withReferenceEndpoint',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withReferenceEndpointInternal(endpointReference));
    }

    /** @internal */
    private async _withEndpointInternal(port?: number, targetPort?: number, scheme?: string, name?: string, env?: string, isProxied?: boolean, isExternal?: boolean, protocol?: ProtocolType): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (scheme !== undefined) rpcArgs.scheme = scheme;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined) rpcArgs.isExternal = isExternal;
        if (protocol !== undefined) rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withEndpoint',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): ContainerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new ContainerResourcePromise(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol));
    }

    /** @internal */
    private async _withHttpEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withHttpEndpoint',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): ContainerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ContainerResourcePromise(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withHttpsEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withHttpsEndpoint',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): ContainerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ContainerResourcePromise(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withExternalHttpEndpointsInternal(): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withExternalHttpEndpoints',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withExternalHttpEndpointsInternal());
    }

    /** Gets an endpoint reference */
    async getEndpoint(name: string): Promise<EndpointReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<EndpointReference>(
            'Aspire.Hosting/getEndpoint',
            rpcArgs
        );
    }

    /** @internal */
    private async _asHttp2ServiceInternal(): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/asHttp2Service',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._asHttp2ServiceInternal());
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<ContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<ContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): ContainerResourcePromise {
        const displayText = options?.displayText;
        return new ContainerResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): ContainerResourcePromise {
        const displayText = options?.displayText;
        return new ContainerResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<ContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _withUrlForEndpointFactoryInternal(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): Promise<ContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EndpointReferenceHandle;
            const arg = new EndpointReference(argHandle, this._client);
            return await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withUrlForEndpointFactory',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withUrlForEndpointFactoryInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _waitForInternal(dependency: ResourceBuilderBase): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/waitFor',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): ContainerResourcePromise {
        return new ContainerResourcePromise(this._waitForInternal(dependency));
    }

    /** @internal */
    private async _waitForWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/waitForWithBehavior',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ContainerResourcePromise {
        return new ContainerResourcePromise(this._waitForWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _waitForStartInternal(dependency: ResourceBuilderBase): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/waitForStart',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): ContainerResourcePromise {
        return new ContainerResourcePromise(this._waitForStartInternal(dependency));
    }

    /** @internal */
    private async _waitForStartWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/waitForStartWithBehavior',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ContainerResourcePromise {
        return new ContainerResourcePromise(this._waitForStartWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _waitForCompletionInternal(dependency: ResourceBuilderBase, exitCode?: number): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        if (exitCode !== undefined) rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/waitForCompletion',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): ContainerResourcePromise {
        const exitCode = options?.exitCode;
        return new ContainerResourcePromise(this._waitForCompletionInternal(dependency, exitCode));
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withHttpHealthCheckInternal(path?: string, statusCode?: number, endpointName?: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (statusCode !== undefined) rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withHttpHealthCheck',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): ContainerResourcePromise {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new ContainerResourcePromise(this._withHttpHealthCheckInternal(path, statusCode, endpointName));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<ContainerResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): ContainerResourcePromise {
        const commandOptions = options?.commandOptions;
        return new ContainerResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withDeveloperCertificateTrustInternal(trust: boolean): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, trust };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withDeveloperCertificateTrust',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withDeveloperCertificateTrustInternal(trust));
    }

    /** @internal */
    private async _withCertificateTrustScopeInternal(scope: CertificateTrustScope): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, scope };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withCertificateTrustScope',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withCertificateTrustScopeInternal(scope));
    }

    /** @internal */
    private async _withHttpsDeveloperCertificateInternal(password?: ParameterResource): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (password !== undefined) rpcArgs.password = password;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withHttpsDeveloperCertificate',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): ContainerResourcePromise {
        const password = options?.password;
        return new ContainerResourcePromise(this._withHttpsDeveloperCertificateInternal(password));
    }

    /** @internal */
    private async _withoutHttpsCertificateInternal(): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withoutHttpsCertificate',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withoutHttpsCertificateInternal());
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): ContainerResourcePromise {
        const iconVariant = options?.iconVariant;
        return new ContainerResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _withHttpProbeInternal(probeType: ProbeType, path?: string, initialDelaySeconds?: number, periodSeconds?: number, timeoutSeconds?: number, failureThreshold?: number, successThreshold?: number, endpointName?: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, probeType };
        if (path !== undefined) rpcArgs.path = path;
        if (initialDelaySeconds !== undefined) rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined) rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined) rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined) rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined) rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withHttpProbe',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): ContainerResourcePromise {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new ContainerResourcePromise(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withRemoteImageNameInternal(remoteImageName: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withRemoteImageName',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withRemoteImageNameInternal(remoteImageName));
    }

    /** @internal */
    private async _withRemoteImageTagInternal(remoteImageTag: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withRemoteImageTag',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withRemoteImageTagInternal(remoteImageTag));
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<ContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): ContainerResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new ContainerResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<ContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<ContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** @internal */
    private async _withVolumeInternal(target: string, name?: string, isReadOnly?: boolean): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, target };
        if (name !== undefined) rpcArgs.name = name;
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/withVolume',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): ContainerResourcePromise {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new ContainerResourcePromise(this._withVolumeInternal(target, name, isReadOnly));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<ContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<ContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<ContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceEndpointsAllocatedInternal(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): Promise<ContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceEndpointsAllocatedEventHandle;
            const arg = new ResourceEndpointsAllocatedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/onResourceEndpointsAllocated',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._onResourceEndpointsAllocatedInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<ContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _publishAsAzureContainerAppInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): Promise<ContainerResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { container: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishContainerAsAzureContainerApp',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._publishAsAzureContainerAppInternal(configure));
    }

    /** @internal */
    private async _publishAsConfiguredAzureContainerAppJobInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<ContainerResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { resource: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredAzureContainerAppJob',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._publishAsConfiguredAzureContainerAppJobInternal(configure));
    }

    /** @internal */
    private async _publishAsAzureContainerAppJobInternal(): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsAzureContainerAppJob',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._publishAsAzureContainerAppJobInternal());
    }

    /** @internal */
    private async _publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression: string, configure?: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<ContainerResource> {
        const configureId = configure ? registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        if (configure !== undefined) rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): ContainerResourcePromise {
        const configure = options?.configure;
        return new ContainerResourcePromise(this._publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression, configure));
    }

    /** @internal */
    private async _publishAsScheduledAzureContainerAppJobInternal(cronExpression: string): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._publishAsScheduledAzureContainerAppJobInternal(cronExpression));
    }

    /** @internal */
    private async _withEnvironmentFromOutputInternal(name: string, bicepOutputReference: BicepOutputReference): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, bicepOutputReference };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromOutput',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withEnvironmentFromOutputInternal(name, bicepOutputReference));
    }

    /** @internal */
    private async _withEnvironmentFromKeyVaultSecretInternal(name: string, secretReference: ResourceBuilderBase): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, secretReference };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromKeyVaultSecret',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withEnvironmentFromKeyVaultSecretInternal(name, secretReference));
    }

    /** @internal */
    private async _withAzureUserAssignedIdentityInternal(identityResourceBuilder: AzureUserAssignedIdentityResource): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, identityResourceBuilder };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting.Azure/withUserAssignedIdentityAzureUserAssignedIdentity',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withAzureUserAssignedIdentityInternal(identityResourceBuilder));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<ContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<ContainerResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new ContainerResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): ContainerResourcePromise {
        return new ContainerResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for ContainerResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class ContainerResourcePromise implements PromiseLike<ContainerResource> {
    constructor(private _promise: Promise<ContainerResource>) {}

    then<TResult1 = ContainerResource, TResult2 = never>(
        onfulfilled?: ((value: ContainerResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withBindMount(source, target, options)));
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withEntrypoint(entrypoint)));
    }

    /** Sets the container image tag */
    withImageTag(tag: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withImageTag(tag)));
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withImageRegistry(registry)));
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withImage(image, options)));
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withImageSHA256(sha256)));
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withContainerRuntimeArgs(args)));
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withLifetime(lifetime)));
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)));
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.publishAsContainer()));
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withDockerfile(contextPath, options)));
    }

    /** Sets the container name */
    withContainerName(name: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withContainerName(name)));
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withBuildArg(name, value)));
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withBuildSecret(name, value)));
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withContainerNetworkAlias(alias)));
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withMcpServer(options)));
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withOtlpExporter()));
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withOtlpExporterProtocol(protocol)));
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withEnvironment(name, value)));
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentExpression(name, value)));
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentCallback(callback)));
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentCallbackAsync(callback)));
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentEndpoint(name, endpointReference)));
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentParameter(name, parameter)));
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentConnectionString(envVarName, resource)));
    }

    /** Adds arguments */
    withArgs(args: string[]): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withArgs(args)));
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withArgsCallback(callback)));
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withArgsCallbackAsync(callback)));
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withReference(source, options)));
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withServiceReference(source)));
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withServiceReferenceNamed(source, name)));
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withReferenceUri(name, uri)));
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withReferenceExternalService(externalService)));
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withReferenceEndpoint(endpointReference)));
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withEndpoint(options)));
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withHttpEndpoint(options)));
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withHttpsEndpoint(options)));
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withExternalHttpEndpoints()));
    }

    /** Gets an endpoint reference */
    getEndpoint(name: string): Promise<EndpointReference> {
        return this._promise.then(obj => obj.getEndpoint(name));
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.asHttp2Service()));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withUrlForEndpointFactory(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.waitFor(dependency)));
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.waitForWithBehavior(dependency, waitBehavior)));
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.waitForStart(dependency)));
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.waitForStartWithBehavior(dependency, waitBehavior)));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.waitForCompletion(dependency, options)));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withHttpHealthCheck(options)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)));
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withCertificateTrustScope(scope)));
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)));
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withoutHttpsCertificate()));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withHttpProbe(probeType, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)));
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withVolume(target, options)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerApp(configure)));
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.publishAsConfiguredAzureContainerAppJob(configure)));
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerAppJob()));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.publishAsConfiguredScheduledAzureContainerAppJob(cronExpression, options)));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.publishAsScheduledAzureContainerAppJob(cronExpression)));
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentFromOutput(name, bicepOutputReference)));
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentFromKeyVaultSecret(name, secretReference)));
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withAzureUserAssignedIdentity(identityResourceBuilder)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): ContainerResourcePromise {
        return new ContainerResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// CSharpAppResource
// ============================================================================

export class CSharpAppResource extends ResourceBuilderBase<CSharpAppResourceHandle> {
    constructor(handle: CSharpAppResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): CSharpAppResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new CSharpAppResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withMcpServerInternal(path?: string, endpointName?: string): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withMcpServer',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): CSharpAppResourcePromise {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new CSharpAppResourcePromise(this._withMcpServerInternal(path, endpointName));
    }

    /** @internal */
    private async _withOtlpExporterInternal(): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withOtlpExporter',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withOtlpExporterInternal());
    }

    /** @internal */
    private async _withOtlpExporterProtocolInternal(protocol: OtlpProtocol): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, protocol };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withOtlpExporterProtocol',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withOtlpExporterProtocolInternal(protocol));
    }

    /** @internal */
    private async _withReplicasInternal(replicas: number): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, replicas };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withReplicas',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets the number of replicas */
    withReplicas(replicas: number): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withReplicasInternal(replicas));
    }

    /** @internal */
    private async _disableForwardedHeadersInternal(): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/disableForwardedHeaders',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Disables forwarded headers for the project */
    disableForwardedHeaders(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._disableForwardedHeadersInternal());
    }

    /** @internal */
    private async _publishAsDockerFileInternal(configure?: (obj: ContainerResource) => Promise<void>): Promise<CSharpAppResource> {
        const configureId = configure ? registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ContainerResourceHandle;
            const obj = new ContainerResource(objHandle, this._client);
            await configure(obj);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (configure !== undefined) rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/publishProjectAsDockerFileWithConfigure',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Publishes a project as a Docker file with optional container configuration */
    publishAsDockerFile(options?: PublishAsDockerFileOptions): CSharpAppResourcePromise {
        const configure = options?.configure;
        return new CSharpAppResourcePromise(this._publishAsDockerFileInternal(configure));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): CSharpAppResourcePromise {
        const helpLink = options?.helpLink;
        return new CSharpAppResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withEnvironmentInternal(name: string, value: string): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withEnvironment',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withEnvironmentInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentExpressionInternal(name: string, value: ReferenceExpression): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withEnvironmentExpression',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withEnvironmentExpressionInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentCallbackInternal(callback: (obj: EnvironmentCallbackContext) => Promise<void>): Promise<CSharpAppResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as EnvironmentCallbackContextHandle;
            const obj = new EnvironmentCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallback',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withEnvironmentCallbackInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentCallbackAsyncInternal(callback: (arg: EnvironmentCallbackContext) => Promise<void>): Promise<CSharpAppResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EnvironmentCallbackContextHandle;
            const arg = new EnvironmentCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallbackAsync',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withEnvironmentCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentEndpointInternal(name: string, endpointReference: EndpointReference): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, endpointReference };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withEnvironmentEndpoint',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withEnvironmentEndpointInternal(name, endpointReference));
    }

    /** @internal */
    private async _withEnvironmentParameterInternal(name: string, parameter: ParameterResource): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, parameter };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withEnvironmentParameter',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withEnvironmentParameterInternal(name, parameter));
    }

    /** @internal */
    private async _withEnvironmentConnectionStringInternal(envVarName: string, resource: ResourceBuilderBase): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, envVarName, resource };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withEnvironmentConnectionString',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withEnvironmentConnectionStringInternal(envVarName, resource));
    }

    /** @internal */
    private async _withArgsInternal(args: string[]): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withArgs',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds arguments */
    withArgs(args: string[]): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withArgsInternal(args));
    }

    /** @internal */
    private async _withArgsCallbackInternal(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): Promise<CSharpAppResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as CommandLineArgsCallbackContextHandle;
            const obj = new CommandLineArgsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withArgsCallback',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withArgsCallbackInternal(callback));
    }

    /** @internal */
    private async _withArgsCallbackAsyncInternal(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): Promise<CSharpAppResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as CommandLineArgsCallbackContextHandle;
            const arg = new CommandLineArgsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withArgsCallbackAsync',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withArgsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withReferenceInternal(source: ResourceBuilderBase, connectionName?: string, optional?: boolean): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (connectionName !== undefined) rpcArgs.connectionName = connectionName;
        if (optional !== undefined) rpcArgs.optional = optional;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withReference',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): CSharpAppResourcePromise {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        return new CSharpAppResourcePromise(this._withReferenceInternal(source, connectionName, optional));
    }

    /** @internal */
    private async _withServiceReferenceInternal(source: ResourceBuilderBase): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withServiceReference',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withServiceReferenceInternal(source));
    }

    /** @internal */
    private async _withServiceReferenceNamedInternal(source: ResourceBuilderBase, name: string): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, name };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withServiceReferenceNamed',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withServiceReferenceNamedInternal(source, name));
    }

    /** @internal */
    private async _withReferenceUriInternal(name: string, uri: string): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, uri };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withReferenceUri',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withReferenceUriInternal(name, uri));
    }

    /** @internal */
    private async _withReferenceExternalServiceInternal(externalService: ExternalServiceResource): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, externalService };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withReferenceExternalService',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withReferenceExternalServiceInternal(externalService));
    }

    /** @internal */
    private async _withReferenceEndpointInternal(endpointReference: EndpointReference): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointReference };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withReferenceEndpoint',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withReferenceEndpointInternal(endpointReference));
    }

    /** @internal */
    private async _withEndpointInternal(port?: number, targetPort?: number, scheme?: string, name?: string, env?: string, isProxied?: boolean, isExternal?: boolean, protocol?: ProtocolType): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (scheme !== undefined) rpcArgs.scheme = scheme;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined) rpcArgs.isExternal = isExternal;
        if (protocol !== undefined) rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withEndpoint',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): CSharpAppResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new CSharpAppResourcePromise(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol));
    }

    /** @internal */
    private async _withHttpEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withHttpEndpoint',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): CSharpAppResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new CSharpAppResourcePromise(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withHttpsEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withHttpsEndpoint',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): CSharpAppResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new CSharpAppResourcePromise(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withExternalHttpEndpointsInternal(): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withExternalHttpEndpoints',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withExternalHttpEndpointsInternal());
    }

    /** Gets an endpoint reference */
    async getEndpoint(name: string): Promise<EndpointReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<EndpointReference>(
            'Aspire.Hosting/getEndpoint',
            rpcArgs
        );
    }

    /** @internal */
    private async _asHttp2ServiceInternal(): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/asHttp2Service',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._asHttp2ServiceInternal());
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<CSharpAppResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<CSharpAppResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): CSharpAppResourcePromise {
        const displayText = options?.displayText;
        return new CSharpAppResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): CSharpAppResourcePromise {
        const displayText = options?.displayText;
        return new CSharpAppResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<CSharpAppResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _withUrlForEndpointFactoryInternal(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): Promise<CSharpAppResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EndpointReferenceHandle;
            const arg = new EndpointReference(argHandle, this._client);
            return await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withUrlForEndpointFactory',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withUrlForEndpointFactoryInternal(endpointName, callback));
    }

    /** @internal */
    private async _publishWithContainerFilesInternal(source: ResourceBuilderBase, destinationPath: string): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, destinationPath };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/publishWithContainerFiles',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Configures the resource to copy container files from the specified source during publishing */
    publishWithContainerFiles(source: ResourceBuilderBase, destinationPath: string): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._publishWithContainerFilesInternal(source, destinationPath));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _waitForInternal(dependency: ResourceBuilderBase): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/waitFor',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._waitForInternal(dependency));
    }

    /** @internal */
    private async _waitForWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/waitForWithBehavior',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._waitForWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _waitForStartInternal(dependency: ResourceBuilderBase): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/waitForStart',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._waitForStartInternal(dependency));
    }

    /** @internal */
    private async _waitForStartWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/waitForStartWithBehavior',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._waitForStartWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _waitForCompletionInternal(dependency: ResourceBuilderBase, exitCode?: number): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        if (exitCode !== undefined) rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/waitForCompletion',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): CSharpAppResourcePromise {
        const exitCode = options?.exitCode;
        return new CSharpAppResourcePromise(this._waitForCompletionInternal(dependency, exitCode));
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withHttpHealthCheckInternal(path?: string, statusCode?: number, endpointName?: string): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (statusCode !== undefined) rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withHttpHealthCheck',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): CSharpAppResourcePromise {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new CSharpAppResourcePromise(this._withHttpHealthCheckInternal(path, statusCode, endpointName));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<CSharpAppResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): CSharpAppResourcePromise {
        const commandOptions = options?.commandOptions;
        return new CSharpAppResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withDeveloperCertificateTrustInternal(trust: boolean): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, trust };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withDeveloperCertificateTrust',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withDeveloperCertificateTrustInternal(trust));
    }

    /** @internal */
    private async _withCertificateTrustScopeInternal(scope: CertificateTrustScope): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, scope };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withCertificateTrustScope',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withCertificateTrustScopeInternal(scope));
    }

    /** @internal */
    private async _withHttpsDeveloperCertificateInternal(password?: ParameterResource): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (password !== undefined) rpcArgs.password = password;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withHttpsDeveloperCertificate',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): CSharpAppResourcePromise {
        const password = options?.password;
        return new CSharpAppResourcePromise(this._withHttpsDeveloperCertificateInternal(password));
    }

    /** @internal */
    private async _withoutHttpsCertificateInternal(): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withoutHttpsCertificate',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withoutHttpsCertificateInternal());
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): CSharpAppResourcePromise {
        const iconVariant = options?.iconVariant;
        return new CSharpAppResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _withHttpProbeInternal(probeType: ProbeType, path?: string, initialDelaySeconds?: number, periodSeconds?: number, timeoutSeconds?: number, failureThreshold?: number, successThreshold?: number, endpointName?: string): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, probeType };
        if (path !== undefined) rpcArgs.path = path;
        if (initialDelaySeconds !== undefined) rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined) rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined) rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined) rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined) rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withHttpProbe',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): CSharpAppResourcePromise {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new CSharpAppResourcePromise(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withRemoteImageNameInternal(remoteImageName: string): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withRemoteImageName',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withRemoteImageNameInternal(remoteImageName));
    }

    /** @internal */
    private async _withRemoteImageTagInternal(remoteImageTag: string): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withRemoteImageTag',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withRemoteImageTagInternal(remoteImageTag));
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<CSharpAppResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): CSharpAppResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new CSharpAppResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<CSharpAppResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<CSharpAppResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<CSharpAppResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<CSharpAppResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<CSharpAppResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceEndpointsAllocatedInternal(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): Promise<CSharpAppResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceEndpointsAllocatedEventHandle;
            const arg = new ResourceEndpointsAllocatedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/onResourceEndpointsAllocated',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._onResourceEndpointsAllocatedInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<CSharpAppResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _publishAsAzureContainerAppInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): Promise<CSharpAppResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { project: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishProjectAsAzureContainerApp',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Configures the project resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._publishAsAzureContainerAppInternal(configure));
    }

    /** @internal */
    private async _publishAsConfiguredAzureContainerAppJobInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<CSharpAppResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { resource: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredAzureContainerAppJob',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._publishAsConfiguredAzureContainerAppJobInternal(configure));
    }

    /** @internal */
    private async _publishAsAzureContainerAppJobInternal(): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsAzureContainerAppJob',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._publishAsAzureContainerAppJobInternal());
    }

    /** @internal */
    private async _publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression: string, configure?: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<CSharpAppResource> {
        const configureId = configure ? registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        if (configure !== undefined) rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): CSharpAppResourcePromise {
        const configure = options?.configure;
        return new CSharpAppResourcePromise(this._publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression, configure));
    }

    /** @internal */
    private async _publishAsScheduledAzureContainerAppJobInternal(cronExpression: string): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._publishAsScheduledAzureContainerAppJobInternal(cronExpression));
    }

    /** @internal */
    private async _withEnvironmentFromOutputInternal(name: string, bicepOutputReference: BicepOutputReference): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, bicepOutputReference };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromOutput',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withEnvironmentFromOutputInternal(name, bicepOutputReference));
    }

    /** @internal */
    private async _withEnvironmentFromKeyVaultSecretInternal(name: string, secretReference: ResourceBuilderBase): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, secretReference };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromKeyVaultSecret',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withEnvironmentFromKeyVaultSecretInternal(name, secretReference));
    }

    /** @internal */
    private async _withAzureUserAssignedIdentityInternal(identityResourceBuilder: AzureUserAssignedIdentityResource): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, identityResourceBuilder };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting.Azure/withUserAssignedIdentityAzureUserAssignedIdentity',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withAzureUserAssignedIdentityInternal(identityResourceBuilder));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<CSharpAppResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<CSharpAppResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new CSharpAppResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for CSharpAppResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class CSharpAppResourcePromise implements PromiseLike<CSharpAppResource> {
    constructor(private _promise: Promise<CSharpAppResource>) {}

    then<TResult1 = CSharpAppResource, TResult2 = never>(
        onfulfilled?: ((value: CSharpAppResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withMcpServer(options)));
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withOtlpExporter()));
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withOtlpExporterProtocol(protocol)));
    }

    /** Sets the number of replicas */
    withReplicas(replicas: number): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withReplicas(replicas)));
    }

    /** Disables forwarded headers for the project */
    disableForwardedHeaders(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.disableForwardedHeaders()));
    }

    /** Publishes a project as a Docker file with optional container configuration */
    publishAsDockerFile(options?: PublishAsDockerFileOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.publishAsDockerFile(options)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withEnvironment(name, value)));
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withEnvironmentExpression(name, value)));
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withEnvironmentCallback(callback)));
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withEnvironmentCallbackAsync(callback)));
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withEnvironmentEndpoint(name, endpointReference)));
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withEnvironmentParameter(name, parameter)));
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withEnvironmentConnectionString(envVarName, resource)));
    }

    /** Adds arguments */
    withArgs(args: string[]): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withArgs(args)));
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withArgsCallback(callback)));
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withArgsCallbackAsync(callback)));
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withReference(source, options)));
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withServiceReference(source)));
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withServiceReferenceNamed(source, name)));
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withReferenceUri(name, uri)));
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withReferenceExternalService(externalService)));
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withReferenceEndpoint(endpointReference)));
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withEndpoint(options)));
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withHttpEndpoint(options)));
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withHttpsEndpoint(options)));
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withExternalHttpEndpoints()));
    }

    /** Gets an endpoint reference */
    getEndpoint(name: string): Promise<EndpointReference> {
        return this._promise.then(obj => obj.getEndpoint(name));
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.asHttp2Service()));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withUrlForEndpointFactory(endpointName, callback)));
    }

    /** Configures the resource to copy container files from the specified source during publishing */
    publishWithContainerFiles(source: ResourceBuilderBase, destinationPath: string): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.publishWithContainerFiles(source, destinationPath)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.waitFor(dependency)));
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.waitForWithBehavior(dependency, waitBehavior)));
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.waitForStart(dependency)));
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.waitForStartWithBehavior(dependency, waitBehavior)));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.waitForCompletion(dependency, options)));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withHttpHealthCheck(options)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)));
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withCertificateTrustScope(scope)));
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)));
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withoutHttpsCertificate()));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withHttpProbe(probeType, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)));
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures the project resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerApp(configure)));
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.publishAsConfiguredAzureContainerAppJob(configure)));
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerAppJob()));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.publishAsConfiguredScheduledAzureContainerAppJob(cronExpression, options)));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.publishAsScheduledAzureContainerAppJob(cronExpression)));
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withEnvironmentFromOutput(name, bicepOutputReference)));
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withEnvironmentFromKeyVaultSecret(name, secretReference)));
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withAzureUserAssignedIdentity(identityResourceBuilder)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): CSharpAppResourcePromise {
        return new CSharpAppResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// DotnetToolResource
// ============================================================================

export class DotnetToolResource extends ResourceBuilderBase<DotnetToolResourceHandle> {
    constructor(handle: DotnetToolResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): DotnetToolResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new DotnetToolResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withToolPackageInternal(packageId: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, packageId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withToolPackage',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets the tool package ID */
    withToolPackage(packageId: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withToolPackageInternal(packageId));
    }

    /** @internal */
    private async _withToolVersionInternal(version: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, version };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withToolVersion',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets the tool version */
    withToolVersion(version: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withToolVersionInternal(version));
    }

    /** @internal */
    private async _withToolPrereleaseInternal(): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withToolPrerelease',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Allows prerelease tool versions */
    withToolPrerelease(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withToolPrereleaseInternal());
    }

    /** @internal */
    private async _withToolSourceInternal(source: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withToolSource',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds a NuGet source for the tool */
    withToolSource(source: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withToolSourceInternal(source));
    }

    /** @internal */
    private async _withToolIgnoreExistingFeedsInternal(): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withToolIgnoreExistingFeeds',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Ignores existing NuGet feeds */
    withToolIgnoreExistingFeeds(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withToolIgnoreExistingFeedsInternal());
    }

    /** @internal */
    private async _withToolIgnoreFailedSourcesInternal(): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withToolIgnoreFailedSources',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Ignores failed NuGet sources */
    withToolIgnoreFailedSources(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withToolIgnoreFailedSourcesInternal());
    }

    /** @internal */
    private async _publishAsDockerFileInternal(): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/publishAsDockerFile',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Publishes the executable as a Docker container */
    publishAsDockerFile(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._publishAsDockerFileInternal());
    }

    /** @internal */
    private async _publishAsDockerFileWithConfigureInternal(configure: (obj: ContainerResource) => Promise<void>): Promise<DotnetToolResource> {
        const configureId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ContainerResourceHandle;
            const obj = new ContainerResource(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/publishAsDockerFileWithConfigure',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Publishes an executable as a Docker file with optional container configuration */
    publishAsDockerFileWithConfigure(configure: (obj: ContainerResource) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._publishAsDockerFileWithConfigureInternal(configure));
    }

    /** @internal */
    private async _withExecutableCommandInternal(command: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withExecutableCommand',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets the executable command */
    withExecutableCommand(command: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withExecutableCommandInternal(command));
    }

    /** @internal */
    private async _withWorkingDirectoryInternal(workingDirectory: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, workingDirectory };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withWorkingDirectory',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets the executable working directory */
    withWorkingDirectory(workingDirectory: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withWorkingDirectoryInternal(workingDirectory));
    }

    /** @internal */
    private async _withMcpServerInternal(path?: string, endpointName?: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withMcpServer',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): DotnetToolResourcePromise {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new DotnetToolResourcePromise(this._withMcpServerInternal(path, endpointName));
    }

    /** @internal */
    private async _withOtlpExporterInternal(): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withOtlpExporter',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withOtlpExporterInternal());
    }

    /** @internal */
    private async _withOtlpExporterProtocolInternal(protocol: OtlpProtocol): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, protocol };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withOtlpExporterProtocol',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withOtlpExporterProtocolInternal(protocol));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): DotnetToolResourcePromise {
        const helpLink = options?.helpLink;
        return new DotnetToolResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withEnvironmentInternal(name: string, value: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withEnvironment',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withEnvironmentInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentExpressionInternal(name: string, value: ReferenceExpression): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withEnvironmentExpression',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withEnvironmentExpressionInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentCallbackInternal(callback: (obj: EnvironmentCallbackContext) => Promise<void>): Promise<DotnetToolResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as EnvironmentCallbackContextHandle;
            const obj = new EnvironmentCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallback',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withEnvironmentCallbackInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentCallbackAsyncInternal(callback: (arg: EnvironmentCallbackContext) => Promise<void>): Promise<DotnetToolResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EnvironmentCallbackContextHandle;
            const arg = new EnvironmentCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallbackAsync',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withEnvironmentCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentEndpointInternal(name: string, endpointReference: EndpointReference): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, endpointReference };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withEnvironmentEndpoint',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withEnvironmentEndpointInternal(name, endpointReference));
    }

    /** @internal */
    private async _withEnvironmentParameterInternal(name: string, parameter: ParameterResource): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, parameter };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withEnvironmentParameter',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withEnvironmentParameterInternal(name, parameter));
    }

    /** @internal */
    private async _withEnvironmentConnectionStringInternal(envVarName: string, resource: ResourceBuilderBase): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, envVarName, resource };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withEnvironmentConnectionString',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withEnvironmentConnectionStringInternal(envVarName, resource));
    }

    /** @internal */
    private async _withArgsInternal(args: string[]): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withArgs',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds arguments */
    withArgs(args: string[]): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withArgsInternal(args));
    }

    /** @internal */
    private async _withArgsCallbackInternal(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): Promise<DotnetToolResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as CommandLineArgsCallbackContextHandle;
            const obj = new CommandLineArgsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withArgsCallback',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withArgsCallbackInternal(callback));
    }

    /** @internal */
    private async _withArgsCallbackAsyncInternal(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): Promise<DotnetToolResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as CommandLineArgsCallbackContextHandle;
            const arg = new CommandLineArgsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withArgsCallbackAsync',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withArgsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withReferenceInternal(source: ResourceBuilderBase, connectionName?: string, optional?: boolean): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (connectionName !== undefined) rpcArgs.connectionName = connectionName;
        if (optional !== undefined) rpcArgs.optional = optional;
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withReference',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): DotnetToolResourcePromise {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        return new DotnetToolResourcePromise(this._withReferenceInternal(source, connectionName, optional));
    }

    /** @internal */
    private async _withServiceReferenceInternal(source: ResourceBuilderBase): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withServiceReference',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withServiceReferenceInternal(source));
    }

    /** @internal */
    private async _withServiceReferenceNamedInternal(source: ResourceBuilderBase, name: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, name };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withServiceReferenceNamed',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withServiceReferenceNamedInternal(source, name));
    }

    /** @internal */
    private async _withReferenceUriInternal(name: string, uri: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, uri };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withReferenceUri',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withReferenceUriInternal(name, uri));
    }

    /** @internal */
    private async _withReferenceExternalServiceInternal(externalService: ExternalServiceResource): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, externalService };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withReferenceExternalService',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withReferenceExternalServiceInternal(externalService));
    }

    /** @internal */
    private async _withReferenceEndpointInternal(endpointReference: EndpointReference): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointReference };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withReferenceEndpoint',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withReferenceEndpointInternal(endpointReference));
    }

    /** @internal */
    private async _withEndpointInternal(port?: number, targetPort?: number, scheme?: string, name?: string, env?: string, isProxied?: boolean, isExternal?: boolean, protocol?: ProtocolType): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (scheme !== undefined) rpcArgs.scheme = scheme;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined) rpcArgs.isExternal = isExternal;
        if (protocol !== undefined) rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withEndpoint',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): DotnetToolResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new DotnetToolResourcePromise(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol));
    }

    /** @internal */
    private async _withHttpEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withHttpEndpoint',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): DotnetToolResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new DotnetToolResourcePromise(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withHttpsEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withHttpsEndpoint',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): DotnetToolResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new DotnetToolResourcePromise(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withExternalHttpEndpointsInternal(): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withExternalHttpEndpoints',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withExternalHttpEndpointsInternal());
    }

    /** Gets an endpoint reference */
    async getEndpoint(name: string): Promise<EndpointReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<EndpointReference>(
            'Aspire.Hosting/getEndpoint',
            rpcArgs
        );
    }

    /** @internal */
    private async _asHttp2ServiceInternal(): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/asHttp2Service',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._asHttp2ServiceInternal());
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<DotnetToolResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<DotnetToolResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): DotnetToolResourcePromise {
        const displayText = options?.displayText;
        return new DotnetToolResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): DotnetToolResourcePromise {
        const displayText = options?.displayText;
        return new DotnetToolResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<DotnetToolResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _withUrlForEndpointFactoryInternal(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): Promise<DotnetToolResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EndpointReferenceHandle;
            const arg = new EndpointReference(argHandle, this._client);
            return await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withUrlForEndpointFactory',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withUrlForEndpointFactoryInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _waitForInternal(dependency: ResourceBuilderBase): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/waitFor',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._waitForInternal(dependency));
    }

    /** @internal */
    private async _waitForWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/waitForWithBehavior',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._waitForWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _waitForStartInternal(dependency: ResourceBuilderBase): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/waitForStart',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._waitForStartInternal(dependency));
    }

    /** @internal */
    private async _waitForStartWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/waitForStartWithBehavior',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._waitForStartWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _waitForCompletionInternal(dependency: ResourceBuilderBase, exitCode?: number): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        if (exitCode !== undefined) rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/waitForCompletion',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): DotnetToolResourcePromise {
        const exitCode = options?.exitCode;
        return new DotnetToolResourcePromise(this._waitForCompletionInternal(dependency, exitCode));
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withHttpHealthCheckInternal(path?: string, statusCode?: number, endpointName?: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (statusCode !== undefined) rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withHttpHealthCheck',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): DotnetToolResourcePromise {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new DotnetToolResourcePromise(this._withHttpHealthCheckInternal(path, statusCode, endpointName));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<DotnetToolResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): DotnetToolResourcePromise {
        const commandOptions = options?.commandOptions;
        return new DotnetToolResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withDeveloperCertificateTrustInternal(trust: boolean): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, trust };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withDeveloperCertificateTrust',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withDeveloperCertificateTrustInternal(trust));
    }

    /** @internal */
    private async _withCertificateTrustScopeInternal(scope: CertificateTrustScope): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, scope };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withCertificateTrustScope',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withCertificateTrustScopeInternal(scope));
    }

    /** @internal */
    private async _withHttpsDeveloperCertificateInternal(password?: ParameterResource): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (password !== undefined) rpcArgs.password = password;
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withHttpsDeveloperCertificate',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): DotnetToolResourcePromise {
        const password = options?.password;
        return new DotnetToolResourcePromise(this._withHttpsDeveloperCertificateInternal(password));
    }

    /** @internal */
    private async _withoutHttpsCertificateInternal(): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withoutHttpsCertificate',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withoutHttpsCertificateInternal());
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): DotnetToolResourcePromise {
        const iconVariant = options?.iconVariant;
        return new DotnetToolResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _withHttpProbeInternal(probeType: ProbeType, path?: string, initialDelaySeconds?: number, periodSeconds?: number, timeoutSeconds?: number, failureThreshold?: number, successThreshold?: number, endpointName?: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, probeType };
        if (path !== undefined) rpcArgs.path = path;
        if (initialDelaySeconds !== undefined) rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined) rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined) rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined) rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined) rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withHttpProbe',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): DotnetToolResourcePromise {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new DotnetToolResourcePromise(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withRemoteImageNameInternal(remoteImageName: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withRemoteImageName',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withRemoteImageNameInternal(remoteImageName));
    }

    /** @internal */
    private async _withRemoteImageTagInternal(remoteImageTag: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withRemoteImageTag',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withRemoteImageTagInternal(remoteImageTag));
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<DotnetToolResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): DotnetToolResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new DotnetToolResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<DotnetToolResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<DotnetToolResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<DotnetToolResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<DotnetToolResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<DotnetToolResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceEndpointsAllocatedInternal(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): Promise<DotnetToolResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceEndpointsAllocatedEventHandle;
            const arg = new ResourceEndpointsAllocatedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/onResourceEndpointsAllocated',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._onResourceEndpointsAllocatedInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<DotnetToolResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _publishAsAzureContainerAppInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): Promise<DotnetToolResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { executable: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishExecutableAsAzureContainerApp',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Configures the executable resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._publishAsAzureContainerAppInternal(configure));
    }

    /** @internal */
    private async _publishAsConfiguredAzureContainerAppJobInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<DotnetToolResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { resource: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredAzureContainerAppJob',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._publishAsConfiguredAzureContainerAppJobInternal(configure));
    }

    /** @internal */
    private async _publishAsAzureContainerAppJobInternal(): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsAzureContainerAppJob',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._publishAsAzureContainerAppJobInternal());
    }

    /** @internal */
    private async _publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression: string, configure?: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<DotnetToolResource> {
        const configureId = configure ? registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        if (configure !== undefined) rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): DotnetToolResourcePromise {
        const configure = options?.configure;
        return new DotnetToolResourcePromise(this._publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression, configure));
    }

    /** @internal */
    private async _publishAsScheduledAzureContainerAppJobInternal(cronExpression: string): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._publishAsScheduledAzureContainerAppJobInternal(cronExpression));
    }

    /** @internal */
    private async _withEnvironmentFromOutputInternal(name: string, bicepOutputReference: BicepOutputReference): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, bicepOutputReference };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromOutput',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withEnvironmentFromOutputInternal(name, bicepOutputReference));
    }

    /** @internal */
    private async _withEnvironmentFromKeyVaultSecretInternal(name: string, secretReference: ResourceBuilderBase): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, secretReference };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromKeyVaultSecret',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withEnvironmentFromKeyVaultSecretInternal(name, secretReference));
    }

    /** @internal */
    private async _withAzureUserAssignedIdentityInternal(identityResourceBuilder: AzureUserAssignedIdentityResource): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, identityResourceBuilder };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting.Azure/withUserAssignedIdentityAzureUserAssignedIdentity',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withAzureUserAssignedIdentityInternal(identityResourceBuilder));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<DotnetToolResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<DotnetToolResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new DotnetToolResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for DotnetToolResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class DotnetToolResourcePromise implements PromiseLike<DotnetToolResource> {
    constructor(private _promise: Promise<DotnetToolResource>) {}

    then<TResult1 = DotnetToolResource, TResult2 = never>(
        onfulfilled?: ((value: DotnetToolResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Sets the tool package ID */
    withToolPackage(packageId: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withToolPackage(packageId)));
    }

    /** Sets the tool version */
    withToolVersion(version: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withToolVersion(version)));
    }

    /** Allows prerelease tool versions */
    withToolPrerelease(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withToolPrerelease()));
    }

    /** Adds a NuGet source for the tool */
    withToolSource(source: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withToolSource(source)));
    }

    /** Ignores existing NuGet feeds */
    withToolIgnoreExistingFeeds(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withToolIgnoreExistingFeeds()));
    }

    /** Ignores failed NuGet sources */
    withToolIgnoreFailedSources(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withToolIgnoreFailedSources()));
    }

    /** Publishes the executable as a Docker container */
    publishAsDockerFile(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.publishAsDockerFile()));
    }

    /** Publishes an executable as a Docker file with optional container configuration */
    publishAsDockerFileWithConfigure(configure: (obj: ContainerResource) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.publishAsDockerFileWithConfigure(configure)));
    }

    /** Sets the executable command */
    withExecutableCommand(command: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withExecutableCommand(command)));
    }

    /** Sets the executable working directory */
    withWorkingDirectory(workingDirectory: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withWorkingDirectory(workingDirectory)));
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withMcpServer(options)));
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withOtlpExporter()));
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withOtlpExporterProtocol(protocol)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withEnvironment(name, value)));
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withEnvironmentExpression(name, value)));
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withEnvironmentCallback(callback)));
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withEnvironmentCallbackAsync(callback)));
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withEnvironmentEndpoint(name, endpointReference)));
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withEnvironmentParameter(name, parameter)));
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withEnvironmentConnectionString(envVarName, resource)));
    }

    /** Adds arguments */
    withArgs(args: string[]): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withArgs(args)));
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withArgsCallback(callback)));
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withArgsCallbackAsync(callback)));
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withReference(source, options)));
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withServiceReference(source)));
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withServiceReferenceNamed(source, name)));
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withReferenceUri(name, uri)));
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withReferenceExternalService(externalService)));
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withReferenceEndpoint(endpointReference)));
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withEndpoint(options)));
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withHttpEndpoint(options)));
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withHttpsEndpoint(options)));
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withExternalHttpEndpoints()));
    }

    /** Gets an endpoint reference */
    getEndpoint(name: string): Promise<EndpointReference> {
        return this._promise.then(obj => obj.getEndpoint(name));
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.asHttp2Service()));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withUrlForEndpointFactory(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.waitFor(dependency)));
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.waitForWithBehavior(dependency, waitBehavior)));
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.waitForStart(dependency)));
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.waitForStartWithBehavior(dependency, waitBehavior)));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.waitForCompletion(dependency, options)));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withHttpHealthCheck(options)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)));
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withCertificateTrustScope(scope)));
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)));
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withoutHttpsCertificate()));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withHttpProbe(probeType, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)));
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures the executable resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerApp(configure)));
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.publishAsConfiguredAzureContainerAppJob(configure)));
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerAppJob()));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.publishAsConfiguredScheduledAzureContainerAppJob(cronExpression, options)));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.publishAsScheduledAzureContainerAppJob(cronExpression)));
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withEnvironmentFromOutput(name, bicepOutputReference)));
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withEnvironmentFromKeyVaultSecret(name, secretReference)));
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withAzureUserAssignedIdentity(identityResourceBuilder)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): DotnetToolResourcePromise {
        return new DotnetToolResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// ExecutableResource
// ============================================================================

export class ExecutableResource extends ResourceBuilderBase<ExecutableResourceHandle> {
    constructor(handle: ExecutableResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): ExecutableResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new ExecutableResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _publishAsDockerFileInternal(): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/publishAsDockerFile',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Publishes the executable as a Docker container */
    publishAsDockerFile(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._publishAsDockerFileInternal());
    }

    /** @internal */
    private async _publishAsDockerFileWithConfigureInternal(configure: (obj: ContainerResource) => Promise<void>): Promise<ExecutableResource> {
        const configureId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ContainerResourceHandle;
            const obj = new ContainerResource(objHandle, this._client);
            await configure(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/publishAsDockerFileWithConfigure',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Publishes an executable as a Docker file with optional container configuration */
    publishAsDockerFileWithConfigure(configure: (obj: ContainerResource) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._publishAsDockerFileWithConfigureInternal(configure));
    }

    /** @internal */
    private async _withExecutableCommandInternal(command: string): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withExecutableCommand',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets the executable command */
    withExecutableCommand(command: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withExecutableCommandInternal(command));
    }

    /** @internal */
    private async _withWorkingDirectoryInternal(workingDirectory: string): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, workingDirectory };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withWorkingDirectory',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets the executable working directory */
    withWorkingDirectory(workingDirectory: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withWorkingDirectoryInternal(workingDirectory));
    }

    /** @internal */
    private async _withMcpServerInternal(path?: string, endpointName?: string): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withMcpServer',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): ExecutableResourcePromise {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new ExecutableResourcePromise(this._withMcpServerInternal(path, endpointName));
    }

    /** @internal */
    private async _withOtlpExporterInternal(): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withOtlpExporter',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withOtlpExporterInternal());
    }

    /** @internal */
    private async _withOtlpExporterProtocolInternal(protocol: OtlpProtocol): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, protocol };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withOtlpExporterProtocol',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withOtlpExporterProtocolInternal(protocol));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): ExecutableResourcePromise {
        const helpLink = options?.helpLink;
        return new ExecutableResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withEnvironmentInternal(name: string, value: string): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withEnvironment',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withEnvironmentInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentExpressionInternal(name: string, value: ReferenceExpression): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withEnvironmentExpression',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withEnvironmentExpressionInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentCallbackInternal(callback: (obj: EnvironmentCallbackContext) => Promise<void>): Promise<ExecutableResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as EnvironmentCallbackContextHandle;
            const obj = new EnvironmentCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallback',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withEnvironmentCallbackInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentCallbackAsyncInternal(callback: (arg: EnvironmentCallbackContext) => Promise<void>): Promise<ExecutableResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EnvironmentCallbackContextHandle;
            const arg = new EnvironmentCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallbackAsync',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withEnvironmentCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentEndpointInternal(name: string, endpointReference: EndpointReference): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, endpointReference };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withEnvironmentEndpoint',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withEnvironmentEndpointInternal(name, endpointReference));
    }

    /** @internal */
    private async _withEnvironmentParameterInternal(name: string, parameter: ParameterResource): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, parameter };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withEnvironmentParameter',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withEnvironmentParameterInternal(name, parameter));
    }

    /** @internal */
    private async _withEnvironmentConnectionStringInternal(envVarName: string, resource: ResourceBuilderBase): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, envVarName, resource };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withEnvironmentConnectionString',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withEnvironmentConnectionStringInternal(envVarName, resource));
    }

    /** @internal */
    private async _withArgsInternal(args: string[]): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withArgs',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds arguments */
    withArgs(args: string[]): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withArgsInternal(args));
    }

    /** @internal */
    private async _withArgsCallbackInternal(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): Promise<ExecutableResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as CommandLineArgsCallbackContextHandle;
            const obj = new CommandLineArgsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withArgsCallback',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withArgsCallbackInternal(callback));
    }

    /** @internal */
    private async _withArgsCallbackAsyncInternal(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): Promise<ExecutableResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as CommandLineArgsCallbackContextHandle;
            const arg = new CommandLineArgsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withArgsCallbackAsync',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withArgsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withReferenceInternal(source: ResourceBuilderBase, connectionName?: string, optional?: boolean): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (connectionName !== undefined) rpcArgs.connectionName = connectionName;
        if (optional !== undefined) rpcArgs.optional = optional;
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withReference',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): ExecutableResourcePromise {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        return new ExecutableResourcePromise(this._withReferenceInternal(source, connectionName, optional));
    }

    /** @internal */
    private async _withServiceReferenceInternal(source: ResourceBuilderBase): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withServiceReference',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withServiceReferenceInternal(source));
    }

    /** @internal */
    private async _withServiceReferenceNamedInternal(source: ResourceBuilderBase, name: string): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, name };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withServiceReferenceNamed',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withServiceReferenceNamedInternal(source, name));
    }

    /** @internal */
    private async _withReferenceUriInternal(name: string, uri: string): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, uri };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withReferenceUri',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withReferenceUriInternal(name, uri));
    }

    /** @internal */
    private async _withReferenceExternalServiceInternal(externalService: ExternalServiceResource): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, externalService };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withReferenceExternalService',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withReferenceExternalServiceInternal(externalService));
    }

    /** @internal */
    private async _withReferenceEndpointInternal(endpointReference: EndpointReference): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointReference };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withReferenceEndpoint',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withReferenceEndpointInternal(endpointReference));
    }

    /** @internal */
    private async _withEndpointInternal(port?: number, targetPort?: number, scheme?: string, name?: string, env?: string, isProxied?: boolean, isExternal?: boolean, protocol?: ProtocolType): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (scheme !== undefined) rpcArgs.scheme = scheme;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined) rpcArgs.isExternal = isExternal;
        if (protocol !== undefined) rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withEndpoint',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): ExecutableResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new ExecutableResourcePromise(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol));
    }

    /** @internal */
    private async _withHttpEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withHttpEndpoint',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): ExecutableResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ExecutableResourcePromise(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withHttpsEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withHttpsEndpoint',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): ExecutableResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ExecutableResourcePromise(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withExternalHttpEndpointsInternal(): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withExternalHttpEndpoints',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withExternalHttpEndpointsInternal());
    }

    /** Gets an endpoint reference */
    async getEndpoint(name: string): Promise<EndpointReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<EndpointReference>(
            'Aspire.Hosting/getEndpoint',
            rpcArgs
        );
    }

    /** @internal */
    private async _asHttp2ServiceInternal(): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/asHttp2Service',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._asHttp2ServiceInternal());
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<ExecutableResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<ExecutableResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): ExecutableResourcePromise {
        const displayText = options?.displayText;
        return new ExecutableResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): ExecutableResourcePromise {
        const displayText = options?.displayText;
        return new ExecutableResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<ExecutableResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _withUrlForEndpointFactoryInternal(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): Promise<ExecutableResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EndpointReferenceHandle;
            const arg = new EndpointReference(argHandle, this._client);
            return await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withUrlForEndpointFactory',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withUrlForEndpointFactoryInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _waitForInternal(dependency: ResourceBuilderBase): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/waitFor',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._waitForInternal(dependency));
    }

    /** @internal */
    private async _waitForWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/waitForWithBehavior',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._waitForWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _waitForStartInternal(dependency: ResourceBuilderBase): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/waitForStart',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._waitForStartInternal(dependency));
    }

    /** @internal */
    private async _waitForStartWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/waitForStartWithBehavior',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._waitForStartWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _waitForCompletionInternal(dependency: ResourceBuilderBase, exitCode?: number): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        if (exitCode !== undefined) rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/waitForCompletion',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): ExecutableResourcePromise {
        const exitCode = options?.exitCode;
        return new ExecutableResourcePromise(this._waitForCompletionInternal(dependency, exitCode));
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withHttpHealthCheckInternal(path?: string, statusCode?: number, endpointName?: string): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (statusCode !== undefined) rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withHttpHealthCheck',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): ExecutableResourcePromise {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new ExecutableResourcePromise(this._withHttpHealthCheckInternal(path, statusCode, endpointName));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<ExecutableResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): ExecutableResourcePromise {
        const commandOptions = options?.commandOptions;
        return new ExecutableResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withDeveloperCertificateTrustInternal(trust: boolean): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, trust };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withDeveloperCertificateTrust',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withDeveloperCertificateTrustInternal(trust));
    }

    /** @internal */
    private async _withCertificateTrustScopeInternal(scope: CertificateTrustScope): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, scope };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withCertificateTrustScope',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withCertificateTrustScopeInternal(scope));
    }

    /** @internal */
    private async _withHttpsDeveloperCertificateInternal(password?: ParameterResource): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (password !== undefined) rpcArgs.password = password;
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withHttpsDeveloperCertificate',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): ExecutableResourcePromise {
        const password = options?.password;
        return new ExecutableResourcePromise(this._withHttpsDeveloperCertificateInternal(password));
    }

    /** @internal */
    private async _withoutHttpsCertificateInternal(): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withoutHttpsCertificate',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withoutHttpsCertificateInternal());
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): ExecutableResourcePromise {
        const iconVariant = options?.iconVariant;
        return new ExecutableResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _withHttpProbeInternal(probeType: ProbeType, path?: string, initialDelaySeconds?: number, periodSeconds?: number, timeoutSeconds?: number, failureThreshold?: number, successThreshold?: number, endpointName?: string): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, probeType };
        if (path !== undefined) rpcArgs.path = path;
        if (initialDelaySeconds !== undefined) rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined) rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined) rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined) rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined) rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withHttpProbe',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): ExecutableResourcePromise {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new ExecutableResourcePromise(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withRemoteImageNameInternal(remoteImageName: string): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withRemoteImageName',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withRemoteImageNameInternal(remoteImageName));
    }

    /** @internal */
    private async _withRemoteImageTagInternal(remoteImageTag: string): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withRemoteImageTag',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withRemoteImageTagInternal(remoteImageTag));
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<ExecutableResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): ExecutableResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new ExecutableResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<ExecutableResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<ExecutableResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<ExecutableResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<ExecutableResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<ExecutableResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceEndpointsAllocatedInternal(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): Promise<ExecutableResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceEndpointsAllocatedEventHandle;
            const arg = new ResourceEndpointsAllocatedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/onResourceEndpointsAllocated',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._onResourceEndpointsAllocatedInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<ExecutableResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _publishAsAzureContainerAppInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): Promise<ExecutableResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { executable: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishExecutableAsAzureContainerApp',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Configures the executable resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._publishAsAzureContainerAppInternal(configure));
    }

    /** @internal */
    private async _publishAsConfiguredAzureContainerAppJobInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<ExecutableResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { resource: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredAzureContainerAppJob',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._publishAsConfiguredAzureContainerAppJobInternal(configure));
    }

    /** @internal */
    private async _publishAsAzureContainerAppJobInternal(): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsAzureContainerAppJob',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._publishAsAzureContainerAppJobInternal());
    }

    /** @internal */
    private async _publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression: string, configure?: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<ExecutableResource> {
        const configureId = configure ? registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        if (configure !== undefined) rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): ExecutableResourcePromise {
        const configure = options?.configure;
        return new ExecutableResourcePromise(this._publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression, configure));
    }

    /** @internal */
    private async _publishAsScheduledAzureContainerAppJobInternal(cronExpression: string): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._publishAsScheduledAzureContainerAppJobInternal(cronExpression));
    }

    /** @internal */
    private async _withEnvironmentFromOutputInternal(name: string, bicepOutputReference: BicepOutputReference): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, bicepOutputReference };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromOutput',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withEnvironmentFromOutputInternal(name, bicepOutputReference));
    }

    /** @internal */
    private async _withEnvironmentFromKeyVaultSecretInternal(name: string, secretReference: ResourceBuilderBase): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, secretReference };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromKeyVaultSecret',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withEnvironmentFromKeyVaultSecretInternal(name, secretReference));
    }

    /** @internal */
    private async _withAzureUserAssignedIdentityInternal(identityResourceBuilder: AzureUserAssignedIdentityResource): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, identityResourceBuilder };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting.Azure/withUserAssignedIdentityAzureUserAssignedIdentity',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withAzureUserAssignedIdentityInternal(identityResourceBuilder));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<ExecutableResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<ExecutableResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new ExecutableResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for ExecutableResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class ExecutableResourcePromise implements PromiseLike<ExecutableResource> {
    constructor(private _promise: Promise<ExecutableResource>) {}

    then<TResult1 = ExecutableResource, TResult2 = never>(
        onfulfilled?: ((value: ExecutableResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Publishes the executable as a Docker container */
    publishAsDockerFile(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.publishAsDockerFile()));
    }

    /** Publishes an executable as a Docker file with optional container configuration */
    publishAsDockerFileWithConfigure(configure: (obj: ContainerResource) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.publishAsDockerFileWithConfigure(configure)));
    }

    /** Sets the executable command */
    withExecutableCommand(command: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withExecutableCommand(command)));
    }

    /** Sets the executable working directory */
    withWorkingDirectory(workingDirectory: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withWorkingDirectory(workingDirectory)));
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withMcpServer(options)));
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withOtlpExporter()));
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withOtlpExporterProtocol(protocol)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withEnvironment(name, value)));
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withEnvironmentExpression(name, value)));
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withEnvironmentCallback(callback)));
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withEnvironmentCallbackAsync(callback)));
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withEnvironmentEndpoint(name, endpointReference)));
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withEnvironmentParameter(name, parameter)));
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withEnvironmentConnectionString(envVarName, resource)));
    }

    /** Adds arguments */
    withArgs(args: string[]): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withArgs(args)));
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withArgsCallback(callback)));
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withArgsCallbackAsync(callback)));
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withReference(source, options)));
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withServiceReference(source)));
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withServiceReferenceNamed(source, name)));
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withReferenceUri(name, uri)));
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withReferenceExternalService(externalService)));
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withReferenceEndpoint(endpointReference)));
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withEndpoint(options)));
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withHttpEndpoint(options)));
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withHttpsEndpoint(options)));
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withExternalHttpEndpoints()));
    }

    /** Gets an endpoint reference */
    getEndpoint(name: string): Promise<EndpointReference> {
        return this._promise.then(obj => obj.getEndpoint(name));
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.asHttp2Service()));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withUrlForEndpointFactory(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.waitFor(dependency)));
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.waitForWithBehavior(dependency, waitBehavior)));
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.waitForStart(dependency)));
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.waitForStartWithBehavior(dependency, waitBehavior)));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.waitForCompletion(dependency, options)));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withHttpHealthCheck(options)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)));
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withCertificateTrustScope(scope)));
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)));
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withoutHttpsCertificate()));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withHttpProbe(probeType, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)));
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures the executable resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerApp(configure)));
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.publishAsConfiguredAzureContainerAppJob(configure)));
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerAppJob()));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.publishAsConfiguredScheduledAzureContainerAppJob(cronExpression, options)));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.publishAsScheduledAzureContainerAppJob(cronExpression)));
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withEnvironmentFromOutput(name, bicepOutputReference)));
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withEnvironmentFromKeyVaultSecret(name, secretReference)));
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withAzureUserAssignedIdentity(identityResourceBuilder)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): ExecutableResourcePromise {
        return new ExecutableResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// ExternalServiceResource
// ============================================================================

export class ExternalServiceResource extends ResourceBuilderBase<ExternalServiceResourceHandle> {
    constructor(handle: ExternalServiceResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<ExternalServiceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<ExternalServiceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): ExternalServiceResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new ExternalServiceResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withExternalServiceHttpHealthCheckInternal(path?: string, statusCode?: number): Promise<ExternalServiceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (statusCode !== undefined) rpcArgs.statusCode = statusCode;
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withExternalServiceHttpHealthCheck',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Adds an HTTP health check to an external service */
    withExternalServiceHttpHealthCheck(options?: WithExternalServiceHttpHealthCheckOptions): ExternalServiceResourcePromise {
        const path = options?.path;
        const statusCode = options?.statusCode;
        return new ExternalServiceResourcePromise(this._withExternalServiceHttpHealthCheckInternal(path, statusCode));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<ExternalServiceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): ExternalServiceResourcePromise {
        const helpLink = options?.helpLink;
        return new ExternalServiceResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<ExternalServiceResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<ExternalServiceResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<ExternalServiceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): ExternalServiceResourcePromise {
        const displayText = options?.displayText;
        return new ExternalServiceResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<ExternalServiceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): ExternalServiceResourcePromise {
        const displayText = options?.displayText;
        return new ExternalServiceResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<ExternalServiceResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<ExternalServiceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<ExternalServiceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<ExternalServiceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<ExternalServiceResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): ExternalServiceResourcePromise {
        const commandOptions = options?.commandOptions;
        return new ExternalServiceResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<ExternalServiceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<ExternalServiceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<ExternalServiceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): ExternalServiceResourcePromise {
        const iconVariant = options?.iconVariant;
        return new ExternalServiceResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<ExternalServiceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<ExternalServiceResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): ExternalServiceResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new ExternalServiceResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<ExternalServiceResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<ExternalServiceResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<ExternalServiceResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<ExternalServiceResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<ExternalServiceResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<ExternalServiceResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<ExternalServiceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<ExternalServiceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<ExternalServiceResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<ExternalServiceResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new ExternalServiceResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for ExternalServiceResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class ExternalServiceResourcePromise implements PromiseLike<ExternalServiceResource> {
    constructor(private _promise: Promise<ExternalServiceResource>) {}

    then<TResult1 = ExternalServiceResource, TResult2 = never>(
        onfulfilled?: ((value: ExternalServiceResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds an HTTP health check to an external service */
    withExternalServiceHttpHealthCheck(options?: WithExternalServiceHttpHealthCheckOptions): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withExternalServiceHttpHealthCheck(options)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): ExternalServiceResourcePromise {
        return new ExternalServiceResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// ParameterResource
// ============================================================================

export class ParameterResource extends ResourceBuilderBase<ParameterResourceHandle> {
    constructor(handle: ParameterResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): ParameterResourcePromise {
        return new ParameterResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): ParameterResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new ParameterResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withDescriptionInternal(description: string, enableMarkdown?: boolean): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, description };
        if (enableMarkdown !== undefined) rpcArgs.enableMarkdown = enableMarkdown;
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withDescription',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Sets a parameter description */
    withDescription(description: string, options?: WithDescriptionOptions): ParameterResourcePromise {
        const enableMarkdown = options?.enableMarkdown;
        return new ParameterResourcePromise(this._withDescriptionInternal(description, enableMarkdown));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): ParameterResourcePromise {
        const helpLink = options?.helpLink;
        return new ParameterResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<ParameterResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<ParameterResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): ParameterResourcePromise {
        const displayText = options?.displayText;
        return new ParameterResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): ParameterResourcePromise {
        const displayText = options?.displayText;
        return new ParameterResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<ParameterResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): ParameterResourcePromise {
        return new ParameterResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): ParameterResourcePromise {
        return new ParameterResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): ParameterResourcePromise {
        return new ParameterResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<ParameterResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): ParameterResourcePromise {
        const commandOptions = options?.commandOptions;
        return new ParameterResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): ParameterResourcePromise {
        return new ParameterResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): ParameterResourcePromise {
        return new ParameterResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): ParameterResourcePromise {
        const iconVariant = options?.iconVariant;
        return new ParameterResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): ParameterResourcePromise {
        return new ParameterResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<ParameterResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): ParameterResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new ParameterResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<ParameterResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<ParameterResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<ParameterResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<ParameterResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<ParameterResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<ParameterResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): ParameterResourcePromise {
        return new ParameterResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): ParameterResourcePromise {
        return new ParameterResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<ParameterResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<ParameterResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new ParameterResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): ParameterResourcePromise {
        return new ParameterResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for ParameterResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class ParameterResourcePromise implements PromiseLike<ParameterResource> {
    constructor(private _promise: Promise<ParameterResource>) {}

    then<TResult1 = ParameterResource, TResult2 = never>(
        onfulfilled?: ((value: ParameterResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Sets a parameter description */
    withDescription(description: string, options?: WithDescriptionOptions): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withDescription(description, options)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): ParameterResourcePromise {
        return new ParameterResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// PgAdminContainerResource
// ============================================================================

export class PgAdminContainerResource extends ResourceBuilderBase<PgAdminContainerResourceHandle> {
    constructor(handle: PgAdminContainerResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withBindMountInternal(source: string, target: string, isReadOnly?: boolean): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, target };
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withBindMount',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): PgAdminContainerResourcePromise {
        const isReadOnly = options?.isReadOnly;
        return new PgAdminContainerResourcePromise(this._withBindMountInternal(source, target, isReadOnly));
    }

    /** @internal */
    private async _withEntrypointInternal(entrypoint: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withEntrypoint',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withEntrypointInternal(entrypoint));
    }

    /** @internal */
    private async _withImageTagInternal(tag: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, tag };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withImageTag',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets the container image tag */
    withImageTag(tag: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withImageTagInternal(tag));
    }

    /** @internal */
    private async _withImageRegistryInternal(registry: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withImageRegistry',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withImageRegistryInternal(registry));
    }

    /** @internal */
    private async _withImageInternal(image: string, tag?: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, image };
        if (tag !== undefined) rpcArgs.tag = tag;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withImage',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): PgAdminContainerResourcePromise {
        const tag = options?.tag;
        return new PgAdminContainerResourcePromise(this._withImageInternal(image, tag));
    }

    /** @internal */
    private async _withImageSHA256Internal(sha256: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withImageSHA256',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withImageSHA256Internal(sha256));
    }

    /** @internal */
    private async _withContainerRuntimeArgsInternal(args: string[]): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withContainerRuntimeArgs',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withContainerRuntimeArgsInternal(args));
    }

    /** @internal */
    private async _withLifetimeInternal(lifetime: ContainerLifetime): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withLifetime',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withLifetimeInternal(lifetime));
    }

    /** @internal */
    private async _withImagePullPolicyInternal(pullPolicy: ImagePullPolicy): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withImagePullPolicy',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withImagePullPolicyInternal(pullPolicy));
    }

    /** @internal */
    private async _publishAsContainerInternal(): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/publishAsContainer',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._publishAsContainerInternal());
    }

    /** @internal */
    private async _withDockerfileInternal(contextPath: string, dockerfilePath?: string, stage?: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined) rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined) rpcArgs.stage = stage;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withDockerfile',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): PgAdminContainerResourcePromise {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new PgAdminContainerResourcePromise(this._withDockerfileInternal(contextPath, dockerfilePath, stage));
    }

    /** @internal */
    private async _withContainerNameInternal(name: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withContainerName',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets the container name */
    withContainerName(name: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withContainerNameInternal(name));
    }

    /** @internal */
    private async _withBuildArgInternal(name: string, value: ParameterResource): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withBuildArg',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withBuildArgInternal(name, value));
    }

    /** @internal */
    private async _withBuildSecretInternal(name: string, value: ParameterResource): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withBuildSecret',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withBuildSecretInternal(name, value));
    }

    /** @internal */
    private async _withEndpointProxySupportInternal(proxyEnabled: boolean): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withEndpointProxySupport',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withEndpointProxySupportInternal(proxyEnabled));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): PgAdminContainerResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new PgAdminContainerResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withContainerNetworkAliasInternal(alias: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, alias };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withContainerNetworkAlias',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withContainerNetworkAliasInternal(alias));
    }

    /** @internal */
    private async _withMcpServerInternal(path?: string, endpointName?: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withMcpServer',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): PgAdminContainerResourcePromise {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new PgAdminContainerResourcePromise(this._withMcpServerInternal(path, endpointName));
    }

    /** @internal */
    private async _withOtlpExporterInternal(): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withOtlpExporter',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withOtlpExporterInternal());
    }

    /** @internal */
    private async _withOtlpExporterProtocolInternal(protocol: OtlpProtocol): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, protocol };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withOtlpExporterProtocol',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withOtlpExporterProtocolInternal(protocol));
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/publishAsConnectionString',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): PgAdminContainerResourcePromise {
        const helpLink = options?.helpLink;
        return new PgAdminContainerResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withEnvironmentInternal(name: string, value: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withEnvironment',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withEnvironmentInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentExpressionInternal(name: string, value: ReferenceExpression): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentExpression',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withEnvironmentExpressionInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentCallbackInternal(callback: (obj: EnvironmentCallbackContext) => Promise<void>): Promise<PgAdminContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as EnvironmentCallbackContextHandle;
            const obj = new EnvironmentCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallback',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withEnvironmentCallbackInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentCallbackAsyncInternal(callback: (arg: EnvironmentCallbackContext) => Promise<void>): Promise<PgAdminContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EnvironmentCallbackContextHandle;
            const arg = new EnvironmentCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallbackAsync',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withEnvironmentCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentEndpointInternal(name: string, endpointReference: EndpointReference): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, endpointReference };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentEndpoint',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withEnvironmentEndpointInternal(name, endpointReference));
    }

    /** @internal */
    private async _withEnvironmentParameterInternal(name: string, parameter: ParameterResource): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, parameter };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentParameter',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withEnvironmentParameterInternal(name, parameter));
    }

    /** @internal */
    private async _withEnvironmentConnectionStringInternal(envVarName: string, resource: ResourceBuilderBase): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, envVarName, resource };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentConnectionString',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withEnvironmentConnectionStringInternal(envVarName, resource));
    }

    /** @internal */
    private async _withArgsInternal(args: string[]): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withArgs',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds arguments */
    withArgs(args: string[]): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withArgsInternal(args));
    }

    /** @internal */
    private async _withArgsCallbackInternal(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): Promise<PgAdminContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as CommandLineArgsCallbackContextHandle;
            const obj = new CommandLineArgsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withArgsCallback',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withArgsCallbackInternal(callback));
    }

    /** @internal */
    private async _withArgsCallbackAsyncInternal(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): Promise<PgAdminContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as CommandLineArgsCallbackContextHandle;
            const arg = new CommandLineArgsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withArgsCallbackAsync',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withArgsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withReferenceInternal(source: ResourceBuilderBase, connectionName?: string, optional?: boolean): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (connectionName !== undefined) rpcArgs.connectionName = connectionName;
        if (optional !== undefined) rpcArgs.optional = optional;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withReference',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): PgAdminContainerResourcePromise {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        return new PgAdminContainerResourcePromise(this._withReferenceInternal(source, connectionName, optional));
    }

    /** @internal */
    private async _withServiceReferenceInternal(source: ResourceBuilderBase): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withServiceReference',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withServiceReferenceInternal(source));
    }

    /** @internal */
    private async _withServiceReferenceNamedInternal(source: ResourceBuilderBase, name: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, name };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withServiceReferenceNamed',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withServiceReferenceNamedInternal(source, name));
    }

    /** @internal */
    private async _withReferenceUriInternal(name: string, uri: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, uri };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withReferenceUri',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withReferenceUriInternal(name, uri));
    }

    /** @internal */
    private async _withReferenceExternalServiceInternal(externalService: ExternalServiceResource): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, externalService };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withReferenceExternalService',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withReferenceExternalServiceInternal(externalService));
    }

    /** @internal */
    private async _withReferenceEndpointInternal(endpointReference: EndpointReference): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointReference };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withReferenceEndpoint',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withReferenceEndpointInternal(endpointReference));
    }

    /** @internal */
    private async _withEndpointInternal(port?: number, targetPort?: number, scheme?: string, name?: string, env?: string, isProxied?: boolean, isExternal?: boolean, protocol?: ProtocolType): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (scheme !== undefined) rpcArgs.scheme = scheme;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined) rpcArgs.isExternal = isExternal;
        if (protocol !== undefined) rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withEndpoint',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): PgAdminContainerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new PgAdminContainerResourcePromise(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol));
    }

    /** @internal */
    private async _withHttpEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withHttpEndpoint',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): PgAdminContainerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new PgAdminContainerResourcePromise(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withHttpsEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withHttpsEndpoint',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): PgAdminContainerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new PgAdminContainerResourcePromise(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withExternalHttpEndpointsInternal(): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withExternalHttpEndpoints',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withExternalHttpEndpointsInternal());
    }

    /** Gets an endpoint reference */
    async getEndpoint(name: string): Promise<EndpointReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<EndpointReference>(
            'Aspire.Hosting/getEndpoint',
            rpcArgs
        );
    }

    /** @internal */
    private async _asHttp2ServiceInternal(): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/asHttp2Service',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._asHttp2ServiceInternal());
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<PgAdminContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<PgAdminContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): PgAdminContainerResourcePromise {
        const displayText = options?.displayText;
        return new PgAdminContainerResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): PgAdminContainerResourcePromise {
        const displayText = options?.displayText;
        return new PgAdminContainerResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<PgAdminContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _withUrlForEndpointFactoryInternal(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): Promise<PgAdminContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EndpointReferenceHandle;
            const arg = new EndpointReference(argHandle, this._client);
            return await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withUrlForEndpointFactory',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withUrlForEndpointFactoryInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _waitForInternal(dependency: ResourceBuilderBase): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/waitFor',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._waitForInternal(dependency));
    }

    /** @internal */
    private async _waitForWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/waitForWithBehavior',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._waitForWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _waitForStartInternal(dependency: ResourceBuilderBase): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/waitForStart',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._waitForStartInternal(dependency));
    }

    /** @internal */
    private async _waitForStartWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/waitForStartWithBehavior',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._waitForStartWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _waitForCompletionInternal(dependency: ResourceBuilderBase, exitCode?: number): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        if (exitCode !== undefined) rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/waitForCompletion',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): PgAdminContainerResourcePromise {
        const exitCode = options?.exitCode;
        return new PgAdminContainerResourcePromise(this._waitForCompletionInternal(dependency, exitCode));
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withHttpHealthCheckInternal(path?: string, statusCode?: number, endpointName?: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (statusCode !== undefined) rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withHttpHealthCheck',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): PgAdminContainerResourcePromise {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new PgAdminContainerResourcePromise(this._withHttpHealthCheckInternal(path, statusCode, endpointName));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<PgAdminContainerResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): PgAdminContainerResourcePromise {
        const commandOptions = options?.commandOptions;
        return new PgAdminContainerResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withDeveloperCertificateTrustInternal(trust: boolean): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, trust };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withDeveloperCertificateTrust',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withDeveloperCertificateTrustInternal(trust));
    }

    /** @internal */
    private async _withCertificateTrustScopeInternal(scope: CertificateTrustScope): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, scope };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withCertificateTrustScope',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withCertificateTrustScopeInternal(scope));
    }

    /** @internal */
    private async _withHttpsDeveloperCertificateInternal(password?: ParameterResource): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (password !== undefined) rpcArgs.password = password;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withHttpsDeveloperCertificate',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): PgAdminContainerResourcePromise {
        const password = options?.password;
        return new PgAdminContainerResourcePromise(this._withHttpsDeveloperCertificateInternal(password));
    }

    /** @internal */
    private async _withoutHttpsCertificateInternal(): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withoutHttpsCertificate',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withoutHttpsCertificateInternal());
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): PgAdminContainerResourcePromise {
        const iconVariant = options?.iconVariant;
        return new PgAdminContainerResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _withHttpProbeInternal(probeType: ProbeType, path?: string, initialDelaySeconds?: number, periodSeconds?: number, timeoutSeconds?: number, failureThreshold?: number, successThreshold?: number, endpointName?: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, probeType };
        if (path !== undefined) rpcArgs.path = path;
        if (initialDelaySeconds !== undefined) rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined) rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined) rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined) rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined) rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withHttpProbe',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): PgAdminContainerResourcePromise {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new PgAdminContainerResourcePromise(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withRemoteImageNameInternal(remoteImageName: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withRemoteImageName',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withRemoteImageNameInternal(remoteImageName));
    }

    /** @internal */
    private async _withRemoteImageTagInternal(remoteImageTag: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withRemoteImageTag',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withRemoteImageTagInternal(remoteImageTag));
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<PgAdminContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): PgAdminContainerResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new PgAdminContainerResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<PgAdminContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<PgAdminContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** @internal */
    private async _withVolumeInternal(target: string, name?: string, isReadOnly?: boolean): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, target };
        if (name !== undefined) rpcArgs.name = name;
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/withVolume',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): PgAdminContainerResourcePromise {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new PgAdminContainerResourcePromise(this._withVolumeInternal(target, name, isReadOnly));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<PgAdminContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<PgAdminContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<PgAdminContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceEndpointsAllocatedInternal(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): Promise<PgAdminContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceEndpointsAllocatedEventHandle;
            const arg = new ResourceEndpointsAllocatedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/onResourceEndpointsAllocated',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._onResourceEndpointsAllocatedInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<PgAdminContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withHostPortInternal(port?: number): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting.PostgreSQL/withPgAdminHostPort',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets the host port for pgAdmin */
    withHostPort(options?: WithHostPortOptions): PgAdminContainerResourcePromise {
        const port = options?.port;
        return new PgAdminContainerResourcePromise(this._withHostPortInternal(port));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _publishAsAzureContainerAppInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): Promise<PgAdminContainerResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { container: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishContainerAsAzureContainerApp',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._publishAsAzureContainerAppInternal(configure));
    }

    /** @internal */
    private async _publishAsConfiguredAzureContainerAppJobInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<PgAdminContainerResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { resource: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredAzureContainerAppJob',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._publishAsConfiguredAzureContainerAppJobInternal(configure));
    }

    /** @internal */
    private async _publishAsAzureContainerAppJobInternal(): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsAzureContainerAppJob',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._publishAsAzureContainerAppJobInternal());
    }

    /** @internal */
    private async _publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression: string, configure?: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<PgAdminContainerResource> {
        const configureId = configure ? registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        if (configure !== undefined) rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): PgAdminContainerResourcePromise {
        const configure = options?.configure;
        return new PgAdminContainerResourcePromise(this._publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression, configure));
    }

    /** @internal */
    private async _publishAsScheduledAzureContainerAppJobInternal(cronExpression: string): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._publishAsScheduledAzureContainerAppJobInternal(cronExpression));
    }

    /** @internal */
    private async _withEnvironmentFromOutputInternal(name: string, bicepOutputReference: BicepOutputReference): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, bicepOutputReference };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromOutput',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withEnvironmentFromOutputInternal(name, bicepOutputReference));
    }

    /** @internal */
    private async _withEnvironmentFromKeyVaultSecretInternal(name: string, secretReference: ResourceBuilderBase): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, secretReference };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromKeyVaultSecret',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withEnvironmentFromKeyVaultSecretInternal(name, secretReference));
    }

    /** @internal */
    private async _withAzureUserAssignedIdentityInternal(identityResourceBuilder: AzureUserAssignedIdentityResource): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, identityResourceBuilder };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting.Azure/withUserAssignedIdentityAzureUserAssignedIdentity',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withAzureUserAssignedIdentityInternal(identityResourceBuilder));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<PgAdminContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<PgAdminContainerResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new PgAdminContainerResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for PgAdminContainerResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class PgAdminContainerResourcePromise implements PromiseLike<PgAdminContainerResource> {
    constructor(private _promise: Promise<PgAdminContainerResource>) {}

    then<TResult1 = PgAdminContainerResource, TResult2 = never>(
        onfulfilled?: ((value: PgAdminContainerResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withBindMount(source, target, options)));
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withEntrypoint(entrypoint)));
    }

    /** Sets the container image tag */
    withImageTag(tag: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withImageTag(tag)));
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withImageRegistry(registry)));
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withImage(image, options)));
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withImageSHA256(sha256)));
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withContainerRuntimeArgs(args)));
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withLifetime(lifetime)));
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)));
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.publishAsContainer()));
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withDockerfile(contextPath, options)));
    }

    /** Sets the container name */
    withContainerName(name: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withContainerName(name)));
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withBuildArg(name, value)));
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withBuildSecret(name, value)));
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withContainerNetworkAlias(alias)));
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withMcpServer(options)));
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withOtlpExporter()));
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withOtlpExporterProtocol(protocol)));
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withEnvironment(name, value)));
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentExpression(name, value)));
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentCallback(callback)));
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentCallbackAsync(callback)));
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentEndpoint(name, endpointReference)));
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentParameter(name, parameter)));
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentConnectionString(envVarName, resource)));
    }

    /** Adds arguments */
    withArgs(args: string[]): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withArgs(args)));
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withArgsCallback(callback)));
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withArgsCallbackAsync(callback)));
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withReference(source, options)));
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withServiceReference(source)));
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withServiceReferenceNamed(source, name)));
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withReferenceUri(name, uri)));
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withReferenceExternalService(externalService)));
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withReferenceEndpoint(endpointReference)));
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withEndpoint(options)));
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withHttpEndpoint(options)));
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withHttpsEndpoint(options)));
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withExternalHttpEndpoints()));
    }

    /** Gets an endpoint reference */
    getEndpoint(name: string): Promise<EndpointReference> {
        return this._promise.then(obj => obj.getEndpoint(name));
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.asHttp2Service()));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withUrlForEndpointFactory(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.waitFor(dependency)));
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.waitForWithBehavior(dependency, waitBehavior)));
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.waitForStart(dependency)));
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.waitForStartWithBehavior(dependency, waitBehavior)));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.waitForCompletion(dependency, options)));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withHttpHealthCheck(options)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)));
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withCertificateTrustScope(scope)));
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)));
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withoutHttpsCertificate()));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withHttpProbe(probeType, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)));
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withVolume(target, options)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Sets the host port for pgAdmin */
    withHostPort(options?: WithHostPortOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withHostPort(options)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerApp(configure)));
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.publishAsConfiguredAzureContainerAppJob(configure)));
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerAppJob()));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.publishAsConfiguredScheduledAzureContainerAppJob(cronExpression, options)));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.publishAsScheduledAzureContainerAppJob(cronExpression)));
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentFromOutput(name, bicepOutputReference)));
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentFromKeyVaultSecret(name, secretReference)));
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withAzureUserAssignedIdentity(identityResourceBuilder)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): PgAdminContainerResourcePromise {
        return new PgAdminContainerResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// PgWebContainerResource
// ============================================================================

export class PgWebContainerResource extends ResourceBuilderBase<PgWebContainerResourceHandle> {
    constructor(handle: PgWebContainerResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withBindMountInternal(source: string, target: string, isReadOnly?: boolean): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, target };
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withBindMount',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): PgWebContainerResourcePromise {
        const isReadOnly = options?.isReadOnly;
        return new PgWebContainerResourcePromise(this._withBindMountInternal(source, target, isReadOnly));
    }

    /** @internal */
    private async _withEntrypointInternal(entrypoint: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withEntrypoint',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withEntrypointInternal(entrypoint));
    }

    /** @internal */
    private async _withImageTagInternal(tag: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, tag };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withImageTag',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets the container image tag */
    withImageTag(tag: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withImageTagInternal(tag));
    }

    /** @internal */
    private async _withImageRegistryInternal(registry: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withImageRegistry',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withImageRegistryInternal(registry));
    }

    /** @internal */
    private async _withImageInternal(image: string, tag?: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, image };
        if (tag !== undefined) rpcArgs.tag = tag;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withImage',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): PgWebContainerResourcePromise {
        const tag = options?.tag;
        return new PgWebContainerResourcePromise(this._withImageInternal(image, tag));
    }

    /** @internal */
    private async _withImageSHA256Internal(sha256: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withImageSHA256',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withImageSHA256Internal(sha256));
    }

    /** @internal */
    private async _withContainerRuntimeArgsInternal(args: string[]): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withContainerRuntimeArgs',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withContainerRuntimeArgsInternal(args));
    }

    /** @internal */
    private async _withLifetimeInternal(lifetime: ContainerLifetime): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withLifetime',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withLifetimeInternal(lifetime));
    }

    /** @internal */
    private async _withImagePullPolicyInternal(pullPolicy: ImagePullPolicy): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withImagePullPolicy',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withImagePullPolicyInternal(pullPolicy));
    }

    /** @internal */
    private async _publishAsContainerInternal(): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/publishAsContainer',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._publishAsContainerInternal());
    }

    /** @internal */
    private async _withDockerfileInternal(contextPath: string, dockerfilePath?: string, stage?: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined) rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined) rpcArgs.stage = stage;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withDockerfile',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): PgWebContainerResourcePromise {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new PgWebContainerResourcePromise(this._withDockerfileInternal(contextPath, dockerfilePath, stage));
    }

    /** @internal */
    private async _withContainerNameInternal(name: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withContainerName',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets the container name */
    withContainerName(name: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withContainerNameInternal(name));
    }

    /** @internal */
    private async _withBuildArgInternal(name: string, value: ParameterResource): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withBuildArg',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withBuildArgInternal(name, value));
    }

    /** @internal */
    private async _withBuildSecretInternal(name: string, value: ParameterResource): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withBuildSecret',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withBuildSecretInternal(name, value));
    }

    /** @internal */
    private async _withEndpointProxySupportInternal(proxyEnabled: boolean): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withEndpointProxySupport',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withEndpointProxySupportInternal(proxyEnabled));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): PgWebContainerResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new PgWebContainerResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withContainerNetworkAliasInternal(alias: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, alias };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withContainerNetworkAlias',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withContainerNetworkAliasInternal(alias));
    }

    /** @internal */
    private async _withMcpServerInternal(path?: string, endpointName?: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withMcpServer',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): PgWebContainerResourcePromise {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new PgWebContainerResourcePromise(this._withMcpServerInternal(path, endpointName));
    }

    /** @internal */
    private async _withOtlpExporterInternal(): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withOtlpExporter',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withOtlpExporterInternal());
    }

    /** @internal */
    private async _withOtlpExporterProtocolInternal(protocol: OtlpProtocol): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, protocol };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withOtlpExporterProtocol',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withOtlpExporterProtocolInternal(protocol));
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/publishAsConnectionString',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): PgWebContainerResourcePromise {
        const helpLink = options?.helpLink;
        return new PgWebContainerResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withEnvironmentInternal(name: string, value: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withEnvironment',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withEnvironmentInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentExpressionInternal(name: string, value: ReferenceExpression): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentExpression',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withEnvironmentExpressionInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentCallbackInternal(callback: (obj: EnvironmentCallbackContext) => Promise<void>): Promise<PgWebContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as EnvironmentCallbackContextHandle;
            const obj = new EnvironmentCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallback',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withEnvironmentCallbackInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentCallbackAsyncInternal(callback: (arg: EnvironmentCallbackContext) => Promise<void>): Promise<PgWebContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EnvironmentCallbackContextHandle;
            const arg = new EnvironmentCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallbackAsync',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withEnvironmentCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentEndpointInternal(name: string, endpointReference: EndpointReference): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, endpointReference };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentEndpoint',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withEnvironmentEndpointInternal(name, endpointReference));
    }

    /** @internal */
    private async _withEnvironmentParameterInternal(name: string, parameter: ParameterResource): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, parameter };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentParameter',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withEnvironmentParameterInternal(name, parameter));
    }

    /** @internal */
    private async _withEnvironmentConnectionStringInternal(envVarName: string, resource: ResourceBuilderBase): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, envVarName, resource };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentConnectionString',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withEnvironmentConnectionStringInternal(envVarName, resource));
    }

    /** @internal */
    private async _withArgsInternal(args: string[]): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withArgs',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds arguments */
    withArgs(args: string[]): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withArgsInternal(args));
    }

    /** @internal */
    private async _withArgsCallbackInternal(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): Promise<PgWebContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as CommandLineArgsCallbackContextHandle;
            const obj = new CommandLineArgsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withArgsCallback',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withArgsCallbackInternal(callback));
    }

    /** @internal */
    private async _withArgsCallbackAsyncInternal(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): Promise<PgWebContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as CommandLineArgsCallbackContextHandle;
            const arg = new CommandLineArgsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withArgsCallbackAsync',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withArgsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withReferenceInternal(source: ResourceBuilderBase, connectionName?: string, optional?: boolean): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (connectionName !== undefined) rpcArgs.connectionName = connectionName;
        if (optional !== undefined) rpcArgs.optional = optional;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withReference',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): PgWebContainerResourcePromise {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        return new PgWebContainerResourcePromise(this._withReferenceInternal(source, connectionName, optional));
    }

    /** @internal */
    private async _withServiceReferenceInternal(source: ResourceBuilderBase): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withServiceReference',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withServiceReferenceInternal(source));
    }

    /** @internal */
    private async _withServiceReferenceNamedInternal(source: ResourceBuilderBase, name: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, name };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withServiceReferenceNamed',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withServiceReferenceNamedInternal(source, name));
    }

    /** @internal */
    private async _withReferenceUriInternal(name: string, uri: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, uri };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withReferenceUri',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withReferenceUriInternal(name, uri));
    }

    /** @internal */
    private async _withReferenceExternalServiceInternal(externalService: ExternalServiceResource): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, externalService };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withReferenceExternalService',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withReferenceExternalServiceInternal(externalService));
    }

    /** @internal */
    private async _withReferenceEndpointInternal(endpointReference: EndpointReference): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointReference };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withReferenceEndpoint',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withReferenceEndpointInternal(endpointReference));
    }

    /** @internal */
    private async _withEndpointInternal(port?: number, targetPort?: number, scheme?: string, name?: string, env?: string, isProxied?: boolean, isExternal?: boolean, protocol?: ProtocolType): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (scheme !== undefined) rpcArgs.scheme = scheme;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined) rpcArgs.isExternal = isExternal;
        if (protocol !== undefined) rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withEndpoint',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): PgWebContainerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new PgWebContainerResourcePromise(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol));
    }

    /** @internal */
    private async _withHttpEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withHttpEndpoint',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): PgWebContainerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new PgWebContainerResourcePromise(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withHttpsEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withHttpsEndpoint',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): PgWebContainerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new PgWebContainerResourcePromise(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withExternalHttpEndpointsInternal(): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withExternalHttpEndpoints',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withExternalHttpEndpointsInternal());
    }

    /** Gets an endpoint reference */
    async getEndpoint(name: string): Promise<EndpointReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<EndpointReference>(
            'Aspire.Hosting/getEndpoint',
            rpcArgs
        );
    }

    /** @internal */
    private async _asHttp2ServiceInternal(): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/asHttp2Service',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._asHttp2ServiceInternal());
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<PgWebContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<PgWebContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): PgWebContainerResourcePromise {
        const displayText = options?.displayText;
        return new PgWebContainerResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): PgWebContainerResourcePromise {
        const displayText = options?.displayText;
        return new PgWebContainerResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<PgWebContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _withUrlForEndpointFactoryInternal(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): Promise<PgWebContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EndpointReferenceHandle;
            const arg = new EndpointReference(argHandle, this._client);
            return await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withUrlForEndpointFactory',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withUrlForEndpointFactoryInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _waitForInternal(dependency: ResourceBuilderBase): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/waitFor',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._waitForInternal(dependency));
    }

    /** @internal */
    private async _waitForWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/waitForWithBehavior',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._waitForWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _waitForStartInternal(dependency: ResourceBuilderBase): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/waitForStart',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._waitForStartInternal(dependency));
    }

    /** @internal */
    private async _waitForStartWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/waitForStartWithBehavior',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._waitForStartWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _waitForCompletionInternal(dependency: ResourceBuilderBase, exitCode?: number): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        if (exitCode !== undefined) rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/waitForCompletion',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): PgWebContainerResourcePromise {
        const exitCode = options?.exitCode;
        return new PgWebContainerResourcePromise(this._waitForCompletionInternal(dependency, exitCode));
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withHttpHealthCheckInternal(path?: string, statusCode?: number, endpointName?: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (statusCode !== undefined) rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withHttpHealthCheck',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): PgWebContainerResourcePromise {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new PgWebContainerResourcePromise(this._withHttpHealthCheckInternal(path, statusCode, endpointName));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<PgWebContainerResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): PgWebContainerResourcePromise {
        const commandOptions = options?.commandOptions;
        return new PgWebContainerResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withDeveloperCertificateTrustInternal(trust: boolean): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, trust };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withDeveloperCertificateTrust',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withDeveloperCertificateTrustInternal(trust));
    }

    /** @internal */
    private async _withCertificateTrustScopeInternal(scope: CertificateTrustScope): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, scope };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withCertificateTrustScope',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withCertificateTrustScopeInternal(scope));
    }

    /** @internal */
    private async _withHttpsDeveloperCertificateInternal(password?: ParameterResource): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (password !== undefined) rpcArgs.password = password;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withHttpsDeveloperCertificate',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): PgWebContainerResourcePromise {
        const password = options?.password;
        return new PgWebContainerResourcePromise(this._withHttpsDeveloperCertificateInternal(password));
    }

    /** @internal */
    private async _withoutHttpsCertificateInternal(): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withoutHttpsCertificate',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withoutHttpsCertificateInternal());
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): PgWebContainerResourcePromise {
        const iconVariant = options?.iconVariant;
        return new PgWebContainerResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _withHttpProbeInternal(probeType: ProbeType, path?: string, initialDelaySeconds?: number, periodSeconds?: number, timeoutSeconds?: number, failureThreshold?: number, successThreshold?: number, endpointName?: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, probeType };
        if (path !== undefined) rpcArgs.path = path;
        if (initialDelaySeconds !== undefined) rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined) rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined) rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined) rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined) rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withHttpProbe',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): PgWebContainerResourcePromise {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new PgWebContainerResourcePromise(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withRemoteImageNameInternal(remoteImageName: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withRemoteImageName',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withRemoteImageNameInternal(remoteImageName));
    }

    /** @internal */
    private async _withRemoteImageTagInternal(remoteImageTag: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withRemoteImageTag',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withRemoteImageTagInternal(remoteImageTag));
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<PgWebContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): PgWebContainerResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new PgWebContainerResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<PgWebContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<PgWebContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** @internal */
    private async _withVolumeInternal(target: string, name?: string, isReadOnly?: boolean): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, target };
        if (name !== undefined) rpcArgs.name = name;
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/withVolume',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): PgWebContainerResourcePromise {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new PgWebContainerResourcePromise(this._withVolumeInternal(target, name, isReadOnly));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<PgWebContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<PgWebContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<PgWebContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceEndpointsAllocatedInternal(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): Promise<PgWebContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceEndpointsAllocatedEventHandle;
            const arg = new ResourceEndpointsAllocatedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/onResourceEndpointsAllocated',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._onResourceEndpointsAllocatedInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<PgWebContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withHostPortInternal(port?: number): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting.PostgreSQL/withPgWebHostPort',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets the host port for pgweb */
    withHostPort(options?: WithHostPortOptions): PgWebContainerResourcePromise {
        const port = options?.port;
        return new PgWebContainerResourcePromise(this._withHostPortInternal(port));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _publishAsAzureContainerAppInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): Promise<PgWebContainerResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { container: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishContainerAsAzureContainerApp',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._publishAsAzureContainerAppInternal(configure));
    }

    /** @internal */
    private async _publishAsConfiguredAzureContainerAppJobInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<PgWebContainerResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { resource: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredAzureContainerAppJob',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._publishAsConfiguredAzureContainerAppJobInternal(configure));
    }

    /** @internal */
    private async _publishAsAzureContainerAppJobInternal(): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsAzureContainerAppJob',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._publishAsAzureContainerAppJobInternal());
    }

    /** @internal */
    private async _publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression: string, configure?: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<PgWebContainerResource> {
        const configureId = configure ? registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        if (configure !== undefined) rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): PgWebContainerResourcePromise {
        const configure = options?.configure;
        return new PgWebContainerResourcePromise(this._publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression, configure));
    }

    /** @internal */
    private async _publishAsScheduledAzureContainerAppJobInternal(cronExpression: string): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._publishAsScheduledAzureContainerAppJobInternal(cronExpression));
    }

    /** @internal */
    private async _withEnvironmentFromOutputInternal(name: string, bicepOutputReference: BicepOutputReference): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, bicepOutputReference };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromOutput',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withEnvironmentFromOutputInternal(name, bicepOutputReference));
    }

    /** @internal */
    private async _withEnvironmentFromKeyVaultSecretInternal(name: string, secretReference: ResourceBuilderBase): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, secretReference };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromKeyVaultSecret',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withEnvironmentFromKeyVaultSecretInternal(name, secretReference));
    }

    /** @internal */
    private async _withAzureUserAssignedIdentityInternal(identityResourceBuilder: AzureUserAssignedIdentityResource): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, identityResourceBuilder };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting.Azure/withUserAssignedIdentityAzureUserAssignedIdentity',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withAzureUserAssignedIdentityInternal(identityResourceBuilder));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<PgWebContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<PgWebContainerResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new PgWebContainerResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for PgWebContainerResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class PgWebContainerResourcePromise implements PromiseLike<PgWebContainerResource> {
    constructor(private _promise: Promise<PgWebContainerResource>) {}

    then<TResult1 = PgWebContainerResource, TResult2 = never>(
        onfulfilled?: ((value: PgWebContainerResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withBindMount(source, target, options)));
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withEntrypoint(entrypoint)));
    }

    /** Sets the container image tag */
    withImageTag(tag: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withImageTag(tag)));
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withImageRegistry(registry)));
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withImage(image, options)));
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withImageSHA256(sha256)));
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withContainerRuntimeArgs(args)));
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withLifetime(lifetime)));
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)));
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.publishAsContainer()));
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withDockerfile(contextPath, options)));
    }

    /** Sets the container name */
    withContainerName(name: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withContainerName(name)));
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withBuildArg(name, value)));
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withBuildSecret(name, value)));
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withContainerNetworkAlias(alias)));
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withMcpServer(options)));
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withOtlpExporter()));
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withOtlpExporterProtocol(protocol)));
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withEnvironment(name, value)));
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentExpression(name, value)));
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentCallback(callback)));
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentCallbackAsync(callback)));
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentEndpoint(name, endpointReference)));
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentParameter(name, parameter)));
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentConnectionString(envVarName, resource)));
    }

    /** Adds arguments */
    withArgs(args: string[]): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withArgs(args)));
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withArgsCallback(callback)));
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withArgsCallbackAsync(callback)));
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withReference(source, options)));
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withServiceReference(source)));
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withServiceReferenceNamed(source, name)));
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withReferenceUri(name, uri)));
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withReferenceExternalService(externalService)));
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withReferenceEndpoint(endpointReference)));
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withEndpoint(options)));
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withHttpEndpoint(options)));
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withHttpsEndpoint(options)));
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withExternalHttpEndpoints()));
    }

    /** Gets an endpoint reference */
    getEndpoint(name: string): Promise<EndpointReference> {
        return this._promise.then(obj => obj.getEndpoint(name));
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.asHttp2Service()));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withUrlForEndpointFactory(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.waitFor(dependency)));
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.waitForWithBehavior(dependency, waitBehavior)));
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.waitForStart(dependency)));
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.waitForStartWithBehavior(dependency, waitBehavior)));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.waitForCompletion(dependency, options)));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withHttpHealthCheck(options)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)));
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withCertificateTrustScope(scope)));
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)));
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withoutHttpsCertificate()));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withHttpProbe(probeType, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)));
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withVolume(target, options)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Sets the host port for pgweb */
    withHostPort(options?: WithHostPortOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withHostPort(options)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerApp(configure)));
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.publishAsConfiguredAzureContainerAppJob(configure)));
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerAppJob()));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.publishAsConfiguredScheduledAzureContainerAppJob(cronExpression, options)));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.publishAsScheduledAzureContainerAppJob(cronExpression)));
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentFromOutput(name, bicepOutputReference)));
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentFromKeyVaultSecret(name, secretReference)));
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withAzureUserAssignedIdentity(identityResourceBuilder)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): PgWebContainerResourcePromise {
        return new PgWebContainerResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// PostgresDatabaseResource
// ============================================================================

export class PostgresDatabaseResource extends ResourceBuilderBase<PostgresDatabaseResourceHandle> {
    constructor(handle: PostgresDatabaseResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** Gets the Parent property */
    parent = {
        get: async (): Promise<PostgresServerResource> => {
            const handle = await this._client.invokeCapability<PostgresServerResourceHandle>(
                'Aspire.Hosting.ApplicationModel/PostgresDatabaseResource.parent',
                { context: this._handle }
            );
            return new PostgresServerResource(handle, this._client);
        },
    };

    /** Gets the ConnectionStringExpression property */
    connectionStringExpression = {
        get: async (): Promise<ReferenceExpression> => {
            return await this._client.invokeCapability<ReferenceExpression>(
                'Aspire.Hosting.ApplicationModel/PostgresDatabaseResource.connectionStringExpression',
                { context: this._handle }
            );
        },
    };

    /** Gets the DatabaseName property */
    databaseName = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/PostgresDatabaseResource.databaseName',
                { context: this._handle }
            );
        },
    };

    /** Gets the UriExpression property */
    uriExpression = {
        get: async (): Promise<ReferenceExpression> => {
            return await this._client.invokeCapability<ReferenceExpression>(
                'Aspire.Hosting.ApplicationModel/PostgresDatabaseResource.uriExpression',
                { context: this._handle }
            );
        },
    };

    /** Gets the JdbcConnectionString property */
    jdbcConnectionString = {
        get: async (): Promise<ReferenceExpression> => {
            return await this._client.invokeCapability<ReferenceExpression>(
                'Aspire.Hosting.ApplicationModel/PostgresDatabaseResource.jdbcConnectionString',
                { context: this._handle }
            );
        },
    };

    /** Gets the Name property */
    name = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/PostgresDatabaseResource.name',
                { context: this._handle }
            );
        },
    };

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): PostgresDatabaseResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new PostgresDatabaseResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): PostgresDatabaseResourcePromise {
        const helpLink = options?.helpLink;
        return new PostgresDatabaseResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withConnectionPropertyInternal(name: string, value: ReferenceExpression): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withConnectionProperty',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Adds a connection property with a reference expression */
    withConnectionProperty(name: string, value: ReferenceExpression): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._withConnectionPropertyInternal(name, value));
    }

    /** @internal */
    private async _withConnectionPropertyValueInternal(name: string, value: string): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withConnectionPropertyValue',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Adds a connection property with a string value */
    withConnectionPropertyValue(name: string, value: string): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._withConnectionPropertyValueInternal(name, value));
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<PostgresDatabaseResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<PostgresDatabaseResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): PostgresDatabaseResourcePromise {
        const displayText = options?.displayText;
        return new PostgresDatabaseResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): PostgresDatabaseResourcePromise {
        const displayText = options?.displayText;
        return new PostgresDatabaseResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<PostgresDatabaseResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<PostgresDatabaseResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): PostgresDatabaseResourcePromise {
        const commandOptions = options?.commandOptions;
        return new PostgresDatabaseResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): PostgresDatabaseResourcePromise {
        const iconVariant = options?.iconVariant;
        return new PostgresDatabaseResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<PostgresDatabaseResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): PostgresDatabaseResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new PostgresDatabaseResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<PostgresDatabaseResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<PostgresDatabaseResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<PostgresDatabaseResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<PostgresDatabaseResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onConnectionStringAvailableInternal(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): Promise<PostgresDatabaseResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ConnectionStringAvailableEventHandle;
            const arg = new ConnectionStringAvailableEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/onConnectionStringAvailable',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Subscribes to the ConnectionStringAvailable event */
    onConnectionStringAvailable(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._onConnectionStringAvailableInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<PostgresDatabaseResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<PostgresDatabaseResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withPostgresMcpInternal(configureContainer?: (obj: PostgresMcpContainerResource) => Promise<void>, containerName?: string): Promise<PostgresDatabaseResource> {
        const configureContainerId = configureContainer ? registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PostgresMcpContainerResourceHandle;
            const obj = new PostgresMcpContainerResource(objHandle, this._client);
            await configureContainer(obj);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (configureContainer !== undefined) rpcArgs.configureContainer = configureContainerId;
        if (containerName !== undefined) rpcArgs.containerName = containerName;
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting.PostgreSQL/withPostgresMcp',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Adds Postgres MCP server */
    withPostgresMcp(options?: WithPostgresMcpOptions): PostgresDatabaseResourcePromise {
        const configureContainer = options?.configureContainer;
        const containerName = options?.containerName;
        return new PostgresDatabaseResourcePromise(this._withPostgresMcpInternal(configureContainer, containerName));
    }

    /** @internal */
    private async _withCreationScriptInternal(script: string): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, script };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting.PostgreSQL/withCreationScript',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Defines the SQL script for database creation */
    withCreationScript(script: string): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._withCreationScriptInternal(script));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for PostgresDatabaseResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class PostgresDatabaseResourcePromise implements PromiseLike<PostgresDatabaseResource> {
    constructor(private _promise: Promise<PostgresDatabaseResource>) {}

    then<TResult1 = PostgresDatabaseResource, TResult2 = never>(
        onfulfilled?: ((value: PostgresDatabaseResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Adds a connection property with a reference expression */
    withConnectionProperty(name: string, value: ReferenceExpression): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withConnectionProperty(name, value)));
    }

    /** Adds a connection property with a string value */
    withConnectionPropertyValue(name: string, value: string): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withConnectionPropertyValue(name, value)));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the ConnectionStringAvailable event */
    onConnectionStringAvailable(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.onConnectionStringAvailable(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Adds Postgres MCP server */
    withPostgresMcp(options?: WithPostgresMcpOptions): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withPostgresMcp(options)));
    }

    /** Defines the SQL script for database creation */
    withCreationScript(script: string): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withCreationScript(script)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// PostgresMcpContainerResource
// ============================================================================

export class PostgresMcpContainerResource extends ResourceBuilderBase<PostgresMcpContainerResourceHandle> {
    constructor(handle: PostgresMcpContainerResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withBindMountInternal(source: string, target: string, isReadOnly?: boolean): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, target };
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withBindMount',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): PostgresMcpContainerResourcePromise {
        const isReadOnly = options?.isReadOnly;
        return new PostgresMcpContainerResourcePromise(this._withBindMountInternal(source, target, isReadOnly));
    }

    /** @internal */
    private async _withEntrypointInternal(entrypoint: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withEntrypoint',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withEntrypointInternal(entrypoint));
    }

    /** @internal */
    private async _withImageTagInternal(tag: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, tag };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withImageTag',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets the container image tag */
    withImageTag(tag: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withImageTagInternal(tag));
    }

    /** @internal */
    private async _withImageRegistryInternal(registry: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withImageRegistry',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withImageRegistryInternal(registry));
    }

    /** @internal */
    private async _withImageInternal(image: string, tag?: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, image };
        if (tag !== undefined) rpcArgs.tag = tag;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withImage',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): PostgresMcpContainerResourcePromise {
        const tag = options?.tag;
        return new PostgresMcpContainerResourcePromise(this._withImageInternal(image, tag));
    }

    /** @internal */
    private async _withImageSHA256Internal(sha256: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withImageSHA256',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withImageSHA256Internal(sha256));
    }

    /** @internal */
    private async _withContainerRuntimeArgsInternal(args: string[]): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withContainerRuntimeArgs',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withContainerRuntimeArgsInternal(args));
    }

    /** @internal */
    private async _withLifetimeInternal(lifetime: ContainerLifetime): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withLifetime',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withLifetimeInternal(lifetime));
    }

    /** @internal */
    private async _withImagePullPolicyInternal(pullPolicy: ImagePullPolicy): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withImagePullPolicy',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withImagePullPolicyInternal(pullPolicy));
    }

    /** @internal */
    private async _publishAsContainerInternal(): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/publishAsContainer',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._publishAsContainerInternal());
    }

    /** @internal */
    private async _withDockerfileInternal(contextPath: string, dockerfilePath?: string, stage?: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined) rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined) rpcArgs.stage = stage;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withDockerfile',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): PostgresMcpContainerResourcePromise {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new PostgresMcpContainerResourcePromise(this._withDockerfileInternal(contextPath, dockerfilePath, stage));
    }

    /** @internal */
    private async _withContainerNameInternal(name: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withContainerName',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets the container name */
    withContainerName(name: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withContainerNameInternal(name));
    }

    /** @internal */
    private async _withBuildArgInternal(name: string, value: ParameterResource): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withBuildArg',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withBuildArgInternal(name, value));
    }

    /** @internal */
    private async _withBuildSecretInternal(name: string, value: ParameterResource): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withBuildSecret',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withBuildSecretInternal(name, value));
    }

    /** @internal */
    private async _withEndpointProxySupportInternal(proxyEnabled: boolean): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withEndpointProxySupport',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withEndpointProxySupportInternal(proxyEnabled));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): PostgresMcpContainerResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new PostgresMcpContainerResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withContainerNetworkAliasInternal(alias: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, alias };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withContainerNetworkAlias',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withContainerNetworkAliasInternal(alias));
    }

    /** @internal */
    private async _withMcpServerInternal(path?: string, endpointName?: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withMcpServer',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): PostgresMcpContainerResourcePromise {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new PostgresMcpContainerResourcePromise(this._withMcpServerInternal(path, endpointName));
    }

    /** @internal */
    private async _withOtlpExporterInternal(): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withOtlpExporter',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withOtlpExporterInternal());
    }

    /** @internal */
    private async _withOtlpExporterProtocolInternal(protocol: OtlpProtocol): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, protocol };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withOtlpExporterProtocol',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withOtlpExporterProtocolInternal(protocol));
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/publishAsConnectionString',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): PostgresMcpContainerResourcePromise {
        const helpLink = options?.helpLink;
        return new PostgresMcpContainerResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withEnvironmentInternal(name: string, value: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withEnvironment',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withEnvironmentInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentExpressionInternal(name: string, value: ReferenceExpression): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentExpression',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withEnvironmentExpressionInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentCallbackInternal(callback: (obj: EnvironmentCallbackContext) => Promise<void>): Promise<PostgresMcpContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as EnvironmentCallbackContextHandle;
            const obj = new EnvironmentCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallback',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withEnvironmentCallbackInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentCallbackAsyncInternal(callback: (arg: EnvironmentCallbackContext) => Promise<void>): Promise<PostgresMcpContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EnvironmentCallbackContextHandle;
            const arg = new EnvironmentCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallbackAsync',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withEnvironmentCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentEndpointInternal(name: string, endpointReference: EndpointReference): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, endpointReference };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentEndpoint',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withEnvironmentEndpointInternal(name, endpointReference));
    }

    /** @internal */
    private async _withEnvironmentParameterInternal(name: string, parameter: ParameterResource): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, parameter };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentParameter',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withEnvironmentParameterInternal(name, parameter));
    }

    /** @internal */
    private async _withEnvironmentConnectionStringInternal(envVarName: string, resource: ResourceBuilderBase): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, envVarName, resource };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withEnvironmentConnectionString',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withEnvironmentConnectionStringInternal(envVarName, resource));
    }

    /** @internal */
    private async _withArgsInternal(args: string[]): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withArgs',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds arguments */
    withArgs(args: string[]): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withArgsInternal(args));
    }

    /** @internal */
    private async _withArgsCallbackInternal(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): Promise<PostgresMcpContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as CommandLineArgsCallbackContextHandle;
            const obj = new CommandLineArgsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withArgsCallback',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withArgsCallbackInternal(callback));
    }

    /** @internal */
    private async _withArgsCallbackAsyncInternal(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): Promise<PostgresMcpContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as CommandLineArgsCallbackContextHandle;
            const arg = new CommandLineArgsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withArgsCallbackAsync',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withArgsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withReferenceInternal(source: ResourceBuilderBase, connectionName?: string, optional?: boolean): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (connectionName !== undefined) rpcArgs.connectionName = connectionName;
        if (optional !== undefined) rpcArgs.optional = optional;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withReference',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): PostgresMcpContainerResourcePromise {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        return new PostgresMcpContainerResourcePromise(this._withReferenceInternal(source, connectionName, optional));
    }

    /** @internal */
    private async _withServiceReferenceInternal(source: ResourceBuilderBase): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withServiceReference',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withServiceReferenceInternal(source));
    }

    /** @internal */
    private async _withServiceReferenceNamedInternal(source: ResourceBuilderBase, name: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, name };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withServiceReferenceNamed',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withServiceReferenceNamedInternal(source, name));
    }

    /** @internal */
    private async _withReferenceUriInternal(name: string, uri: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, uri };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withReferenceUri',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withReferenceUriInternal(name, uri));
    }

    /** @internal */
    private async _withReferenceExternalServiceInternal(externalService: ExternalServiceResource): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, externalService };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withReferenceExternalService',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withReferenceExternalServiceInternal(externalService));
    }

    /** @internal */
    private async _withReferenceEndpointInternal(endpointReference: EndpointReference): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointReference };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withReferenceEndpoint',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withReferenceEndpointInternal(endpointReference));
    }

    /** @internal */
    private async _withEndpointInternal(port?: number, targetPort?: number, scheme?: string, name?: string, env?: string, isProxied?: boolean, isExternal?: boolean, protocol?: ProtocolType): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (scheme !== undefined) rpcArgs.scheme = scheme;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined) rpcArgs.isExternal = isExternal;
        if (protocol !== undefined) rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withEndpoint',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): PostgresMcpContainerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new PostgresMcpContainerResourcePromise(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol));
    }

    /** @internal */
    private async _withHttpEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withHttpEndpoint',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): PostgresMcpContainerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new PostgresMcpContainerResourcePromise(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withHttpsEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withHttpsEndpoint',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): PostgresMcpContainerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new PostgresMcpContainerResourcePromise(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withExternalHttpEndpointsInternal(): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withExternalHttpEndpoints',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withExternalHttpEndpointsInternal());
    }

    /** Gets an endpoint reference */
    async getEndpoint(name: string): Promise<EndpointReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<EndpointReference>(
            'Aspire.Hosting/getEndpoint',
            rpcArgs
        );
    }

    /** @internal */
    private async _asHttp2ServiceInternal(): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/asHttp2Service',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._asHttp2ServiceInternal());
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<PostgresMcpContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<PostgresMcpContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): PostgresMcpContainerResourcePromise {
        const displayText = options?.displayText;
        return new PostgresMcpContainerResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): PostgresMcpContainerResourcePromise {
        const displayText = options?.displayText;
        return new PostgresMcpContainerResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<PostgresMcpContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _withUrlForEndpointFactoryInternal(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): Promise<PostgresMcpContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EndpointReferenceHandle;
            const arg = new EndpointReference(argHandle, this._client);
            return await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withUrlForEndpointFactory',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withUrlForEndpointFactoryInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _waitForInternal(dependency: ResourceBuilderBase): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/waitFor',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._waitForInternal(dependency));
    }

    /** @internal */
    private async _waitForWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/waitForWithBehavior',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._waitForWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _waitForStartInternal(dependency: ResourceBuilderBase): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/waitForStart',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._waitForStartInternal(dependency));
    }

    /** @internal */
    private async _waitForStartWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/waitForStartWithBehavior',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._waitForStartWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _waitForCompletionInternal(dependency: ResourceBuilderBase, exitCode?: number): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        if (exitCode !== undefined) rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/waitForCompletion',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): PostgresMcpContainerResourcePromise {
        const exitCode = options?.exitCode;
        return new PostgresMcpContainerResourcePromise(this._waitForCompletionInternal(dependency, exitCode));
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withHttpHealthCheckInternal(path?: string, statusCode?: number, endpointName?: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (statusCode !== undefined) rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withHttpHealthCheck',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): PostgresMcpContainerResourcePromise {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new PostgresMcpContainerResourcePromise(this._withHttpHealthCheckInternal(path, statusCode, endpointName));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<PostgresMcpContainerResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): PostgresMcpContainerResourcePromise {
        const commandOptions = options?.commandOptions;
        return new PostgresMcpContainerResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withDeveloperCertificateTrustInternal(trust: boolean): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, trust };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withDeveloperCertificateTrust',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withDeveloperCertificateTrustInternal(trust));
    }

    /** @internal */
    private async _withCertificateTrustScopeInternal(scope: CertificateTrustScope): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, scope };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withCertificateTrustScope',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withCertificateTrustScopeInternal(scope));
    }

    /** @internal */
    private async _withHttpsDeveloperCertificateInternal(password?: ParameterResource): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (password !== undefined) rpcArgs.password = password;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withHttpsDeveloperCertificate',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): PostgresMcpContainerResourcePromise {
        const password = options?.password;
        return new PostgresMcpContainerResourcePromise(this._withHttpsDeveloperCertificateInternal(password));
    }

    /** @internal */
    private async _withoutHttpsCertificateInternal(): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withoutHttpsCertificate',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withoutHttpsCertificateInternal());
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): PostgresMcpContainerResourcePromise {
        const iconVariant = options?.iconVariant;
        return new PostgresMcpContainerResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _withHttpProbeInternal(probeType: ProbeType, path?: string, initialDelaySeconds?: number, periodSeconds?: number, timeoutSeconds?: number, failureThreshold?: number, successThreshold?: number, endpointName?: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, probeType };
        if (path !== undefined) rpcArgs.path = path;
        if (initialDelaySeconds !== undefined) rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined) rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined) rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined) rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined) rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withHttpProbe',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): PostgresMcpContainerResourcePromise {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new PostgresMcpContainerResourcePromise(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withRemoteImageNameInternal(remoteImageName: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withRemoteImageName',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withRemoteImageNameInternal(remoteImageName));
    }

    /** @internal */
    private async _withRemoteImageTagInternal(remoteImageTag: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withRemoteImageTag',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withRemoteImageTagInternal(remoteImageTag));
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<PostgresMcpContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): PostgresMcpContainerResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new PostgresMcpContainerResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<PostgresMcpContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<PostgresMcpContainerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** @internal */
    private async _withVolumeInternal(target: string, name?: string, isReadOnly?: boolean): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, target };
        if (name !== undefined) rpcArgs.name = name;
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/withVolume',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): PostgresMcpContainerResourcePromise {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new PostgresMcpContainerResourcePromise(this._withVolumeInternal(target, name, isReadOnly));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<PostgresMcpContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<PostgresMcpContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<PostgresMcpContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceEndpointsAllocatedInternal(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): Promise<PostgresMcpContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceEndpointsAllocatedEventHandle;
            const arg = new ResourceEndpointsAllocatedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/onResourceEndpointsAllocated',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._onResourceEndpointsAllocatedInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<PostgresMcpContainerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _publishAsAzureContainerAppInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): Promise<PostgresMcpContainerResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { container: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishContainerAsAzureContainerApp',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._publishAsAzureContainerAppInternal(configure));
    }

    /** @internal */
    private async _publishAsConfiguredAzureContainerAppJobInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<PostgresMcpContainerResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { resource: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredAzureContainerAppJob',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._publishAsConfiguredAzureContainerAppJobInternal(configure));
    }

    /** @internal */
    private async _publishAsAzureContainerAppJobInternal(): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsAzureContainerAppJob',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._publishAsAzureContainerAppJobInternal());
    }

    /** @internal */
    private async _publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression: string, configure?: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<PostgresMcpContainerResource> {
        const configureId = configure ? registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        if (configure !== undefined) rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): PostgresMcpContainerResourcePromise {
        const configure = options?.configure;
        return new PostgresMcpContainerResourcePromise(this._publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression, configure));
    }

    /** @internal */
    private async _publishAsScheduledAzureContainerAppJobInternal(cronExpression: string): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._publishAsScheduledAzureContainerAppJobInternal(cronExpression));
    }

    /** @internal */
    private async _withEnvironmentFromOutputInternal(name: string, bicepOutputReference: BicepOutputReference): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, bicepOutputReference };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromOutput',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withEnvironmentFromOutputInternal(name, bicepOutputReference));
    }

    /** @internal */
    private async _withEnvironmentFromKeyVaultSecretInternal(name: string, secretReference: ResourceBuilderBase): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, secretReference };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromKeyVaultSecret',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withEnvironmentFromKeyVaultSecretInternal(name, secretReference));
    }

    /** @internal */
    private async _withAzureUserAssignedIdentityInternal(identityResourceBuilder: AzureUserAssignedIdentityResource): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, identityResourceBuilder };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting.Azure/withUserAssignedIdentityAzureUserAssignedIdentity',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withAzureUserAssignedIdentityInternal(identityResourceBuilder));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<PostgresMcpContainerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<PostgresMcpContainerResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new PostgresMcpContainerResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for PostgresMcpContainerResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class PostgresMcpContainerResourcePromise implements PromiseLike<PostgresMcpContainerResource> {
    constructor(private _promise: Promise<PostgresMcpContainerResource>) {}

    then<TResult1 = PostgresMcpContainerResource, TResult2 = never>(
        onfulfilled?: ((value: PostgresMcpContainerResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withBindMount(source, target, options)));
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withEntrypoint(entrypoint)));
    }

    /** Sets the container image tag */
    withImageTag(tag: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withImageTag(tag)));
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withImageRegistry(registry)));
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withImage(image, options)));
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withImageSHA256(sha256)));
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withContainerRuntimeArgs(args)));
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withLifetime(lifetime)));
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)));
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.publishAsContainer()));
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withDockerfile(contextPath, options)));
    }

    /** Sets the container name */
    withContainerName(name: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withContainerName(name)));
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withBuildArg(name, value)));
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withBuildSecret(name, value)));
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withContainerNetworkAlias(alias)));
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withMcpServer(options)));
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withOtlpExporter()));
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withOtlpExporterProtocol(protocol)));
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withEnvironment(name, value)));
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentExpression(name, value)));
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentCallback(callback)));
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentCallbackAsync(callback)));
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentEndpoint(name, endpointReference)));
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentParameter(name, parameter)));
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentConnectionString(envVarName, resource)));
    }

    /** Adds arguments */
    withArgs(args: string[]): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withArgs(args)));
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withArgsCallback(callback)));
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withArgsCallbackAsync(callback)));
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withReference(source, options)));
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withServiceReference(source)));
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withServiceReferenceNamed(source, name)));
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withReferenceUri(name, uri)));
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withReferenceExternalService(externalService)));
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withReferenceEndpoint(endpointReference)));
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withEndpoint(options)));
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withHttpEndpoint(options)));
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withHttpsEndpoint(options)));
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withExternalHttpEndpoints()));
    }

    /** Gets an endpoint reference */
    getEndpoint(name: string): Promise<EndpointReference> {
        return this._promise.then(obj => obj.getEndpoint(name));
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.asHttp2Service()));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withUrlForEndpointFactory(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.waitFor(dependency)));
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.waitForWithBehavior(dependency, waitBehavior)));
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.waitForStart(dependency)));
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.waitForStartWithBehavior(dependency, waitBehavior)));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.waitForCompletion(dependency, options)));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withHttpHealthCheck(options)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)));
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withCertificateTrustScope(scope)));
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)));
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withoutHttpsCertificate()));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withHttpProbe(probeType, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)));
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withVolume(target, options)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerApp(configure)));
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.publishAsConfiguredAzureContainerAppJob(configure)));
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerAppJob()));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.publishAsConfiguredScheduledAzureContainerAppJob(cronExpression, options)));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.publishAsScheduledAzureContainerAppJob(cronExpression)));
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentFromOutput(name, bicepOutputReference)));
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withEnvironmentFromKeyVaultSecret(name, secretReference)));
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withAzureUserAssignedIdentity(identityResourceBuilder)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): PostgresMcpContainerResourcePromise {
        return new PostgresMcpContainerResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// PostgresServerResource
// ============================================================================

export class PostgresServerResource extends ResourceBuilderBase<PostgresServerResourceHandle> {
    constructor(handle: PostgresServerResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** Gets the PrimaryEndpoint property */
    primaryEndpoint = {
        get: async (): Promise<EndpointReference> => {
            const handle = await this._client.invokeCapability<EndpointReferenceHandle>(
                'Aspire.Hosting.ApplicationModel/PostgresServerResource.primaryEndpoint',
                { context: this._handle }
            );
            return new EndpointReference(handle, this._client);
        },
    };

    /** Gets the UserNameParameter property */
    userNameParameter = {
        get: async (): Promise<ParameterResource> => {
            const handle = await this._client.invokeCapability<ParameterResourceHandle>(
                'Aspire.Hosting.ApplicationModel/PostgresServerResource.userNameParameter',
                { context: this._handle }
            );
            return new ParameterResource(handle, this._client);
        },
    };

    /** Gets the UserNameReference property */
    userNameReference = {
        get: async (): Promise<ReferenceExpression> => {
            return await this._client.invokeCapability<ReferenceExpression>(
                'Aspire.Hosting.ApplicationModel/PostgresServerResource.userNameReference',
                { context: this._handle }
            );
        },
    };

    /** Gets the PasswordParameter property */
    passwordParameter = {
        get: async (): Promise<ParameterResource> => {
            const handle = await this._client.invokeCapability<ParameterResourceHandle>(
                'Aspire.Hosting.ApplicationModel/PostgresServerResource.passwordParameter',
                { context: this._handle }
            );
            return new ParameterResource(handle, this._client);
        },
    };

    /** Gets the ConnectionStringExpression property */
    connectionStringExpression = {
        get: async (): Promise<ReferenceExpression> => {
            return await this._client.invokeCapability<ReferenceExpression>(
                'Aspire.Hosting.ApplicationModel/PostgresServerResource.connectionStringExpression',
                { context: this._handle }
            );
        },
    };

    /** Gets the Databases property */
    private _databases?: AspireDict<string, string>;
    get databases(): AspireDict<string, string> {
        if (!this._databases) {
            this._databases = new AspireDict<string, string>(
                this._handle,
                this._client,
                'Aspire.Hosting.ApplicationModel/PostgresServerResource.databases',
                'Aspire.Hosting.ApplicationModel/PostgresServerResource.databases'
            );
        }
        return this._databases;
    }

    /** Gets the Host property */
    host = {
        get: async (): Promise<EndpointReferenceExpression> => {
            const handle = await this._client.invokeCapability<EndpointReferenceExpressionHandle>(
                'Aspire.Hosting.ApplicationModel/PostgresServerResource.host',
                { context: this._handle }
            );
            return new EndpointReferenceExpression(handle, this._client);
        },
    };

    /** Gets the Port property */
    port = {
        get: async (): Promise<EndpointReferenceExpression> => {
            const handle = await this._client.invokeCapability<EndpointReferenceExpressionHandle>(
                'Aspire.Hosting.ApplicationModel/PostgresServerResource.port',
                { context: this._handle }
            );
            return new EndpointReferenceExpression(handle, this._client);
        },
    };

    /** Gets the UriExpression property */
    uriExpression = {
        get: async (): Promise<ReferenceExpression> => {
            return await this._client.invokeCapability<ReferenceExpression>(
                'Aspire.Hosting.ApplicationModel/PostgresServerResource.uriExpression',
                { context: this._handle }
            );
        },
    };

    /** Gets the JdbcConnectionString property */
    jdbcConnectionString = {
        get: async (): Promise<ReferenceExpression> => {
            return await this._client.invokeCapability<ReferenceExpression>(
                'Aspire.Hosting.ApplicationModel/PostgresServerResource.jdbcConnectionString',
                { context: this._handle }
            );
        },
    };

    /** Gets the Entrypoint property */
    entrypoint = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/PostgresServerResource.entrypoint',
                { context: this._handle }
            );
        },
        set: async (value: string): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.ApplicationModel/PostgresServerResource.setEntrypoint',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the ShellExecution property */
    shellExecution = {
        get: async (): Promise<boolean> => {
            return await this._client.invokeCapability<boolean>(
                'Aspire.Hosting.ApplicationModel/PostgresServerResource.shellExecution',
                { context: this._handle }
            );
        },
        set: async (value: boolean): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.ApplicationModel/PostgresServerResource.setShellExecution',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the Name property */
    name = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/PostgresServerResource.name',
                { context: this._handle }
            );
        },
    };

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withBindMountInternal(source: string, target: string, isReadOnly?: boolean): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, target };
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withBindMount',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): PostgresServerResourcePromise {
        const isReadOnly = options?.isReadOnly;
        return new PostgresServerResourcePromise(this._withBindMountInternal(source, target, isReadOnly));
    }

    /** @internal */
    private async _withEntrypointInternal(entrypoint: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withEntrypoint',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withEntrypointInternal(entrypoint));
    }

    /** @internal */
    private async _withImageTagInternal(tag: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, tag };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withImageTag',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets the container image tag */
    withImageTag(tag: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withImageTagInternal(tag));
    }

    /** @internal */
    private async _withImageRegistryInternal(registry: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withImageRegistry',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withImageRegistryInternal(registry));
    }

    /** @internal */
    private async _withImageInternal(image: string, tag?: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, image };
        if (tag !== undefined) rpcArgs.tag = tag;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withImage',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): PostgresServerResourcePromise {
        const tag = options?.tag;
        return new PostgresServerResourcePromise(this._withImageInternal(image, tag));
    }

    /** @internal */
    private async _withImageSHA256Internal(sha256: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withImageSHA256',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withImageSHA256Internal(sha256));
    }

    /** @internal */
    private async _withContainerRuntimeArgsInternal(args: string[]): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withContainerRuntimeArgs',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withContainerRuntimeArgsInternal(args));
    }

    /** @internal */
    private async _withLifetimeInternal(lifetime: ContainerLifetime): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withLifetime',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withLifetimeInternal(lifetime));
    }

    /** @internal */
    private async _withImagePullPolicyInternal(pullPolicy: ImagePullPolicy): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withImagePullPolicy',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withImagePullPolicyInternal(pullPolicy));
    }

    /** @internal */
    private async _publishAsContainerInternal(): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/publishAsContainer',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._publishAsContainerInternal());
    }

    /** @internal */
    private async _withDockerfileInternal(contextPath: string, dockerfilePath?: string, stage?: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined) rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined) rpcArgs.stage = stage;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withDockerfile',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): PostgresServerResourcePromise {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new PostgresServerResourcePromise(this._withDockerfileInternal(contextPath, dockerfilePath, stage));
    }

    /** @internal */
    private async _withContainerNameInternal(name: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withContainerName',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets the container name */
    withContainerName(name: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withContainerNameInternal(name));
    }

    /** @internal */
    private async _withBuildArgInternal(name: string, value: ParameterResource): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withBuildArg',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withBuildArgInternal(name, value));
    }

    /** @internal */
    private async _withBuildSecretInternal(name: string, value: ParameterResource): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withBuildSecret',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withBuildSecretInternal(name, value));
    }

    /** @internal */
    private async _withEndpointProxySupportInternal(proxyEnabled: boolean): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withEndpointProxySupport',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withEndpointProxySupportInternal(proxyEnabled));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): PostgresServerResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new PostgresServerResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withContainerNetworkAliasInternal(alias: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, alias };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withContainerNetworkAlias',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withContainerNetworkAliasInternal(alias));
    }

    /** @internal */
    private async _withMcpServerInternal(path?: string, endpointName?: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withMcpServer',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): PostgresServerResourcePromise {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new PostgresServerResourcePromise(this._withMcpServerInternal(path, endpointName));
    }

    /** @internal */
    private async _withOtlpExporterInternal(): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withOtlpExporter',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withOtlpExporterInternal());
    }

    /** @internal */
    private async _withOtlpExporterProtocolInternal(protocol: OtlpProtocol): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, protocol };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withOtlpExporterProtocol',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withOtlpExporterProtocolInternal(protocol));
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/publishAsConnectionString',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): PostgresServerResourcePromise {
        const helpLink = options?.helpLink;
        return new PostgresServerResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withEnvironmentInternal(name: string, value: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withEnvironment',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withEnvironmentInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentExpressionInternal(name: string, value: ReferenceExpression): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withEnvironmentExpression',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withEnvironmentExpressionInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentCallbackInternal(callback: (obj: EnvironmentCallbackContext) => Promise<void>): Promise<PostgresServerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as EnvironmentCallbackContextHandle;
            const obj = new EnvironmentCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallback',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withEnvironmentCallbackInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentCallbackAsyncInternal(callback: (arg: EnvironmentCallbackContext) => Promise<void>): Promise<PostgresServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EnvironmentCallbackContextHandle;
            const arg = new EnvironmentCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallbackAsync',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withEnvironmentCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentEndpointInternal(name: string, endpointReference: EndpointReference): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, endpointReference };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withEnvironmentEndpoint',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withEnvironmentEndpointInternal(name, endpointReference));
    }

    /** @internal */
    private async _withEnvironmentParameterInternal(name: string, parameter: ParameterResource): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, parameter };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withEnvironmentParameter',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withEnvironmentParameterInternal(name, parameter));
    }

    /** @internal */
    private async _withEnvironmentConnectionStringInternal(envVarName: string, resource: ResourceBuilderBase): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, envVarName, resource };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withEnvironmentConnectionString',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withEnvironmentConnectionStringInternal(envVarName, resource));
    }

    /** @internal */
    private async _withConnectionPropertyInternal(name: string, value: ReferenceExpression): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withConnectionProperty',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a connection property with a reference expression */
    withConnectionProperty(name: string, value: ReferenceExpression): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withConnectionPropertyInternal(name, value));
    }

    /** @internal */
    private async _withConnectionPropertyValueInternal(name: string, value: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withConnectionPropertyValue',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a connection property with a string value */
    withConnectionPropertyValue(name: string, value: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withConnectionPropertyValueInternal(name, value));
    }

    /** @internal */
    private async _withArgsInternal(args: string[]): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withArgs',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds arguments */
    withArgs(args: string[]): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withArgsInternal(args));
    }

    /** @internal */
    private async _withArgsCallbackInternal(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): Promise<PostgresServerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as CommandLineArgsCallbackContextHandle;
            const obj = new CommandLineArgsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withArgsCallback',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withArgsCallbackInternal(callback));
    }

    /** @internal */
    private async _withArgsCallbackAsyncInternal(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): Promise<PostgresServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as CommandLineArgsCallbackContextHandle;
            const arg = new CommandLineArgsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withArgsCallbackAsync',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withArgsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withReferenceInternal(source: ResourceBuilderBase, connectionName?: string, optional?: boolean): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (connectionName !== undefined) rpcArgs.connectionName = connectionName;
        if (optional !== undefined) rpcArgs.optional = optional;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withReference',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): PostgresServerResourcePromise {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        return new PostgresServerResourcePromise(this._withReferenceInternal(source, connectionName, optional));
    }

    /** @internal */
    private async _withServiceReferenceInternal(source: ResourceBuilderBase): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withServiceReference',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withServiceReferenceInternal(source));
    }

    /** @internal */
    private async _withServiceReferenceNamedInternal(source: ResourceBuilderBase, name: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, name };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withServiceReferenceNamed',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withServiceReferenceNamedInternal(source, name));
    }

    /** @internal */
    private async _withReferenceUriInternal(name: string, uri: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, uri };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withReferenceUri',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withReferenceUriInternal(name, uri));
    }

    /** @internal */
    private async _withReferenceExternalServiceInternal(externalService: ExternalServiceResource): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, externalService };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withReferenceExternalService',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withReferenceExternalServiceInternal(externalService));
    }

    /** @internal */
    private async _withReferenceEndpointInternal(endpointReference: EndpointReference): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointReference };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withReferenceEndpoint',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withReferenceEndpointInternal(endpointReference));
    }

    /** @internal */
    private async _withEndpointInternal(port?: number, targetPort?: number, scheme?: string, name?: string, env?: string, isProxied?: boolean, isExternal?: boolean, protocol?: ProtocolType): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (scheme !== undefined) rpcArgs.scheme = scheme;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined) rpcArgs.isExternal = isExternal;
        if (protocol !== undefined) rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withEndpoint',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): PostgresServerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new PostgresServerResourcePromise(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol));
    }

    /** @internal */
    private async _withHttpEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withHttpEndpoint',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): PostgresServerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new PostgresServerResourcePromise(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withHttpsEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withHttpsEndpoint',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): PostgresServerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new PostgresServerResourcePromise(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withExternalHttpEndpointsInternal(): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withExternalHttpEndpoints',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withExternalHttpEndpointsInternal());
    }

    /** Gets an endpoint reference */
    async getEndpoint(name: string): Promise<EndpointReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<EndpointReference>(
            'Aspire.Hosting/getEndpoint',
            rpcArgs
        );
    }

    /** @internal */
    private async _asHttp2ServiceInternal(): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/asHttp2Service',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._asHttp2ServiceInternal());
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<PostgresServerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<PostgresServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): PostgresServerResourcePromise {
        const displayText = options?.displayText;
        return new PostgresServerResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): PostgresServerResourcePromise {
        const displayText = options?.displayText;
        return new PostgresServerResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<PostgresServerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _withUrlForEndpointFactoryInternal(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): Promise<PostgresServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EndpointReferenceHandle;
            const arg = new EndpointReference(argHandle, this._client);
            return await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withUrlForEndpointFactory',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withUrlForEndpointFactoryInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _waitForInternal(dependency: ResourceBuilderBase): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/waitFor',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._waitForInternal(dependency));
    }

    /** @internal */
    private async _waitForWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/waitForWithBehavior',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._waitForWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _waitForStartInternal(dependency: ResourceBuilderBase): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/waitForStart',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._waitForStartInternal(dependency));
    }

    /** @internal */
    private async _waitForStartWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/waitForStartWithBehavior',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._waitForStartWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _waitForCompletionInternal(dependency: ResourceBuilderBase, exitCode?: number): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        if (exitCode !== undefined) rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/waitForCompletion',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): PostgresServerResourcePromise {
        const exitCode = options?.exitCode;
        return new PostgresServerResourcePromise(this._waitForCompletionInternal(dependency, exitCode));
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withHttpHealthCheckInternal(path?: string, statusCode?: number, endpointName?: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (statusCode !== undefined) rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withHttpHealthCheck',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): PostgresServerResourcePromise {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new PostgresServerResourcePromise(this._withHttpHealthCheckInternal(path, statusCode, endpointName));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<PostgresServerResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): PostgresServerResourcePromise {
        const commandOptions = options?.commandOptions;
        return new PostgresServerResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withDeveloperCertificateTrustInternal(trust: boolean): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, trust };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withDeveloperCertificateTrust',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withDeveloperCertificateTrustInternal(trust));
    }

    /** @internal */
    private async _withCertificateTrustScopeInternal(scope: CertificateTrustScope): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, scope };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withCertificateTrustScope',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withCertificateTrustScopeInternal(scope));
    }

    /** @internal */
    private async _withHttpsDeveloperCertificateInternal(password?: ParameterResource): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (password !== undefined) rpcArgs.password = password;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withHttpsDeveloperCertificate',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): PostgresServerResourcePromise {
        const password = options?.password;
        return new PostgresServerResourcePromise(this._withHttpsDeveloperCertificateInternal(password));
    }

    /** @internal */
    private async _withoutHttpsCertificateInternal(): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withoutHttpsCertificate',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withoutHttpsCertificateInternal());
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): PostgresServerResourcePromise {
        const iconVariant = options?.iconVariant;
        return new PostgresServerResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _withHttpProbeInternal(probeType: ProbeType, path?: string, initialDelaySeconds?: number, periodSeconds?: number, timeoutSeconds?: number, failureThreshold?: number, successThreshold?: number, endpointName?: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, probeType };
        if (path !== undefined) rpcArgs.path = path;
        if (initialDelaySeconds !== undefined) rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined) rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined) rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined) rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined) rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withHttpProbe',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): PostgresServerResourcePromise {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new PostgresServerResourcePromise(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withRemoteImageNameInternal(remoteImageName: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withRemoteImageName',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withRemoteImageNameInternal(remoteImageName));
    }

    /** @internal */
    private async _withRemoteImageTagInternal(remoteImageTag: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withRemoteImageTag',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withRemoteImageTagInternal(remoteImageTag));
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<PostgresServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): PostgresServerResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new PostgresServerResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<PostgresServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<PostgresServerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** @internal */
    private async _withVolumeInternal(target: string, name?: string, isReadOnly?: boolean): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, target };
        if (name !== undefined) rpcArgs.name = name;
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/withVolume',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): PostgresServerResourcePromise {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new PostgresServerResourcePromise(this._withVolumeInternal(target, name, isReadOnly));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<PostgresServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<PostgresServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onConnectionStringAvailableInternal(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): Promise<PostgresServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ConnectionStringAvailableEventHandle;
            const arg = new ConnectionStringAvailableEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/onConnectionStringAvailable',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Subscribes to the ConnectionStringAvailable event */
    onConnectionStringAvailable(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._onConnectionStringAvailableInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<PostgresServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceEndpointsAllocatedInternal(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): Promise<PostgresServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceEndpointsAllocatedEventHandle;
            const arg = new ResourceEndpointsAllocatedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/onResourceEndpointsAllocated',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._onResourceEndpointsAllocatedInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<PostgresServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _addDatabaseInternal(name: string, databaseName?: string): Promise<PostgresDatabaseResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        if (databaseName !== undefined) rpcArgs.databaseName = databaseName;
        const result = await this._client.invokeCapability<PostgresDatabaseResourceHandle>(
            'Aspire.Hosting.PostgreSQL/addDatabase',
            rpcArgs
        );
        return new PostgresDatabaseResource(result, this._client);
    }

    /** Adds a PostgreSQL database */
    addDatabase(name: string, options?: AddDatabaseOptions): PostgresDatabaseResourcePromise {
        const databaseName = options?.databaseName;
        return new PostgresDatabaseResourcePromise(this._addDatabaseInternal(name, databaseName));
    }

    /** @internal */
    private async _withPgAdminInternal(configureContainer?: (obj: PgAdminContainerResource) => Promise<void>, containerName?: string): Promise<PostgresServerResource> {
        const configureContainerId = configureContainer ? registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PgAdminContainerResourceHandle;
            const obj = new PgAdminContainerResource(objHandle, this._client);
            await configureContainer(obj);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (configureContainer !== undefined) rpcArgs.configureContainer = configureContainerId;
        if (containerName !== undefined) rpcArgs.containerName = containerName;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.PostgreSQL/withPgAdmin',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds pgAdmin 4 management UI */
    withPgAdmin(options?: WithPgAdminOptions): PostgresServerResourcePromise {
        const configureContainer = options?.configureContainer;
        const containerName = options?.containerName;
        return new PostgresServerResourcePromise(this._withPgAdminInternal(configureContainer, containerName));
    }

    /** @internal */
    private async _withPgWebInternal(configureContainer?: (obj: PgWebContainerResource) => Promise<void>, containerName?: string): Promise<PostgresServerResource> {
        const configureContainerId = configureContainer ? registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PgWebContainerResourceHandle;
            const obj = new PgWebContainerResource(objHandle, this._client);
            await configureContainer(obj);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (configureContainer !== undefined) rpcArgs.configureContainer = configureContainerId;
        if (containerName !== undefined) rpcArgs.containerName = containerName;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.PostgreSQL/withPgWeb',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds pgweb management UI */
    withPgWeb(options?: WithPgWebOptions): PostgresServerResourcePromise {
        const configureContainer = options?.configureContainer;
        const containerName = options?.containerName;
        return new PostgresServerResourcePromise(this._withPgWebInternal(configureContainer, containerName));
    }

    /** @internal */
    private async _withDataVolumeInternal(name?: string, isReadOnly?: boolean): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (name !== undefined) rpcArgs.name = name;
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.PostgreSQL/withDataVolume',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a data volume for PostgreSQL */
    withDataVolume(options?: WithDataVolumeOptions): PostgresServerResourcePromise {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new PostgresServerResourcePromise(this._withDataVolumeInternal(name, isReadOnly));
    }

    /** @internal */
    private async _withDataBindMountInternal(source: string, isReadOnly?: boolean): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.PostgreSQL/withDataBindMount',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Adds a data bind mount for PostgreSQL */
    withDataBindMount(source: string, options?: WithDataBindMountOptions): PostgresServerResourcePromise {
        const isReadOnly = options?.isReadOnly;
        return new PostgresServerResourcePromise(this._withDataBindMountInternal(source, isReadOnly));
    }

    /** @internal */
    private async _withInitFilesInternal(source: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.PostgreSQL/withInitFiles',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Copies init files to PostgreSQL */
    withInitFiles(source: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withInitFilesInternal(source));
    }

    /** @internal */
    private async _withPasswordInternal(password: ParameterResource): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, password };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.PostgreSQL/withPassword',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures the PostgreSQL password */
    withPassword(password: ParameterResource): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withPasswordInternal(password));
    }

    /** @internal */
    private async _withUserNameInternal(userName: ParameterResource): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, userName };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.PostgreSQL/withUserName',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures the PostgreSQL user name */
    withUserName(userName: ParameterResource): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withUserNameInternal(userName));
    }

    /** @internal */
    private async _withHostPortInternal(port?: number): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.PostgreSQL/withPostgresHostPort',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets the host port for PostgreSQL */
    withHostPort(options?: WithHostPortOptions): PostgresServerResourcePromise {
        const port = options?.port;
        return new PostgresServerResourcePromise(this._withHostPortInternal(port));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _publishAsAzureContainerAppInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): Promise<PostgresServerResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { container: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishContainerAsAzureContainerApp',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._publishAsAzureContainerAppInternal(configure));
    }

    /** @internal */
    private async _publishAsConfiguredAzureContainerAppJobInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<PostgresServerResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { resource: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredAzureContainerAppJob',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._publishAsConfiguredAzureContainerAppJobInternal(configure));
    }

    /** @internal */
    private async _publishAsAzureContainerAppJobInternal(): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsAzureContainerAppJob',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._publishAsAzureContainerAppJobInternal());
    }

    /** @internal */
    private async _publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression: string, configure?: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<PostgresServerResource> {
        const configureId = configure ? registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        if (configure !== undefined) rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): PostgresServerResourcePromise {
        const configure = options?.configure;
        return new PostgresServerResourcePromise(this._publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression, configure));
    }

    /** @internal */
    private async _publishAsScheduledAzureContainerAppJobInternal(cronExpression: string): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._publishAsScheduledAzureContainerAppJobInternal(cronExpression));
    }

    /** @internal */
    private async _withEnvironmentFromOutputInternal(name: string, bicepOutputReference: BicepOutputReference): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, bicepOutputReference };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromOutput',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withEnvironmentFromOutputInternal(name, bicepOutputReference));
    }

    /** @internal */
    private async _withEnvironmentFromKeyVaultSecretInternal(name: string, secretReference: ResourceBuilderBase): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, secretReference };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromKeyVaultSecret',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withEnvironmentFromKeyVaultSecretInternal(name, secretReference));
    }

    /** @internal */
    private async _withAzureUserAssignedIdentityInternal(identityResourceBuilder: AzureUserAssignedIdentityResource): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, identityResourceBuilder };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.Azure/withUserAssignedIdentityAzureUserAssignedIdentity',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withAzureUserAssignedIdentityInternal(identityResourceBuilder));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<PostgresServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<PostgresServerResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new PostgresServerResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for PostgresServerResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class PostgresServerResourcePromise implements PromiseLike<PostgresServerResource> {
    constructor(private _promise: Promise<PostgresServerResource>) {}

    then<TResult1 = PostgresServerResource, TResult2 = never>(
        onfulfilled?: ((value: PostgresServerResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withBindMount(source, target, options)));
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withEntrypoint(entrypoint)));
    }

    /** Sets the container image tag */
    withImageTag(tag: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withImageTag(tag)));
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withImageRegistry(registry)));
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withImage(image, options)));
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withImageSHA256(sha256)));
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withContainerRuntimeArgs(args)));
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withLifetime(lifetime)));
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)));
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.publishAsContainer()));
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withDockerfile(contextPath, options)));
    }

    /** Sets the container name */
    withContainerName(name: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withContainerName(name)));
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withBuildArg(name, value)));
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withBuildSecret(name, value)));
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withContainerNetworkAlias(alias)));
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withMcpServer(options)));
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withOtlpExporter()));
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withOtlpExporterProtocol(protocol)));
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withEnvironment(name, value)));
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withEnvironmentExpression(name, value)));
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withEnvironmentCallback(callback)));
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withEnvironmentCallbackAsync(callback)));
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withEnvironmentEndpoint(name, endpointReference)));
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withEnvironmentParameter(name, parameter)));
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withEnvironmentConnectionString(envVarName, resource)));
    }

    /** Adds a connection property with a reference expression */
    withConnectionProperty(name: string, value: ReferenceExpression): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withConnectionProperty(name, value)));
    }

    /** Adds a connection property with a string value */
    withConnectionPropertyValue(name: string, value: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withConnectionPropertyValue(name, value)));
    }

    /** Adds arguments */
    withArgs(args: string[]): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withArgs(args)));
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withArgsCallback(callback)));
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withArgsCallbackAsync(callback)));
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withReference(source, options)));
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withServiceReference(source)));
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withServiceReferenceNamed(source, name)));
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withReferenceUri(name, uri)));
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withReferenceExternalService(externalService)));
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withReferenceEndpoint(endpointReference)));
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withEndpoint(options)));
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withHttpEndpoint(options)));
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withHttpsEndpoint(options)));
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withExternalHttpEndpoints()));
    }

    /** Gets an endpoint reference */
    getEndpoint(name: string): Promise<EndpointReference> {
        return this._promise.then(obj => obj.getEndpoint(name));
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.asHttp2Service()));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withUrlForEndpointFactory(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.waitFor(dependency)));
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.waitForWithBehavior(dependency, waitBehavior)));
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.waitForStart(dependency)));
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.waitForStartWithBehavior(dependency, waitBehavior)));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.waitForCompletion(dependency, options)));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withHttpHealthCheck(options)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)));
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withCertificateTrustScope(scope)));
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)));
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withoutHttpsCertificate()));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withHttpProbe(probeType, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)));
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withVolume(target, options)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the ConnectionStringAvailable event */
    onConnectionStringAvailable(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.onConnectionStringAvailable(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Adds a PostgreSQL database */
    addDatabase(name: string, options?: AddDatabaseOptions): PostgresDatabaseResourcePromise {
        return new PostgresDatabaseResourcePromise(this._promise.then(obj => obj.addDatabase(name, options)));
    }

    /** Adds pgAdmin 4 management UI */
    withPgAdmin(options?: WithPgAdminOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withPgAdmin(options)));
    }

    /** Adds pgweb management UI */
    withPgWeb(options?: WithPgWebOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withPgWeb(options)));
    }

    /** Adds a data volume for PostgreSQL */
    withDataVolume(options?: WithDataVolumeOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withDataVolume(options)));
    }

    /** Adds a data bind mount for PostgreSQL */
    withDataBindMount(source: string, options?: WithDataBindMountOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withDataBindMount(source, options)));
    }

    /** Copies init files to PostgreSQL */
    withInitFiles(source: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withInitFiles(source)));
    }

    /** Configures the PostgreSQL password */
    withPassword(password: ParameterResource): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withPassword(password)));
    }

    /** Configures the PostgreSQL user name */
    withUserName(userName: ParameterResource): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withUserName(userName)));
    }

    /** Sets the host port for PostgreSQL */
    withHostPort(options?: WithHostPortOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withHostPort(options)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerApp(configure)));
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.publishAsConfiguredAzureContainerAppJob(configure)));
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerAppJob()));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.publishAsConfiguredScheduledAzureContainerAppJob(cronExpression, options)));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.publishAsScheduledAzureContainerAppJob(cronExpression)));
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withEnvironmentFromOutput(name, bicepOutputReference)));
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withEnvironmentFromKeyVaultSecret(name, secretReference)));
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withAzureUserAssignedIdentity(identityResourceBuilder)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): PostgresServerResourcePromise {
        return new PostgresServerResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// ProjectResource
// ============================================================================

export class ProjectResource extends ResourceBuilderBase<ProjectResourceHandle> {
    constructor(handle: ProjectResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): ProjectResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new ProjectResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withMcpServerInternal(path?: string, endpointName?: string): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withMcpServer',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): ProjectResourcePromise {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new ProjectResourcePromise(this._withMcpServerInternal(path, endpointName));
    }

    /** @internal */
    private async _withOtlpExporterInternal(): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withOtlpExporter',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withOtlpExporterInternal());
    }

    /** @internal */
    private async _withOtlpExporterProtocolInternal(protocol: OtlpProtocol): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, protocol };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withOtlpExporterProtocol',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withOtlpExporterProtocolInternal(protocol));
    }

    /** @internal */
    private async _withReplicasInternal(replicas: number): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, replicas };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withReplicas',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets the number of replicas */
    withReplicas(replicas: number): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withReplicasInternal(replicas));
    }

    /** @internal */
    private async _disableForwardedHeadersInternal(): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/disableForwardedHeaders',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Disables forwarded headers for the project */
    disableForwardedHeaders(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._disableForwardedHeadersInternal());
    }

    /** @internal */
    private async _publishAsDockerFileInternal(configure?: (obj: ContainerResource) => Promise<void>): Promise<ProjectResource> {
        const configureId = configure ? registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ContainerResourceHandle;
            const obj = new ContainerResource(objHandle, this._client);
            await configure(obj);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (configure !== undefined) rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/publishProjectAsDockerFileWithConfigure',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Publishes a project as a Docker file with optional container configuration */
    publishAsDockerFile(options?: PublishAsDockerFileOptions): ProjectResourcePromise {
        const configure = options?.configure;
        return new ProjectResourcePromise(this._publishAsDockerFileInternal(configure));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): ProjectResourcePromise {
        const helpLink = options?.helpLink;
        return new ProjectResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withEnvironmentInternal(name: string, value: string): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withEnvironment',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withEnvironmentInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentExpressionInternal(name: string, value: ReferenceExpression): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withEnvironmentExpression',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withEnvironmentExpressionInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentCallbackInternal(callback: (obj: EnvironmentCallbackContext) => Promise<void>): Promise<ProjectResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as EnvironmentCallbackContextHandle;
            const obj = new EnvironmentCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallback',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withEnvironmentCallbackInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentCallbackAsyncInternal(callback: (arg: EnvironmentCallbackContext) => Promise<void>): Promise<ProjectResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EnvironmentCallbackContextHandle;
            const arg = new EnvironmentCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallbackAsync',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withEnvironmentCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentEndpointInternal(name: string, endpointReference: EndpointReference): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, endpointReference };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withEnvironmentEndpoint',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withEnvironmentEndpointInternal(name, endpointReference));
    }

    /** @internal */
    private async _withEnvironmentParameterInternal(name: string, parameter: ParameterResource): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, parameter };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withEnvironmentParameter',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withEnvironmentParameterInternal(name, parameter));
    }

    /** @internal */
    private async _withEnvironmentConnectionStringInternal(envVarName: string, resource: ResourceBuilderBase): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, envVarName, resource };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withEnvironmentConnectionString',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withEnvironmentConnectionStringInternal(envVarName, resource));
    }

    /** @internal */
    private async _withArgsInternal(args: string[]): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withArgs',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds arguments */
    withArgs(args: string[]): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withArgsInternal(args));
    }

    /** @internal */
    private async _withArgsCallbackInternal(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): Promise<ProjectResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as CommandLineArgsCallbackContextHandle;
            const obj = new CommandLineArgsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withArgsCallback',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withArgsCallbackInternal(callback));
    }

    /** @internal */
    private async _withArgsCallbackAsyncInternal(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): Promise<ProjectResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as CommandLineArgsCallbackContextHandle;
            const arg = new CommandLineArgsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withArgsCallbackAsync',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withArgsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withReferenceInternal(source: ResourceBuilderBase, connectionName?: string, optional?: boolean): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (connectionName !== undefined) rpcArgs.connectionName = connectionName;
        if (optional !== undefined) rpcArgs.optional = optional;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withReference',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): ProjectResourcePromise {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        return new ProjectResourcePromise(this._withReferenceInternal(source, connectionName, optional));
    }

    /** @internal */
    private async _withServiceReferenceInternal(source: ResourceBuilderBase): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withServiceReference',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withServiceReferenceInternal(source));
    }

    /** @internal */
    private async _withServiceReferenceNamedInternal(source: ResourceBuilderBase, name: string): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, name };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withServiceReferenceNamed',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withServiceReferenceNamedInternal(source, name));
    }

    /** @internal */
    private async _withReferenceUriInternal(name: string, uri: string): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, uri };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withReferenceUri',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withReferenceUriInternal(name, uri));
    }

    /** @internal */
    private async _withReferenceExternalServiceInternal(externalService: ExternalServiceResource): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, externalService };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withReferenceExternalService',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withReferenceExternalServiceInternal(externalService));
    }

    /** @internal */
    private async _withReferenceEndpointInternal(endpointReference: EndpointReference): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointReference };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withReferenceEndpoint',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withReferenceEndpointInternal(endpointReference));
    }

    /** @internal */
    private async _withEndpointInternal(port?: number, targetPort?: number, scheme?: string, name?: string, env?: string, isProxied?: boolean, isExternal?: boolean, protocol?: ProtocolType): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (scheme !== undefined) rpcArgs.scheme = scheme;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined) rpcArgs.isExternal = isExternal;
        if (protocol !== undefined) rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withEndpoint',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): ProjectResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new ProjectResourcePromise(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol));
    }

    /** @internal */
    private async _withHttpEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withHttpEndpoint',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): ProjectResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ProjectResourcePromise(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withHttpsEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withHttpsEndpoint',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): ProjectResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ProjectResourcePromise(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withExternalHttpEndpointsInternal(): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withExternalHttpEndpoints',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withExternalHttpEndpointsInternal());
    }

    /** Gets an endpoint reference */
    async getEndpoint(name: string): Promise<EndpointReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<EndpointReference>(
            'Aspire.Hosting/getEndpoint',
            rpcArgs
        );
    }

    /** @internal */
    private async _asHttp2ServiceInternal(): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/asHttp2Service',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._asHttp2ServiceInternal());
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<ProjectResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<ProjectResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): ProjectResourcePromise {
        const displayText = options?.displayText;
        return new ProjectResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): ProjectResourcePromise {
        const displayText = options?.displayText;
        return new ProjectResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<ProjectResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _withUrlForEndpointFactoryInternal(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): Promise<ProjectResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EndpointReferenceHandle;
            const arg = new EndpointReference(argHandle, this._client);
            return await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withUrlForEndpointFactory',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withUrlForEndpointFactoryInternal(endpointName, callback));
    }

    /** @internal */
    private async _publishWithContainerFilesInternal(source: ResourceBuilderBase, destinationPath: string): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, destinationPath };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/publishWithContainerFiles',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Configures the resource to copy container files from the specified source during publishing */
    publishWithContainerFiles(source: ResourceBuilderBase, destinationPath: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._publishWithContainerFilesInternal(source, destinationPath));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _waitForInternal(dependency: ResourceBuilderBase): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/waitFor',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): ProjectResourcePromise {
        return new ProjectResourcePromise(this._waitForInternal(dependency));
    }

    /** @internal */
    private async _waitForWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/waitForWithBehavior',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ProjectResourcePromise {
        return new ProjectResourcePromise(this._waitForWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _waitForStartInternal(dependency: ResourceBuilderBase): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/waitForStart',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): ProjectResourcePromise {
        return new ProjectResourcePromise(this._waitForStartInternal(dependency));
    }

    /** @internal */
    private async _waitForStartWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/waitForStartWithBehavior',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ProjectResourcePromise {
        return new ProjectResourcePromise(this._waitForStartWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _waitForCompletionInternal(dependency: ResourceBuilderBase, exitCode?: number): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        if (exitCode !== undefined) rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/waitForCompletion',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): ProjectResourcePromise {
        const exitCode = options?.exitCode;
        return new ProjectResourcePromise(this._waitForCompletionInternal(dependency, exitCode));
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withHttpHealthCheckInternal(path?: string, statusCode?: number, endpointName?: string): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (statusCode !== undefined) rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withHttpHealthCheck',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): ProjectResourcePromise {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new ProjectResourcePromise(this._withHttpHealthCheckInternal(path, statusCode, endpointName));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<ProjectResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): ProjectResourcePromise {
        const commandOptions = options?.commandOptions;
        return new ProjectResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withDeveloperCertificateTrustInternal(trust: boolean): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, trust };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withDeveloperCertificateTrust',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withDeveloperCertificateTrustInternal(trust));
    }

    /** @internal */
    private async _withCertificateTrustScopeInternal(scope: CertificateTrustScope): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, scope };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withCertificateTrustScope',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withCertificateTrustScopeInternal(scope));
    }

    /** @internal */
    private async _withHttpsDeveloperCertificateInternal(password?: ParameterResource): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (password !== undefined) rpcArgs.password = password;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withHttpsDeveloperCertificate',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): ProjectResourcePromise {
        const password = options?.password;
        return new ProjectResourcePromise(this._withHttpsDeveloperCertificateInternal(password));
    }

    /** @internal */
    private async _withoutHttpsCertificateInternal(): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withoutHttpsCertificate',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withoutHttpsCertificateInternal());
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): ProjectResourcePromise {
        const iconVariant = options?.iconVariant;
        return new ProjectResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _withHttpProbeInternal(probeType: ProbeType, path?: string, initialDelaySeconds?: number, periodSeconds?: number, timeoutSeconds?: number, failureThreshold?: number, successThreshold?: number, endpointName?: string): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, probeType };
        if (path !== undefined) rpcArgs.path = path;
        if (initialDelaySeconds !== undefined) rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined) rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined) rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined) rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined) rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withHttpProbe',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): ProjectResourcePromise {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new ProjectResourcePromise(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withRemoteImageNameInternal(remoteImageName: string): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withRemoteImageName',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withRemoteImageNameInternal(remoteImageName));
    }

    /** @internal */
    private async _withRemoteImageTagInternal(remoteImageTag: string): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withRemoteImageTag',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withRemoteImageTagInternal(remoteImageTag));
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<ProjectResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): ProjectResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new ProjectResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<ProjectResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<ProjectResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<ProjectResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<ProjectResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<ProjectResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceEndpointsAllocatedInternal(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): Promise<ProjectResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceEndpointsAllocatedEventHandle;
            const arg = new ResourceEndpointsAllocatedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/onResourceEndpointsAllocated',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._onResourceEndpointsAllocatedInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<ProjectResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _publishAsAzureContainerAppInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): Promise<ProjectResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { project: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishProjectAsAzureContainerApp',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Configures the project resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._publishAsAzureContainerAppInternal(configure));
    }

    /** @internal */
    private async _publishAsConfiguredAzureContainerAppJobInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<ProjectResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { resource: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredAzureContainerAppJob',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._publishAsConfiguredAzureContainerAppJobInternal(configure));
    }

    /** @internal */
    private async _publishAsAzureContainerAppJobInternal(): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsAzureContainerAppJob',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._publishAsAzureContainerAppJobInternal());
    }

    /** @internal */
    private async _publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression: string, configure?: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<ProjectResource> {
        const configureId = configure ? registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        if (configure !== undefined) rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): ProjectResourcePromise {
        const configure = options?.configure;
        return new ProjectResourcePromise(this._publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression, configure));
    }

    /** @internal */
    private async _publishAsScheduledAzureContainerAppJobInternal(cronExpression: string): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._publishAsScheduledAzureContainerAppJobInternal(cronExpression));
    }

    /** @internal */
    private async _withEnvironmentFromOutputInternal(name: string, bicepOutputReference: BicepOutputReference): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, bicepOutputReference };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromOutput',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withEnvironmentFromOutputInternal(name, bicepOutputReference));
    }

    /** @internal */
    private async _withEnvironmentFromKeyVaultSecretInternal(name: string, secretReference: ResourceBuilderBase): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, secretReference };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromKeyVaultSecret',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withEnvironmentFromKeyVaultSecretInternal(name, secretReference));
    }

    /** @internal */
    private async _withAzureUserAssignedIdentityInternal(identityResourceBuilder: AzureUserAssignedIdentityResource): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, identityResourceBuilder };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting.Azure/withUserAssignedIdentityAzureUserAssignedIdentity',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withAzureUserAssignedIdentityInternal(identityResourceBuilder));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<ProjectResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<ProjectResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new ProjectResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): ProjectResourcePromise {
        return new ProjectResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for ProjectResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class ProjectResourcePromise implements PromiseLike<ProjectResource> {
    constructor(private _promise: Promise<ProjectResource>) {}

    then<TResult1 = ProjectResource, TResult2 = never>(
        onfulfilled?: ((value: ProjectResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withMcpServer(options)));
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withOtlpExporter()));
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withOtlpExporterProtocol(protocol)));
    }

    /** Sets the number of replicas */
    withReplicas(replicas: number): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withReplicas(replicas)));
    }

    /** Disables forwarded headers for the project */
    disableForwardedHeaders(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.disableForwardedHeaders()));
    }

    /** Publishes a project as a Docker file with optional container configuration */
    publishAsDockerFile(options?: PublishAsDockerFileOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.publishAsDockerFile(options)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withEnvironment(name, value)));
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withEnvironmentExpression(name, value)));
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withEnvironmentCallback(callback)));
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withEnvironmentCallbackAsync(callback)));
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withEnvironmentEndpoint(name, endpointReference)));
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withEnvironmentParameter(name, parameter)));
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withEnvironmentConnectionString(envVarName, resource)));
    }

    /** Adds arguments */
    withArgs(args: string[]): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withArgs(args)));
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withArgsCallback(callback)));
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withArgsCallbackAsync(callback)));
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withReference(source, options)));
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withServiceReference(source)));
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withServiceReferenceNamed(source, name)));
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withReferenceUri(name, uri)));
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withReferenceExternalService(externalService)));
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withReferenceEndpoint(endpointReference)));
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withEndpoint(options)));
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withHttpEndpoint(options)));
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withHttpsEndpoint(options)));
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withExternalHttpEndpoints()));
    }

    /** Gets an endpoint reference */
    getEndpoint(name: string): Promise<EndpointReference> {
        return this._promise.then(obj => obj.getEndpoint(name));
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.asHttp2Service()));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withUrlForEndpointFactory(endpointName, callback)));
    }

    /** Configures the resource to copy container files from the specified source during publishing */
    publishWithContainerFiles(source: ResourceBuilderBase, destinationPath: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.publishWithContainerFiles(source, destinationPath)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.waitFor(dependency)));
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.waitForWithBehavior(dependency, waitBehavior)));
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.waitForStart(dependency)));
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.waitForStartWithBehavior(dependency, waitBehavior)));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.waitForCompletion(dependency, options)));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withHttpHealthCheck(options)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)));
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withCertificateTrustScope(scope)));
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)));
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withoutHttpsCertificate()));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withHttpProbe(probeType, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)));
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures the project resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerApp(configure)));
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.publishAsConfiguredAzureContainerAppJob(configure)));
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerAppJob()));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.publishAsConfiguredScheduledAzureContainerAppJob(cronExpression, options)));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.publishAsScheduledAzureContainerAppJob(cronExpression)));
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withEnvironmentFromOutput(name, bicepOutputReference)));
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withEnvironmentFromKeyVaultSecret(name, secretReference)));
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withAzureUserAssignedIdentity(identityResourceBuilder)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): ProjectResourcePromise {
        return new ProjectResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// RabbitMQServerResource
// ============================================================================

export class RabbitMQServerResource extends ResourceBuilderBase<RabbitMQServerResourceHandle> {
    constructor(handle: RabbitMQServerResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** Gets the PrimaryEndpoint property */
    primaryEndpoint = {
        get: async (): Promise<EndpointReference> => {
            const handle = await this._client.invokeCapability<EndpointReferenceHandle>(
                'Aspire.Hosting.ApplicationModel/RabbitMQServerResource.primaryEndpoint',
                { context: this._handle }
            );
            return new EndpointReference(handle, this._client);
        },
    };

    /** Gets the ManagementEndpoint property */
    managementEndpoint = {
        get: async (): Promise<EndpointReference> => {
            const handle = await this._client.invokeCapability<EndpointReferenceHandle>(
                'Aspire.Hosting.ApplicationModel/RabbitMQServerResource.managementEndpoint',
                { context: this._handle }
            );
            return new EndpointReference(handle, this._client);
        },
    };

    /** Gets the Host property */
    host = {
        get: async (): Promise<EndpointReferenceExpression> => {
            const handle = await this._client.invokeCapability<EndpointReferenceExpressionHandle>(
                'Aspire.Hosting.ApplicationModel/RabbitMQServerResource.host',
                { context: this._handle }
            );
            return new EndpointReferenceExpression(handle, this._client);
        },
    };

    /** Gets the Port property */
    port = {
        get: async (): Promise<EndpointReferenceExpression> => {
            const handle = await this._client.invokeCapability<EndpointReferenceExpressionHandle>(
                'Aspire.Hosting.ApplicationModel/RabbitMQServerResource.port',
                { context: this._handle }
            );
            return new EndpointReferenceExpression(handle, this._client);
        },
    };

    /** Gets the UserNameParameter property */
    userNameParameter = {
        get: async (): Promise<ParameterResource> => {
            const handle = await this._client.invokeCapability<ParameterResourceHandle>(
                'Aspire.Hosting.ApplicationModel/RabbitMQServerResource.userNameParameter',
                { context: this._handle }
            );
            return new ParameterResource(handle, this._client);
        },
    };

    /** Gets the UserNameReference property */
    userNameReference = {
        get: async (): Promise<ReferenceExpression> => {
            return await this._client.invokeCapability<ReferenceExpression>(
                'Aspire.Hosting.ApplicationModel/RabbitMQServerResource.userNameReference',
                { context: this._handle }
            );
        },
    };

    /** Gets the PasswordParameter property */
    passwordParameter = {
        get: async (): Promise<ParameterResource> => {
            const handle = await this._client.invokeCapability<ParameterResourceHandle>(
                'Aspire.Hosting.ApplicationModel/RabbitMQServerResource.passwordParameter',
                { context: this._handle }
            );
            return new ParameterResource(handle, this._client);
        },
    };

    /** Gets the ConnectionStringExpression property */
    connectionStringExpression = {
        get: async (): Promise<ReferenceExpression> => {
            return await this._client.invokeCapability<ReferenceExpression>(
                'Aspire.Hosting.ApplicationModel/RabbitMQServerResource.connectionStringExpression',
                { context: this._handle }
            );
        },
    };

    /** Gets the UriExpression property */
    uriExpression = {
        get: async (): Promise<ReferenceExpression> => {
            return await this._client.invokeCapability<ReferenceExpression>(
                'Aspire.Hosting.ApplicationModel/RabbitMQServerResource.uriExpression',
                { context: this._handle }
            );
        },
    };

    /** Gets the Entrypoint property */
    entrypoint = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/RabbitMQServerResource.entrypoint',
                { context: this._handle }
            );
        },
        set: async (value: string): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.ApplicationModel/RabbitMQServerResource.setEntrypoint',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the ShellExecution property */
    shellExecution = {
        get: async (): Promise<boolean> => {
            return await this._client.invokeCapability<boolean>(
                'Aspire.Hosting.ApplicationModel/RabbitMQServerResource.shellExecution',
                { context: this._handle }
            );
        },
        set: async (value: boolean): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.ApplicationModel/RabbitMQServerResource.setShellExecution',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the Name property */
    name = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/RabbitMQServerResource.name',
                { context: this._handle }
            );
        },
    };

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withBindMountInternal(source: string, target: string, isReadOnly?: boolean): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, target };
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withBindMount',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): RabbitMQServerResourcePromise {
        const isReadOnly = options?.isReadOnly;
        return new RabbitMQServerResourcePromise(this._withBindMountInternal(source, target, isReadOnly));
    }

    /** @internal */
    private async _withEntrypointInternal(entrypoint: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withEntrypoint',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withEntrypointInternal(entrypoint));
    }

    /** @internal */
    private async _withImageTagInternal(tag: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, tag };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withImageTag',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets the container image tag */
    withImageTag(tag: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withImageTagInternal(tag));
    }

    /** @internal */
    private async _withImageRegistryInternal(registry: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withImageRegistry',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withImageRegistryInternal(registry));
    }

    /** @internal */
    private async _withImageInternal(image: string, tag?: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, image };
        if (tag !== undefined) rpcArgs.tag = tag;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withImage',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): RabbitMQServerResourcePromise {
        const tag = options?.tag;
        return new RabbitMQServerResourcePromise(this._withImageInternal(image, tag));
    }

    /** @internal */
    private async _withImageSHA256Internal(sha256: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withImageSHA256',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withImageSHA256Internal(sha256));
    }

    /** @internal */
    private async _withContainerRuntimeArgsInternal(args: string[]): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withContainerRuntimeArgs',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withContainerRuntimeArgsInternal(args));
    }

    /** @internal */
    private async _withLifetimeInternal(lifetime: ContainerLifetime): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withLifetime',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withLifetimeInternal(lifetime));
    }

    /** @internal */
    private async _withImagePullPolicyInternal(pullPolicy: ImagePullPolicy): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withImagePullPolicy',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withImagePullPolicyInternal(pullPolicy));
    }

    /** @internal */
    private async _publishAsContainerInternal(): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/publishAsContainer',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._publishAsContainerInternal());
    }

    /** @internal */
    private async _withDockerfileInternal(contextPath: string, dockerfilePath?: string, stage?: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined) rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined) rpcArgs.stage = stage;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withDockerfile',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): RabbitMQServerResourcePromise {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new RabbitMQServerResourcePromise(this._withDockerfileInternal(contextPath, dockerfilePath, stage));
    }

    /** @internal */
    private async _withContainerNameInternal(name: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withContainerName',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets the container name */
    withContainerName(name: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withContainerNameInternal(name));
    }

    /** @internal */
    private async _withBuildArgInternal(name: string, value: ParameterResource): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withBuildArg',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withBuildArgInternal(name, value));
    }

    /** @internal */
    private async _withBuildSecretInternal(name: string, value: ParameterResource): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withBuildSecret',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withBuildSecretInternal(name, value));
    }

    /** @internal */
    private async _withEndpointProxySupportInternal(proxyEnabled: boolean): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withEndpointProxySupport',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withEndpointProxySupportInternal(proxyEnabled));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): RabbitMQServerResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new RabbitMQServerResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withContainerNetworkAliasInternal(alias: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, alias };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withContainerNetworkAlias',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withContainerNetworkAliasInternal(alias));
    }

    /** @internal */
    private async _withMcpServerInternal(path?: string, endpointName?: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withMcpServer',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): RabbitMQServerResourcePromise {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new RabbitMQServerResourcePromise(this._withMcpServerInternal(path, endpointName));
    }

    /** @internal */
    private async _withOtlpExporterInternal(): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withOtlpExporter',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withOtlpExporterInternal());
    }

    /** @internal */
    private async _withOtlpExporterProtocolInternal(protocol: OtlpProtocol): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, protocol };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withOtlpExporterProtocol',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withOtlpExporterProtocolInternal(protocol));
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/publishAsConnectionString',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): RabbitMQServerResourcePromise {
        const helpLink = options?.helpLink;
        return new RabbitMQServerResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withEnvironmentInternal(name: string, value: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withEnvironment',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withEnvironmentInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentExpressionInternal(name: string, value: ReferenceExpression): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withEnvironmentExpression',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withEnvironmentExpressionInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentCallbackInternal(callback: (obj: EnvironmentCallbackContext) => Promise<void>): Promise<RabbitMQServerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as EnvironmentCallbackContextHandle;
            const obj = new EnvironmentCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallback',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withEnvironmentCallbackInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentCallbackAsyncInternal(callback: (arg: EnvironmentCallbackContext) => Promise<void>): Promise<RabbitMQServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EnvironmentCallbackContextHandle;
            const arg = new EnvironmentCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallbackAsync',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withEnvironmentCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentEndpointInternal(name: string, endpointReference: EndpointReference): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, endpointReference };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withEnvironmentEndpoint',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withEnvironmentEndpointInternal(name, endpointReference));
    }

    /** @internal */
    private async _withEnvironmentParameterInternal(name: string, parameter: ParameterResource): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, parameter };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withEnvironmentParameter',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withEnvironmentParameterInternal(name, parameter));
    }

    /** @internal */
    private async _withEnvironmentConnectionStringInternal(envVarName: string, resource: ResourceBuilderBase): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, envVarName, resource };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withEnvironmentConnectionString',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withEnvironmentConnectionStringInternal(envVarName, resource));
    }

    /** @internal */
    private async _withConnectionPropertyInternal(name: string, value: ReferenceExpression): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withConnectionProperty',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a connection property with a reference expression */
    withConnectionProperty(name: string, value: ReferenceExpression): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withConnectionPropertyInternal(name, value));
    }

    /** @internal */
    private async _withConnectionPropertyValueInternal(name: string, value: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withConnectionPropertyValue',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a connection property with a string value */
    withConnectionPropertyValue(name: string, value: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withConnectionPropertyValueInternal(name, value));
    }

    /** @internal */
    private async _withArgsInternal(args: string[]): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withArgs',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds arguments */
    withArgs(args: string[]): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withArgsInternal(args));
    }

    /** @internal */
    private async _withArgsCallbackInternal(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): Promise<RabbitMQServerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as CommandLineArgsCallbackContextHandle;
            const obj = new CommandLineArgsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withArgsCallback',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withArgsCallbackInternal(callback));
    }

    /** @internal */
    private async _withArgsCallbackAsyncInternal(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): Promise<RabbitMQServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as CommandLineArgsCallbackContextHandle;
            const arg = new CommandLineArgsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withArgsCallbackAsync',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withArgsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withReferenceInternal(source: ResourceBuilderBase, connectionName?: string, optional?: boolean): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (connectionName !== undefined) rpcArgs.connectionName = connectionName;
        if (optional !== undefined) rpcArgs.optional = optional;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withReference',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): RabbitMQServerResourcePromise {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        return new RabbitMQServerResourcePromise(this._withReferenceInternal(source, connectionName, optional));
    }

    /** @internal */
    private async _withServiceReferenceInternal(source: ResourceBuilderBase): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withServiceReference',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withServiceReferenceInternal(source));
    }

    /** @internal */
    private async _withServiceReferenceNamedInternal(source: ResourceBuilderBase, name: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, name };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withServiceReferenceNamed',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withServiceReferenceNamedInternal(source, name));
    }

    /** @internal */
    private async _withReferenceUriInternal(name: string, uri: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, uri };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withReferenceUri',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withReferenceUriInternal(name, uri));
    }

    /** @internal */
    private async _withReferenceExternalServiceInternal(externalService: ExternalServiceResource): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, externalService };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withReferenceExternalService',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withReferenceExternalServiceInternal(externalService));
    }

    /** @internal */
    private async _withReferenceEndpointInternal(endpointReference: EndpointReference): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointReference };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withReferenceEndpoint',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withReferenceEndpointInternal(endpointReference));
    }

    /** @internal */
    private async _withEndpointInternal(port?: number, targetPort?: number, scheme?: string, name?: string, env?: string, isProxied?: boolean, isExternal?: boolean, protocol?: ProtocolType): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (scheme !== undefined) rpcArgs.scheme = scheme;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined) rpcArgs.isExternal = isExternal;
        if (protocol !== undefined) rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withEndpoint',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): RabbitMQServerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new RabbitMQServerResourcePromise(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol));
    }

    /** @internal */
    private async _withHttpEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withHttpEndpoint',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): RabbitMQServerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new RabbitMQServerResourcePromise(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withHttpsEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withHttpsEndpoint',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): RabbitMQServerResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new RabbitMQServerResourcePromise(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withExternalHttpEndpointsInternal(): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withExternalHttpEndpoints',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withExternalHttpEndpointsInternal());
    }

    /** Gets an endpoint reference */
    async getEndpoint(name: string): Promise<EndpointReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<EndpointReference>(
            'Aspire.Hosting/getEndpoint',
            rpcArgs
        );
    }

    /** @internal */
    private async _asHttp2ServiceInternal(): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/asHttp2Service',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._asHttp2ServiceInternal());
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<RabbitMQServerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<RabbitMQServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): RabbitMQServerResourcePromise {
        const displayText = options?.displayText;
        return new RabbitMQServerResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): RabbitMQServerResourcePromise {
        const displayText = options?.displayText;
        return new RabbitMQServerResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<RabbitMQServerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _withUrlForEndpointFactoryInternal(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): Promise<RabbitMQServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EndpointReferenceHandle;
            const arg = new EndpointReference(argHandle, this._client);
            return await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withUrlForEndpointFactory',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withUrlForEndpointFactoryInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _waitForInternal(dependency: ResourceBuilderBase): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/waitFor',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._waitForInternal(dependency));
    }

    /** @internal */
    private async _waitForWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/waitForWithBehavior',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._waitForWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _waitForStartInternal(dependency: ResourceBuilderBase): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/waitForStart',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._waitForStartInternal(dependency));
    }

    /** @internal */
    private async _waitForStartWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/waitForStartWithBehavior',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._waitForStartWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _waitForCompletionInternal(dependency: ResourceBuilderBase, exitCode?: number): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        if (exitCode !== undefined) rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/waitForCompletion',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): RabbitMQServerResourcePromise {
        const exitCode = options?.exitCode;
        return new RabbitMQServerResourcePromise(this._waitForCompletionInternal(dependency, exitCode));
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withHttpHealthCheckInternal(path?: string, statusCode?: number, endpointName?: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (statusCode !== undefined) rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withHttpHealthCheck',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): RabbitMQServerResourcePromise {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new RabbitMQServerResourcePromise(this._withHttpHealthCheckInternal(path, statusCode, endpointName));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<RabbitMQServerResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): RabbitMQServerResourcePromise {
        const commandOptions = options?.commandOptions;
        return new RabbitMQServerResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withDeveloperCertificateTrustInternal(trust: boolean): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, trust };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withDeveloperCertificateTrust',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withDeveloperCertificateTrustInternal(trust));
    }

    /** @internal */
    private async _withCertificateTrustScopeInternal(scope: CertificateTrustScope): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, scope };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withCertificateTrustScope',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withCertificateTrustScopeInternal(scope));
    }

    /** @internal */
    private async _withHttpsDeveloperCertificateInternal(password?: ParameterResource): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (password !== undefined) rpcArgs.password = password;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withHttpsDeveloperCertificate',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): RabbitMQServerResourcePromise {
        const password = options?.password;
        return new RabbitMQServerResourcePromise(this._withHttpsDeveloperCertificateInternal(password));
    }

    /** @internal */
    private async _withoutHttpsCertificateInternal(): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withoutHttpsCertificate',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withoutHttpsCertificateInternal());
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): RabbitMQServerResourcePromise {
        const iconVariant = options?.iconVariant;
        return new RabbitMQServerResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _withHttpProbeInternal(probeType: ProbeType, path?: string, initialDelaySeconds?: number, periodSeconds?: number, timeoutSeconds?: number, failureThreshold?: number, successThreshold?: number, endpointName?: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, probeType };
        if (path !== undefined) rpcArgs.path = path;
        if (initialDelaySeconds !== undefined) rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined) rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined) rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined) rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined) rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withHttpProbe',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): RabbitMQServerResourcePromise {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new RabbitMQServerResourcePromise(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withRemoteImageNameInternal(remoteImageName: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withRemoteImageName',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withRemoteImageNameInternal(remoteImageName));
    }

    /** @internal */
    private async _withRemoteImageTagInternal(remoteImageTag: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withRemoteImageTag',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withRemoteImageTagInternal(remoteImageTag));
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<RabbitMQServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): RabbitMQServerResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new RabbitMQServerResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<RabbitMQServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<RabbitMQServerResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** @internal */
    private async _withVolumeInternal(target: string, name?: string, isReadOnly?: boolean): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, target };
        if (name !== undefined) rpcArgs.name = name;
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/withVolume',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): RabbitMQServerResourcePromise {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new RabbitMQServerResourcePromise(this._withVolumeInternal(target, name, isReadOnly));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<RabbitMQServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<RabbitMQServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onConnectionStringAvailableInternal(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): Promise<RabbitMQServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ConnectionStringAvailableEventHandle;
            const arg = new ConnectionStringAvailableEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/onConnectionStringAvailable',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Subscribes to the ConnectionStringAvailable event */
    onConnectionStringAvailable(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._onConnectionStringAvailableInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<RabbitMQServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceEndpointsAllocatedInternal(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): Promise<RabbitMQServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceEndpointsAllocatedEventHandle;
            const arg = new ResourceEndpointsAllocatedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/onResourceEndpointsAllocated',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._onResourceEndpointsAllocatedInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<RabbitMQServerResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withDataVolumeInternal(name?: string, isReadOnly?: boolean): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (name !== undefined) rpcArgs.name = name;
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting.RabbitMQ/withDataVolume',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a data volume to the RabbitMQ container */
    withDataVolume(options?: WithDataVolumeOptions): RabbitMQServerResourcePromise {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new RabbitMQServerResourcePromise(this._withDataVolumeInternal(name, isReadOnly));
    }

    /** @internal */
    private async _withDataBindMountInternal(source: string, isReadOnly?: boolean): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting.RabbitMQ/withDataBindMount',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Adds a data bind mount to the RabbitMQ container */
    withDataBindMount(source: string, options?: WithDataBindMountOptions): RabbitMQServerResourcePromise {
        const isReadOnly = options?.isReadOnly;
        return new RabbitMQServerResourcePromise(this._withDataBindMountInternal(source, isReadOnly));
    }

    /** @internal */
    private async _withManagementPluginInternal(): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting.RabbitMQ/withManagementPlugin',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Enables the RabbitMQ management plugin */
    withManagementPlugin(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withManagementPluginInternal());
    }

    /** @internal */
    private async _withManagementPluginWithPortInternal(port?: number): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting.RabbitMQ/withManagementPluginWithPort',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Enables the RabbitMQ management plugin with a specific port */
    withManagementPluginWithPort(options?: WithManagementPluginWithPortOptions): RabbitMQServerResourcePromise {
        const port = options?.port;
        return new RabbitMQServerResourcePromise(this._withManagementPluginWithPortInternal(port));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _publishAsAzureContainerAppInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): Promise<RabbitMQServerResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { container: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishContainerAsAzureContainerApp',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._publishAsAzureContainerAppInternal(configure));
    }

    /** @internal */
    private async _publishAsConfiguredAzureContainerAppJobInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<RabbitMQServerResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { resource: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredAzureContainerAppJob',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._publishAsConfiguredAzureContainerAppJobInternal(configure));
    }

    /** @internal */
    private async _publishAsAzureContainerAppJobInternal(): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsAzureContainerAppJob',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._publishAsAzureContainerAppJobInternal());
    }

    /** @internal */
    private async _publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression: string, configure?: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<RabbitMQServerResource> {
        const configureId = configure ? registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        if (configure !== undefined) rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): RabbitMQServerResourcePromise {
        const configure = options?.configure;
        return new RabbitMQServerResourcePromise(this._publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression, configure));
    }

    /** @internal */
    private async _publishAsScheduledAzureContainerAppJobInternal(cronExpression: string): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._publishAsScheduledAzureContainerAppJobInternal(cronExpression));
    }

    /** @internal */
    private async _withEnvironmentFromOutputInternal(name: string, bicepOutputReference: BicepOutputReference): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, bicepOutputReference };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromOutput',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withEnvironmentFromOutputInternal(name, bicepOutputReference));
    }

    /** @internal */
    private async _withEnvironmentFromKeyVaultSecretInternal(name: string, secretReference: ResourceBuilderBase): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, secretReference };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromKeyVaultSecret',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withEnvironmentFromKeyVaultSecretInternal(name, secretReference));
    }

    /** @internal */
    private async _withAzureUserAssignedIdentityInternal(identityResourceBuilder: AzureUserAssignedIdentityResource): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, identityResourceBuilder };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting.Azure/withUserAssignedIdentityAzureUserAssignedIdentity',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withAzureUserAssignedIdentityInternal(identityResourceBuilder));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<RabbitMQServerResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<RabbitMQServerResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new RabbitMQServerResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for RabbitMQServerResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class RabbitMQServerResourcePromise implements PromiseLike<RabbitMQServerResource> {
    constructor(private _promise: Promise<RabbitMQServerResource>) {}

    then<TResult1 = RabbitMQServerResource, TResult2 = never>(
        onfulfilled?: ((value: RabbitMQServerResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withBindMount(source, target, options)));
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withEntrypoint(entrypoint)));
    }

    /** Sets the container image tag */
    withImageTag(tag: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withImageTag(tag)));
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withImageRegistry(registry)));
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withImage(image, options)));
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withImageSHA256(sha256)));
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withContainerRuntimeArgs(args)));
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withLifetime(lifetime)));
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)));
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.publishAsContainer()));
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withDockerfile(contextPath, options)));
    }

    /** Sets the container name */
    withContainerName(name: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withContainerName(name)));
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withBuildArg(name, value)));
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withBuildSecret(name, value)));
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withContainerNetworkAlias(alias)));
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withMcpServer(options)));
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withOtlpExporter()));
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withOtlpExporterProtocol(protocol)));
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withEnvironment(name, value)));
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withEnvironmentExpression(name, value)));
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withEnvironmentCallback(callback)));
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withEnvironmentCallbackAsync(callback)));
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withEnvironmentEndpoint(name, endpointReference)));
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withEnvironmentParameter(name, parameter)));
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withEnvironmentConnectionString(envVarName, resource)));
    }

    /** Adds a connection property with a reference expression */
    withConnectionProperty(name: string, value: ReferenceExpression): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withConnectionProperty(name, value)));
    }

    /** Adds a connection property with a string value */
    withConnectionPropertyValue(name: string, value: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withConnectionPropertyValue(name, value)));
    }

    /** Adds arguments */
    withArgs(args: string[]): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withArgs(args)));
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withArgsCallback(callback)));
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withArgsCallbackAsync(callback)));
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withReference(source, options)));
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withServiceReference(source)));
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withServiceReferenceNamed(source, name)));
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withReferenceUri(name, uri)));
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withReferenceExternalService(externalService)));
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withReferenceEndpoint(endpointReference)));
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withEndpoint(options)));
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withHttpEndpoint(options)));
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withHttpsEndpoint(options)));
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withExternalHttpEndpoints()));
    }

    /** Gets an endpoint reference */
    getEndpoint(name: string): Promise<EndpointReference> {
        return this._promise.then(obj => obj.getEndpoint(name));
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.asHttp2Service()));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withUrlForEndpointFactory(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.waitFor(dependency)));
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.waitForWithBehavior(dependency, waitBehavior)));
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.waitForStart(dependency)));
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.waitForStartWithBehavior(dependency, waitBehavior)));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.waitForCompletion(dependency, options)));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withHttpHealthCheck(options)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)));
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withCertificateTrustScope(scope)));
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)));
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withoutHttpsCertificate()));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withHttpProbe(probeType, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)));
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withVolume(target, options)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the ConnectionStringAvailable event */
    onConnectionStringAvailable(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.onConnectionStringAvailable(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Adds a data volume to the RabbitMQ container */
    withDataVolume(options?: WithDataVolumeOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withDataVolume(options)));
    }

    /** Adds a data bind mount to the RabbitMQ container */
    withDataBindMount(source: string, options?: WithDataBindMountOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withDataBindMount(source, options)));
    }

    /** Enables the RabbitMQ management plugin */
    withManagementPlugin(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withManagementPlugin()));
    }

    /** Enables the RabbitMQ management plugin with a specific port */
    withManagementPluginWithPort(options?: WithManagementPluginWithPortOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withManagementPluginWithPort(options)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerApp(configure)));
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.publishAsConfiguredAzureContainerAppJob(configure)));
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerAppJob()));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.publishAsConfiguredScheduledAzureContainerAppJob(cronExpression, options)));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.publishAsScheduledAzureContainerAppJob(cronExpression)));
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withEnvironmentFromOutput(name, bicepOutputReference)));
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withEnvironmentFromKeyVaultSecret(name, secretReference)));
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withAzureUserAssignedIdentity(identityResourceBuilder)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): RabbitMQServerResourcePromise {
        return new RabbitMQServerResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// RedisCommanderResource
// ============================================================================

export class RedisCommanderResource extends ResourceBuilderBase<RedisCommanderResourceHandle> {
    constructor(handle: RedisCommanderResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withBindMountInternal(source: string, target: string, isReadOnly?: boolean): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, target };
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withBindMount',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): RedisCommanderResourcePromise {
        const isReadOnly = options?.isReadOnly;
        return new RedisCommanderResourcePromise(this._withBindMountInternal(source, target, isReadOnly));
    }

    /** @internal */
    private async _withEntrypointInternal(entrypoint: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withEntrypoint',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withEntrypointInternal(entrypoint));
    }

    /** @internal */
    private async _withImageTagInternal(tag: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, tag };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withImageTag',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets the container image tag */
    withImageTag(tag: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withImageTagInternal(tag));
    }

    /** @internal */
    private async _withImageRegistryInternal(registry: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withImageRegistry',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withImageRegistryInternal(registry));
    }

    /** @internal */
    private async _withImageInternal(image: string, tag?: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, image };
        if (tag !== undefined) rpcArgs.tag = tag;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withImage',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): RedisCommanderResourcePromise {
        const tag = options?.tag;
        return new RedisCommanderResourcePromise(this._withImageInternal(image, tag));
    }

    /** @internal */
    private async _withImageSHA256Internal(sha256: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withImageSHA256',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withImageSHA256Internal(sha256));
    }

    /** @internal */
    private async _withContainerRuntimeArgsInternal(args: string[]): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withContainerRuntimeArgs',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withContainerRuntimeArgsInternal(args));
    }

    /** @internal */
    private async _withLifetimeInternal(lifetime: ContainerLifetime): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withLifetime',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withLifetimeInternal(lifetime));
    }

    /** @internal */
    private async _withImagePullPolicyInternal(pullPolicy: ImagePullPolicy): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withImagePullPolicy',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withImagePullPolicyInternal(pullPolicy));
    }

    /** @internal */
    private async _publishAsContainerInternal(): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/publishAsContainer',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._publishAsContainerInternal());
    }

    /** @internal */
    private async _withDockerfileInternal(contextPath: string, dockerfilePath?: string, stage?: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined) rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined) rpcArgs.stage = stage;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withDockerfile',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): RedisCommanderResourcePromise {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new RedisCommanderResourcePromise(this._withDockerfileInternal(contextPath, dockerfilePath, stage));
    }

    /** @internal */
    private async _withContainerNameInternal(name: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withContainerName',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets the container name */
    withContainerName(name: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withContainerNameInternal(name));
    }

    /** @internal */
    private async _withBuildArgInternal(name: string, value: ParameterResource): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withBuildArg',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withBuildArgInternal(name, value));
    }

    /** @internal */
    private async _withBuildSecretInternal(name: string, value: ParameterResource): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withBuildSecret',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withBuildSecretInternal(name, value));
    }

    /** @internal */
    private async _withEndpointProxySupportInternal(proxyEnabled: boolean): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withEndpointProxySupport',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withEndpointProxySupportInternal(proxyEnabled));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): RedisCommanderResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new RedisCommanderResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withContainerNetworkAliasInternal(alias: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, alias };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withContainerNetworkAlias',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withContainerNetworkAliasInternal(alias));
    }

    /** @internal */
    private async _withMcpServerInternal(path?: string, endpointName?: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withMcpServer',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): RedisCommanderResourcePromise {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new RedisCommanderResourcePromise(this._withMcpServerInternal(path, endpointName));
    }

    /** @internal */
    private async _withOtlpExporterInternal(): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withOtlpExporter',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withOtlpExporterInternal());
    }

    /** @internal */
    private async _withOtlpExporterProtocolInternal(protocol: OtlpProtocol): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, protocol };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withOtlpExporterProtocol',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withOtlpExporterProtocolInternal(protocol));
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/publishAsConnectionString',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): RedisCommanderResourcePromise {
        const helpLink = options?.helpLink;
        return new RedisCommanderResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withEnvironmentInternal(name: string, value: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withEnvironment',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withEnvironmentInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentExpressionInternal(name: string, value: ReferenceExpression): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withEnvironmentExpression',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withEnvironmentExpressionInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentCallbackInternal(callback: (obj: EnvironmentCallbackContext) => Promise<void>): Promise<RedisCommanderResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as EnvironmentCallbackContextHandle;
            const obj = new EnvironmentCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallback',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withEnvironmentCallbackInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentCallbackAsyncInternal(callback: (arg: EnvironmentCallbackContext) => Promise<void>): Promise<RedisCommanderResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EnvironmentCallbackContextHandle;
            const arg = new EnvironmentCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallbackAsync',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withEnvironmentCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentEndpointInternal(name: string, endpointReference: EndpointReference): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, endpointReference };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withEnvironmentEndpoint',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withEnvironmentEndpointInternal(name, endpointReference));
    }

    /** @internal */
    private async _withEnvironmentParameterInternal(name: string, parameter: ParameterResource): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, parameter };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withEnvironmentParameter',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withEnvironmentParameterInternal(name, parameter));
    }

    /** @internal */
    private async _withEnvironmentConnectionStringInternal(envVarName: string, resource: ResourceBuilderBase): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, envVarName, resource };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withEnvironmentConnectionString',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withEnvironmentConnectionStringInternal(envVarName, resource));
    }

    /** @internal */
    private async _withArgsInternal(args: string[]): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withArgs',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds arguments */
    withArgs(args: string[]): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withArgsInternal(args));
    }

    /** @internal */
    private async _withArgsCallbackInternal(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): Promise<RedisCommanderResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as CommandLineArgsCallbackContextHandle;
            const obj = new CommandLineArgsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withArgsCallback',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withArgsCallbackInternal(callback));
    }

    /** @internal */
    private async _withArgsCallbackAsyncInternal(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): Promise<RedisCommanderResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as CommandLineArgsCallbackContextHandle;
            const arg = new CommandLineArgsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withArgsCallbackAsync',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withArgsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withReferenceInternal(source: ResourceBuilderBase, connectionName?: string, optional?: boolean): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (connectionName !== undefined) rpcArgs.connectionName = connectionName;
        if (optional !== undefined) rpcArgs.optional = optional;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withReference',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): RedisCommanderResourcePromise {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        return new RedisCommanderResourcePromise(this._withReferenceInternal(source, connectionName, optional));
    }

    /** @internal */
    private async _withServiceReferenceInternal(source: ResourceBuilderBase): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withServiceReference',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withServiceReferenceInternal(source));
    }

    /** @internal */
    private async _withServiceReferenceNamedInternal(source: ResourceBuilderBase, name: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, name };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withServiceReferenceNamed',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withServiceReferenceNamedInternal(source, name));
    }

    /** @internal */
    private async _withReferenceUriInternal(name: string, uri: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, uri };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withReferenceUri',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withReferenceUriInternal(name, uri));
    }

    /** @internal */
    private async _withReferenceExternalServiceInternal(externalService: ExternalServiceResource): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, externalService };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withReferenceExternalService',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withReferenceExternalServiceInternal(externalService));
    }

    /** @internal */
    private async _withReferenceEndpointInternal(endpointReference: EndpointReference): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointReference };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withReferenceEndpoint',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withReferenceEndpointInternal(endpointReference));
    }

    /** @internal */
    private async _withEndpointInternal(port?: number, targetPort?: number, scheme?: string, name?: string, env?: string, isProxied?: boolean, isExternal?: boolean, protocol?: ProtocolType): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (scheme !== undefined) rpcArgs.scheme = scheme;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined) rpcArgs.isExternal = isExternal;
        if (protocol !== undefined) rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withEndpoint',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): RedisCommanderResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new RedisCommanderResourcePromise(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol));
    }

    /** @internal */
    private async _withHttpEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withHttpEndpoint',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): RedisCommanderResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new RedisCommanderResourcePromise(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withHttpsEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withHttpsEndpoint',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): RedisCommanderResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new RedisCommanderResourcePromise(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withExternalHttpEndpointsInternal(): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withExternalHttpEndpoints',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withExternalHttpEndpointsInternal());
    }

    /** Gets an endpoint reference */
    async getEndpoint(name: string): Promise<EndpointReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<EndpointReference>(
            'Aspire.Hosting/getEndpoint',
            rpcArgs
        );
    }

    /** @internal */
    private async _asHttp2ServiceInternal(): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/asHttp2Service',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._asHttp2ServiceInternal());
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<RedisCommanderResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<RedisCommanderResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): RedisCommanderResourcePromise {
        const displayText = options?.displayText;
        return new RedisCommanderResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): RedisCommanderResourcePromise {
        const displayText = options?.displayText;
        return new RedisCommanderResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<RedisCommanderResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _withUrlForEndpointFactoryInternal(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): Promise<RedisCommanderResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EndpointReferenceHandle;
            const arg = new EndpointReference(argHandle, this._client);
            return await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withUrlForEndpointFactory',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withUrlForEndpointFactoryInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _waitForInternal(dependency: ResourceBuilderBase): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/waitFor',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._waitForInternal(dependency));
    }

    /** @internal */
    private async _waitForWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/waitForWithBehavior',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._waitForWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _waitForStartInternal(dependency: ResourceBuilderBase): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/waitForStart',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._waitForStartInternal(dependency));
    }

    /** @internal */
    private async _waitForStartWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/waitForStartWithBehavior',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._waitForStartWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _waitForCompletionInternal(dependency: ResourceBuilderBase, exitCode?: number): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        if (exitCode !== undefined) rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/waitForCompletion',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): RedisCommanderResourcePromise {
        const exitCode = options?.exitCode;
        return new RedisCommanderResourcePromise(this._waitForCompletionInternal(dependency, exitCode));
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withHttpHealthCheckInternal(path?: string, statusCode?: number, endpointName?: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (statusCode !== undefined) rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withHttpHealthCheck',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): RedisCommanderResourcePromise {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new RedisCommanderResourcePromise(this._withHttpHealthCheckInternal(path, statusCode, endpointName));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<RedisCommanderResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): RedisCommanderResourcePromise {
        const commandOptions = options?.commandOptions;
        return new RedisCommanderResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withDeveloperCertificateTrustInternal(trust: boolean): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, trust };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withDeveloperCertificateTrust',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withDeveloperCertificateTrustInternal(trust));
    }

    /** @internal */
    private async _withCertificateTrustScopeInternal(scope: CertificateTrustScope): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, scope };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withCertificateTrustScope',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withCertificateTrustScopeInternal(scope));
    }

    /** @internal */
    private async _withHttpsDeveloperCertificateInternal(password?: ParameterResource): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (password !== undefined) rpcArgs.password = password;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withHttpsDeveloperCertificate',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): RedisCommanderResourcePromise {
        const password = options?.password;
        return new RedisCommanderResourcePromise(this._withHttpsDeveloperCertificateInternal(password));
    }

    /** @internal */
    private async _withoutHttpsCertificateInternal(): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withoutHttpsCertificate',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withoutHttpsCertificateInternal());
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): RedisCommanderResourcePromise {
        const iconVariant = options?.iconVariant;
        return new RedisCommanderResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _withHttpProbeInternal(probeType: ProbeType, path?: string, initialDelaySeconds?: number, periodSeconds?: number, timeoutSeconds?: number, failureThreshold?: number, successThreshold?: number, endpointName?: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, probeType };
        if (path !== undefined) rpcArgs.path = path;
        if (initialDelaySeconds !== undefined) rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined) rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined) rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined) rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined) rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withHttpProbe',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): RedisCommanderResourcePromise {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new RedisCommanderResourcePromise(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withRemoteImageNameInternal(remoteImageName: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withRemoteImageName',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withRemoteImageNameInternal(remoteImageName));
    }

    /** @internal */
    private async _withRemoteImageTagInternal(remoteImageTag: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withRemoteImageTag',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withRemoteImageTagInternal(remoteImageTag));
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<RedisCommanderResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): RedisCommanderResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new RedisCommanderResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<RedisCommanderResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<RedisCommanderResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** @internal */
    private async _withVolumeInternal(target: string, name?: string, isReadOnly?: boolean): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, target };
        if (name !== undefined) rpcArgs.name = name;
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/withVolume',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): RedisCommanderResourcePromise {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new RedisCommanderResourcePromise(this._withVolumeInternal(target, name, isReadOnly));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<RedisCommanderResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<RedisCommanderResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<RedisCommanderResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceEndpointsAllocatedInternal(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): Promise<RedisCommanderResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceEndpointsAllocatedEventHandle;
            const arg = new ResourceEndpointsAllocatedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/onResourceEndpointsAllocated',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._onResourceEndpointsAllocatedInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<RedisCommanderResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withHostPortInternal(port?: number): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting.Redis/withRedisCommanderHostPort',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets the host port for Redis Commander */
    withHostPort(options?: WithHostPortOptions): RedisCommanderResourcePromise {
        const port = options?.port;
        return new RedisCommanderResourcePromise(this._withHostPortInternal(port));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _publishAsAzureContainerAppInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): Promise<RedisCommanderResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { container: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishContainerAsAzureContainerApp',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._publishAsAzureContainerAppInternal(configure));
    }

    /** @internal */
    private async _publishAsConfiguredAzureContainerAppJobInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<RedisCommanderResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { resource: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredAzureContainerAppJob',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._publishAsConfiguredAzureContainerAppJobInternal(configure));
    }

    /** @internal */
    private async _publishAsAzureContainerAppJobInternal(): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsAzureContainerAppJob',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._publishAsAzureContainerAppJobInternal());
    }

    /** @internal */
    private async _publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression: string, configure?: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<RedisCommanderResource> {
        const configureId = configure ? registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        if (configure !== undefined) rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): RedisCommanderResourcePromise {
        const configure = options?.configure;
        return new RedisCommanderResourcePromise(this._publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression, configure));
    }

    /** @internal */
    private async _publishAsScheduledAzureContainerAppJobInternal(cronExpression: string): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._publishAsScheduledAzureContainerAppJobInternal(cronExpression));
    }

    /** @internal */
    private async _withEnvironmentFromOutputInternal(name: string, bicepOutputReference: BicepOutputReference): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, bicepOutputReference };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromOutput',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withEnvironmentFromOutputInternal(name, bicepOutputReference));
    }

    /** @internal */
    private async _withEnvironmentFromKeyVaultSecretInternal(name: string, secretReference: ResourceBuilderBase): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, secretReference };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromKeyVaultSecret',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withEnvironmentFromKeyVaultSecretInternal(name, secretReference));
    }

    /** @internal */
    private async _withAzureUserAssignedIdentityInternal(identityResourceBuilder: AzureUserAssignedIdentityResource): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, identityResourceBuilder };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting.Azure/withUserAssignedIdentityAzureUserAssignedIdentity',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withAzureUserAssignedIdentityInternal(identityResourceBuilder));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<RedisCommanderResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<RedisCommanderResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new RedisCommanderResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for RedisCommanderResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class RedisCommanderResourcePromise implements PromiseLike<RedisCommanderResource> {
    constructor(private _promise: Promise<RedisCommanderResource>) {}

    then<TResult1 = RedisCommanderResource, TResult2 = never>(
        onfulfilled?: ((value: RedisCommanderResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withBindMount(source, target, options)));
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withEntrypoint(entrypoint)));
    }

    /** Sets the container image tag */
    withImageTag(tag: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withImageTag(tag)));
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withImageRegistry(registry)));
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withImage(image, options)));
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withImageSHA256(sha256)));
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withContainerRuntimeArgs(args)));
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withLifetime(lifetime)));
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)));
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.publishAsContainer()));
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withDockerfile(contextPath, options)));
    }

    /** Sets the container name */
    withContainerName(name: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withContainerName(name)));
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withBuildArg(name, value)));
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withBuildSecret(name, value)));
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withContainerNetworkAlias(alias)));
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withMcpServer(options)));
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withOtlpExporter()));
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withOtlpExporterProtocol(protocol)));
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withEnvironment(name, value)));
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withEnvironmentExpression(name, value)));
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withEnvironmentCallback(callback)));
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withEnvironmentCallbackAsync(callback)));
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withEnvironmentEndpoint(name, endpointReference)));
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withEnvironmentParameter(name, parameter)));
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withEnvironmentConnectionString(envVarName, resource)));
    }

    /** Adds arguments */
    withArgs(args: string[]): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withArgs(args)));
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withArgsCallback(callback)));
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withArgsCallbackAsync(callback)));
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withReference(source, options)));
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withServiceReference(source)));
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withServiceReferenceNamed(source, name)));
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withReferenceUri(name, uri)));
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withReferenceExternalService(externalService)));
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withReferenceEndpoint(endpointReference)));
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withEndpoint(options)));
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withHttpEndpoint(options)));
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withHttpsEndpoint(options)));
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withExternalHttpEndpoints()));
    }

    /** Gets an endpoint reference */
    getEndpoint(name: string): Promise<EndpointReference> {
        return this._promise.then(obj => obj.getEndpoint(name));
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.asHttp2Service()));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withUrlForEndpointFactory(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.waitFor(dependency)));
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.waitForWithBehavior(dependency, waitBehavior)));
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.waitForStart(dependency)));
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.waitForStartWithBehavior(dependency, waitBehavior)));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.waitForCompletion(dependency, options)));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withHttpHealthCheck(options)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)));
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withCertificateTrustScope(scope)));
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)));
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withoutHttpsCertificate()));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withHttpProbe(probeType, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)));
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withVolume(target, options)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Sets the host port for Redis Commander */
    withHostPort(options?: WithHostPortOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withHostPort(options)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerApp(configure)));
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.publishAsConfiguredAzureContainerAppJob(configure)));
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerAppJob()));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.publishAsConfiguredScheduledAzureContainerAppJob(cronExpression, options)));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.publishAsScheduledAzureContainerAppJob(cronExpression)));
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withEnvironmentFromOutput(name, bicepOutputReference)));
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withEnvironmentFromKeyVaultSecret(name, secretReference)));
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withAzureUserAssignedIdentity(identityResourceBuilder)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): RedisCommanderResourcePromise {
        return new RedisCommanderResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// RedisInsightResource
// ============================================================================

export class RedisInsightResource extends ResourceBuilderBase<RedisInsightResourceHandle> {
    constructor(handle: RedisInsightResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withBindMountInternal(source: string, target: string, isReadOnly?: boolean): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, target };
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withBindMount',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): RedisInsightResourcePromise {
        const isReadOnly = options?.isReadOnly;
        return new RedisInsightResourcePromise(this._withBindMountInternal(source, target, isReadOnly));
    }

    /** @internal */
    private async _withEntrypointInternal(entrypoint: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withEntrypoint',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withEntrypointInternal(entrypoint));
    }

    /** @internal */
    private async _withImageTagInternal(tag: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, tag };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withImageTag',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets the container image tag */
    withImageTag(tag: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withImageTagInternal(tag));
    }

    /** @internal */
    private async _withImageRegistryInternal(registry: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withImageRegistry',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withImageRegistryInternal(registry));
    }

    /** @internal */
    private async _withImageInternal(image: string, tag?: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, image };
        if (tag !== undefined) rpcArgs.tag = tag;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withImage',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): RedisInsightResourcePromise {
        const tag = options?.tag;
        return new RedisInsightResourcePromise(this._withImageInternal(image, tag));
    }

    /** @internal */
    private async _withImageSHA256Internal(sha256: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withImageSHA256',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withImageSHA256Internal(sha256));
    }

    /** @internal */
    private async _withContainerRuntimeArgsInternal(args: string[]): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withContainerRuntimeArgs',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withContainerRuntimeArgsInternal(args));
    }

    /** @internal */
    private async _withLifetimeInternal(lifetime: ContainerLifetime): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withLifetime',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withLifetimeInternal(lifetime));
    }

    /** @internal */
    private async _withImagePullPolicyInternal(pullPolicy: ImagePullPolicy): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withImagePullPolicy',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withImagePullPolicyInternal(pullPolicy));
    }

    /** @internal */
    private async _publishAsContainerInternal(): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/publishAsContainer',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._publishAsContainerInternal());
    }

    /** @internal */
    private async _withDockerfileInternal(contextPath: string, dockerfilePath?: string, stage?: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined) rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined) rpcArgs.stage = stage;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withDockerfile',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): RedisInsightResourcePromise {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new RedisInsightResourcePromise(this._withDockerfileInternal(contextPath, dockerfilePath, stage));
    }

    /** @internal */
    private async _withContainerNameInternal(name: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withContainerName',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets the container name */
    withContainerName(name: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withContainerNameInternal(name));
    }

    /** @internal */
    private async _withBuildArgInternal(name: string, value: ParameterResource): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withBuildArg',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withBuildArgInternal(name, value));
    }

    /** @internal */
    private async _withBuildSecretInternal(name: string, value: ParameterResource): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withBuildSecret',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withBuildSecretInternal(name, value));
    }

    /** @internal */
    private async _withEndpointProxySupportInternal(proxyEnabled: boolean): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withEndpointProxySupport',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withEndpointProxySupportInternal(proxyEnabled));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): RedisInsightResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new RedisInsightResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withContainerNetworkAliasInternal(alias: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, alias };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withContainerNetworkAlias',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withContainerNetworkAliasInternal(alias));
    }

    /** @internal */
    private async _withMcpServerInternal(path?: string, endpointName?: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withMcpServer',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): RedisInsightResourcePromise {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new RedisInsightResourcePromise(this._withMcpServerInternal(path, endpointName));
    }

    /** @internal */
    private async _withOtlpExporterInternal(): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withOtlpExporter',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withOtlpExporterInternal());
    }

    /** @internal */
    private async _withOtlpExporterProtocolInternal(protocol: OtlpProtocol): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, protocol };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withOtlpExporterProtocol',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withOtlpExporterProtocolInternal(protocol));
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/publishAsConnectionString',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): RedisInsightResourcePromise {
        const helpLink = options?.helpLink;
        return new RedisInsightResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withEnvironmentInternal(name: string, value: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withEnvironment',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withEnvironmentInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentExpressionInternal(name: string, value: ReferenceExpression): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withEnvironmentExpression',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withEnvironmentExpressionInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentCallbackInternal(callback: (obj: EnvironmentCallbackContext) => Promise<void>): Promise<RedisInsightResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as EnvironmentCallbackContextHandle;
            const obj = new EnvironmentCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallback',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withEnvironmentCallbackInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentCallbackAsyncInternal(callback: (arg: EnvironmentCallbackContext) => Promise<void>): Promise<RedisInsightResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EnvironmentCallbackContextHandle;
            const arg = new EnvironmentCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallbackAsync',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withEnvironmentCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentEndpointInternal(name: string, endpointReference: EndpointReference): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, endpointReference };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withEnvironmentEndpoint',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withEnvironmentEndpointInternal(name, endpointReference));
    }

    /** @internal */
    private async _withEnvironmentParameterInternal(name: string, parameter: ParameterResource): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, parameter };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withEnvironmentParameter',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withEnvironmentParameterInternal(name, parameter));
    }

    /** @internal */
    private async _withEnvironmentConnectionStringInternal(envVarName: string, resource: ResourceBuilderBase): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, envVarName, resource };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withEnvironmentConnectionString',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withEnvironmentConnectionStringInternal(envVarName, resource));
    }

    /** @internal */
    private async _withArgsInternal(args: string[]): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withArgs',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds arguments */
    withArgs(args: string[]): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withArgsInternal(args));
    }

    /** @internal */
    private async _withArgsCallbackInternal(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): Promise<RedisInsightResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as CommandLineArgsCallbackContextHandle;
            const obj = new CommandLineArgsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withArgsCallback',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withArgsCallbackInternal(callback));
    }

    /** @internal */
    private async _withArgsCallbackAsyncInternal(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): Promise<RedisInsightResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as CommandLineArgsCallbackContextHandle;
            const arg = new CommandLineArgsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withArgsCallbackAsync',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withArgsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withReferenceInternal(source: ResourceBuilderBase, connectionName?: string, optional?: boolean): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (connectionName !== undefined) rpcArgs.connectionName = connectionName;
        if (optional !== undefined) rpcArgs.optional = optional;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withReference',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): RedisInsightResourcePromise {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        return new RedisInsightResourcePromise(this._withReferenceInternal(source, connectionName, optional));
    }

    /** @internal */
    private async _withServiceReferenceInternal(source: ResourceBuilderBase): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withServiceReference',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withServiceReferenceInternal(source));
    }

    /** @internal */
    private async _withServiceReferenceNamedInternal(source: ResourceBuilderBase, name: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, name };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withServiceReferenceNamed',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withServiceReferenceNamedInternal(source, name));
    }

    /** @internal */
    private async _withReferenceUriInternal(name: string, uri: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, uri };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withReferenceUri',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withReferenceUriInternal(name, uri));
    }

    /** @internal */
    private async _withReferenceExternalServiceInternal(externalService: ExternalServiceResource): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, externalService };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withReferenceExternalService',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withReferenceExternalServiceInternal(externalService));
    }

    /** @internal */
    private async _withReferenceEndpointInternal(endpointReference: EndpointReference): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointReference };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withReferenceEndpoint',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withReferenceEndpointInternal(endpointReference));
    }

    /** @internal */
    private async _withEndpointInternal(port?: number, targetPort?: number, scheme?: string, name?: string, env?: string, isProxied?: boolean, isExternal?: boolean, protocol?: ProtocolType): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (scheme !== undefined) rpcArgs.scheme = scheme;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined) rpcArgs.isExternal = isExternal;
        if (protocol !== undefined) rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withEndpoint',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): RedisInsightResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new RedisInsightResourcePromise(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol));
    }

    /** @internal */
    private async _withHttpEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withHttpEndpoint',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): RedisInsightResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new RedisInsightResourcePromise(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withHttpsEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withHttpsEndpoint',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): RedisInsightResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new RedisInsightResourcePromise(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withExternalHttpEndpointsInternal(): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withExternalHttpEndpoints',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withExternalHttpEndpointsInternal());
    }

    /** Gets an endpoint reference */
    async getEndpoint(name: string): Promise<EndpointReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<EndpointReference>(
            'Aspire.Hosting/getEndpoint',
            rpcArgs
        );
    }

    /** @internal */
    private async _asHttp2ServiceInternal(): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/asHttp2Service',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._asHttp2ServiceInternal());
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<RedisInsightResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<RedisInsightResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): RedisInsightResourcePromise {
        const displayText = options?.displayText;
        return new RedisInsightResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): RedisInsightResourcePromise {
        const displayText = options?.displayText;
        return new RedisInsightResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<RedisInsightResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _withUrlForEndpointFactoryInternal(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): Promise<RedisInsightResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EndpointReferenceHandle;
            const arg = new EndpointReference(argHandle, this._client);
            return await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withUrlForEndpointFactory',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withUrlForEndpointFactoryInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _waitForInternal(dependency: ResourceBuilderBase): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/waitFor',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._waitForInternal(dependency));
    }

    /** @internal */
    private async _waitForWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/waitForWithBehavior',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._waitForWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _waitForStartInternal(dependency: ResourceBuilderBase): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/waitForStart',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._waitForStartInternal(dependency));
    }

    /** @internal */
    private async _waitForStartWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/waitForStartWithBehavior',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._waitForStartWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _waitForCompletionInternal(dependency: ResourceBuilderBase, exitCode?: number): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        if (exitCode !== undefined) rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/waitForCompletion',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): RedisInsightResourcePromise {
        const exitCode = options?.exitCode;
        return new RedisInsightResourcePromise(this._waitForCompletionInternal(dependency, exitCode));
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withHttpHealthCheckInternal(path?: string, statusCode?: number, endpointName?: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (statusCode !== undefined) rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withHttpHealthCheck',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): RedisInsightResourcePromise {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new RedisInsightResourcePromise(this._withHttpHealthCheckInternal(path, statusCode, endpointName));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<RedisInsightResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): RedisInsightResourcePromise {
        const commandOptions = options?.commandOptions;
        return new RedisInsightResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withDeveloperCertificateTrustInternal(trust: boolean): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, trust };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withDeveloperCertificateTrust',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withDeveloperCertificateTrustInternal(trust));
    }

    /** @internal */
    private async _withCertificateTrustScopeInternal(scope: CertificateTrustScope): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, scope };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withCertificateTrustScope',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withCertificateTrustScopeInternal(scope));
    }

    /** @internal */
    private async _withHttpsDeveloperCertificateInternal(password?: ParameterResource): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (password !== undefined) rpcArgs.password = password;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withHttpsDeveloperCertificate',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): RedisInsightResourcePromise {
        const password = options?.password;
        return new RedisInsightResourcePromise(this._withHttpsDeveloperCertificateInternal(password));
    }

    /** @internal */
    private async _withoutHttpsCertificateInternal(): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withoutHttpsCertificate',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withoutHttpsCertificateInternal());
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): RedisInsightResourcePromise {
        const iconVariant = options?.iconVariant;
        return new RedisInsightResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _withHttpProbeInternal(probeType: ProbeType, path?: string, initialDelaySeconds?: number, periodSeconds?: number, timeoutSeconds?: number, failureThreshold?: number, successThreshold?: number, endpointName?: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, probeType };
        if (path !== undefined) rpcArgs.path = path;
        if (initialDelaySeconds !== undefined) rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined) rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined) rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined) rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined) rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withHttpProbe',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): RedisInsightResourcePromise {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new RedisInsightResourcePromise(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withRemoteImageNameInternal(remoteImageName: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withRemoteImageName',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withRemoteImageNameInternal(remoteImageName));
    }

    /** @internal */
    private async _withRemoteImageTagInternal(remoteImageTag: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withRemoteImageTag',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withRemoteImageTagInternal(remoteImageTag));
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<RedisInsightResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): RedisInsightResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new RedisInsightResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<RedisInsightResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<RedisInsightResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** @internal */
    private async _withVolumeInternal(target: string, name?: string, isReadOnly?: boolean): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, target };
        if (name !== undefined) rpcArgs.name = name;
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/withVolume',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): RedisInsightResourcePromise {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new RedisInsightResourcePromise(this._withVolumeInternal(target, name, isReadOnly));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<RedisInsightResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<RedisInsightResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<RedisInsightResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceEndpointsAllocatedInternal(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): Promise<RedisInsightResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceEndpointsAllocatedEventHandle;
            const arg = new ResourceEndpointsAllocatedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/onResourceEndpointsAllocated',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._onResourceEndpointsAllocatedInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<RedisInsightResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withHostPortInternal(port?: number): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting.Redis/withRedisInsightHostPort',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets the host port for Redis Insight */
    withHostPort(options?: WithHostPortOptions): RedisInsightResourcePromise {
        const port = options?.port;
        return new RedisInsightResourcePromise(this._withHostPortInternal(port));
    }

    /** @internal */
    private async _withDataVolumeInternal(name?: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (name !== undefined) rpcArgs.name = name;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting.Redis/withRedisInsightDataVolume',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a data volume for Redis Insight */
    withDataVolume(options?: WithDataVolumeOptions): RedisInsightResourcePromise {
        const name = options?.name;
        return new RedisInsightResourcePromise(this._withDataVolumeInternal(name));
    }

    /** @internal */
    private async _withDataBindMountInternal(source: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting.Redis/withRedisInsightDataBindMount',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Adds a data bind mount for Redis Insight */
    withDataBindMount(source: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withDataBindMountInternal(source));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _publishAsAzureContainerAppInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): Promise<RedisInsightResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { container: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishContainerAsAzureContainerApp',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._publishAsAzureContainerAppInternal(configure));
    }

    /** @internal */
    private async _publishAsConfiguredAzureContainerAppJobInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<RedisInsightResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { resource: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredAzureContainerAppJob',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._publishAsConfiguredAzureContainerAppJobInternal(configure));
    }

    /** @internal */
    private async _publishAsAzureContainerAppJobInternal(): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsAzureContainerAppJob',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._publishAsAzureContainerAppJobInternal());
    }

    /** @internal */
    private async _publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression: string, configure?: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<RedisInsightResource> {
        const configureId = configure ? registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        if (configure !== undefined) rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): RedisInsightResourcePromise {
        const configure = options?.configure;
        return new RedisInsightResourcePromise(this._publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression, configure));
    }

    /** @internal */
    private async _publishAsScheduledAzureContainerAppJobInternal(cronExpression: string): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._publishAsScheduledAzureContainerAppJobInternal(cronExpression));
    }

    /** @internal */
    private async _withEnvironmentFromOutputInternal(name: string, bicepOutputReference: BicepOutputReference): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, bicepOutputReference };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromOutput',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withEnvironmentFromOutputInternal(name, bicepOutputReference));
    }

    /** @internal */
    private async _withEnvironmentFromKeyVaultSecretInternal(name: string, secretReference: ResourceBuilderBase): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, secretReference };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromKeyVaultSecret',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withEnvironmentFromKeyVaultSecretInternal(name, secretReference));
    }

    /** @internal */
    private async _withAzureUserAssignedIdentityInternal(identityResourceBuilder: AzureUserAssignedIdentityResource): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, identityResourceBuilder };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting.Azure/withUserAssignedIdentityAzureUserAssignedIdentity',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withAzureUserAssignedIdentityInternal(identityResourceBuilder));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<RedisInsightResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<RedisInsightResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new RedisInsightResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for RedisInsightResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class RedisInsightResourcePromise implements PromiseLike<RedisInsightResource> {
    constructor(private _promise: Promise<RedisInsightResource>) {}

    then<TResult1 = RedisInsightResource, TResult2 = never>(
        onfulfilled?: ((value: RedisInsightResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withBindMount(source, target, options)));
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withEntrypoint(entrypoint)));
    }

    /** Sets the container image tag */
    withImageTag(tag: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withImageTag(tag)));
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withImageRegistry(registry)));
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withImage(image, options)));
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withImageSHA256(sha256)));
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withContainerRuntimeArgs(args)));
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withLifetime(lifetime)));
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)));
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.publishAsContainer()));
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withDockerfile(contextPath, options)));
    }

    /** Sets the container name */
    withContainerName(name: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withContainerName(name)));
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withBuildArg(name, value)));
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withBuildSecret(name, value)));
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withContainerNetworkAlias(alias)));
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withMcpServer(options)));
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withOtlpExporter()));
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withOtlpExporterProtocol(protocol)));
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withEnvironment(name, value)));
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withEnvironmentExpression(name, value)));
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withEnvironmentCallback(callback)));
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withEnvironmentCallbackAsync(callback)));
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withEnvironmentEndpoint(name, endpointReference)));
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withEnvironmentParameter(name, parameter)));
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withEnvironmentConnectionString(envVarName, resource)));
    }

    /** Adds arguments */
    withArgs(args: string[]): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withArgs(args)));
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withArgsCallback(callback)));
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withArgsCallbackAsync(callback)));
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withReference(source, options)));
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withServiceReference(source)));
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withServiceReferenceNamed(source, name)));
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withReferenceUri(name, uri)));
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withReferenceExternalService(externalService)));
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withReferenceEndpoint(endpointReference)));
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withEndpoint(options)));
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withHttpEndpoint(options)));
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withHttpsEndpoint(options)));
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withExternalHttpEndpoints()));
    }

    /** Gets an endpoint reference */
    getEndpoint(name: string): Promise<EndpointReference> {
        return this._promise.then(obj => obj.getEndpoint(name));
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.asHttp2Service()));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withUrlForEndpointFactory(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.waitFor(dependency)));
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.waitForWithBehavior(dependency, waitBehavior)));
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.waitForStart(dependency)));
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.waitForStartWithBehavior(dependency, waitBehavior)));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.waitForCompletion(dependency, options)));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withHttpHealthCheck(options)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)));
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withCertificateTrustScope(scope)));
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)));
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withoutHttpsCertificate()));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withHttpProbe(probeType, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)));
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withVolume(target, options)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Sets the host port for Redis Insight */
    withHostPort(options?: WithHostPortOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withHostPort(options)));
    }

    /** Adds a data volume for Redis Insight */
    withDataVolume(options?: WithDataVolumeOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withDataVolume(options)));
    }

    /** Adds a data bind mount for Redis Insight */
    withDataBindMount(source: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withDataBindMount(source)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerApp(configure)));
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.publishAsConfiguredAzureContainerAppJob(configure)));
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerAppJob()));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.publishAsConfiguredScheduledAzureContainerAppJob(cronExpression, options)));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.publishAsScheduledAzureContainerAppJob(cronExpression)));
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withEnvironmentFromOutput(name, bicepOutputReference)));
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withEnvironmentFromKeyVaultSecret(name, secretReference)));
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withAzureUserAssignedIdentity(identityResourceBuilder)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): RedisInsightResourcePromise {
        return new RedisInsightResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// RedisResource
// ============================================================================

export class RedisResource extends ResourceBuilderBase<RedisResourceHandle> {
    constructor(handle: RedisResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** Gets the PrimaryEndpoint property */
    primaryEndpoint = {
        get: async (): Promise<EndpointReference> => {
            const handle = await this._client.invokeCapability<EndpointReferenceHandle>(
                'Aspire.Hosting.ApplicationModel/RedisResource.primaryEndpoint',
                { context: this._handle }
            );
            return new EndpointReference(handle, this._client);
        },
    };

    /** Gets the Host property */
    host = {
        get: async (): Promise<EndpointReferenceExpression> => {
            const handle = await this._client.invokeCapability<EndpointReferenceExpressionHandle>(
                'Aspire.Hosting.ApplicationModel/RedisResource.host',
                { context: this._handle }
            );
            return new EndpointReferenceExpression(handle, this._client);
        },
    };

    /** Gets the Port property */
    port = {
        get: async (): Promise<EndpointReferenceExpression> => {
            const handle = await this._client.invokeCapability<EndpointReferenceExpressionHandle>(
                'Aspire.Hosting.ApplicationModel/RedisResource.port',
                { context: this._handle }
            );
            return new EndpointReferenceExpression(handle, this._client);
        },
    };

    /** Gets the PasswordParameter property */
    passwordParameter = {
        get: async (): Promise<ParameterResource> => {
            const handle = await this._client.invokeCapability<ParameterResourceHandle>(
                'Aspire.Hosting.ApplicationModel/RedisResource.passwordParameter',
                { context: this._handle }
            );
            return new ParameterResource(handle, this._client);
        },
    };

    /** Gets the TlsEnabled property */
    tlsEnabled = {
        get: async (): Promise<boolean> => {
            return await this._client.invokeCapability<boolean>(
                'Aspire.Hosting.ApplicationModel/RedisResource.tlsEnabled',
                { context: this._handle }
            );
        },
    };

    /** Gets the ConnectionStringExpression property */
    connectionStringExpression = {
        get: async (): Promise<ReferenceExpression> => {
            return await this._client.invokeCapability<ReferenceExpression>(
                'Aspire.Hosting.ApplicationModel/RedisResource.connectionStringExpression',
                { context: this._handle }
            );
        },
    };

    /** Gets the UriExpression property */
    uriExpression = {
        get: async (): Promise<ReferenceExpression> => {
            return await this._client.invokeCapability<ReferenceExpression>(
                'Aspire.Hosting.ApplicationModel/RedisResource.uriExpression',
                { context: this._handle }
            );
        },
    };

    /** Gets the Entrypoint property */
    entrypoint = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/RedisResource.entrypoint',
                { context: this._handle }
            );
        },
        set: async (value: string): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.ApplicationModel/RedisResource.setEntrypoint',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the ShellExecution property */
    shellExecution = {
        get: async (): Promise<boolean> => {
            return await this._client.invokeCapability<boolean>(
                'Aspire.Hosting.ApplicationModel/RedisResource.shellExecution',
                { context: this._handle }
            );
        },
        set: async (value: boolean): Promise<void> => {
            await this._client.invokeCapability<void>(
                'Aspire.Hosting.ApplicationModel/RedisResource.setShellExecution',
                { context: this._handle, value }
            );
        }
    };

    /** Gets the Name property */
    name = {
        get: async (): Promise<string> => {
            return await this._client.invokeCapability<string>(
                'Aspire.Hosting.ApplicationModel/RedisResource.name',
                { context: this._handle }
            );
        },
    };

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): RedisResourcePromise {
        return new RedisResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withBindMountInternal(source: string, target: string, isReadOnly?: boolean): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, target };
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withBindMount',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): RedisResourcePromise {
        const isReadOnly = options?.isReadOnly;
        return new RedisResourcePromise(this._withBindMountInternal(source, target, isReadOnly));
    }

    /** @internal */
    private async _withEntrypointInternal(entrypoint: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withEntrypoint',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): RedisResourcePromise {
        return new RedisResourcePromise(this._withEntrypointInternal(entrypoint));
    }

    /** @internal */
    private async _withImageTagInternal(tag: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, tag };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withImageTag',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets the container image tag */
    withImageTag(tag: string): RedisResourcePromise {
        return new RedisResourcePromise(this._withImageTagInternal(tag));
    }

    /** @internal */
    private async _withImageRegistryInternal(registry: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withImageRegistry',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): RedisResourcePromise {
        return new RedisResourcePromise(this._withImageRegistryInternal(registry));
    }

    /** @internal */
    private async _withImageInternal(image: string, tag?: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, image };
        if (tag !== undefined) rpcArgs.tag = tag;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withImage',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): RedisResourcePromise {
        const tag = options?.tag;
        return new RedisResourcePromise(this._withImageInternal(image, tag));
    }

    /** @internal */
    private async _withImageSHA256Internal(sha256: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withImageSHA256',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): RedisResourcePromise {
        return new RedisResourcePromise(this._withImageSHA256Internal(sha256));
    }

    /** @internal */
    private async _withContainerRuntimeArgsInternal(args: string[]): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withContainerRuntimeArgs',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): RedisResourcePromise {
        return new RedisResourcePromise(this._withContainerRuntimeArgsInternal(args));
    }

    /** @internal */
    private async _withLifetimeInternal(lifetime: ContainerLifetime): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withLifetime',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): RedisResourcePromise {
        return new RedisResourcePromise(this._withLifetimeInternal(lifetime));
    }

    /** @internal */
    private async _withImagePullPolicyInternal(pullPolicy: ImagePullPolicy): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withImagePullPolicy',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): RedisResourcePromise {
        return new RedisResourcePromise(this._withImagePullPolicyInternal(pullPolicy));
    }

    /** @internal */
    private async _publishAsContainerInternal(): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/publishAsContainer',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): RedisResourcePromise {
        return new RedisResourcePromise(this._publishAsContainerInternal());
    }

    /** @internal */
    private async _withDockerfileInternal(contextPath: string, dockerfilePath?: string, stage?: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined) rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined) rpcArgs.stage = stage;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withDockerfile',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): RedisResourcePromise {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new RedisResourcePromise(this._withDockerfileInternal(contextPath, dockerfilePath, stage));
    }

    /** @internal */
    private async _withContainerNameInternal(name: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withContainerName',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets the container name */
    withContainerName(name: string): RedisResourcePromise {
        return new RedisResourcePromise(this._withContainerNameInternal(name));
    }

    /** @internal */
    private async _withBuildArgInternal(name: string, value: ParameterResource): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withBuildArg',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): RedisResourcePromise {
        return new RedisResourcePromise(this._withBuildArgInternal(name, value));
    }

    /** @internal */
    private async _withBuildSecretInternal(name: string, value: ParameterResource): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withBuildSecret',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): RedisResourcePromise {
        return new RedisResourcePromise(this._withBuildSecretInternal(name, value));
    }

    /** @internal */
    private async _withEndpointProxySupportInternal(proxyEnabled: boolean): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withEndpointProxySupport',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): RedisResourcePromise {
        return new RedisResourcePromise(this._withEndpointProxySupportInternal(proxyEnabled));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): RedisResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new RedisResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withContainerNetworkAliasInternal(alias: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, alias };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withContainerNetworkAlias',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): RedisResourcePromise {
        return new RedisResourcePromise(this._withContainerNetworkAliasInternal(alias));
    }

    /** @internal */
    private async _withMcpServerInternal(path?: string, endpointName?: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withMcpServer',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): RedisResourcePromise {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new RedisResourcePromise(this._withMcpServerInternal(path, endpointName));
    }

    /** @internal */
    private async _withOtlpExporterInternal(): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withOtlpExporter',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): RedisResourcePromise {
        return new RedisResourcePromise(this._withOtlpExporterInternal());
    }

    /** @internal */
    private async _withOtlpExporterProtocolInternal(protocol: OtlpProtocol): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, protocol };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withOtlpExporterProtocol',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): RedisResourcePromise {
        return new RedisResourcePromise(this._withOtlpExporterProtocolInternal(protocol));
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/publishAsConnectionString',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): RedisResourcePromise {
        return new RedisResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): RedisResourcePromise {
        const helpLink = options?.helpLink;
        return new RedisResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withEnvironmentInternal(name: string, value: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withEnvironment',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): RedisResourcePromise {
        return new RedisResourcePromise(this._withEnvironmentInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentExpressionInternal(name: string, value: ReferenceExpression): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withEnvironmentExpression',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): RedisResourcePromise {
        return new RedisResourcePromise(this._withEnvironmentExpressionInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentCallbackInternal(callback: (obj: EnvironmentCallbackContext) => Promise<void>): Promise<RedisResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as EnvironmentCallbackContextHandle;
            const obj = new EnvironmentCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallback',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._withEnvironmentCallbackInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentCallbackAsyncInternal(callback: (arg: EnvironmentCallbackContext) => Promise<void>): Promise<RedisResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EnvironmentCallbackContextHandle;
            const arg = new EnvironmentCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallbackAsync',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._withEnvironmentCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentEndpointInternal(name: string, endpointReference: EndpointReference): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, endpointReference };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withEnvironmentEndpoint',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): RedisResourcePromise {
        return new RedisResourcePromise(this._withEnvironmentEndpointInternal(name, endpointReference));
    }

    /** @internal */
    private async _withEnvironmentParameterInternal(name: string, parameter: ParameterResource): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, parameter };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withEnvironmentParameter',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): RedisResourcePromise {
        return new RedisResourcePromise(this._withEnvironmentParameterInternal(name, parameter));
    }

    /** @internal */
    private async _withEnvironmentConnectionStringInternal(envVarName: string, resource: ResourceBuilderBase): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, envVarName, resource };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withEnvironmentConnectionString',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): RedisResourcePromise {
        return new RedisResourcePromise(this._withEnvironmentConnectionStringInternal(envVarName, resource));
    }

    /** @internal */
    private async _withConnectionPropertyInternal(name: string, value: ReferenceExpression): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withConnectionProperty',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a connection property with a reference expression */
    withConnectionProperty(name: string, value: ReferenceExpression): RedisResourcePromise {
        return new RedisResourcePromise(this._withConnectionPropertyInternal(name, value));
    }

    /** @internal */
    private async _withConnectionPropertyValueInternal(name: string, value: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withConnectionPropertyValue',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a connection property with a string value */
    withConnectionPropertyValue(name: string, value: string): RedisResourcePromise {
        return new RedisResourcePromise(this._withConnectionPropertyValueInternal(name, value));
    }

    /** @internal */
    private async _withArgsInternal(args: string[]): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withArgs',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds arguments */
    withArgs(args: string[]): RedisResourcePromise {
        return new RedisResourcePromise(this._withArgsInternal(args));
    }

    /** @internal */
    private async _withArgsCallbackInternal(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): Promise<RedisResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as CommandLineArgsCallbackContextHandle;
            const obj = new CommandLineArgsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withArgsCallback',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._withArgsCallbackInternal(callback));
    }

    /** @internal */
    private async _withArgsCallbackAsyncInternal(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): Promise<RedisResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as CommandLineArgsCallbackContextHandle;
            const arg = new CommandLineArgsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withArgsCallbackAsync',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._withArgsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withReferenceInternal(source: ResourceBuilderBase, connectionName?: string, optional?: boolean): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (connectionName !== undefined) rpcArgs.connectionName = connectionName;
        if (optional !== undefined) rpcArgs.optional = optional;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withReference',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): RedisResourcePromise {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        return new RedisResourcePromise(this._withReferenceInternal(source, connectionName, optional));
    }

    /** @internal */
    private async _withServiceReferenceInternal(source: ResourceBuilderBase): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withServiceReference',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): RedisResourcePromise {
        return new RedisResourcePromise(this._withServiceReferenceInternal(source));
    }

    /** @internal */
    private async _withServiceReferenceNamedInternal(source: ResourceBuilderBase, name: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, name };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withServiceReferenceNamed',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): RedisResourcePromise {
        return new RedisResourcePromise(this._withServiceReferenceNamedInternal(source, name));
    }

    /** @internal */
    private async _withReferenceUriInternal(name: string, uri: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, uri };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withReferenceUri',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): RedisResourcePromise {
        return new RedisResourcePromise(this._withReferenceUriInternal(name, uri));
    }

    /** @internal */
    private async _withReferenceExternalServiceInternal(externalService: ExternalServiceResource): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, externalService };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withReferenceExternalService',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): RedisResourcePromise {
        return new RedisResourcePromise(this._withReferenceExternalServiceInternal(externalService));
    }

    /** @internal */
    private async _withReferenceEndpointInternal(endpointReference: EndpointReference): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointReference };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withReferenceEndpoint',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): RedisResourcePromise {
        return new RedisResourcePromise(this._withReferenceEndpointInternal(endpointReference));
    }

    /** @internal */
    private async _withEndpointInternal(port?: number, targetPort?: number, scheme?: string, name?: string, env?: string, isProxied?: boolean, isExternal?: boolean, protocol?: ProtocolType): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (scheme !== undefined) rpcArgs.scheme = scheme;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined) rpcArgs.isExternal = isExternal;
        if (protocol !== undefined) rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withEndpoint',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): RedisResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new RedisResourcePromise(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol));
    }

    /** @internal */
    private async _withHttpEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withHttpEndpoint',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): RedisResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new RedisResourcePromise(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withHttpsEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withHttpsEndpoint',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): RedisResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new RedisResourcePromise(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withExternalHttpEndpointsInternal(): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withExternalHttpEndpoints',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): RedisResourcePromise {
        return new RedisResourcePromise(this._withExternalHttpEndpointsInternal());
    }

    /** Gets an endpoint reference */
    async getEndpoint(name: string): Promise<EndpointReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<EndpointReference>(
            'Aspire.Hosting/getEndpoint',
            rpcArgs
        );
    }

    /** @internal */
    private async _asHttp2ServiceInternal(): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/asHttp2Service',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): RedisResourcePromise {
        return new RedisResourcePromise(this._asHttp2ServiceInternal());
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<RedisResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<RedisResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): RedisResourcePromise {
        const displayText = options?.displayText;
        return new RedisResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): RedisResourcePromise {
        const displayText = options?.displayText;
        return new RedisResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<RedisResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _withUrlForEndpointFactoryInternal(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): Promise<RedisResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EndpointReferenceHandle;
            const arg = new EndpointReference(argHandle, this._client);
            return await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withUrlForEndpointFactory',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): RedisResourcePromise {
        return new RedisResourcePromise(this._withUrlForEndpointFactoryInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): RedisResourcePromise {
        return new RedisResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _waitForInternal(dependency: ResourceBuilderBase): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/waitFor',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): RedisResourcePromise {
        return new RedisResourcePromise(this._waitForInternal(dependency));
    }

    /** @internal */
    private async _waitForWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/waitForWithBehavior',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): RedisResourcePromise {
        return new RedisResourcePromise(this._waitForWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _waitForStartInternal(dependency: ResourceBuilderBase): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/waitForStart',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): RedisResourcePromise {
        return new RedisResourcePromise(this._waitForStartInternal(dependency));
    }

    /** @internal */
    private async _waitForStartWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/waitForStartWithBehavior',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): RedisResourcePromise {
        return new RedisResourcePromise(this._waitForStartWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): RedisResourcePromise {
        return new RedisResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _waitForCompletionInternal(dependency: ResourceBuilderBase, exitCode?: number): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        if (exitCode !== undefined) rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/waitForCompletion',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): RedisResourcePromise {
        const exitCode = options?.exitCode;
        return new RedisResourcePromise(this._waitForCompletionInternal(dependency, exitCode));
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): RedisResourcePromise {
        return new RedisResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withHttpHealthCheckInternal(path?: string, statusCode?: number, endpointName?: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (statusCode !== undefined) rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withHttpHealthCheck',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): RedisResourcePromise {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new RedisResourcePromise(this._withHttpHealthCheckInternal(path, statusCode, endpointName));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<RedisResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): RedisResourcePromise {
        const commandOptions = options?.commandOptions;
        return new RedisResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withDeveloperCertificateTrustInternal(trust: boolean): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, trust };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withDeveloperCertificateTrust',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): RedisResourcePromise {
        return new RedisResourcePromise(this._withDeveloperCertificateTrustInternal(trust));
    }

    /** @internal */
    private async _withCertificateTrustScopeInternal(scope: CertificateTrustScope): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, scope };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withCertificateTrustScope',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): RedisResourcePromise {
        return new RedisResourcePromise(this._withCertificateTrustScopeInternal(scope));
    }

    /** @internal */
    private async _withHttpsDeveloperCertificateInternal(password?: ParameterResource): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (password !== undefined) rpcArgs.password = password;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withHttpsDeveloperCertificate',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): RedisResourcePromise {
        const password = options?.password;
        return new RedisResourcePromise(this._withHttpsDeveloperCertificateInternal(password));
    }

    /** @internal */
    private async _withoutHttpsCertificateInternal(): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withoutHttpsCertificate',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): RedisResourcePromise {
        return new RedisResourcePromise(this._withoutHttpsCertificateInternal());
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): RedisResourcePromise {
        return new RedisResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): RedisResourcePromise {
        return new RedisResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): RedisResourcePromise {
        const iconVariant = options?.iconVariant;
        return new RedisResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _withHttpProbeInternal(probeType: ProbeType, path?: string, initialDelaySeconds?: number, periodSeconds?: number, timeoutSeconds?: number, failureThreshold?: number, successThreshold?: number, endpointName?: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, probeType };
        if (path !== undefined) rpcArgs.path = path;
        if (initialDelaySeconds !== undefined) rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined) rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined) rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined) rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined) rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withHttpProbe',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): RedisResourcePromise {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new RedisResourcePromise(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): RedisResourcePromise {
        return new RedisResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withRemoteImageNameInternal(remoteImageName: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withRemoteImageName',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): RedisResourcePromise {
        return new RedisResourcePromise(this._withRemoteImageNameInternal(remoteImageName));
    }

    /** @internal */
    private async _withRemoteImageTagInternal(remoteImageTag: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withRemoteImageTag',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): RedisResourcePromise {
        return new RedisResourcePromise(this._withRemoteImageTagInternal(remoteImageTag));
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<RedisResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): RedisResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new RedisResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<RedisResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<RedisResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** @internal */
    private async _withVolumeInternal(target: string, name?: string, isReadOnly?: boolean): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, target };
        if (name !== undefined) rpcArgs.name = name;
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/withVolume',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): RedisResourcePromise {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new RedisResourcePromise(this._withVolumeInternal(target, name, isReadOnly));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<RedisResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<RedisResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onConnectionStringAvailableInternal(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): Promise<RedisResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ConnectionStringAvailableEventHandle;
            const arg = new ConnectionStringAvailableEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/onConnectionStringAvailable',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Subscribes to the ConnectionStringAvailable event */
    onConnectionStringAvailable(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._onConnectionStringAvailableInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<RedisResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceEndpointsAllocatedInternal(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): Promise<RedisResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceEndpointsAllocatedEventHandle;
            const arg = new ResourceEndpointsAllocatedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/onResourceEndpointsAllocated',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._onResourceEndpointsAllocatedInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<RedisResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withRedisCommanderInternal(configureContainer?: (obj: RedisCommanderResource) => Promise<void>, containerName?: string): Promise<RedisResource> {
        const configureContainerId = configureContainer ? registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as RedisCommanderResourceHandle;
            const obj = new RedisCommanderResource(objHandle, this._client);
            await configureContainer(obj);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (configureContainer !== undefined) rpcArgs.configureContainer = configureContainerId;
        if (containerName !== undefined) rpcArgs.containerName = containerName;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Redis/withRedisCommander',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds Redis Commander management UI */
    withRedisCommander(options?: WithRedisCommanderOptions): RedisResourcePromise {
        const configureContainer = options?.configureContainer;
        const containerName = options?.containerName;
        return new RedisResourcePromise(this._withRedisCommanderInternal(configureContainer, containerName));
    }

    /** @internal */
    private async _withRedisInsightInternal(configureContainer?: (obj: RedisInsightResource) => Promise<void>, containerName?: string): Promise<RedisResource> {
        const configureContainerId = configureContainer ? registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as RedisInsightResourceHandle;
            const obj = new RedisInsightResource(objHandle, this._client);
            await configureContainer(obj);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (configureContainer !== undefined) rpcArgs.configureContainer = configureContainerId;
        if (containerName !== undefined) rpcArgs.containerName = containerName;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Redis/withRedisInsight',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds Redis Insight management UI */
    withRedisInsight(options?: WithRedisInsightOptions): RedisResourcePromise {
        const configureContainer = options?.configureContainer;
        const containerName = options?.containerName;
        return new RedisResourcePromise(this._withRedisInsightInternal(configureContainer, containerName));
    }

    /** @internal */
    private async _withDataVolumeInternal(name?: string, isReadOnly?: boolean): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (name !== undefined) rpcArgs.name = name;
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Redis/withDataVolume',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a data volume with persistence */
    withDataVolume(options?: WithDataVolumeOptions): RedisResourcePromise {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new RedisResourcePromise(this._withDataVolumeInternal(name, isReadOnly));
    }

    /** @internal */
    private async _withDataBindMountInternal(source: string, isReadOnly?: boolean): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Redis/withDataBindMount',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Adds a data bind mount with persistence */
    withDataBindMount(source: string, options?: WithDataBindMountOptions): RedisResourcePromise {
        const isReadOnly = options?.isReadOnly;
        return new RedisResourcePromise(this._withDataBindMountInternal(source, isReadOnly));
    }

    /** @internal */
    private async _withPersistenceInternal(interval?: number, keysChangedThreshold?: number): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (interval !== undefined) rpcArgs.interval = interval;
        if (keysChangedThreshold !== undefined) rpcArgs.keysChangedThreshold = keysChangedThreshold;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Redis/withPersistence',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures Redis persistence */
    withPersistence(options?: WithPersistenceOptions): RedisResourcePromise {
        const interval = options?.interval;
        const keysChangedThreshold = options?.keysChangedThreshold;
        return new RedisResourcePromise(this._withPersistenceInternal(interval, keysChangedThreshold));
    }

    /** @internal */
    private async _withPasswordInternal(password: ParameterResource): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, password };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Redis/withPassword',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures the password for Redis */
    withPassword(password: ParameterResource): RedisResourcePromise {
        return new RedisResourcePromise(this._withPasswordInternal(password));
    }

    /** @internal */
    private async _withHostPortInternal(port?: number): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Redis/withHostPort',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets the host port for Redis */
    withHostPort(options?: WithHostPortOptions): RedisResourcePromise {
        const port = options?.port;
        return new RedisResourcePromise(this._withHostPortInternal(port));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): RedisResourcePromise {
        return new RedisResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _publishAsAzureContainerAppInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): Promise<RedisResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { container: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishContainerAsAzureContainerApp',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._publishAsAzureContainerAppInternal(configure));
    }

    /** @internal */
    private async _publishAsConfiguredAzureContainerAppJobInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<RedisResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { resource: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredAzureContainerAppJob',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._publishAsConfiguredAzureContainerAppJobInternal(configure));
    }

    /** @internal */
    private async _publishAsAzureContainerAppJobInternal(): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsAzureContainerAppJob',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): RedisResourcePromise {
        return new RedisResourcePromise(this._publishAsAzureContainerAppJobInternal());
    }

    /** @internal */
    private async _publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression: string, configure?: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<RedisResource> {
        const configureId = configure ? registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        if (configure !== undefined) rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): RedisResourcePromise {
        const configure = options?.configure;
        return new RedisResourcePromise(this._publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression, configure));
    }

    /** @internal */
    private async _publishAsScheduledAzureContainerAppJobInternal(cronExpression: string): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): RedisResourcePromise {
        return new RedisResourcePromise(this._publishAsScheduledAzureContainerAppJobInternal(cronExpression));
    }

    /** @internal */
    private async _withEnvironmentFromOutputInternal(name: string, bicepOutputReference: BicepOutputReference): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, bicepOutputReference };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromOutput',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): RedisResourcePromise {
        return new RedisResourcePromise(this._withEnvironmentFromOutputInternal(name, bicepOutputReference));
    }

    /** @internal */
    private async _withEnvironmentFromKeyVaultSecretInternal(name: string, secretReference: ResourceBuilderBase): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, secretReference };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromKeyVaultSecret',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): RedisResourcePromise {
        return new RedisResourcePromise(this._withEnvironmentFromKeyVaultSecretInternal(name, secretReference));
    }

    /** @internal */
    private async _withAzureUserAssignedIdentityInternal(identityResourceBuilder: AzureUserAssignedIdentityResource): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, identityResourceBuilder };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Azure/withUserAssignedIdentityAzureUserAssignedIdentity',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): RedisResourcePromise {
        return new RedisResourcePromise(this._withAzureUserAssignedIdentityInternal(identityResourceBuilder));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): RedisResourcePromise {
        return new RedisResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<RedisResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<RedisResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new RedisResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): RedisResourcePromise {
        return new RedisResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for RedisResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class RedisResourcePromise implements PromiseLike<RedisResource> {
    constructor(private _promise: Promise<RedisResource>) {}

    then<TResult1 = RedisResource, TResult2 = never>(
        onfulfilled?: ((value: RedisResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withBindMount(source, target, options)));
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withEntrypoint(entrypoint)));
    }

    /** Sets the container image tag */
    withImageTag(tag: string): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withImageTag(tag)));
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withImageRegistry(registry)));
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withImage(image, options)));
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withImageSHA256(sha256)));
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withContainerRuntimeArgs(args)));
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withLifetime(lifetime)));
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)));
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.publishAsContainer()));
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withDockerfile(contextPath, options)));
    }

    /** Sets the container name */
    withContainerName(name: string): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withContainerName(name)));
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withBuildArg(name, value)));
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withBuildSecret(name, value)));
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withContainerNetworkAlias(alias)));
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withMcpServer(options)));
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withOtlpExporter()));
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withOtlpExporterProtocol(protocol)));
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withEnvironment(name, value)));
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withEnvironmentExpression(name, value)));
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withEnvironmentCallback(callback)));
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withEnvironmentCallbackAsync(callback)));
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withEnvironmentEndpoint(name, endpointReference)));
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withEnvironmentParameter(name, parameter)));
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withEnvironmentConnectionString(envVarName, resource)));
    }

    /** Adds a connection property with a reference expression */
    withConnectionProperty(name: string, value: ReferenceExpression): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withConnectionProperty(name, value)));
    }

    /** Adds a connection property with a string value */
    withConnectionPropertyValue(name: string, value: string): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withConnectionPropertyValue(name, value)));
    }

    /** Adds arguments */
    withArgs(args: string[]): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withArgs(args)));
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withArgsCallback(callback)));
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withArgsCallbackAsync(callback)));
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withReference(source, options)));
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withServiceReference(source)));
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withServiceReferenceNamed(source, name)));
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withReferenceUri(name, uri)));
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withReferenceExternalService(externalService)));
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withReferenceEndpoint(endpointReference)));
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withEndpoint(options)));
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withHttpEndpoint(options)));
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withHttpsEndpoint(options)));
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withExternalHttpEndpoints()));
    }

    /** Gets an endpoint reference */
    getEndpoint(name: string): Promise<EndpointReference> {
        return this._promise.then(obj => obj.getEndpoint(name));
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.asHttp2Service()));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withUrlForEndpointFactory(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.waitFor(dependency)));
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.waitForWithBehavior(dependency, waitBehavior)));
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.waitForStart(dependency)));
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.waitForStartWithBehavior(dependency, waitBehavior)));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.waitForCompletion(dependency, options)));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withHttpHealthCheck(options)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)));
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withCertificateTrustScope(scope)));
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)));
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withoutHttpsCertificate()));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withHttpProbe(probeType, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)));
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withVolume(target, options)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the ConnectionStringAvailable event */
    onConnectionStringAvailable(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.onConnectionStringAvailable(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Adds Redis Commander management UI */
    withRedisCommander(options?: WithRedisCommanderOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withRedisCommander(options)));
    }

    /** Adds Redis Insight management UI */
    withRedisInsight(options?: WithRedisInsightOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withRedisInsight(options)));
    }

    /** Adds a data volume with persistence */
    withDataVolume(options?: WithDataVolumeOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withDataVolume(options)));
    }

    /** Adds a data bind mount with persistence */
    withDataBindMount(source: string, options?: WithDataBindMountOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withDataBindMount(source, options)));
    }

    /** Configures Redis persistence */
    withPersistence(options?: WithPersistenceOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withPersistence(options)));
    }

    /** Configures the password for Redis */
    withPassword(password: ParameterResource): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withPassword(password)));
    }

    /** Sets the host port for Redis */
    withHostPort(options?: WithHostPortOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withHostPort(options)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerApp(configure)));
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.publishAsConfiguredAzureContainerAppJob(configure)));
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerAppJob()));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.publishAsConfiguredScheduledAzureContainerAppJob(cronExpression, options)));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.publishAsScheduledAzureContainerAppJob(cronExpression)));
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withEnvironmentFromOutput(name, bicepOutputReference)));
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withEnvironmentFromKeyVaultSecret(name, secretReference)));
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withAzureUserAssignedIdentity(identityResourceBuilder)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): RedisResourcePromise {
        return new RedisResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// YarpResource
// ============================================================================

export class YarpResource extends ResourceBuilderBase<YarpResourceHandle> {
    constructor(handle: YarpResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withBindMountInternal(source: string, target: string, isReadOnly?: boolean): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, target };
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withBindMount',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): YarpResourcePromise {
        const isReadOnly = options?.isReadOnly;
        return new YarpResourcePromise(this._withBindMountInternal(source, target, isReadOnly));
    }

    /** @internal */
    private async _withEntrypointInternal(entrypoint: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, entrypoint };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withEntrypoint',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): YarpResourcePromise {
        return new YarpResourcePromise(this._withEntrypointInternal(entrypoint));
    }

    /** @internal */
    private async _withImageTagInternal(tag: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, tag };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withImageTag',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets the container image tag */
    withImageTag(tag: string): YarpResourcePromise {
        return new YarpResourcePromise(this._withImageTagInternal(tag));
    }

    /** @internal */
    private async _withImageRegistryInternal(registry: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withImageRegistry',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): YarpResourcePromise {
        return new YarpResourcePromise(this._withImageRegistryInternal(registry));
    }

    /** @internal */
    private async _withImageInternal(image: string, tag?: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, image };
        if (tag !== undefined) rpcArgs.tag = tag;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withImage',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): YarpResourcePromise {
        const tag = options?.tag;
        return new YarpResourcePromise(this._withImageInternal(image, tag));
    }

    /** @internal */
    private async _withImageSHA256Internal(sha256: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, sha256 };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withImageSHA256',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): YarpResourcePromise {
        return new YarpResourcePromise(this._withImageSHA256Internal(sha256));
    }

    /** @internal */
    private async _withContainerRuntimeArgsInternal(args: string[]): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withContainerRuntimeArgs',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): YarpResourcePromise {
        return new YarpResourcePromise(this._withContainerRuntimeArgsInternal(args));
    }

    /** @internal */
    private async _withLifetimeInternal(lifetime: ContainerLifetime): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, lifetime };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withLifetime',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): YarpResourcePromise {
        return new YarpResourcePromise(this._withLifetimeInternal(lifetime));
    }

    /** @internal */
    private async _withImagePullPolicyInternal(pullPolicy: ImagePullPolicy): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, pullPolicy };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withImagePullPolicy',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): YarpResourcePromise {
        return new YarpResourcePromise(this._withImagePullPolicyInternal(pullPolicy));
    }

    /** @internal */
    private async _publishAsContainerInternal(): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/publishAsContainer',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): YarpResourcePromise {
        return new YarpResourcePromise(this._publishAsContainerInternal());
    }

    /** @internal */
    private async _withDockerfileInternal(contextPath: string, dockerfilePath?: string, stage?: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, contextPath };
        if (dockerfilePath !== undefined) rpcArgs.dockerfilePath = dockerfilePath;
        if (stage !== undefined) rpcArgs.stage = stage;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withDockerfile',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): YarpResourcePromise {
        const dockerfilePath = options?.dockerfilePath;
        const stage = options?.stage;
        return new YarpResourcePromise(this._withDockerfileInternal(contextPath, dockerfilePath, stage));
    }

    /** @internal */
    private async _withContainerNameInternal(name: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withContainerName',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets the container name */
    withContainerName(name: string): YarpResourcePromise {
        return new YarpResourcePromise(this._withContainerNameInternal(name));
    }

    /** @internal */
    private async _withBuildArgInternal(name: string, value: ParameterResource): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withBuildArg',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): YarpResourcePromise {
        return new YarpResourcePromise(this._withBuildArgInternal(name, value));
    }

    /** @internal */
    private async _withBuildSecretInternal(name: string, value: ParameterResource): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withBuildSecret',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): YarpResourcePromise {
        return new YarpResourcePromise(this._withBuildSecretInternal(name, value));
    }

    /** @internal */
    private async _withEndpointProxySupportInternal(proxyEnabled: boolean): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, proxyEnabled };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withEndpointProxySupport',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): YarpResourcePromise {
        return new YarpResourcePromise(this._withEndpointProxySupportInternal(proxyEnabled));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): YarpResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new YarpResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withContainerNetworkAliasInternal(alias: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, alias };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withContainerNetworkAlias',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): YarpResourcePromise {
        return new YarpResourcePromise(this._withContainerNetworkAliasInternal(alias));
    }

    /** @internal */
    private async _withMcpServerInternal(path?: string, endpointName?: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withMcpServer',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): YarpResourcePromise {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new YarpResourcePromise(this._withMcpServerInternal(path, endpointName));
    }

    /** @internal */
    private async _withOtlpExporterInternal(): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withOtlpExporter',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): YarpResourcePromise {
        return new YarpResourcePromise(this._withOtlpExporterInternal());
    }

    /** @internal */
    private async _withOtlpExporterProtocolInternal(protocol: OtlpProtocol): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, protocol };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withOtlpExporterProtocol',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): YarpResourcePromise {
        return new YarpResourcePromise(this._withOtlpExporterProtocolInternal(protocol));
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/publishAsConnectionString',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): YarpResourcePromise {
        return new YarpResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): YarpResourcePromise {
        const helpLink = options?.helpLink;
        return new YarpResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withEnvironmentInternal(name: string, value: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withEnvironment',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): YarpResourcePromise {
        return new YarpResourcePromise(this._withEnvironmentInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentExpressionInternal(name: string, value: ReferenceExpression): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withEnvironmentExpression',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): YarpResourcePromise {
        return new YarpResourcePromise(this._withEnvironmentExpressionInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentCallbackInternal(callback: (obj: EnvironmentCallbackContext) => Promise<void>): Promise<YarpResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as EnvironmentCallbackContextHandle;
            const obj = new EnvironmentCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallback',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._withEnvironmentCallbackInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentCallbackAsyncInternal(callback: (arg: EnvironmentCallbackContext) => Promise<void>): Promise<YarpResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EnvironmentCallbackContextHandle;
            const arg = new EnvironmentCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withEnvironmentCallbackAsync',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._withEnvironmentCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentEndpointInternal(name: string, endpointReference: EndpointReference): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, endpointReference };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withEnvironmentEndpoint',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): YarpResourcePromise {
        return new YarpResourcePromise(this._withEnvironmentEndpointInternal(name, endpointReference));
    }

    /** @internal */
    private async _withEnvironmentParameterInternal(name: string, parameter: ParameterResource): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, parameter };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withEnvironmentParameter',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): YarpResourcePromise {
        return new YarpResourcePromise(this._withEnvironmentParameterInternal(name, parameter));
    }

    /** @internal */
    private async _withEnvironmentConnectionStringInternal(envVarName: string, resource: ResourceBuilderBase): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, envVarName, resource };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withEnvironmentConnectionString',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._withEnvironmentConnectionStringInternal(envVarName, resource));
    }

    /** @internal */
    private async _withArgsInternal(args: string[]): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withArgs',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds arguments */
    withArgs(args: string[]): YarpResourcePromise {
        return new YarpResourcePromise(this._withArgsInternal(args));
    }

    /** @internal */
    private async _withArgsCallbackInternal(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): Promise<YarpResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as CommandLineArgsCallbackContextHandle;
            const obj = new CommandLineArgsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withArgsCallback',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._withArgsCallbackInternal(callback));
    }

    /** @internal */
    private async _withArgsCallbackAsyncInternal(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): Promise<YarpResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as CommandLineArgsCallbackContextHandle;
            const arg = new CommandLineArgsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withArgsCallbackAsync',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._withArgsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withReferenceInternal(source: ResourceBuilderBase, connectionName?: string, optional?: boolean): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (connectionName !== undefined) rpcArgs.connectionName = connectionName;
        if (optional !== undefined) rpcArgs.optional = optional;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withReference',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): YarpResourcePromise {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        return new YarpResourcePromise(this._withReferenceInternal(source, connectionName, optional));
    }

    /** @internal */
    private async _withServiceReferenceInternal(source: ResourceBuilderBase): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withServiceReference',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._withServiceReferenceInternal(source));
    }

    /** @internal */
    private async _withServiceReferenceNamedInternal(source: ResourceBuilderBase, name: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, name };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withServiceReferenceNamed',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): YarpResourcePromise {
        return new YarpResourcePromise(this._withServiceReferenceNamedInternal(source, name));
    }

    /** @internal */
    private async _withReferenceUriInternal(name: string, uri: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, uri };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withReferenceUri',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): YarpResourcePromise {
        return new YarpResourcePromise(this._withReferenceUriInternal(name, uri));
    }

    /** @internal */
    private async _withReferenceExternalServiceInternal(externalService: ExternalServiceResource): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, externalService };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withReferenceExternalService',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): YarpResourcePromise {
        return new YarpResourcePromise(this._withReferenceExternalServiceInternal(externalService));
    }

    /** @internal */
    private async _withReferenceEndpointInternal(endpointReference: EndpointReference): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointReference };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withReferenceEndpoint',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): YarpResourcePromise {
        return new YarpResourcePromise(this._withReferenceEndpointInternal(endpointReference));
    }

    /** @internal */
    private async _withEndpointInternal(port?: number, targetPort?: number, scheme?: string, name?: string, env?: string, isProxied?: boolean, isExternal?: boolean, protocol?: ProtocolType): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (scheme !== undefined) rpcArgs.scheme = scheme;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined) rpcArgs.isExternal = isExternal;
        if (protocol !== undefined) rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withEndpoint',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): YarpResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new YarpResourcePromise(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol));
    }

    /** @internal */
    private async _withHttpEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withHttpEndpoint',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): YarpResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new YarpResourcePromise(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withHttpsEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withHttpsEndpoint',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): YarpResourcePromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new YarpResourcePromise(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withExternalHttpEndpointsInternal(): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withExternalHttpEndpoints',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): YarpResourcePromise {
        return new YarpResourcePromise(this._withExternalHttpEndpointsInternal());
    }

    /** Gets an endpoint reference */
    async getEndpoint(name: string): Promise<EndpointReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<EndpointReference>(
            'Aspire.Hosting/getEndpoint',
            rpcArgs
        );
    }

    /** @internal */
    private async _asHttp2ServiceInternal(): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/asHttp2Service',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): YarpResourcePromise {
        return new YarpResourcePromise(this._asHttp2ServiceInternal());
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<YarpResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<YarpResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): YarpResourcePromise {
        const displayText = options?.displayText;
        return new YarpResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): YarpResourcePromise {
        const displayText = options?.displayText;
        return new YarpResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<YarpResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _withUrlForEndpointFactoryInternal(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): Promise<YarpResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EndpointReferenceHandle;
            const arg = new EndpointReference(argHandle, this._client);
            return await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withUrlForEndpointFactory',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): YarpResourcePromise {
        return new YarpResourcePromise(this._withUrlForEndpointFactoryInternal(endpointName, callback));
    }

    /** @internal */
    private async _publishWithContainerFilesInternal(source: ResourceBuilderBase, destinationPath: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, destinationPath };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/publishWithContainerFiles',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures the resource to copy container files from the specified source during publishing */
    publishWithContainerFiles(source: ResourceBuilderBase, destinationPath: string): YarpResourcePromise {
        return new YarpResourcePromise(this._publishWithContainerFilesInternal(source, destinationPath));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): YarpResourcePromise {
        return new YarpResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _waitForInternal(dependency: ResourceBuilderBase): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/waitFor',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._waitForInternal(dependency));
    }

    /** @internal */
    private async _waitForWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/waitForWithBehavior',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): YarpResourcePromise {
        return new YarpResourcePromise(this._waitForWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _waitForStartInternal(dependency: ResourceBuilderBase): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/waitForStart',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._waitForStartInternal(dependency));
    }

    /** @internal */
    private async _waitForStartWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/waitForStartWithBehavior',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): YarpResourcePromise {
        return new YarpResourcePromise(this._waitForStartWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): YarpResourcePromise {
        return new YarpResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _waitForCompletionInternal(dependency: ResourceBuilderBase, exitCode?: number): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        if (exitCode !== undefined) rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/waitForCompletion',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): YarpResourcePromise {
        const exitCode = options?.exitCode;
        return new YarpResourcePromise(this._waitForCompletionInternal(dependency, exitCode));
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): YarpResourcePromise {
        return new YarpResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withHttpHealthCheckInternal(path?: string, statusCode?: number, endpointName?: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (statusCode !== undefined) rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withHttpHealthCheck',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): YarpResourcePromise {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new YarpResourcePromise(this._withHttpHealthCheckInternal(path, statusCode, endpointName));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<YarpResource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): YarpResourcePromise {
        const commandOptions = options?.commandOptions;
        return new YarpResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withDeveloperCertificateTrustInternal(trust: boolean): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, trust };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withDeveloperCertificateTrust',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): YarpResourcePromise {
        return new YarpResourcePromise(this._withDeveloperCertificateTrustInternal(trust));
    }

    /** @internal */
    private async _withCertificateTrustScopeInternal(scope: CertificateTrustScope): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, scope };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withCertificateTrustScope',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): YarpResourcePromise {
        return new YarpResourcePromise(this._withCertificateTrustScopeInternal(scope));
    }

    /** @internal */
    private async _withHttpsDeveloperCertificateInternal(password?: ParameterResource): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (password !== undefined) rpcArgs.password = password;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withHttpsDeveloperCertificate',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): YarpResourcePromise {
        const password = options?.password;
        return new YarpResourcePromise(this._withHttpsDeveloperCertificateInternal(password));
    }

    /** @internal */
    private async _withoutHttpsCertificateInternal(): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withoutHttpsCertificate',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): YarpResourcePromise {
        return new YarpResourcePromise(this._withoutHttpsCertificateInternal());
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): YarpResourcePromise {
        const iconVariant = options?.iconVariant;
        return new YarpResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _withHttpProbeInternal(probeType: ProbeType, path?: string, initialDelaySeconds?: number, periodSeconds?: number, timeoutSeconds?: number, failureThreshold?: number, successThreshold?: number, endpointName?: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, probeType };
        if (path !== undefined) rpcArgs.path = path;
        if (initialDelaySeconds !== undefined) rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined) rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined) rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined) rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined) rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withHttpProbe',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): YarpResourcePromise {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new YarpResourcePromise(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): YarpResourcePromise {
        return new YarpResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withRemoteImageNameInternal(remoteImageName: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withRemoteImageName',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): YarpResourcePromise {
        return new YarpResourcePromise(this._withRemoteImageNameInternal(remoteImageName));
    }

    /** @internal */
    private async _withRemoteImageTagInternal(remoteImageTag: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withRemoteImageTag',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): YarpResourcePromise {
        return new YarpResourcePromise(this._withRemoteImageTagInternal(remoteImageTag));
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<YarpResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): YarpResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new YarpResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<YarpResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<YarpResource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** @internal */
    private async _withVolumeInternal(target: string, name?: string, isReadOnly?: boolean): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, target };
        if (name !== undefined) rpcArgs.name = name;
        if (isReadOnly !== undefined) rpcArgs.isReadOnly = isReadOnly;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/withVolume',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): YarpResourcePromise {
        const name = options?.name;
        const isReadOnly = options?.isReadOnly;
        return new YarpResourcePromise(this._withVolumeInternal(target, name, isReadOnly));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<YarpResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<YarpResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<YarpResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceEndpointsAllocatedInternal(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): Promise<YarpResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceEndpointsAllocatedEventHandle;
            const arg = new ResourceEndpointsAllocatedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/onResourceEndpointsAllocated',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._onResourceEndpointsAllocatedInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<YarpResource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): YarpResourcePromise {
        return new YarpResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _withConfigurationInternal(configurationBuilder: (obj: YarpConfigurationBuilder) => Promise<void>): Promise<YarpResource> {
        const configurationBuilderId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as IYarpConfigurationBuilderHandle;
            const obj = new YarpConfigurationBuilder(objHandle, this._client);
            await configurationBuilder(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, configurationBuilder: configurationBuilderId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting.Yarp/withConfiguration',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configure the YARP resource. */
    withConfiguration(configurationBuilder: (obj: YarpConfigurationBuilder) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._withConfigurationInternal(configurationBuilder));
    }

    /** @internal */
    private async _withHostPortInternal(port?: number): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting.Yarp/withHostPort',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures the host port that the YARP resource is exposed on instead of using randomly assigned port. */
    withHostPort(options?: WithHostPortOptions): YarpResourcePromise {
        const port = options?.port;
        return new YarpResourcePromise(this._withHostPortInternal(port));
    }

    /** @internal */
    private async _withHostHttpsPortInternal(port?: number): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting.Yarp/withHostHttpsPort',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures the host HTTPS port that the YARP resource is exposed on instead of using randomly assigned port. */
    withHostHttpsPort(options?: WithHostHttpsPortOptions): YarpResourcePromise {
        const port = options?.port;
        return new YarpResourcePromise(this._withHostHttpsPortInternal(port));
    }

    /** @internal */
    private async _withStaticFilesInternal(): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting.Yarp/withStaticFiles1',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Enables static file serving in the YARP resource. Static files are served from the wwwroot folder. */
    withStaticFiles(): YarpResourcePromise {
        return new YarpResourcePromise(this._withStaticFilesInternal());
    }

    /** @internal */
    private async _publishWithStaticFilesInternal(resourceWithFiles: ResourceBuilderBase): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, resourceWithFiles };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting.Yarp/publishWithStaticFiles',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** In publish mode, generates a Dockerfile that copies static files from the specified resource into /app/wwwroot. */
    publishWithStaticFiles(resourceWithFiles: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._publishWithStaticFilesInternal(resourceWithFiles));
    }

    /** @internal */
    private async _publishAsAzureContainerAppInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): Promise<YarpResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { container: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishContainerAsAzureContainerApp',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._publishAsAzureContainerAppInternal(configure));
    }

    /** @internal */
    private async _publishAsConfiguredAzureContainerAppJobInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<YarpResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { resource: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredAzureContainerAppJob',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._publishAsConfiguredAzureContainerAppJobInternal(configure));
    }

    /** @internal */
    private async _publishAsAzureContainerAppJobInternal(): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsAzureContainerAppJob',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): YarpResourcePromise {
        return new YarpResourcePromise(this._publishAsAzureContainerAppJobInternal());
    }

    /** @internal */
    private async _publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression: string, configure?: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<YarpResource> {
        const configureId = configure ? registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        if (configure !== undefined) rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): YarpResourcePromise {
        const configure = options?.configure;
        return new YarpResourcePromise(this._publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression, configure));
    }

    /** @internal */
    private async _publishAsScheduledAzureContainerAppJobInternal(cronExpression: string): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): YarpResourcePromise {
        return new YarpResourcePromise(this._publishAsScheduledAzureContainerAppJobInternal(cronExpression));
    }

    /** @internal */
    private async _withEnvironmentFromOutputInternal(name: string, bicepOutputReference: BicepOutputReference): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, bicepOutputReference };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromOutput',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): YarpResourcePromise {
        return new YarpResourcePromise(this._withEnvironmentFromOutputInternal(name, bicepOutputReference));
    }

    /** @internal */
    private async _withEnvironmentFromKeyVaultSecretInternal(name: string, secretReference: ResourceBuilderBase): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, secretReference };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromKeyVaultSecret',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._withEnvironmentFromKeyVaultSecretInternal(name, secretReference));
    }

    /** @internal */
    private async _withAzureUserAssignedIdentityInternal(identityResourceBuilder: AzureUserAssignedIdentityResource): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, identityResourceBuilder };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting.Azure/withUserAssignedIdentityAzureUserAssignedIdentity',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): YarpResourcePromise {
        return new YarpResourcePromise(this._withAzureUserAssignedIdentityInternal(identityResourceBuilder));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): YarpResourcePromise {
        return new YarpResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<YarpResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<YarpResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new YarpResource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): YarpResourcePromise {
        return new YarpResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for YarpResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class YarpResourcePromise implements PromiseLike<YarpResource> {
    constructor(private _promise: Promise<YarpResource>) {}

    then<TResult1 = YarpResource, TResult2 = never>(
        onfulfilled?: ((value: YarpResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Adds a bind mount */
    withBindMount(source: string, target: string, options?: WithBindMountOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withBindMount(source, target, options)));
    }

    /** Sets the container entrypoint */
    withEntrypoint(entrypoint: string): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withEntrypoint(entrypoint)));
    }

    /** Sets the container image tag */
    withImageTag(tag: string): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withImageTag(tag)));
    }

    /** Sets the container image registry */
    withImageRegistry(registry: string): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withImageRegistry(registry)));
    }

    /** Sets the container image */
    withImage(image: string, options?: WithImageOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withImage(image, options)));
    }

    /** Sets the image SHA256 digest */
    withImageSHA256(sha256: string): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withImageSHA256(sha256)));
    }

    /** Adds runtime arguments for the container */
    withContainerRuntimeArgs(args: string[]): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withContainerRuntimeArgs(args)));
    }

    /** Sets the lifetime behavior of the container resource */
    withLifetime(lifetime: ContainerLifetime): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withLifetime(lifetime)));
    }

    /** Sets the container image pull policy */
    withImagePullPolicy(pullPolicy: ImagePullPolicy): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withImagePullPolicy(pullPolicy)));
    }

    /** Configures the resource to be published as a container */
    publishAsContainer(): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.publishAsContainer()));
    }

    /** Configures the resource to use a Dockerfile */
    withDockerfile(contextPath: string, options?: WithDockerfileOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withDockerfile(contextPath, options)));
    }

    /** Sets the container name */
    withContainerName(name: string): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withContainerName(name)));
    }

    /** Adds a build argument from a parameter resource */
    withBuildArg(name: string, value: ParameterResource): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withBuildArg(name, value)));
    }

    /** Adds a build secret from a parameter resource */
    withBuildSecret(name: string, value: ParameterResource): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withBuildSecret(name, value)));
    }

    /** Configures endpoint proxy support */
    withEndpointProxySupport(proxyEnabled: boolean): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withEndpointProxySupport(proxyEnabled)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a network alias for the container */
    withContainerNetworkAlias(alias: string): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withContainerNetworkAlias(alias)));
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withMcpServer(options)));
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withOtlpExporter()));
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withOtlpExporterProtocol(protocol)));
    }

    /** Publishes the resource as a connection string */
    publishAsConnectionString(): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withEnvironment(name, value)));
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withEnvironmentExpression(name, value)));
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withEnvironmentCallback(callback)));
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withEnvironmentCallbackAsync(callback)));
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withEnvironmentEndpoint(name, endpointReference)));
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withEnvironmentParameter(name, parameter)));
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withEnvironmentConnectionString(envVarName, resource)));
    }

    /** Adds arguments */
    withArgs(args: string[]): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withArgs(args)));
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withArgsCallback(callback)));
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withArgsCallbackAsync(callback)));
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withReference(source, options)));
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withServiceReference(source)));
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withServiceReferenceNamed(source, name)));
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withReferenceUri(name, uri)));
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withReferenceExternalService(externalService)));
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withReferenceEndpoint(endpointReference)));
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withEndpoint(options)));
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withHttpEndpoint(options)));
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withHttpsEndpoint(options)));
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withExternalHttpEndpoints()));
    }

    /** Gets an endpoint reference */
    getEndpoint(name: string): Promise<EndpointReference> {
        return this._promise.then(obj => obj.getEndpoint(name));
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.asHttp2Service()));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withUrlForEndpointFactory(endpointName, callback)));
    }

    /** Configures the resource to copy container files from the specified source during publishing */
    publishWithContainerFiles(source: ResourceBuilderBase, destinationPath: string): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.publishWithContainerFiles(source, destinationPath)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.waitFor(dependency)));
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.waitForWithBehavior(dependency, waitBehavior)));
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.waitForStart(dependency)));
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.waitForStartWithBehavior(dependency, waitBehavior)));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.waitForCompletion(dependency, options)));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withHttpHealthCheck(options)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)));
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withCertificateTrustScope(scope)));
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)));
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withoutHttpsCertificate()));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withHttpProbe(probeType, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)));
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Adds a volume */
    withVolume(target: string, options?: WithVolumeOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withVolume(target, options)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configure the YARP resource. */
    withConfiguration(configurationBuilder: (obj: YarpConfigurationBuilder) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withConfiguration(configurationBuilder)));
    }

    /** Configures the host port that the YARP resource is exposed on instead of using randomly assigned port. */
    withHostPort(options?: WithHostPortOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withHostPort(options)));
    }

    /** Configures the host HTTPS port that the YARP resource is exposed on instead of using randomly assigned port. */
    withHostHttpsPort(options?: WithHostHttpsPortOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withHostHttpsPort(options)));
    }

    /** Enables static file serving in the YARP resource. Static files are served from the wwwroot folder. */
    withStaticFiles(): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withStaticFiles()));
    }

    /** In publish mode, generates a Dockerfile that copies static files from the specified resource into /app/wwwroot. */
    publishWithStaticFiles(resourceWithFiles: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.publishWithStaticFiles(resourceWithFiles)));
    }

    /** Configures the container resource to be published as an Azure Container App */
    publishAsAzureContainerApp(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppHandle) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerApp(configure)));
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.publishAsConfiguredAzureContainerAppJob(configure)));
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerAppJob()));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.publishAsConfiguredScheduledAzureContainerAppJob(cronExpression, options)));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.publishAsScheduledAzureContainerAppJob(cronExpression)));
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withEnvironmentFromOutput(name, bicepOutputReference)));
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withEnvironmentFromKeyVaultSecret(name, secretReference)));
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withAzureUserAssignedIdentity(identityResourceBuilder)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): YarpResourcePromise {
        return new YarpResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// AzureResource
// ============================================================================

export class AzureResource extends ResourceBuilderBase<IAzureResourceHandle> {
    constructor(handle: IAzureResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _publishAsConnectionStringInternal(): Promise<AzureResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<IAzureResourceHandle>(
            'Aspire.Hosting.Azure/publishAsConnectionString',
            rpcArgs
        );
        return new AzureResource(result, this._client);
    }

    /** Publishes an Azure resource to the manifest as a connection string */
    publishAsConnectionString(): AzureResourcePromise {
        return new AzureResourcePromise(this._publishAsConnectionStringInternal());
    }

    /** Gets the normalized Bicep identifier for an Azure resource */
    async getBicepIdentifier(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting.Azure/getBicepIdentifier',
            rpcArgs
        );
    }

    /** @internal */
    private async _clearDefaultRoleAssignmentsInternal(): Promise<AzureResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<IAzureResourceHandle>(
            'Aspire.Hosting.Azure/clearDefaultRoleAssignments',
            rpcArgs
        );
        return new AzureResource(result, this._client);
    }

    /** Clears the default Azure role assignments from a resource */
    clearDefaultRoleAssignments(): AzureResourcePromise {
        return new AzureResourcePromise(this._clearDefaultRoleAssignmentsInternal());
    }

    /** Determines whether a resource is marked as existing */
    async isExisting(): Promise<boolean> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<boolean>(
            'Aspire.Hosting.Azure/isExisting',
            rpcArgs
        );
    }

    /** @internal */
    private async _runAsExistingFromParametersInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<IAzureResourceHandle>(
            'Aspire.Hosting.Azure/runAsExistingFromParameters',
            rpcArgs
        );
        return new AzureResource(result, this._client);
    }

    /** Marks an Azure resource as existing in run mode by using parameter resources */
    runAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureResourcePromise {
        return new AzureResourcePromise(this._runAsExistingFromParametersInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _runAsExistingInternal(name: string, resourceGroup: string): Promise<AzureResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, resourceGroup };
        const result = await this._client.invokeCapability<IAzureResourceHandle>(
            'Aspire.Hosting.Azure/runAsExisting',
            rpcArgs
        );
        return new AzureResource(result, this._client);
    }

    /** Marks an Azure resource as existing in run mode */
    runAsExisting(name: string, resourceGroup: string): AzureResourcePromise {
        return new AzureResourcePromise(this._runAsExistingInternal(name, resourceGroup));
    }

    /** @internal */
    private async _publishAsExistingFromParametersInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<IAzureResourceHandle>(
            'Aspire.Hosting.Azure/publishAsExistingFromParameters',
            rpcArgs
        );
        return new AzureResource(result, this._client);
    }

    /** Marks an Azure resource as existing in publish mode by using parameter resources */
    publishAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureResourcePromise {
        return new AzureResourcePromise(this._publishAsExistingFromParametersInternal(nameParameter, resourceGroupParameter));
    }

    /** @internal */
    private async _publishAsExistingInternal(name: string, resourceGroup: string): Promise<AzureResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, resourceGroup };
        const result = await this._client.invokeCapability<IAzureResourceHandle>(
            'Aspire.Hosting.Azure/publishAsExisting',
            rpcArgs
        );
        return new AzureResource(result, this._client);
    }

    /** Marks an Azure resource as existing in publish mode */
    publishAsExisting(name: string, resourceGroup: string): AzureResourcePromise {
        return new AzureResourcePromise(this._publishAsExistingInternal(name, resourceGroup));
    }

    /** @internal */
    private async _asExistingInternal(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): Promise<AzureResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, nameParameter, resourceGroupParameter };
        const result = await this._client.invokeCapability<IAzureResourceHandle>(
            'Aspire.Hosting.Azure/asExistingFromParameters',
            rpcArgs
        );
        return new AzureResource(result, this._client);
    }

    /** Marks an Azure resource as existing in both run and publish modes by using parameter resources */
    asExisting(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureResourcePromise {
        return new AzureResourcePromise(this._asExistingInternal(nameParameter, resourceGroupParameter));
    }

}

/**
 * Thenable wrapper for AzureResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class AzureResourcePromise implements PromiseLike<AzureResource> {
    constructor(private _promise: Promise<AzureResource>) {}

    then<TResult1 = AzureResource, TResult2 = never>(
        onfulfilled?: ((value: AzureResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Publishes an Azure resource to the manifest as a connection string */
    publishAsConnectionString(): AzureResourcePromise {
        return new AzureResourcePromise(this._promise.then(obj => obj.publishAsConnectionString()));
    }

    /** Gets the normalized Bicep identifier for an Azure resource */
    getBicepIdentifier(): Promise<string> {
        return this._promise.then(obj => obj.getBicepIdentifier());
    }

    /** Clears the default Azure role assignments from a resource */
    clearDefaultRoleAssignments(): AzureResourcePromise {
        return new AzureResourcePromise(this._promise.then(obj => obj.clearDefaultRoleAssignments()));
    }

    /** Determines whether a resource is marked as existing */
    isExisting(): Promise<boolean> {
        return this._promise.then(obj => obj.isExisting());
    }

    /** Marks an Azure resource as existing in run mode by using parameter resources */
    runAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureResourcePromise {
        return new AzureResourcePromise(this._promise.then(obj => obj.runAsExistingFromParameters(nameParameter, resourceGroupParameter)));
    }

    /** Marks an Azure resource as existing in run mode */
    runAsExisting(name: string, resourceGroup: string): AzureResourcePromise {
        return new AzureResourcePromise(this._promise.then(obj => obj.runAsExisting(name, resourceGroup)));
    }

    /** Marks an Azure resource as existing in publish mode by using parameter resources */
    publishAsExistingFromParameters(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureResourcePromise {
        return new AzureResourcePromise(this._promise.then(obj => obj.publishAsExistingFromParameters(nameParameter, resourceGroupParameter)));
    }

    /** Marks an Azure resource as existing in publish mode */
    publishAsExisting(name: string, resourceGroup: string): AzureResourcePromise {
        return new AzureResourcePromise(this._promise.then(obj => obj.publishAsExisting(name, resourceGroup)));
    }

    /** Marks an Azure resource as existing in both run and publish modes by using parameter resources */
    asExisting(nameParameter: ParameterResource, resourceGroupParameter: ParameterResource): AzureResourcePromise {
        return new AzureResourcePromise(this._promise.then(obj => obj.asExisting(nameParameter, resourceGroupParameter)));
    }

}

// ============================================================================
// ComputeResource
// ============================================================================

export class ComputeResource extends ResourceBuilderBase<IComputeResourceHandle> {
    constructor(handle: IComputeResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withRemoteImageNameInternal(remoteImageName: string): Promise<ComputeResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageName };
        const result = await this._client.invokeCapability<IComputeResourceHandle>(
            'Aspire.Hosting/withRemoteImageName',
            rpcArgs
        );
        return new ComputeResource(result, this._client);
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): ComputeResourcePromise {
        return new ComputeResourcePromise(this._withRemoteImageNameInternal(remoteImageName));
    }

    /** @internal */
    private async _withRemoteImageTagInternal(remoteImageTag: string): Promise<ComputeResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, remoteImageTag };
        const result = await this._client.invokeCapability<IComputeResourceHandle>(
            'Aspire.Hosting/withRemoteImageTag',
            rpcArgs
        );
        return new ComputeResource(result, this._client);
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): ComputeResourcePromise {
        return new ComputeResourcePromise(this._withRemoteImageTagInternal(remoteImageTag));
    }

    /** @internal */
    private async _publishAsConfiguredAzureContainerAppJobInternal(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<ComputeResource> {
        const configureId = registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        });
        const rpcArgs: Record<string, unknown> = { resource: this._handle, configure: configureId };
        const result = await this._client.invokeCapability<IComputeResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredAzureContainerAppJob',
            rpcArgs
        );
        return new ComputeResource(result, this._client);
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): ComputeResourcePromise {
        return new ComputeResourcePromise(this._publishAsConfiguredAzureContainerAppJobInternal(configure));
    }

    /** @internal */
    private async _publishAsAzureContainerAppJobInternal(): Promise<ComputeResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        const result = await this._client.invokeCapability<IComputeResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsAzureContainerAppJob',
            rpcArgs
        );
        return new ComputeResource(result, this._client);
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): ComputeResourcePromise {
        return new ComputeResourcePromise(this._publishAsAzureContainerAppJobInternal());
    }

    /** @internal */
    private async _publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression: string, configure?: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): Promise<ComputeResource> {
        const configureId = configure ? registerCallback(async (argsData: unknown) => {
            const args = argsData as { p0: unknown, p1: unknown };
            const arg1Handle = wrapIfHandle(args.p0) as AzureResourceInfrastructureHandle;
            const arg1 = new AzureResourceInfrastructure(arg1Handle, this._client);
            const arg2 = wrapIfHandle(args.p1) as ContainerAppJobHandle;
            await configure(arg1, arg2);
        }) : undefined;
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        if (configure !== undefined) rpcArgs.configure = configureId;
        const result = await this._client.invokeCapability<IComputeResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsConfiguredScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new ComputeResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): ComputeResourcePromise {
        const configure = options?.configure;
        return new ComputeResourcePromise(this._publishAsConfiguredScheduledAzureContainerAppJobInternal(cronExpression, configure));
    }

    /** @internal */
    private async _publishAsScheduledAzureContainerAppJobInternal(cronExpression: string): Promise<ComputeResource> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle, cronExpression };
        const result = await this._client.invokeCapability<IComputeResourceHandle>(
            'Aspire.Hosting.Azure.AppContainers/publishAsScheduledAzureContainerAppJob',
            rpcArgs
        );
        return new ComputeResource(result, this._client);
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): ComputeResourcePromise {
        return new ComputeResourcePromise(this._publishAsScheduledAzureContainerAppJobInternal(cronExpression));
    }

    /** @internal */
    private async _withAzureUserAssignedIdentityInternal(identityResourceBuilder: AzureUserAssignedIdentityResource): Promise<ComputeResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, identityResourceBuilder };
        const result = await this._client.invokeCapability<IComputeResourceHandle>(
            'Aspire.Hosting.Azure/withUserAssignedIdentityAzureUserAssignedIdentity',
            rpcArgs
        );
        return new ComputeResource(result, this._client);
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): ComputeResourcePromise {
        return new ComputeResourcePromise(this._withAzureUserAssignedIdentityInternal(identityResourceBuilder));
    }

}

/**
 * Thenable wrapper for ComputeResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class ComputeResourcePromise implements PromiseLike<ComputeResource> {
    constructor(private _promise: Promise<ComputeResource>) {}

    then<TResult1 = ComputeResource, TResult2 = never>(
        onfulfilled?: ((value: ComputeResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Sets the remote image name for publishing */
    withRemoteImageName(remoteImageName: string): ComputeResourcePromise {
        return new ComputeResourcePromise(this._promise.then(obj => obj.withRemoteImageName(remoteImageName)));
    }

    /** Sets the remote image tag for publishing */
    withRemoteImageTag(remoteImageTag: string): ComputeResourcePromise {
        return new ComputeResourcePromise(this._promise.then(obj => obj.withRemoteImageTag(remoteImageTag)));
    }

    /** Configures the compute resource as an Azure Container App Job with custom configuration */
    publishAsConfiguredAzureContainerAppJob(configure: (arg1: AzureResourceInfrastructure, arg2: ContainerAppJobHandle) => Promise<void>): ComputeResourcePromise {
        return new ComputeResourcePromise(this._promise.then(obj => obj.publishAsConfiguredAzureContainerAppJob(configure)));
    }

    /** Configures the compute resource as a manually triggered Azure Container App Job */
    publishAsAzureContainerAppJob(): ComputeResourcePromise {
        return new ComputeResourcePromise(this._promise.then(obj => obj.publishAsAzureContainerAppJob()));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job with custom configuration */
    publishAsConfiguredScheduledAzureContainerAppJob(cronExpression: string, options?: PublishAsConfiguredScheduledAzureContainerAppJobOptions): ComputeResourcePromise {
        return new ComputeResourcePromise(this._promise.then(obj => obj.publishAsConfiguredScheduledAzureContainerAppJob(cronExpression, options)));
    }

    /** Configures the compute resource as a scheduled Azure Container App Job */
    publishAsScheduledAzureContainerAppJob(cronExpression: string): ComputeResourcePromise {
        return new ComputeResourcePromise(this._promise.then(obj => obj.publishAsScheduledAzureContainerAppJob(cronExpression)));
    }

    /** Associates an Azure user-assigned identity with a compute resource */
    withAzureUserAssignedIdentity(identityResourceBuilder: AzureUserAssignedIdentityResource): ComputeResourcePromise {
        return new ComputeResourcePromise(this._promise.then(obj => obj.withAzureUserAssignedIdentity(identityResourceBuilder)));
    }

}

// ============================================================================
// ContainerFilesDestinationResource
// ============================================================================

export class ContainerFilesDestinationResource extends ResourceBuilderBase<IContainerFilesDestinationResourceHandle> {
    constructor(handle: IContainerFilesDestinationResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _publishWithContainerFilesInternal(source: ResourceBuilderBase, destinationPath: string): Promise<ContainerFilesDestinationResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, destinationPath };
        const result = await this._client.invokeCapability<IContainerFilesDestinationResourceHandle>(
            'Aspire.Hosting/publishWithContainerFiles',
            rpcArgs
        );
        return new ContainerFilesDestinationResource(result, this._client);
    }

    /** Configures the resource to copy container files from the specified source during publishing */
    publishWithContainerFiles(source: ResourceBuilderBase, destinationPath: string): ContainerFilesDestinationResourcePromise {
        return new ContainerFilesDestinationResourcePromise(this._publishWithContainerFilesInternal(source, destinationPath));
    }

}

/**
 * Thenable wrapper for ContainerFilesDestinationResource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class ContainerFilesDestinationResourcePromise implements PromiseLike<ContainerFilesDestinationResource> {
    constructor(private _promise: Promise<ContainerFilesDestinationResource>) {}

    then<TResult1 = ContainerFilesDestinationResource, TResult2 = never>(
        onfulfilled?: ((value: ContainerFilesDestinationResource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures the resource to copy container files from the specified source during publishing */
    publishWithContainerFiles(source: ResourceBuilderBase, destinationPath: string): ContainerFilesDestinationResourcePromise {
        return new ContainerFilesDestinationResourcePromise(this._promise.then(obj => obj.publishWithContainerFiles(source, destinationPath)));
    }

}

// ============================================================================
// Resource
// ============================================================================

export class Resource extends ResourceBuilderBase<IResourceHandle> {
    constructor(handle: IResourceHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerRegistryInternal(registry: ResourceBuilderBase): Promise<Resource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registry };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/withContainerRegistry',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): ResourcePromise {
        return new ResourcePromise(this._withContainerRegistryInternal(registry));
    }

    /** @internal */
    private async _withDockerfileBaseImageInternal(buildImage?: string, runtimeImage?: string): Promise<Resource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (buildImage !== undefined) rpcArgs.buildImage = buildImage;
        if (runtimeImage !== undefined) rpcArgs.runtimeImage = runtimeImage;
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/withDockerfileBaseImage',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): ResourcePromise {
        const buildImage = options?.buildImage;
        const runtimeImage = options?.runtimeImage;
        return new ResourcePromise(this._withDockerfileBaseImageInternal(buildImage, runtimeImage));
    }

    /** @internal */
    private async _withRequiredCommandInternal(command: string, helpLink?: string): Promise<Resource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, command };
        if (helpLink !== undefined) rpcArgs.helpLink = helpLink;
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/withRequiredCommand',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): ResourcePromise {
        const helpLink = options?.helpLink;
        return new ResourcePromise(this._withRequiredCommandInternal(command, helpLink));
    }

    /** @internal */
    private async _withUrlsCallbackInternal(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): Promise<Resource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as ResourceUrlsCallbackContextHandle;
            const obj = new ResourceUrlsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/withUrlsCallback',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._withUrlsCallbackInternal(callback));
    }

    /** @internal */
    private async _withUrlsCallbackAsyncInternal(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): Promise<Resource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceUrlsCallbackContextHandle;
            const arg = new ResourceUrlsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/withUrlsCallbackAsync',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._withUrlsCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withUrlInternal(url: string, displayText?: string): Promise<Resource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/withUrl',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): ResourcePromise {
        const displayText = options?.displayText;
        return new ResourcePromise(this._withUrlInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlExpressionInternal(url: ReferenceExpression, displayText?: string): Promise<Resource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, url };
        if (displayText !== undefined) rpcArgs.displayText = displayText;
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/withUrlExpression',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): ResourcePromise {
        const displayText = options?.displayText;
        return new ResourcePromise(this._withUrlExpressionInternal(url, displayText));
    }

    /** @internal */
    private async _withUrlForEndpointInternal(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): Promise<Resource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const obj = wrapIfHandle(objData) as ResourceUrlAnnotation;
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/withUrlForEndpoint',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._withUrlForEndpointInternal(endpointName, callback));
    }

    /** @internal */
    private async _excludeFromManifestInternal(): Promise<Resource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/excludeFromManifest',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): ResourcePromise {
        return new ResourcePromise(this._excludeFromManifestInternal());
    }

    /** @internal */
    private async _withExplicitStartInternal(): Promise<Resource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/withExplicitStart',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): ResourcePromise {
        return new ResourcePromise(this._withExplicitStartInternal());
    }

    /** @internal */
    private async _withHealthCheckInternal(key: string): Promise<Resource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, key };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/withHealthCheck',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): ResourcePromise {
        return new ResourcePromise(this._withHealthCheckInternal(key));
    }

    /** @internal */
    private async _withCommandInternal(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, commandOptions?: CommandOptions): Promise<Resource> {
        const executeCommandId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ExecuteCommandContextHandle;
            const arg = new ExecuteCommandContext(argHandle, this._client);
            return await executeCommand(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, displayName, executeCommand: executeCommandId };
        if (commandOptions !== undefined) rpcArgs.commandOptions = commandOptions;
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/withCommand',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): ResourcePromise {
        const commandOptions = options?.commandOptions;
        return new ResourcePromise(this._withCommandInternal(name, displayName, executeCommand, commandOptions));
    }

    /** @internal */
    private async _withParentRelationshipInternal(parent: ResourceBuilderBase): Promise<Resource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, parent };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/withParentRelationship',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): ResourcePromise {
        return new ResourcePromise(this._withParentRelationshipInternal(parent));
    }

    /** @internal */
    private async _withChildRelationshipInternal(child: ResourceBuilderBase): Promise<Resource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, child };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/withChildRelationship',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): ResourcePromise {
        return new ResourcePromise(this._withChildRelationshipInternal(child));
    }

    /** @internal */
    private async _withIconNameInternal(iconName: string, iconVariant?: IconVariant): Promise<Resource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, iconName };
        if (iconVariant !== undefined) rpcArgs.iconVariant = iconVariant;
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/withIconName',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): ResourcePromise {
        const iconVariant = options?.iconVariant;
        return new ResourcePromise(this._withIconNameInternal(iconName, iconVariant));
    }

    /** @internal */
    private async _excludeFromMcpInternal(): Promise<Resource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/excludeFromMcp',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): ResourcePromise {
        return new ResourcePromise(this._excludeFromMcpInternal());
    }

    /** @internal */
    private async _withPipelineStepFactoryInternal(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, dependsOn?: string[], requiredBy?: string[], tags?: string[], description?: string): Promise<Resource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineStepContextHandle;
            const arg = new PipelineStepContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, stepName, callback: callbackId };
        if (dependsOn !== undefined) rpcArgs.dependsOn = dependsOn;
        if (requiredBy !== undefined) rpcArgs.requiredBy = requiredBy;
        if (tags !== undefined) rpcArgs.tags = tags;
        if (description !== undefined) rpcArgs.description = description;
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/withPipelineStepFactory',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): ResourcePromise {
        const dependsOn = options?.dependsOn;
        const requiredBy = options?.requiredBy;
        const tags = options?.tags;
        const description = options?.description;
        return new ResourcePromise(this._withPipelineStepFactoryInternal(stepName, callback, dependsOn, requiredBy, tags, description));
    }

    /** @internal */
    private async _withPipelineConfigurationAsyncInternal(callback: (arg: PipelineConfigurationContext) => Promise<void>): Promise<Resource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as PipelineConfigurationContextHandle;
            const arg = new PipelineConfigurationContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/withPipelineConfigurationAsync',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._withPipelineConfigurationAsyncInternal(callback));
    }

    /** @internal */
    private async _withPipelineConfigurationInternal(callback: (obj: PipelineConfigurationContext) => Promise<void>): Promise<Resource> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as PipelineConfigurationContextHandle;
            const obj = new PipelineConfigurationContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/withPipelineConfiguration',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._withPipelineConfigurationInternal(callback));
    }

    /** Gets the resource name */
    async getResourceName(): Promise<string> {
        const rpcArgs: Record<string, unknown> = { resource: this._handle };
        return await this._client.invokeCapability<string>(
            'Aspire.Hosting/getResourceName',
            rpcArgs
        );
    }

    /** @internal */
    private async _onBeforeResourceStartedInternal(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): Promise<Resource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as BeforeResourceStartedEventHandle;
            const arg = new BeforeResourceStartedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/onBeforeResourceStarted',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._onBeforeResourceStartedInternal(callback));
    }

    /** @internal */
    private async _onResourceStoppedInternal(callback: (arg: ResourceStoppedEvent) => Promise<void>): Promise<Resource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceStoppedEventHandle;
            const arg = new ResourceStoppedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/onResourceStopped',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._onResourceStoppedInternal(callback));
    }

    /** @internal */
    private async _onInitializeResourceInternal(callback: (arg: InitializeResourceEvent) => Promise<void>): Promise<Resource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as InitializeResourceEventHandle;
            const arg = new InitializeResourceEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/onInitializeResource',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._onInitializeResourceInternal(callback));
    }

    /** @internal */
    private async _onResourceReadyInternal(callback: (arg: ResourceReadyEvent) => Promise<void>): Promise<Resource> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceReadyEventHandle;
            const arg = new ResourceReadyEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting/onResourceReady',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._onResourceReadyInternal(callback));
    }

    /** @internal */
    private async _withCognitiveServicesRoleAssignmentsInternal(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): Promise<Resource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting.Azure.CognitiveServices/withCognitiveServicesRoleAssignments',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): ResourcePromise {
        return new ResourcePromise(this._withCognitiveServicesRoleAssignmentsInternal(target, roles));
    }

    /** @internal */
    private async _withAzureContainerRegistryInternal(registryBuilder: AzureContainerRegistryResource): Promise<Resource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, registryBuilder };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryAzureContainerRegistry',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): ResourcePromise {
        return new ResourcePromise(this._withAzureContainerRegistryInternal(registryBuilder));
    }

    /** @internal */
    private async _getAzureContainerRegistryInternal(): Promise<AzureContainerRegistryResource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<AzureContainerRegistryResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/getAzureContainerRegistry',
            rpcArgs
        );
        return new AzureContainerRegistryResource(result, this._client);
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._getAzureContainerRegistryInternal());
    }

    /** @internal */
    private async _withContainerRegistryRoleAssignmentsInternal(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): Promise<Resource> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, target, roles };
        const result = await this._client.invokeCapability<IResourceHandle>(
            'Aspire.Hosting.Azure.ContainerRegistry/withContainerRegistryRoleAssignments',
            rpcArgs
        );
        return new Resource(result, this._client);
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): ResourcePromise {
        return new ResourcePromise(this._withContainerRegistryRoleAssignmentsInternal(target, roles));
    }

}

/**
 * Thenable wrapper for Resource that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class ResourcePromise implements PromiseLike<Resource> {
    constructor(private _promise: Promise<Resource>) {}

    then<TResult1 = Resource, TResult2 = never>(
        onfulfilled?: ((value: Resource) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures a resource to use a container registry */
    withContainerRegistry(registry: ResourceBuilderBase): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withContainerRegistry(registry)));
    }

    /** Sets the base image for a Dockerfile build */
    withDockerfileBaseImage(options?: WithDockerfileBaseImageOptions): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withDockerfileBaseImage(options)));
    }

    /** Adds a required command dependency */
    withRequiredCommand(command: string, options?: WithRequiredCommandOptions): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withRequiredCommand(command, options)));
    }

    /** Customizes displayed URLs via callback */
    withUrlsCallback(callback: (obj: ResourceUrlsCallbackContext) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withUrlsCallback(callback)));
    }

    /** Customizes displayed URLs via async callback */
    withUrlsCallbackAsync(callback: (arg: ResourceUrlsCallbackContext) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withUrlsCallbackAsync(callback)));
    }

    /** Adds or modifies displayed URLs */
    withUrl(url: string, options?: WithUrlOptions): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withUrl(url, options)));
    }

    /** Adds a URL using a reference expression */
    withUrlExpression(url: ReferenceExpression, options?: WithUrlExpressionOptions): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withUrlExpression(url, options)));
    }

    /** Customizes the URL for a specific endpoint via callback */
    withUrlForEndpoint(endpointName: string, callback: (obj: ResourceUrlAnnotation) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withUrlForEndpoint(endpointName, callback)));
    }

    /** Excludes the resource from the deployment manifest */
    excludeFromManifest(): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.excludeFromManifest()));
    }

    /** Prevents resource from starting automatically */
    withExplicitStart(): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withExplicitStart()));
    }

    /** Adds a health check by key */
    withHealthCheck(key: string): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withHealthCheck(key)));
    }

    /** Adds a resource command */
    withCommand(name: string, displayName: string, executeCommand: (arg: ExecuteCommandContext) => Promise<ExecuteCommandResult>, options?: WithCommandOptions): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withCommand(name, displayName, executeCommand, options)));
    }

    /** Sets the parent relationship */
    withParentRelationship(parent: ResourceBuilderBase): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withParentRelationship(parent)));
    }

    /** Sets a child relationship */
    withChildRelationship(child: ResourceBuilderBase): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withChildRelationship(child)));
    }

    /** Sets the icon for the resource */
    withIconName(iconName: string, options?: WithIconNameOptions): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withIconName(iconName, options)));
    }

    /** Excludes the resource from MCP server exposure */
    excludeFromMcp(): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.excludeFromMcp()));
    }

    /** Adds a pipeline step to the resource */
    withPipelineStepFactory(stepName: string, callback: (arg: PipelineStepContext) => Promise<void>, options?: WithPipelineStepFactoryOptions): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withPipelineStepFactory(stepName, callback, options)));
    }

    /** Configures pipeline step dependencies via an async callback */
    withPipelineConfigurationAsync(callback: (arg: PipelineConfigurationContext) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withPipelineConfigurationAsync(callback)));
    }

    /** Configures pipeline step dependencies via a callback */
    withPipelineConfiguration(callback: (obj: PipelineConfigurationContext) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withPipelineConfiguration(callback)));
    }

    /** Gets the resource name */
    getResourceName(): Promise<string> {
        return this._promise.then(obj => obj.getResourceName());
    }

    /** Subscribes to the BeforeResourceStarted event */
    onBeforeResourceStarted(callback: (arg: BeforeResourceStartedEvent) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.onBeforeResourceStarted(callback)));
    }

    /** Subscribes to the ResourceStopped event */
    onResourceStopped(callback: (arg: ResourceStoppedEvent) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.onResourceStopped(callback)));
    }

    /** Subscribes to the InitializeResource event */
    onInitializeResource(callback: (arg: InitializeResourceEvent) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.onInitializeResource(callback)));
    }

    /** Subscribes to the ResourceReady event */
    onResourceReady(callback: (arg: ResourceReadyEvent) => Promise<void>): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.onResourceReady(callback)));
    }

    /** Assigns Cognitive Services roles to a resource */
    withCognitiveServicesRoleAssignments(target: AzureOpenAIResource, roles: AzureOpenAIRole[]): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withCognitiveServicesRoleAssignments(target, roles)));
    }

    /** Configures a compute environment resource to use an Azure Container Registry. */
    withAzureContainerRegistry(registryBuilder: AzureContainerRegistryResource): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withAzureContainerRegistry(registryBuilder)));
    }

    /** Gets the Azure Container Registry associated with a compute environment resource. */
    getAzureContainerRegistry(): AzureContainerRegistryResourcePromise {
        return new AzureContainerRegistryResourcePromise(this._promise.then(obj => obj.getAzureContainerRegistry()));
    }

    /** Assigns Azure Container Registry roles to a resource. */
    withContainerRegistryRoleAssignments(target: AzureContainerRegistryResource, roles: AzureContainerRegistryRole[]): ResourcePromise {
        return new ResourcePromise(this._promise.then(obj => obj.withContainerRegistryRoleAssignments(target, roles)));
    }

}

// ============================================================================
// ResourceWithArgs
// ============================================================================

export class ResourceWithArgs extends ResourceBuilderBase<IResourceWithArgsHandle> {
    constructor(handle: IResourceWithArgsHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withArgsInternal(args: string[]): Promise<ResourceWithArgs> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, args };
        const result = await this._client.invokeCapability<IResourceWithArgsHandle>(
            'Aspire.Hosting/withArgs',
            rpcArgs
        );
        return new ResourceWithArgs(result, this._client);
    }

    /** Adds arguments */
    withArgs(args: string[]): ResourceWithArgsPromise {
        return new ResourceWithArgsPromise(this._withArgsInternal(args));
    }

    /** @internal */
    private async _withArgsCallbackInternal(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): Promise<ResourceWithArgs> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as CommandLineArgsCallbackContextHandle;
            const obj = new CommandLineArgsCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<IResourceWithArgsHandle>(
            'Aspire.Hosting/withArgsCallback',
            rpcArgs
        );
        return new ResourceWithArgs(result, this._client);
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): ResourceWithArgsPromise {
        return new ResourceWithArgsPromise(this._withArgsCallbackInternal(callback));
    }

    /** @internal */
    private async _withArgsCallbackAsyncInternal(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): Promise<ResourceWithArgs> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as CommandLineArgsCallbackContextHandle;
            const arg = new CommandLineArgsCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<IResourceWithArgsHandle>(
            'Aspire.Hosting/withArgsCallbackAsync',
            rpcArgs
        );
        return new ResourceWithArgs(result, this._client);
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): ResourceWithArgsPromise {
        return new ResourceWithArgsPromise(this._withArgsCallbackAsyncInternal(callback));
    }

}

/**
 * Thenable wrapper for ResourceWithArgs that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class ResourceWithArgsPromise implements PromiseLike<ResourceWithArgs> {
    constructor(private _promise: Promise<ResourceWithArgs>) {}

    then<TResult1 = ResourceWithArgs, TResult2 = never>(
        onfulfilled?: ((value: ResourceWithArgs) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Adds arguments */
    withArgs(args: string[]): ResourceWithArgsPromise {
        return new ResourceWithArgsPromise(this._promise.then(obj => obj.withArgs(args)));
    }

    /** Sets command-line arguments via callback */
    withArgsCallback(callback: (obj: CommandLineArgsCallbackContext) => Promise<void>): ResourceWithArgsPromise {
        return new ResourceWithArgsPromise(this._promise.then(obj => obj.withArgsCallback(callback)));
    }

    /** Sets command-line arguments via async callback */
    withArgsCallbackAsync(callback: (arg: CommandLineArgsCallbackContext) => Promise<void>): ResourceWithArgsPromise {
        return new ResourceWithArgsPromise(this._promise.then(obj => obj.withArgsCallbackAsync(callback)));
    }

}

// ============================================================================
// ResourceWithConnectionString
// ============================================================================

export class ResourceWithConnectionString extends ResourceBuilderBase<IResourceWithConnectionStringHandle> {
    constructor(handle: IResourceWithConnectionStringHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withConnectionPropertyInternal(name: string, value: ReferenceExpression): Promise<ResourceWithConnectionString> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<IResourceWithConnectionStringHandle>(
            'Aspire.Hosting/withConnectionProperty',
            rpcArgs
        );
        return new ResourceWithConnectionString(result, this._client);
    }

    /** Adds a connection property with a reference expression */
    withConnectionProperty(name: string, value: ReferenceExpression): ResourceWithConnectionStringPromise {
        return new ResourceWithConnectionStringPromise(this._withConnectionPropertyInternal(name, value));
    }

    /** @internal */
    private async _withConnectionPropertyValueInternal(name: string, value: string): Promise<ResourceWithConnectionString> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<IResourceWithConnectionStringHandle>(
            'Aspire.Hosting/withConnectionPropertyValue',
            rpcArgs
        );
        return new ResourceWithConnectionString(result, this._client);
    }

    /** Adds a connection property with a string value */
    withConnectionPropertyValue(name: string, value: string): ResourceWithConnectionStringPromise {
        return new ResourceWithConnectionStringPromise(this._withConnectionPropertyValueInternal(name, value));
    }

    /** @internal */
    private async _onConnectionStringAvailableInternal(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): Promise<ResourceWithConnectionString> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ConnectionStringAvailableEventHandle;
            const arg = new ConnectionStringAvailableEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<IResourceWithConnectionStringHandle>(
            'Aspire.Hosting/onConnectionStringAvailable',
            rpcArgs
        );
        return new ResourceWithConnectionString(result, this._client);
    }

    /** Subscribes to the ConnectionStringAvailable event */
    onConnectionStringAvailable(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): ResourceWithConnectionStringPromise {
        return new ResourceWithConnectionStringPromise(this._onConnectionStringAvailableInternal(callback));
    }

}

/**
 * Thenable wrapper for ResourceWithConnectionString that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class ResourceWithConnectionStringPromise implements PromiseLike<ResourceWithConnectionString> {
    constructor(private _promise: Promise<ResourceWithConnectionString>) {}

    then<TResult1 = ResourceWithConnectionString, TResult2 = never>(
        onfulfilled?: ((value: ResourceWithConnectionString) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Adds a connection property with a reference expression */
    withConnectionProperty(name: string, value: ReferenceExpression): ResourceWithConnectionStringPromise {
        return new ResourceWithConnectionStringPromise(this._promise.then(obj => obj.withConnectionProperty(name, value)));
    }

    /** Adds a connection property with a string value */
    withConnectionPropertyValue(name: string, value: string): ResourceWithConnectionStringPromise {
        return new ResourceWithConnectionStringPromise(this._promise.then(obj => obj.withConnectionPropertyValue(name, value)));
    }

    /** Subscribes to the ConnectionStringAvailable event */
    onConnectionStringAvailable(callback: (arg: ConnectionStringAvailableEvent) => Promise<void>): ResourceWithConnectionStringPromise {
        return new ResourceWithConnectionStringPromise(this._promise.then(obj => obj.onConnectionStringAvailable(callback)));
    }

}

// ============================================================================
// ResourceWithContainerFiles
// ============================================================================

export class ResourceWithContainerFiles extends ResourceBuilderBase<IResourceWithContainerFilesHandle> {
    constructor(handle: IResourceWithContainerFilesHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withContainerFilesSourceInternal(sourcePath: string): Promise<ResourceWithContainerFiles> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, sourcePath };
        const result = await this._client.invokeCapability<IResourceWithContainerFilesHandle>(
            'Aspire.Hosting/withContainerFilesSource',
            rpcArgs
        );
        return new ResourceWithContainerFiles(result, this._client);
    }

    /** Sets the source directory for container files */
    withContainerFilesSource(sourcePath: string): ResourceWithContainerFilesPromise {
        return new ResourceWithContainerFilesPromise(this._withContainerFilesSourceInternal(sourcePath));
    }

    /** @internal */
    private async _clearContainerFilesSourcesInternal(): Promise<ResourceWithContainerFiles> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<IResourceWithContainerFilesHandle>(
            'Aspire.Hosting/clearContainerFilesSources',
            rpcArgs
        );
        return new ResourceWithContainerFiles(result, this._client);
    }

    /** Clears all container file sources */
    clearContainerFilesSources(): ResourceWithContainerFilesPromise {
        return new ResourceWithContainerFilesPromise(this._clearContainerFilesSourcesInternal());
    }

}

/**
 * Thenable wrapper for ResourceWithContainerFiles that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class ResourceWithContainerFilesPromise implements PromiseLike<ResourceWithContainerFiles> {
    constructor(private _promise: Promise<ResourceWithContainerFiles>) {}

    then<TResult1 = ResourceWithContainerFiles, TResult2 = never>(
        onfulfilled?: ((value: ResourceWithContainerFiles) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Sets the source directory for container files */
    withContainerFilesSource(sourcePath: string): ResourceWithContainerFilesPromise {
        return new ResourceWithContainerFilesPromise(this._promise.then(obj => obj.withContainerFilesSource(sourcePath)));
    }

    /** Clears all container file sources */
    clearContainerFilesSources(): ResourceWithContainerFilesPromise {
        return new ResourceWithContainerFilesPromise(this._promise.then(obj => obj.clearContainerFilesSources()));
    }

}

// ============================================================================
// ResourceWithEndpoints
// ============================================================================

export class ResourceWithEndpoints extends ResourceBuilderBase<IResourceWithEndpointsHandle> {
    constructor(handle: IResourceWithEndpointsHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withMcpServerInternal(path?: string, endpointName?: string): Promise<ResourceWithEndpoints> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<IResourceWithEndpointsHandle>(
            'Aspire.Hosting/withMcpServer',
            rpcArgs
        );
        return new ResourceWithEndpoints(result, this._client);
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): ResourceWithEndpointsPromise {
        const path = options?.path;
        const endpointName = options?.endpointName;
        return new ResourceWithEndpointsPromise(this._withMcpServerInternal(path, endpointName));
    }

    /** @internal */
    private async _withEndpointInternal(port?: number, targetPort?: number, scheme?: string, name?: string, env?: string, isProxied?: boolean, isExternal?: boolean, protocol?: ProtocolType): Promise<ResourceWithEndpoints> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (scheme !== undefined) rpcArgs.scheme = scheme;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        if (isExternal !== undefined) rpcArgs.isExternal = isExternal;
        if (protocol !== undefined) rpcArgs.protocol = protocol;
        const result = await this._client.invokeCapability<IResourceWithEndpointsHandle>(
            'Aspire.Hosting/withEndpoint',
            rpcArgs
        );
        return new ResourceWithEndpoints(result, this._client);
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): ResourceWithEndpointsPromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const scheme = options?.scheme;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        const isExternal = options?.isExternal;
        const protocol = options?.protocol;
        return new ResourceWithEndpointsPromise(this._withEndpointInternal(port, targetPort, scheme, name, env, isProxied, isExternal, protocol));
    }

    /** @internal */
    private async _withHttpEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<ResourceWithEndpoints> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<IResourceWithEndpointsHandle>(
            'Aspire.Hosting/withHttpEndpoint',
            rpcArgs
        );
        return new ResourceWithEndpoints(result, this._client);
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): ResourceWithEndpointsPromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ResourceWithEndpointsPromise(this._withHttpEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withHttpsEndpointInternal(port?: number, targetPort?: number, name?: string, env?: string, isProxied?: boolean): Promise<ResourceWithEndpoints> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (port !== undefined) rpcArgs.port = port;
        if (targetPort !== undefined) rpcArgs.targetPort = targetPort;
        if (name !== undefined) rpcArgs.name = name;
        if (env !== undefined) rpcArgs.env = env;
        if (isProxied !== undefined) rpcArgs.isProxied = isProxied;
        const result = await this._client.invokeCapability<IResourceWithEndpointsHandle>(
            'Aspire.Hosting/withHttpsEndpoint',
            rpcArgs
        );
        return new ResourceWithEndpoints(result, this._client);
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): ResourceWithEndpointsPromise {
        const port = options?.port;
        const targetPort = options?.targetPort;
        const name = options?.name;
        const env = options?.env;
        const isProxied = options?.isProxied;
        return new ResourceWithEndpointsPromise(this._withHttpsEndpointInternal(port, targetPort, name, env, isProxied));
    }

    /** @internal */
    private async _withExternalHttpEndpointsInternal(): Promise<ResourceWithEndpoints> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<IResourceWithEndpointsHandle>(
            'Aspire.Hosting/withExternalHttpEndpoints',
            rpcArgs
        );
        return new ResourceWithEndpoints(result, this._client);
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): ResourceWithEndpointsPromise {
        return new ResourceWithEndpointsPromise(this._withExternalHttpEndpointsInternal());
    }

    /** Gets an endpoint reference */
    async getEndpoint(name: string): Promise<EndpointReference> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name };
        return await this._client.invokeCapability<EndpointReference>(
            'Aspire.Hosting/getEndpoint',
            rpcArgs
        );
    }

    /** @internal */
    private async _asHttp2ServiceInternal(): Promise<ResourceWithEndpoints> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<IResourceWithEndpointsHandle>(
            'Aspire.Hosting/asHttp2Service',
            rpcArgs
        );
        return new ResourceWithEndpoints(result, this._client);
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): ResourceWithEndpointsPromise {
        return new ResourceWithEndpointsPromise(this._asHttp2ServiceInternal());
    }

    /** @internal */
    private async _withUrlForEndpointFactoryInternal(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): Promise<ResourceWithEndpoints> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EndpointReferenceHandle;
            const arg = new EndpointReference(argHandle, this._client);
            return await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointName, callback: callbackId };
        const result = await this._client.invokeCapability<IResourceWithEndpointsHandle>(
            'Aspire.Hosting/withUrlForEndpointFactory',
            rpcArgs
        );
        return new ResourceWithEndpoints(result, this._client);
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): ResourceWithEndpointsPromise {
        return new ResourceWithEndpointsPromise(this._withUrlForEndpointFactoryInternal(endpointName, callback));
    }

    /** @internal */
    private async _withHttpHealthCheckInternal(path?: string, statusCode?: number, endpointName?: string): Promise<ResourceWithEndpoints> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (path !== undefined) rpcArgs.path = path;
        if (statusCode !== undefined) rpcArgs.statusCode = statusCode;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<IResourceWithEndpointsHandle>(
            'Aspire.Hosting/withHttpHealthCheck',
            rpcArgs
        );
        return new ResourceWithEndpoints(result, this._client);
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): ResourceWithEndpointsPromise {
        const path = options?.path;
        const statusCode = options?.statusCode;
        const endpointName = options?.endpointName;
        return new ResourceWithEndpointsPromise(this._withHttpHealthCheckInternal(path, statusCode, endpointName));
    }

    /** @internal */
    private async _withHttpProbeInternal(probeType: ProbeType, path?: string, initialDelaySeconds?: number, periodSeconds?: number, timeoutSeconds?: number, failureThreshold?: number, successThreshold?: number, endpointName?: string): Promise<ResourceWithEndpoints> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, probeType };
        if (path !== undefined) rpcArgs.path = path;
        if (initialDelaySeconds !== undefined) rpcArgs.initialDelaySeconds = initialDelaySeconds;
        if (periodSeconds !== undefined) rpcArgs.periodSeconds = periodSeconds;
        if (timeoutSeconds !== undefined) rpcArgs.timeoutSeconds = timeoutSeconds;
        if (failureThreshold !== undefined) rpcArgs.failureThreshold = failureThreshold;
        if (successThreshold !== undefined) rpcArgs.successThreshold = successThreshold;
        if (endpointName !== undefined) rpcArgs.endpointName = endpointName;
        const result = await this._client.invokeCapability<IResourceWithEndpointsHandle>(
            'Aspire.Hosting/withHttpProbe',
            rpcArgs
        );
        return new ResourceWithEndpoints(result, this._client);
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): ResourceWithEndpointsPromise {
        const path = options?.path;
        const initialDelaySeconds = options?.initialDelaySeconds;
        const periodSeconds = options?.periodSeconds;
        const timeoutSeconds = options?.timeoutSeconds;
        const failureThreshold = options?.failureThreshold;
        const successThreshold = options?.successThreshold;
        const endpointName = options?.endpointName;
        return new ResourceWithEndpointsPromise(this._withHttpProbeInternal(probeType, path, initialDelaySeconds, periodSeconds, timeoutSeconds, failureThreshold, successThreshold, endpointName));
    }

    /** @internal */
    private async _onResourceEndpointsAllocatedInternal(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): Promise<ResourceWithEndpoints> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as ResourceEndpointsAllocatedEventHandle;
            const arg = new ResourceEndpointsAllocatedEvent(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<IResourceWithEndpointsHandle>(
            'Aspire.Hosting/onResourceEndpointsAllocated',
            rpcArgs
        );
        return new ResourceWithEndpoints(result, this._client);
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): ResourceWithEndpointsPromise {
        return new ResourceWithEndpointsPromise(this._onResourceEndpointsAllocatedInternal(callback));
    }

}

/**
 * Thenable wrapper for ResourceWithEndpoints that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class ResourceWithEndpointsPromise implements PromiseLike<ResourceWithEndpoints> {
    constructor(private _promise: Promise<ResourceWithEndpoints>) {}

    then<TResult1 = ResourceWithEndpoints, TResult2 = never>(
        onfulfilled?: ((value: ResourceWithEndpoints) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures an MCP server endpoint on the resource */
    withMcpServer(options?: WithMcpServerOptions): ResourceWithEndpointsPromise {
        return new ResourceWithEndpointsPromise(this._promise.then(obj => obj.withMcpServer(options)));
    }

    /** Adds a network endpoint */
    withEndpoint(options?: WithEndpointOptions): ResourceWithEndpointsPromise {
        return new ResourceWithEndpointsPromise(this._promise.then(obj => obj.withEndpoint(options)));
    }

    /** Adds an HTTP endpoint */
    withHttpEndpoint(options?: WithHttpEndpointOptions): ResourceWithEndpointsPromise {
        return new ResourceWithEndpointsPromise(this._promise.then(obj => obj.withHttpEndpoint(options)));
    }

    /** Adds an HTTPS endpoint */
    withHttpsEndpoint(options?: WithHttpsEndpointOptions): ResourceWithEndpointsPromise {
        return new ResourceWithEndpointsPromise(this._promise.then(obj => obj.withHttpsEndpoint(options)));
    }

    /** Makes HTTP endpoints externally accessible */
    withExternalHttpEndpoints(): ResourceWithEndpointsPromise {
        return new ResourceWithEndpointsPromise(this._promise.then(obj => obj.withExternalHttpEndpoints()));
    }

    /** Gets an endpoint reference */
    getEndpoint(name: string): Promise<EndpointReference> {
        return this._promise.then(obj => obj.getEndpoint(name));
    }

    /** Configures resource for HTTP/2 */
    asHttp2Service(): ResourceWithEndpointsPromise {
        return new ResourceWithEndpointsPromise(this._promise.then(obj => obj.asHttp2Service()));
    }

    /** Adds a URL for a specific endpoint via factory callback */
    withUrlForEndpointFactory(endpointName: string, callback: (arg: EndpointReference) => Promise<ResourceUrlAnnotation>): ResourceWithEndpointsPromise {
        return new ResourceWithEndpointsPromise(this._promise.then(obj => obj.withUrlForEndpointFactory(endpointName, callback)));
    }

    /** Adds an HTTP health check */
    withHttpHealthCheck(options?: WithHttpHealthCheckOptions): ResourceWithEndpointsPromise {
        return new ResourceWithEndpointsPromise(this._promise.then(obj => obj.withHttpHealthCheck(options)));
    }

    /** Adds an HTTP health probe to the resource */
    withHttpProbe(probeType: ProbeType, options?: WithHttpProbeOptions): ResourceWithEndpointsPromise {
        return new ResourceWithEndpointsPromise(this._promise.then(obj => obj.withHttpProbe(probeType, options)));
    }

    /** Subscribes to the ResourceEndpointsAllocated event */
    onResourceEndpointsAllocated(callback: (arg: ResourceEndpointsAllocatedEvent) => Promise<void>): ResourceWithEndpointsPromise {
        return new ResourceWithEndpointsPromise(this._promise.then(obj => obj.onResourceEndpointsAllocated(callback)));
    }

}

// ============================================================================
// ResourceWithEnvironment
// ============================================================================

export class ResourceWithEnvironment extends ResourceBuilderBase<IResourceWithEnvironmentHandle> {
    constructor(handle: IResourceWithEnvironmentHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _withOtlpExporterInternal(): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withOtlpExporter',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withOtlpExporterInternal());
    }

    /** @internal */
    private async _withOtlpExporterProtocolInternal(protocol: OtlpProtocol): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, protocol };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withOtlpExporterProtocol',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withOtlpExporterProtocolInternal(protocol));
    }

    /** @internal */
    private async _withEnvironmentInternal(name: string, value: string): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withEnvironment',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withEnvironmentInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentExpressionInternal(name: string, value: ReferenceExpression): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, value };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withEnvironmentExpression',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withEnvironmentExpressionInternal(name, value));
    }

    /** @internal */
    private async _withEnvironmentCallbackInternal(callback: (obj: EnvironmentCallbackContext) => Promise<void>): Promise<ResourceWithEnvironment> {
        const callbackId = registerCallback(async (objData: unknown) => {
            const objHandle = wrapIfHandle(objData) as EnvironmentCallbackContextHandle;
            const obj = new EnvironmentCallbackContext(objHandle, this._client);
            await callback(obj);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withEnvironmentCallback',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withEnvironmentCallbackInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentCallbackAsyncInternal(callback: (arg: EnvironmentCallbackContext) => Promise<void>): Promise<ResourceWithEnvironment> {
        const callbackId = registerCallback(async (argData: unknown) => {
            const argHandle = wrapIfHandle(argData) as EnvironmentCallbackContextHandle;
            const arg = new EnvironmentCallbackContext(argHandle, this._client);
            await callback(arg);
        });
        const rpcArgs: Record<string, unknown> = { builder: this._handle, callback: callbackId };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withEnvironmentCallbackAsync',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withEnvironmentCallbackAsyncInternal(callback));
    }

    /** @internal */
    private async _withEnvironmentEndpointInternal(name: string, endpointReference: EndpointReference): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, endpointReference };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withEnvironmentEndpoint',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withEnvironmentEndpointInternal(name, endpointReference));
    }

    /** @internal */
    private async _withEnvironmentParameterInternal(name: string, parameter: ParameterResource): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, parameter };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withEnvironmentParameter',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withEnvironmentParameterInternal(name, parameter));
    }

    /** @internal */
    private async _withEnvironmentConnectionStringInternal(envVarName: string, resource: ResourceBuilderBase): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, envVarName, resource };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withEnvironmentConnectionString',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withEnvironmentConnectionStringInternal(envVarName, resource));
    }

    /** @internal */
    private async _withReferenceInternal(source: ResourceBuilderBase, connectionName?: string, optional?: boolean): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        if (connectionName !== undefined) rpcArgs.connectionName = connectionName;
        if (optional !== undefined) rpcArgs.optional = optional;
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withReference',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): ResourceWithEnvironmentPromise {
        const connectionName = options?.connectionName;
        const optional = options?.optional;
        return new ResourceWithEnvironmentPromise(this._withReferenceInternal(source, connectionName, optional));
    }

    /** @internal */
    private async _withServiceReferenceInternal(source: ResourceBuilderBase): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withServiceReference',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withServiceReferenceInternal(source));
    }

    /** @internal */
    private async _withServiceReferenceNamedInternal(source: ResourceBuilderBase, name: string): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, source, name };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withServiceReferenceNamed',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withServiceReferenceNamedInternal(source, name));
    }

    /** @internal */
    private async _withReferenceUriInternal(name: string, uri: string): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, uri };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withReferenceUri',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withReferenceUriInternal(name, uri));
    }

    /** @internal */
    private async _withReferenceExternalServiceInternal(externalService: ExternalServiceResource): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, externalService };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withReferenceExternalService',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withReferenceExternalServiceInternal(externalService));
    }

    /** @internal */
    private async _withReferenceEndpointInternal(endpointReference: EndpointReference): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, endpointReference };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withReferenceEndpoint',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withReferenceEndpointInternal(endpointReference));
    }

    /** @internal */
    private async _withDeveloperCertificateTrustInternal(trust: boolean): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, trust };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withDeveloperCertificateTrust',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withDeveloperCertificateTrustInternal(trust));
    }

    /** @internal */
    private async _withCertificateTrustScopeInternal(scope: CertificateTrustScope): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, scope };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withCertificateTrustScope',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withCertificateTrustScopeInternal(scope));
    }

    /** @internal */
    private async _withHttpsDeveloperCertificateInternal(password?: ParameterResource): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        if (password !== undefined) rpcArgs.password = password;
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withHttpsDeveloperCertificate',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): ResourceWithEnvironmentPromise {
        const password = options?.password;
        return new ResourceWithEnvironmentPromise(this._withHttpsDeveloperCertificateInternal(password));
    }

    /** @internal */
    private async _withoutHttpsCertificateInternal(): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting/withoutHttpsCertificate',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withoutHttpsCertificateInternal());
    }

    /** @internal */
    private async _withEnvironmentFromOutputInternal(name: string, bicepOutputReference: BicepOutputReference): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, bicepOutputReference };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromOutput',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withEnvironmentFromOutputInternal(name, bicepOutputReference));
    }

    /** @internal */
    private async _withEnvironmentFromKeyVaultSecretInternal(name: string, secretReference: ResourceBuilderBase): Promise<ResourceWithEnvironment> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, name, secretReference };
        const result = await this._client.invokeCapability<IResourceWithEnvironmentHandle>(
            'Aspire.Hosting.Azure/withEnvironmentFromKeyVaultSecret',
            rpcArgs
        );
        return new ResourceWithEnvironment(result, this._client);
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._withEnvironmentFromKeyVaultSecretInternal(name, secretReference));
    }

}

/**
 * Thenable wrapper for ResourceWithEnvironment that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class ResourceWithEnvironmentPromise implements PromiseLike<ResourceWithEnvironment> {
    constructor(private _promise: Promise<ResourceWithEnvironment>) {}

    then<TResult1 = ResourceWithEnvironment, TResult2 = never>(
        onfulfilled?: ((value: ResourceWithEnvironment) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Configures OTLP telemetry export */
    withOtlpExporter(): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withOtlpExporter()));
    }

    /** Configures OTLP telemetry export with specific protocol */
    withOtlpExporterProtocol(protocol: OtlpProtocol): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withOtlpExporterProtocol(protocol)));
    }

    /** Sets an environment variable */
    withEnvironment(name: string, value: string): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withEnvironment(name, value)));
    }

    /** Adds an environment variable with a reference expression */
    withEnvironmentExpression(name: string, value: ReferenceExpression): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withEnvironmentExpression(name, value)));
    }

    /** Sets environment variables via callback */
    withEnvironmentCallback(callback: (obj: EnvironmentCallbackContext) => Promise<void>): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withEnvironmentCallback(callback)));
    }

    /** Sets environment variables via async callback */
    withEnvironmentCallbackAsync(callback: (arg: EnvironmentCallbackContext) => Promise<void>): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withEnvironmentCallbackAsync(callback)));
    }

    /** Sets an environment variable from an endpoint reference */
    withEnvironmentEndpoint(name: string, endpointReference: EndpointReference): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withEnvironmentEndpoint(name, endpointReference)));
    }

    /** Sets an environment variable from a parameter resource */
    withEnvironmentParameter(name: string, parameter: ParameterResource): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withEnvironmentParameter(name, parameter)));
    }

    /** Sets an environment variable from a connection string resource */
    withEnvironmentConnectionString(envVarName: string, resource: ResourceBuilderBase): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withEnvironmentConnectionString(envVarName, resource)));
    }

    /** Adds a reference to another resource */
    withReference(source: ResourceBuilderBase, options?: WithReferenceOptions): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withReference(source, options)));
    }

    /** Adds a service discovery reference to another resource */
    withServiceReference(source: ResourceBuilderBase): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withServiceReference(source)));
    }

    /** Adds a named service discovery reference */
    withServiceReferenceNamed(source: ResourceBuilderBase, name: string): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withServiceReferenceNamed(source, name)));
    }

    /** Adds a reference to a URI */
    withReferenceUri(name: string, uri: string): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withReferenceUri(name, uri)));
    }

    /** Adds a reference to an external service */
    withReferenceExternalService(externalService: ExternalServiceResource): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withReferenceExternalService(externalService)));
    }

    /** Adds a reference to an endpoint */
    withReferenceEndpoint(endpointReference: EndpointReference): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withReferenceEndpoint(endpointReference)));
    }

    /** Configures developer certificate trust */
    withDeveloperCertificateTrust(trust: boolean): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withDeveloperCertificateTrust(trust)));
    }

    /** Sets the certificate trust scope */
    withCertificateTrustScope(scope: CertificateTrustScope): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withCertificateTrustScope(scope)));
    }

    /** Configures HTTPS with a developer certificate */
    withHttpsDeveloperCertificate(options?: WithHttpsDeveloperCertificateOptions): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withHttpsDeveloperCertificate(options)));
    }

    /** Removes HTTPS certificate configuration */
    withoutHttpsCertificate(): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withoutHttpsCertificate()));
    }

    /** Sets an environment variable from a Bicep output reference */
    withEnvironmentFromOutput(name: string, bicepOutputReference: BicepOutputReference): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withEnvironmentFromOutput(name, bicepOutputReference)));
    }

    /** Sets an environment variable from an Azure Key Vault secret reference */
    withEnvironmentFromKeyVaultSecret(name: string, secretReference: ResourceBuilderBase): ResourceWithEnvironmentPromise {
        return new ResourceWithEnvironmentPromise(this._promise.then(obj => obj.withEnvironmentFromKeyVaultSecret(name, secretReference)));
    }

}

// ============================================================================
// ResourceWithServiceDiscovery
// ============================================================================

export class ResourceWithServiceDiscovery extends ResourceBuilderBase<IResourceWithServiceDiscoveryHandle> {
    constructor(handle: IResourceWithServiceDiscoveryHandle, client: AspireClientRpc) {
        super(handle, client);
    }

}

/**
 * Thenable wrapper for ResourceWithServiceDiscovery that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class ResourceWithServiceDiscoveryPromise implements PromiseLike<ResourceWithServiceDiscovery> {
    constructor(private _promise: Promise<ResourceWithServiceDiscovery>) {}

    then<TResult1 = ResourceWithServiceDiscovery, TResult2 = never>(
        onfulfilled?: ((value: ResourceWithServiceDiscovery) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

}

// ============================================================================
// ResourceWithWaitSupport
// ============================================================================

export class ResourceWithWaitSupport extends ResourceBuilderBase<IResourceWithWaitSupportHandle> {
    constructor(handle: IResourceWithWaitSupportHandle, client: AspireClientRpc) {
        super(handle, client);
    }

    /** @internal */
    private async _waitForInternal(dependency: ResourceBuilderBase): Promise<ResourceWithWaitSupport> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<IResourceWithWaitSupportHandle>(
            'Aspire.Hosting/waitFor',
            rpcArgs
        );
        return new ResourceWithWaitSupport(result, this._client);
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): ResourceWithWaitSupportPromise {
        return new ResourceWithWaitSupportPromise(this._waitForInternal(dependency));
    }

    /** @internal */
    private async _waitForWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<ResourceWithWaitSupport> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<IResourceWithWaitSupportHandle>(
            'Aspire.Hosting/waitForWithBehavior',
            rpcArgs
        );
        return new ResourceWithWaitSupport(result, this._client);
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ResourceWithWaitSupportPromise {
        return new ResourceWithWaitSupportPromise(this._waitForWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _waitForStartInternal(dependency: ResourceBuilderBase): Promise<ResourceWithWaitSupport> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        const result = await this._client.invokeCapability<IResourceWithWaitSupportHandle>(
            'Aspire.Hosting/waitForStart',
            rpcArgs
        );
        return new ResourceWithWaitSupport(result, this._client);
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): ResourceWithWaitSupportPromise {
        return new ResourceWithWaitSupportPromise(this._waitForStartInternal(dependency));
    }

    /** @internal */
    private async _waitForStartWithBehaviorInternal(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): Promise<ResourceWithWaitSupport> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency, waitBehavior };
        const result = await this._client.invokeCapability<IResourceWithWaitSupportHandle>(
            'Aspire.Hosting/waitForStartWithBehavior',
            rpcArgs
        );
        return new ResourceWithWaitSupport(result, this._client);
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ResourceWithWaitSupportPromise {
        return new ResourceWithWaitSupportPromise(this._waitForStartWithBehaviorInternal(dependency, waitBehavior));
    }

    /** @internal */
    private async _waitForCompletionInternal(dependency: ResourceBuilderBase, exitCode?: number): Promise<ResourceWithWaitSupport> {
        const rpcArgs: Record<string, unknown> = { builder: this._handle, dependency };
        if (exitCode !== undefined) rpcArgs.exitCode = exitCode;
        const result = await this._client.invokeCapability<IResourceWithWaitSupportHandle>(
            'Aspire.Hosting/waitForCompletion',
            rpcArgs
        );
        return new ResourceWithWaitSupport(result, this._client);
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): ResourceWithWaitSupportPromise {
        const exitCode = options?.exitCode;
        return new ResourceWithWaitSupportPromise(this._waitForCompletionInternal(dependency, exitCode));
    }

}

/**
 * Thenable wrapper for ResourceWithWaitSupport that enables fluent chaining.
 * @example
 * await builder.addSomething().withX().withY();
 */
export class ResourceWithWaitSupportPromise implements PromiseLike<ResourceWithWaitSupport> {
    constructor(private _promise: Promise<ResourceWithWaitSupport>) {}

    then<TResult1 = ResourceWithWaitSupport, TResult2 = never>(
        onfulfilled?: ((value: ResourceWithWaitSupport) => TResult1 | PromiseLike<TResult1>) | null,
        onrejected?: ((reason: unknown) => TResult2 | PromiseLike<TResult2>) | null
    ): PromiseLike<TResult1 | TResult2> {
        return this._promise.then(onfulfilled, onrejected);
    }

    /** Waits for another resource to be ready */
    waitFor(dependency: ResourceBuilderBase): ResourceWithWaitSupportPromise {
        return new ResourceWithWaitSupportPromise(this._promise.then(obj => obj.waitFor(dependency)));
    }

    /** Waits for another resource with specific behavior */
    waitForWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ResourceWithWaitSupportPromise {
        return new ResourceWithWaitSupportPromise(this._promise.then(obj => obj.waitForWithBehavior(dependency, waitBehavior)));
    }

    /** Waits for another resource to start */
    waitForStart(dependency: ResourceBuilderBase): ResourceWithWaitSupportPromise {
        return new ResourceWithWaitSupportPromise(this._promise.then(obj => obj.waitForStart(dependency)));
    }

    /** Waits for another resource to start with specific behavior */
    waitForStartWithBehavior(dependency: ResourceBuilderBase, waitBehavior: WaitBehavior): ResourceWithWaitSupportPromise {
        return new ResourceWithWaitSupportPromise(this._promise.then(obj => obj.waitForStartWithBehavior(dependency, waitBehavior)));
    }

    /** Waits for resource completion */
    waitForCompletion(dependency: ResourceBuilderBase, options?: WaitForCompletionOptions): ResourceWithWaitSupportPromise {
        return new ResourceWithWaitSupportPromise(this._promise.then(obj => obj.waitForCompletion(dependency, options)));
    }

}

// ============================================================================
// Connection Helper
// ============================================================================

/**
 * Creates and connects to the Aspire AppHost.
 * Reads connection info from environment variables set by `aspire run`.
 */
export async function connect(): Promise<AspireClientRpc> {
    const socketPath = process.env.REMOTE_APP_HOST_SOCKET_PATH;
    if (!socketPath) {
        throw new Error(
            'REMOTE_APP_HOST_SOCKET_PATH environment variable not set. ' +
            'Run this application using `aspire run`.'
        );
    }

    const client = new AspireClientRpc(socketPath);
    await client.connect();

    // Exit the process if the server connection is lost
    client.onDisconnect(() => {
        console.error('Connection to AppHost lost. Exiting...');
        process.exit(1);
    });

    return client;
}

/**
 * Creates a new distributed application builder.
 * This is the entry point for building Aspire applications.
 *
 * @param options - Optional configuration options for the builder
 * @returns A DistributedApplicationBuilder instance
 *
 * @example
 * const builder = await createBuilder();
 * builder.addRedis("cache");
 * builder.addContainer("api", "mcr.microsoft.com/dotnet/samples:aspnetapp");
 * const app = await builder.build();
 * await app.run();
 */
export async function createBuilder(options?: CreateBuilderOptions): Promise<DistributedApplicationBuilder> {
    const client = await connect();

    // Default args, projectDirectory, and appHostFilePath if not provided
    // ASPIRE_APPHOST_FILEPATH is set by the CLI for consistent socket hash computation
    const effectiveOptions: CreateBuilderOptions = {
        ...options,
        args: options?.args ?? process.argv.slice(2),
        projectDirectory: options?.projectDirectory ?? process.env.ASPIRE_PROJECT_DIRECTORY ?? process.cwd(),
        appHostFilePath: options?.appHostFilePath ?? process.env.ASPIRE_APPHOST_FILEPATH
    };

    const handle = await client.invokeCapability<IDistributedApplicationBuilderHandle>(
        'Aspire.Hosting/createBuilderWithOptions',
        { options: effectiveOptions }
    );
    return new DistributedApplicationBuilder(handle, client);
}

// Re-export commonly used types
export { Handle, AppHostUsageError, CapabilityError, registerCallback } from './transport.js';
export { refExpr, ReferenceExpression } from './base.js';

// ============================================================================
// Global Error Handling
// ============================================================================

/**
 * Set up global error handlers to ensure the process exits properly on errors.
 * Node.js doesn't exit on unhandled rejections by default, so we need to handle them.
 */
process.on('unhandledRejection', (reason: unknown) => {
    const error = reason instanceof Error ? reason : new Error(String(reason));

    if (reason instanceof AppHostUsageError) {
        console.error(`\n❌ AppHost Error: ${error.message}`);
    } else if (reason instanceof CapabilityError) {
        console.error(`\n❌ Capability Error: ${error.message}`);
        console.error(`   Code: ${(reason as CapabilityError).code}`);
        if ((reason as CapabilityError).capability) {
            console.error(`   Capability: ${(reason as CapabilityError).capability}`);
        }
    } else {
        console.error(`\n❌ Unhandled Error: ${error.message}`);
        if (error.stack) {
            console.error(error.stack);
        }
    }

    process.exit(1);
});

process.on('uncaughtException', (error: Error) => {
    if (error instanceof AppHostUsageError) {
        console.error(`\n❌ AppHost Error: ${error.message}`);
    } else {
        console.error(`\n❌ Uncaught Exception: ${error.message}`);
    }
    if (!(error instanceof AppHostUsageError) && error.stack) {
        console.error(error.stack);
    }
    process.exit(1);
});

// ============================================================================
// Handle Wrapper Registrations
// ============================================================================

// Register wrapper factories for typed handle wrapping in callbacks
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.AfterResourcesCreatedEvent', (handle, client) => new AfterResourcesCreatedEvent(handle as AfterResourcesCreatedEventHandle, client));
registerHandleWrapper('Aspire.Hosting.Azure/Aspire.Hosting.Azure.AzureResourceInfrastructure', (handle, client) => new AzureResourceInfrastructure(handle as AzureResourceInfrastructureHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.BeforeResourceStartedEvent', (handle, client) => new BeforeResourceStartedEvent(handle as BeforeResourceStartedEventHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.BeforeStartEvent', (handle, client) => new BeforeStartEvent(handle as BeforeStartEventHandle, client));
registerHandleWrapper('Aspire.Hosting.Azure/Aspire.Hosting.Azure.BicepOutputReference', (handle, client) => new BicepOutputReference(handle as BicepOutputReferenceHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.CommandLineArgsCallbackContext', (handle, client) => new CommandLineArgsCallbackContext(handle as CommandLineArgsCallbackContextHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ConnectionStringAvailableEvent', (handle, client) => new ConnectionStringAvailableEvent(handle as ConnectionStringAvailableEventHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.DistributedApplication', (handle, client) => new DistributedApplication(handle as DistributedApplicationHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.DistributedApplicationExecutionContext', (handle, client) => new DistributedApplicationExecutionContext(handle as DistributedApplicationExecutionContextHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.DistributedApplicationModel', (handle, client) => new DistributedApplicationModel(handle as DistributedApplicationModelHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.EndpointReference', (handle, client) => new EndpointReference(handle as EndpointReferenceHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.EndpointReferenceExpression', (handle, client) => new EndpointReferenceExpression(handle as EndpointReferenceExpressionHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.EnvironmentCallbackContext', (handle, client) => new EnvironmentCallbackContext(handle as EnvironmentCallbackContextHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ExecuteCommandContext', (handle, client) => new ExecuteCommandContext(handle as ExecuteCommandContextHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.InitializeResourceEvent', (handle, client) => new InitializeResourceEvent(handle as InitializeResourceEventHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineConfigurationContext', (handle, client) => new PipelineConfigurationContext(handle as PipelineConfigurationContextHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineContext', (handle, client) => new PipelineContext(handle as PipelineContextHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineStep', (handle, client) => new PipelineStep(handle as PipelineStepHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineStepContext', (handle, client) => new PipelineStepContext(handle as PipelineStepContextHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineStepFactoryContext', (handle, client) => new PipelineStepFactoryContext(handle as PipelineStepFactoryContextHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.PipelineSummary', (handle, client) => new PipelineSummary(handle as PipelineSummaryHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ProjectResourceOptions', (handle, client) => new ProjectResourceOptions(handle as ProjectResourceOptionsHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ReferenceExpressionBuilder', (handle, client) => new ReferenceExpressionBuilder(handle as ReferenceExpressionBuilderHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceEndpointsAllocatedEvent', (handle, client) => new ResourceEndpointsAllocatedEvent(handle as ResourceEndpointsAllocatedEventHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceLoggerService', (handle, client) => new ResourceLoggerService(handle as ResourceLoggerServiceHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceNotificationService', (handle, client) => new ResourceNotificationService(handle as ResourceNotificationServiceHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceReadyEvent', (handle, client) => new ResourceReadyEvent(handle as ResourceReadyEventHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceStoppedEvent', (handle, client) => new ResourceStoppedEvent(handle as ResourceStoppedEventHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ResourceUrlsCallbackContext', (handle, client) => new ResourceUrlsCallbackContext(handle as ResourceUrlsCallbackContextHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.UpdateCommandStateContext', (handle, client) => new UpdateCommandStateContext(handle as UpdateCommandStateContextHandle, client));
registerHandleWrapper('Aspire.Hosting.Yarp/Aspire.Hosting.Yarp.YarpCluster', (handle, client) => new YarpCluster(handle as YarpClusterHandle, client));
registerHandleWrapper('Aspire.Hosting.Yarp/Aspire.Hosting.Yarp.YarpRoute', (handle, client) => new YarpRoute(handle as YarpRouteHandle, client));
registerHandleWrapper('Microsoft.Extensions.Configuration.Abstractions/Microsoft.Extensions.Configuration.IConfiguration', (handle, client) => new Configuration(handle as IConfigurationHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.IDistributedApplicationBuilder', (handle, client) => new DistributedApplicationBuilder(handle as IDistributedApplicationBuilderHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Eventing.IDistributedApplicationEventing', (handle, client) => new DistributedApplicationEventing(handle as IDistributedApplicationEventingHandle, client));
registerHandleWrapper('Microsoft.Extensions.Hosting.Abstractions/Microsoft.Extensions.Hosting.IHostEnvironment', (handle, client) => new HostEnvironment(handle as IHostEnvironmentHandle, client));
registerHandleWrapper('Microsoft.Extensions.Logging.Abstractions/Microsoft.Extensions.Logging.ILogger', (handle, client) => new Logger(handle as ILoggerHandle, client));
registerHandleWrapper('Microsoft.Extensions.Logging.Abstractions/Microsoft.Extensions.Logging.ILoggerFactory', (handle, client) => new LoggerFactory(handle as ILoggerFactoryHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.IReportingStep', (handle, client) => new ReportingStep(handle as IReportingStepHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.Pipelines.IReportingTask', (handle, client) => new ReportingTask(handle as IReportingTaskHandle, client));
registerHandleWrapper('System.ComponentModel/System.IServiceProvider', (handle, client) => new ServiceProvider(handle as IServiceProviderHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.IUserSecretsManager', (handle, client) => new UserSecretsManager(handle as IUserSecretsManagerHandle, client));
registerHandleWrapper('Aspire.Hosting.Yarp/Aspire.Hosting.IYarpConfigurationBuilder', (handle, client) => new YarpConfigurationBuilder(handle as IYarpConfigurationBuilderHandle, client));
registerHandleWrapper('Aspire.Hosting.Azure/Aspire.Hosting.Azure.AzureBicepResource', (handle, client) => new AzureBicepResource(handle as AzureBicepResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.Azure.AppContainers/Aspire.Hosting.Azure.AppContainers.AzureContainerAppEnvironmentResource', (handle, client) => new AzureContainerAppEnvironmentResource(handle as AzureContainerAppEnvironmentResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.Azure.ContainerRegistry/Aspire.Hosting.Azure.AzureContainerRegistryResource', (handle, client) => new AzureContainerRegistryResource(handle as AzureContainerRegistryResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.Azure/Aspire.Hosting.Azure.AzureEnvironmentResource', (handle, client) => new AzureEnvironmentResource(handle as AzureEnvironmentResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.Azure.OperationalInsights/Aspire.Hosting.Azure.AzureLogAnalyticsWorkspaceResource', (handle, client) => new AzureLogAnalyticsWorkspaceResource(handle as AzureLogAnalyticsWorkspaceResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.Azure.CognitiveServices/Aspire.Hosting.ApplicationModel.AzureOpenAIDeploymentResource', (handle, client) => new AzureOpenAIDeploymentResource(handle as AzureOpenAIDeploymentResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.Azure.CognitiveServices/Aspire.Hosting.ApplicationModel.AzureOpenAIResource', (handle, client) => new AzureOpenAIResource(handle as AzureOpenAIResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.Azure/Aspire.Hosting.Azure.AzureProvisioningResource', (handle, client) => new AzureProvisioningResource(handle as AzureProvisioningResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.Azure/Aspire.Hosting.Azure.AzureUserAssignedIdentityResource', (handle, client) => new AzureUserAssignedIdentityResource(handle as AzureUserAssignedIdentityResourceHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ConnectionStringResource', (handle, client) => new ConnectionStringResource(handle as ConnectionStringResourceHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ContainerRegistryResource', (handle, client) => new ContainerRegistryResource(handle as ContainerRegistryResourceHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ContainerResource', (handle, client) => new ContainerResource(handle as ContainerResourceHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.CSharpAppResource', (handle, client) => new CSharpAppResource(handle as CSharpAppResourceHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.DotnetToolResource', (handle, client) => new DotnetToolResource(handle as DotnetToolResourceHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ExecutableResource', (handle, client) => new ExecutableResource(handle as ExecutableResourceHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ExternalServiceResource', (handle, client) => new ExternalServiceResource(handle as ExternalServiceResourceHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ParameterResource', (handle, client) => new ParameterResource(handle as ParameterResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.PostgreSQL/Aspire.Hosting.Postgres.PgAdminContainerResource', (handle, client) => new PgAdminContainerResource(handle as PgAdminContainerResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.PostgreSQL/Aspire.Hosting.Postgres.PgWebContainerResource', (handle, client) => new PgWebContainerResource(handle as PgWebContainerResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.PostgreSQL/Aspire.Hosting.ApplicationModel.PostgresDatabaseResource', (handle, client) => new PostgresDatabaseResource(handle as PostgresDatabaseResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.PostgreSQL/Aspire.Hosting.Postgres.PostgresMcpContainerResource', (handle, client) => new PostgresMcpContainerResource(handle as PostgresMcpContainerResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.PostgreSQL/Aspire.Hosting.ApplicationModel.PostgresServerResource', (handle, client) => new PostgresServerResource(handle as PostgresServerResourceHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.ProjectResource', (handle, client) => new ProjectResource(handle as ProjectResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.RabbitMQ/Aspire.Hosting.ApplicationModel.RabbitMQServerResource', (handle, client) => new RabbitMQServerResource(handle as RabbitMQServerResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.Redis/Aspire.Hosting.Redis.RedisCommanderResource', (handle, client) => new RedisCommanderResource(handle as RedisCommanderResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.Redis/Aspire.Hosting.Redis.RedisInsightResource', (handle, client) => new RedisInsightResource(handle as RedisInsightResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.Redis/Aspire.Hosting.ApplicationModel.RedisResource', (handle, client) => new RedisResource(handle as RedisResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.Yarp/Aspire.Hosting.Yarp.YarpResource', (handle, client) => new YarpResource(handle as YarpResourceHandle, client));
registerHandleWrapper('Aspire.Hosting.Azure/Aspire.Hosting.ApplicationModel.IAzureResource', (handle, client) => new AzureResource(handle as IAzureResourceHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IComputeResource', (handle, client) => new ComputeResource(handle as IComputeResourceHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IContainerFilesDestinationResource', (handle, client) => new ContainerFilesDestinationResource(handle as IContainerFilesDestinationResourceHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResource', (handle, client) => new Resource(handle as IResourceHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResourceWithArgs', (handle, client) => new ResourceWithArgs(handle as IResourceWithArgsHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResourceWithConnectionString', (handle, client) => new ResourceWithConnectionString(handle as IResourceWithConnectionStringHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.IResourceWithContainerFiles', (handle, client) => new ResourceWithContainerFiles(handle as IResourceWithContainerFilesHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResourceWithEndpoints', (handle, client) => new ResourceWithEndpoints(handle as IResourceWithEndpointsHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResourceWithEnvironment', (handle, client) => new ResourceWithEnvironment(handle as IResourceWithEnvironmentHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.IResourceWithServiceDiscovery', (handle, client) => new ResourceWithServiceDiscovery(handle as IResourceWithServiceDiscoveryHandle, client));
registerHandleWrapper('Aspire.Hosting/Aspire.Hosting.ApplicationModel.IResourceWithWaitSupport', (handle, client) => new ResourceWithWaitSupport(handle as IResourceWithWaitSupportHandle, client));

